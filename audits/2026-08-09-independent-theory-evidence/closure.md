# Independent theory and evidence audit closure

Date: 2026-08-13
Closure input commit: `0b2cda299bd53c768bcccd8b234211a020f27d63`
Verdict: **closed — 13 of 13 supplied findings are corrected or precisely bounded**

## Scope and inventory

This closure covers the independent course audit supplied on 2026-08-09. The original message is preserved in the local Codex session log at line 35841 of `rollout-2026-08-09T15-07-36-019fe6a2-cc9e-7741-9fd6-84e04f8b670d.jsonl`; the complete log has SHA-256 `88e0ee127e0d1bfabe9820a9a5069403e9166da8624a6e40bf7a03c74629b6df`. A severity-preserving 13-item reconstruction is retained with the first closure run as `.build/runs/20260813T113028Z-close-independent-theory-evidence-audit-01/original-finding-inventory.md`, SHA-256 `f6b05b3c662078d60b6a7cb5f451f6824fdf2cd63346483eda0cca7767c3700f`.

The inventory is exactly three high-severity findings, six medium-severity findings, and four lower-risk gaps. The ten correction commits named below are ancestors of the closure input commit. Every correction has its own completed step, succeeded immutable run, and independent Git commit.

## Finding-to-correction map

| ID | Original finding | Disposition and current boundary | Correction commit |
| --- | --- | --- | --- |
| H1 | Chapter 14's exact reverse-mode equation omitted the selected-output seed. | Corrected. The recurrence is now `bar(v) = s 1[v=o] + sum_(e in E_o(v)) bar(c(e)) d_e`, scoped to tracked nodes reachable from the selected output. The indicator compares node identity, repeated operand uses remain distinct edges, and the already-correct Rust seed installation stayed protected. | `7f6ecf72f998eb2184bdaf6178e751ee63385855` |
| H2 | Chapter 34 claimed that caller-supplied provenance strings and role labels proved underlying lineage. | Bounded. Content and API documentation now distinguish caller assertions, mechanically checked runtime facts, and the concrete fixture's assembly. An adversarial Rust test deliberately accepts reused arbitrary nonblank identifiers and valid role labels, making the trust boundary executable rather than hiding it. Trusted fingerprint derivation and dataset governance remain external requirements. | `d37d4ce092e47be0658c7dbf8b6a052c6e02a39f` |
| H3 | The deliberately selected and permanently checked Chapter 34/39 holdout was presented too strongly as untouched generalization evidence and decoder superiority. | Bounded. The course now states that test data cannot change the frozen model inside one execution, while the exact scores and ordering are fixed-fixture regression evidence—not an untouched repository-level estimate or an architecture-wide comparison. A second diagnostic fixture reverses the ordering without violating the evaluator contract. The Dwork et al. source is used only for its general adaptive-holdout warning. | `3c8f1401877d9edb536a82ae11171d8bf23b9719` |
| M1 | Chapter 35 described a component-state checkpoint as sufficient to resume an entire training job exactly. | Bounded and strengthened. Version 1 remains a sealed tokenizer/model/optimizer/sampling-state component snapshot, and course-owned training captures the selected model and matching optimizer at one boundary. Exact replay covers that persisted state plus an explicitly caller-supplied update; corpus identity, batch order/cursor, training randomness, remaining schedule, clipping/selection policy, gradients, and caches remain caller-owned and are not claimed. | `a24fc35b1a68aee69e143947544eef374cca5992` |
| M2 | Chapter 39's overlapping window-target average was described as conventional once-per-transition corpus perplexity. | Corrected. The evidence names 1,744 ordered overlapping window-target slots versus 442 within-document transition occurrences, reports mean NLL in nats per slot, and reports its exponential as dimensionless window-slot perplexity. The conventional once-per-transition metric and its longest-prefix policy are defined but intentionally not reported. | `26f4aa3b6878aed21807913f43a5b71b455f8e06` |
| M3 | Chapter 13 divided by requested `2h` even when floating-point rounding made the actual probe spacings unequal. | Corrected in Rust and content. The checker derives both represented probe points and actual positive spacings, evaluates minus/center/plus in fixed order, and uses a stable unequal-spacing three-point interpolant. Tests include the rounded `f(x)=x` adversary, a centered quadratic, collapsed probes, overflow-resistant weights, restoration, and error paths. | `77a47065c4825316dace6f0b989361791461dc2c` |
| M4 | Chapter 22 called `1 - eta lambda` shrinkage without constraining the represented decay product. | Corrected. `lambda = 0` is the explicit no-decay case; positive decay requires the represented product `0 < fl(eta lambda) < 1`. Underflow to zero, a product reaching or exceeding one, and nonfinite products fail before parameter or optimizer-state reads, including scheduled-rate overrides. | `178fd95418ce311a420a448b397fec6d7757a0b5` |
| M5 | Chapter 36 equated the rank-retained top-k set with positive represented sampling support. | Corrected. `K_k` is defined only by stable rank, while `S+` contains retained IDs whose final stored `f64` probability is positive. The existing extreme-temperature case now exposes a retained zero-probability token and proves that it owns no categorical interval. | `4fcb221a6d882393533c52e786d9b58497cdf6bd` |
| M6 | Chapter 23 falsely said the 15-update budget was fixed before validation was inspected. | Corrected to the recorded two-stage history. An exploratory benchmark used validation to establish the 15-update budget; the subsequently frozen published run always executes all 15 updates, and measurements at 0, 8, and 15 neither select a checkpoint nor stop or change that run. | `69e3a0fc0234b2e8faddda3b9f00e48eec63ea29` |
| L1 | A global clipping scalar could underflow for extreme finite gradients. | Corrected. Ordinary represented scaling retains its prior arithmetic. When the equivalent uniform scalar would underflow, a structured normalized transform applies `(g_i / s) * (c / r)` without forming the underflowed scalar; an unrepresentable positive `c/r` fails before mutation. Raw gradients and decoupled decay remain unchanged. | `178fd95418ce311a420a448b397fec6d7757a0b5` |
| L2 | A centered finite-difference check could falsely pass at a nondifferentiable kink. | Bounded with an executable diagnostic. Local differentiability and sufficient smoothness across the probe interval are caller preconditions. The `abs(0)` test shows a centered result of zero while the one-sided slopes are -1 and 1; agreement is explicitly not proof of differentiability. | `77a47065c4825316dace6f0b989361791461dc2c` |
| L3 | Chapter 13's purportedly independent NLL oracle shared material production probability code. | Corrected and bounded. The analytic candidate now performs its own row traversal, max shift, exponentiation, normalization, target subtraction, and batch division without the production softmax or indexed-NLL helpers. The numerical side still calls production indexed mean NLL. Their remaining shared logits, targets, tensor/index convention, `f64`, and elementary exponentiation are stated explicitly, so the course claims material path separation rather than total independence. | `77a47065c4825316dace6f0b989361791461dc2c` |
| L4 | Embedding wording implied that different token identities share one parameter row. | Corrected. Chapter 18 now distinguishes one reused vocabulary-by-feature parameter matrix from different row coordinates: different IDs select different rows, repeated occurrences of one ID reuse its row and accumulate there, and numerically equal rows do not merge identity. Input-table reuse across positions is also distinguished from the later output-weight-tying relationship. | `ac9d0595d946d9e300bb78cbc370e85a8960755f` |

No finding is open, silently deferred, or converted into an unsupported stronger claim.

## Evidence boundary

The corrections preserve the independent audit's sound-core conclusion while narrowing what deterministic evidence proves:

- Locked Rust tests and exact demo outputs establish behavior for the frozen implementations, inputs, and represented numerical domains they exercise. They do not establish universal mathematical correctness.
- The Chapter 34/39 scores establish the named fixed-fixture observations and within-execution isolation. They do not establish untouched generalization or general decoder superiority.
- Sampled finite-difference agreement is local evidence for selected coordinates, represented probes, step, tolerance, and a smooth deterministic objective. It does not prove a complete gradient or differentiability.
- Caller-provided provenance identifiers establish assertion consistency only. They do not establish the lineage of their referents.
- The course remains a tiny decoder-only Transformer reference implementation, not a scale or production-readiness claim.

The historical claims used by these corrections remain bounded to their sources. Bengio et al. supports one learned word-feature matrix with one row per vocabulary item; Vaswani et al. supports learned token embeddings and later Transformer context; Dwork et al. supports only the general risk of adaptive holdout reuse. Repository-specific fixture selection, checkpoint scope, numerical traces, and validation chronology are supported by course-owned Rust and append-only repository decisions, not attributed to those papers.

## Validation and prerequisite checkpoints

The closure introduces no Rust, learner content, localization, component, style, dependency, lockfile, or route change. It follows three independently committed rendered-presentation prerequisites after the final semantic correction: the shared Chapter 18 full-view caption-flow correction at `4bab22ac2860c1540bf852f1de0d03a7271a47c9`, the Chapter 2 full-view action/caption alignment at `05960c3b3bc40acdf5f1f872cfe31c8e5763e832`, and the shared inline-code text-floor correction at `0b2cda299bd53c768bcccd8b234211a020f27d63`. Those checkpoints record their own focused static, supported-Firefox, scope, and host-hygiene evidence; they do not change the 13-finding semantic map above.

At exact closure input commit `0b2cda299bd53c768bcccd8b234211a020f27d63`, run `20260813T130929Z-close-independent-theory-evidence-audit-03` has passed one complete `./course check`. The check used the resulting workspace image `sha256:60d824f72236458bbf59dce8980b51e0963239ad5eb1d266eb2765ae96844b7f` and passed the locked Rust workspace and doc tests, Astro diagnostics, complete site/content/parity tests, the 85-page static production build, and link/artifact checks. This is exact-HEAD regression and build evidence; it does not replace the independent semantic audit or the separate rendered-browser gate.

The final clean supported-Firefox replay was the deterministic union of the nineteen E2E specifications changed by the ten semantic correction commits: `ch00-llm-parts`, `ch02-corpus-partitions`, `ch07-language-model-metrics`, `ch12-stable-softmax`, `ch13-gradient-checking`, `ch14-scalar-autodiff`, `ch18-token-embeddings`, `ch22-adamw`, `ch23-neural-ngram`, `ch26-qkv-projections`, `ch33-training-selection`, `ch34-final-evaluation`, `ch35-checkpoints`, `ch36-temperature-top-k`, `ch38-cached-generation`, `ch39-end-to-end-llm`, `cheat-sheets`, `diagram-style-system`, and `formula-rendering`. One invocation against workspace image `sha256:60d824f72236458bbf59dce8980b51e0963239ad5eb1d266eb2765ae96844b7f` and pinned Firefox image `sha256:5b8f294aff9041b7191c34a4bab3ac270157a28774d4b0660e9743297b697e48`, with `--pull=never`, `--network none`, one fresh removed volume, and only the repository's Firefox project, discovered exactly 410 tests and passed 410/410 in 1.5 minutes with no retry. The immutable report is `.build/runs/20260813T130929Z-close-independent-theory-evidence-audit-03/final-clean-19-spec-supported-firefox-replay.md`, SHA-256 `4aa75362e8f7a7b4e0960607a7f459d51e367e6c34740076df57a7d01fcc69a0`.

Each earlier semantic correction's immutable succeeded run records its own focused Rust/static gates, direct English-to-Russian localization, and affected JavaScript-enabled Firefox validation. The later Chapter 18, Chapter 2, and shared typography checkpoints protect or restore rendered presentation without changing the correction map's learner-facing meaning. Closure-specific finalization rechecked the 13-item map, correction ancestry, current claim boundaries, ledger integrity, successor scheduler, actual tracked scope, and host hygiene before publication.

## Scheduler handoff

After this closure is checkpointed, the first actionable pending step is `add-ch02-bpe-cheat-sheet-term`. The intervening diagram and keyboard-navigation steps are explicitly skipped, and the ledger-compaction build contains no actionable step. The network-disabled scheduler audit parsed the final ledger in file order and confirmed this single successor.

## Publication scope

The final tracked diff contains only the canonical `audits/2026-08-09-independent-theory-evidence/closure.md` and the required `BUILD_STATE.yaml` checkpoint update. `DECISIONS.md` is a declared possible output but remains byte-unchanged because all durable correction and recovery decisions were committed in their own predecessor checkpoints. YAML parsing, `git diff --check`, exact-scope inspection, artifact rehash, and `./course audit-host` pass; `site/dist` remains the documented static-release exception.

## Final verdict

The 2026-08-09 independent theory/evidence audit is closed. All 13 supplied findings have exact committed corrections or explicit bounded limitations, English remains canonical with direct Russian parity, and none relies on a false provenance, generalization, numerical, or parameter-identity claim.
