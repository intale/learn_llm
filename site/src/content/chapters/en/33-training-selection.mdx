---
{
  "chapter_id": "33-training-selection",
  "locale": "en",
  "content_revision": 10,
  "order": 33,
  "concept_id": "training-selection",
  "title": "Train every step, select with validation",
  "description": "Learn how a decoder training loop orders backpropagation, gradient clipping, scheduled AdamW updates, graph-free validation, and checkpoint selection without using test data.",
  "objective": "Run every step of a bounded decoder training plan, measure graph-free validation loss at fixed checkpoints, and restore the model state saved at the earliest checkpoint with minimum validation loss, all without consulting test data during this training execution.",
  "worked_inputs": "Train a deterministic one-block, 144-parameter decoder for eight fixed mini-batch updates with an explicit four-segment learning-rate schedule, global-norm clipping at 0.35, and validation measurements at steps 0, 2, 4, 6, and 8.",
  "formula":
    {
      "latex": "\\begin{aligned}g_s&=\\nabla_\\theta\\mathcal{L}_{tr}^{(s)}(\\theta_{s-1}),\\\\ \\widetilde g_s&=\\frac{c}{\\max(c,\\lVert g_s\\rVert_2)}g_s,\\\\ (\\theta_s,m_s,v_s)&=\\operatorname{AdamW}_{\\eta_s}\\!\\left(\\theta_{s-1},\\widetilde g_s,m_{s-1},v_{s-1}\\right),\\quad s=1,\\ldots,8,\\\\ s^*&=\\min\\left\\{s\\in\\mathcal{C}:\\mathcal{L}_{va}(\\theta_s)=\\min_{k\\in\\mathcal{C}}\\mathcal{L}_{va}(\\theta_k)\\right\\}\\end{aligned}",
      "symbols":
        [
          {
            "symbol": "\\theta_s",
            "meaning": "the complete stable-name decoder parameter state after exactly s planned updates; the initialized state has index zero"
          },
          {
            "symbol": "s",
            "meaning": "a one-based update index or a measured checkpoint index"
          },
          {
            "symbol": "\\mathcal{L}_{tr}^{(s)}",
            "meaning": "next-token loss for training mini-batch s, computed only from the training partition"
          },
          {
            "symbol": "g_s",
            "meaning": "the conceptual vector of every finite raw gradient coordinate from every named parameter before update s"
          },
          {
            "symbol": "\\widetilde g_s",
            "meaning": "the globally clipped gradient AdamW uses to update both moments"
          },
          {
            "symbol": "\\alpha_s",
            "meaning": "the single global clipping factor passed to AdamW; it equals one when clipping is unnecessary"
          },
          {
            "symbol": "c",
            "meaning": "the positive global-norm ceiling, 0.35 in the worked fixture"
          },
          {
            "symbol": "\\eta_s",
            "meaning": "the predetermined learning rate for update s"
          },
          {
            "symbol": "m_s,v_s",
            "meaning": "Adam's first- and second-moment states after update s"
          },
          {
            "symbol": "\\operatorname{AdamW}",
            "meaning": "the Chapter 22 optimizer update that advances parameters and both moment states"
          },
          {
            "symbol": "\\mathcal{C}",
            "meaning": "the measured checkpoint set {0, 2, 4, 6, 8}"
          },
          {
            "symbol": "s^*",
            "meaning": "the earliest measured checkpoint with minimum validation loss"
          },
          {
            "symbol": "\\mathcal{L}_{va}",
            "meaning": "the token-weighted graph-free loss on the validation partition; this trainer rejects the test partition during the training execution"
          }
        ]
    },
  "history":
    {
      "llm_evolution":
        {
          "predecessor_kind": "training-practice",
          "limitation": "Full-corpus or per-example updates and training-set-only reporting do not by themselves define a scalable update cadence or an independent rule for choosing among candidate language-model states.",
          "later_advance": "Neural language-model work separated train, validation, and test responsibilities; sequence and Transformer systems added mini-batches, explicit schedules, clipping, and periodic candidates; later text-to-text work stated that validation chooses a checkpoint so test data does not perform model selection.",
          "modern_llm_role": "Decoder-only LLM training repeatedly forms token batches, differentiates the training objective, controls gradient magnitude, applies a step schedule, and measures held-out validation candidates while reserving test evidence for post-selection evaluation; this course's one-use count belongs to one local evaluator instance.",
          "sources":
            [
              {
                "role": "earlier",
                "year": 2003,
                "name": "A Neural Probabilistic Language Model",
                "source_url": "https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf",
                "claim": "Bengio and colleagues separate training, validation, and test text, explicitly associate validation with model selection and early stopping, and describe stochastic per-example parameter updates for a feed-forward neural language model."
              },
              {
                "role": "earlier",
                "year": 2014,
                "name": "Sequence to Sequence Learning with Neural Networks",
                "source_url": "https://arxiv.org/pdf/1409.3215",
                "claim": "Sutskever, Vinyals, and Le report batches of sequences, a predetermined learning-rate reduction policy, and rescaling when the global gradient norm crosses a fixed threshold in a recurrent sequence model."
              },
              {
                "role": "later",
                "year": 2017,
                "name": "Attention Is All You Need",
                "source_url": "https://arxiv.org/pdf/1706.03762",
                "claim": "Vaswani and colleagues train Transformers with token-budgeted batches, a step-indexed warmup and inverse-square-root schedule, and periodically written checkpoints."
              },
              {
                "role": "later",
                "year": 2020,
                "name": "Exploring the Limits of Transfer Learning with a Unified Text-to-Text Transformer",
                "source_url": "https://www.jmlr.org/papers/volume21/20-074/20-074.pdf",
                "claim": "Raffel and colleagues save fine-tuning checkpoints at a fixed cadence, choose the one with the best validation performance, and explicitly avoid using the test set for model selection."
              },
              {
                "role": "later",
                "year": 2020,
                "name": "Language Models are Few-Shot Learners",
                "source_url": "https://arxiv.org/pdf/2005.14165",
                "claim": "Brown and colleagues carry Adam, scheduled learning rates, token-based batch scaling, and global-gradient-norm clipping into decoder-only language-model training at GPT-3 scale."
              }
            ]
        },
      "approach": "Move from fitting and reporting one training state toward predetermined mini-batch updates that produce periodic validation candidates while this trainer rejects the separate test partition throughout the training execution.",
      "summary": "The road to modern LLM training combines partition discipline with reproducible batches, finite gradients, norm control, an explicit learning-rate schedule, periodic graph-free validation, and checkpoint selection. These papers use different architectures and recipes, so the course's fixed seed, exact cadence, and earliest-tie rule are local teaching choices rather than universal practice.",
      "rust_source": "rust/demos/ch33-training-selection/src/lib.rs"
    },
  "rust_sources":
    [
      {
        "path": "rust/crates/llm-from-scratch/src/training/trainer.rs",
        "region": "training-plan",
        "purpose": "Declare the exact update order, one rate per update, validation cadence, and clipping ceiling before any model state can change."
      },
      {
        "path": "rust/crates/llm-from-scratch/src/training/trainer.rs",
        "region": "no-grad-evaluation",
        "purpose": "Measure token-weighted epoch loss without recording parent edges or changing any parameter gradient bits."
      },
      {
        "path": "rust/crates/llm-from-scratch/src/training/trainer.rs",
        "region": "decoder-state-snapshot",
        "purpose": "Name the deep-copy boundary for state that must remain independent, and move an owned graph-free state into a decoder without copying its tensor buffers again."
      },
      {
        "path": "rust/crates/llm-from-scratch/src/training/trainer.rs",
        "region": "complete-training-loop",
        "purpose": "Keep one working decoder and optimizer across every scheduled update, compute one global clipping factor, commit AdamW into the existing parameter nodes, clear their gradients explicitly, measure every declared validation checkpoint, and retain the earliest minimum as independent state."
      },
      {
        "path": "rust/crates/llm-from-scratch/src/training/adamw.rs",
        "region": "adamw-execution-and-trace-api",
        "purpose": "Run an atomic AdamW update with a scheduled learning rate and one validated gradient scale; the ordinary method returns only the new optimizer step number."
      },
      {
        "path": "rust/crates/llm-from-scratch/src/autograd/tensor_core.rs",
        "region": "no-grad-scope",
        "purpose": "Disable tape recording in a nestable, panic-safe scope while retaining ordinary forward checks."
      },
      {
        "path": "rust/crates/llm-from-scratch/src/models/decoder.rs",
        "region": "decoder-parameter-rebuild",
        "purpose": "Apply the shared stable-name-and-shape layout check before binding an exact parameter set into live decoder components; construction re-establishes component aliases and the tied embedding, while ordinary optimizer steps preserve them."
      },
      {
        "path": "rust/demos/ch33-training-selection/src/lib.rs",
        "region": "historical-selection-contrast",
        "purpose": "Contrast a training-only choice with a held-out validation choice without recreating a historical architecture."
      },
      {
        "path": "rust/demos/ch33-training-selection/src/lib.rs",
        "region": "learner-evidence",
        "purpose": "Run the bounded fixture twice and collect selection, clipping, ownership, graph, and replay evidence."
      },
      {
        "path": "rust/demos/ch33-training-selection/src/main.rs",
        "purpose": "Print only the frozen learner report."
      },
      {
        "path": "rust/demos/ch33-training-selection/src/diagram_trace.rs",
        "region": "training-selection-trace",
        "purpose": "Serialize the exact update, checkpoint, axis, selection, and proof records consumed by the static figure."
      }
    ],
  "visualization":
    {
      "decision": "useful",
      "id": "training-validation-checkpoints",
      "rationale": "A discrete checkpoint plot makes train and validation measurements comparable without inventing values between observations, while the selected validation marker and operation-order cards expose why the chosen state is evidence rather than simply the last update."
    },
  "decoder_connection": "The cumulative decoder can now execute a complete bounded training plan and return a frozen validation-selected state; Chapter 34 will give one local evaluator instance post-selection access to the test fixture and compare that state fairly with the frozen baseline."
}
---

import RustSource from "../../../components/RustSource.astro";
import TrainingSelectionDiagram from "../../../components/chapters/TrainingSelectionDiagram.astro";

export const diagramLabels = {
  title: "Separate updates from validation-based selection",
  description:
    "Repeat the same six-operation order for each of eight updates, then compare ten isolated train and validation measurements at five checkpoints without inventing a curve between them.",
  sections: {
    order: "One successful update has six ordered operations",
    checkpoints: "Measure candidates only at declared checkpoints",
    proof: "Keep optimization, selection, and test evidence separate",
  },
  operations: {
    forward: "Training forward",
    backward: "Backward and release",
    finiteCheck: "Finite-gradient check",
    clip: "Global-norm clip",
    adamwStep: "Scheduled AdamW step",
    zeroGrad: "Explicitly clear raw gradients",
  },
  fields: {
    step: "Step",
    trainLoss: "Training loss",
    validationLoss: "Validation loss",
    selected: "Selected state",
    learningRate: "Learning rate",
    rawNorm: "Raw gradient norm",
    clippedNorm: "Clipped gradient norm",
  },
  cues: {
    trainMeasurement: "Circle: measured training loss",
    validationMeasurement: "Diamond: measured validation loss",
    selectedState: "Double underline: selected validation state",
    selectedCell: "Selected",
    gapsUnobserved: "Gaps contain no measured values or interpolated line",
    clipped: "Clipped before the optimizer step",
    notSelected: "Not selected",
    testRejected: "Rejected before any update",
    verified: "Verified by the recorded run",
    parameterNodesPreserved: "Original parameter nodes preserved",
    gradientsCleared: "Raw gradients explicitly cleared",
  },
  proofs: {
    selection: "Earliest validation minimum",
    noGrad: "Train and validation record no graphs",
    partitions: "Partition information boundary",
    optimization: "Clipping, schedule, and replay",
  },
  captions: {
    order:
      "The trainer releases the backward graph, checks every gradient, clips once across the complete decoder, commits the predetermined-rate update into the existing parameter nodes, and clears their raw gradients explicitly.",
    checkpoints:
      "The table and markers preserve the exact six-decimal values. Only observations at steps 0, 2, 4, 6, and 8 exist; their visual spacing is presentation, not new arithmetic.",
    proof:
      "Validation can choose a snapshot but cannot update it; this trainer rejects Test throughout the training execution, before the next chapter creates its local evaluator.",
  },
  scrollers: {
    checkpoints: "Scrollable training and validation checkpoint evidence",
  },
};

{/* chapter-section:worked-example */}

## Plan every update before you train

Run every step of a bounded decoder training plan, measure graph-free validation
loss at fixed checkpoints, and restore the model state saved at the earliest
checkpoint with minimum validation loss, all without consulting test data during
this training execution. The
worked fixture trains the one-block decoder from the previous chapter:
vocabulary size $V=5$, residual width
$d_{\mathrm{model}}=4$, two attention heads, feed-forward width
$d_{\mathrm{ff}}=4$, context length $T=2$, and $144$ trainable scalars.

Train a deterministic one-block, 144-parameter decoder for eight fixed
mini-batch updates with an explicit four-segment learning-rate schedule,
global-norm clipping at $0.35$, and validation measurements at steps
$0,2,4,6,8$. Before executing the first forward pass, the trainer already owns
all of these decisions:

1. the update batches come only from `Train` and have fixed order;
2. every planned update has one finite positive learning rate;
3. each successful update uses `forward>backward>finite-check>clip>adamw-step>zero-grad`;
4. both reported losses are token-weighted means measured without a graph;
5. only `Validation` may select a state, while `Test` is rejected here.

The schedule is

$$
(\eta_1,\ldots,\eta_8)
=(0.040,0.040,0.025,0.025,0.015,0.015,0.008,0.008).
$$

All eight steps execute. Validation does not stop the run early. In this
fixture, the five validation losses are

$$
(1.918167,1.696310,1.687788,1.642599,1.595297),
$$

so the selected snapshot is $s^*=8$. That happens to be the final planned
state in this fixture; the selection rule itself does not prefer the last state.

{/* chapter-section:formula */}

## Update on train, choose on validation

For update $s\in\{1,\ldots,8\}$, the raw gradient is evaluated at the state
before that update, and the parameter and moment states advance together:

$$
\begin{aligned}g_s&=\nabla_\theta\mathcal{L}_{tr}^{(s)}(\theta_{s-1}),\\
\widetilde g_s&=\frac{c}{\max(c,\lVert g_s\rVert_2)}g_s,\\
(\theta_s,m_s,v_s)&=\operatorname{AdamW}_{\eta_s}\!\left(\theta_{s-1},\widetilde g_s,m_{s-1},v_{s-1}\right),\quad s=1,\ldots,8,\\
s^*&=\min\left\{s\in\mathcal{C}:\mathcal{L}_{va}(\theta_s)=\min_{k\in\mathcal{C}}\mathcal{L}_{va}(\theta_k)\right\}\end{aligned}
$$

The two losses have different jobs. The gradient comes from
$\mathcal{L}_{tr}$ and changes the parameters. The value $\mathcal{L}_{va}$ is
measured only at the declared checkpoint steps and may replace the saved
candidate, but it is never differentiated and never updates the decoder.

Treat $g_s$ as one conceptual vector formed by concatenating every coordinate
$g_{s,p,i}$ from every named parameter $p$. Here $\mathcal{P}$ is the set of
all named trainable parameters, and $i$ indexes the scalar coordinates within
parameter $p$. This definition does not require an allocated concatenated
tensor. The one global norm is

$$
\lVert g_s\rVert_2=
\sqrt{\sum_{p\in\mathcal{P}}\sum_i g_{s,p,i}^2}.
$$

For ceiling $c=0.35$, the trainer derives one scale from that complete norm and
applies it to every coordinate:

$$
\alpha_s=\frac{c}{\max(c,\lVert g_s\rVert_2)},
\qquad \widetilde g_s=\alpha_s g_s.
$$

Because $c>0$, the denominator never vanishes. A zero or already-small norm
therefore uses $\alpha_s=1$, while a larger norm is reduced to $c$. At every
declared schedule boundary, AdamW continues the existing moments and step counter
while using the new rate $\eta_s$; changing the rate does not restart the
optimizer.

For example, $\lVert g_s\rVert_2=1.4$ and $c=0.35$ give
$\alpha_s=0.25$. The trainer passes that one scalar with $\eta_s$ to AdamW.
For every parameter $p$ and coordinate $i$, the first-moment recurrence uses
$\widetilde g_{s,p,i}=\alpha_sg_{s,p,i}$, while the second-moment recurrence
uses $\widetilde g_{s,p,i}^2=\alpha_s^2g_{s,p,i}^2$. Thus the effective
gradient has global norm $0.35$, and the scale is applied before squaring. The
raw gradient tensors remain unchanged on the existing parameter leaves until
the trainer clears them after the update. The factor $\alpha_s$ does not scale
AdamW's separate weight-decay term.

Validation averages by predicted-token count, not by number of batches. If
validation batch $j$ contains $n_j$ target tokens and has mean loss
$\mathcal{L}^{(j)}_{va}$, then

$$
\mathcal{L}_{va}
=\frac{\sum_j n_j\mathcal{L}^{(j)}_{va}}{\sum_j n_j}.
$$

Selection ranges only over the measured checkpoint set
$\mathcal{C}=\{0,2,4,6,8\}$. On an exact tie, strict comparison retains the
earlier candidate. Equivalently,

$$
s^*=\min\left\{s\in\mathcal{C}:\mathcal{L}_{va}(\theta_s)
=\min_{k\in\mathcal{C}}\mathcal{L}_{va}(\theta_k)\right\}.
$$

{/* chapter-section:symbol-glossary */}

## Keep steps, gradients, and partitions distinct

- $\theta_0$ is the initialized decoder; $\theta_s$ is its state after update $s$.
- $s\geq1$ is a one-based update index; checkpoint indices also include $0$.
- $\mathcal{L}_{tr}^{(s)}$ is next-token loss for training mini-batch $s$.
- $g_s=\nabla_\theta\mathcal{L}_{tr}^{(s)}(\theta_{s-1})$ conceptually concatenates every finite named-parameter gradient coordinate before clipping.
- $\alpha_s$ is the single global clipping factor passed to AdamW; it is $1$ when no clipping is needed.
- $\widetilde g_s$ is the globally clipped gradient AdamW uses to update both moments.
- $c$ is the positive global-norm ceiling.
- $\eta_s$ is the predetermined learning rate for update $s$.
- $m_s$ and $v_s$ are Adam's continuing first- and second-moment states.
- $\mathcal{L}_{va}$ is graph-free validation loss used only for selection.
- $\mathcal{C}$ is the set of measured checkpoint indices.
- $s^*$ is the earliest measured validation minimum.

The names `Train`, `Validation`, and `Test` are concrete `Partition` variants in
the program. In the explanation, their roles are conceptual: training fits,
validation selects, and test supplies evidence after selection. This trainer
rejects `Test` throughout one training execution; Chapter 34's later one-use
count belongs to one local evaluator instance. A lower training loss cannot
substitute for held-out validation evidence.

Parameter identity connects the registry to the computation. Each registry
entry and its embedding, block, or normalization handle refer to the same
`TensorValue` node. AdamW commits a new tensor value into that existing node, so
all of those aliases observe the update without being rebuilt. The embedding
lookup and output projection also continue to share one tied node. Replacing a
registry entry with a different node would break this guarantee because the
component handles would still refer to the old node.

{/* chapter-section:history */}

## From training-only reports to validation-selected LLM checkpoints

Move from fitting and reporting one training state toward predetermined
mini-batch updates that produce periodic validation candidates while this
trainer rejects the separate test partition throughout the training execution.
These training practices form part of the road to modern LLMs.

[A Neural Probabilistic Language Model](https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf)
provides an early example of this separation in a neural language model. Bengio
and colleagues separate training, validation, and test text, explicitly
associate validation with model selection and early stopping, and describe
stochastic per-example parameter updates for a feed-forward neural language
model. Full-corpus or
per-example updates and training-set-only reporting do not by themselves define
a scalable update cadence or an independent rule for choosing among candidate
language-model states.

[Sequence to Sequence Learning with Neural Networks](https://arxiv.org/pdf/1409.3215)
adds practical sequence-training controls. Sutskever, Vinyals, and Le report
batches of sequences, a predetermined learning-rate reduction policy, and
rescaling when the global gradient norm crosses a fixed threshold in a
recurrent sequence model.

[Attention Is All You Need](https://arxiv.org/pdf/1706.03762) carries those
ideas into the Transformer era. Vaswani and colleagues train Transformers with
token-budgeted batches, a step-indexed warmup and inverse-square-root schedule,
and periodically written checkpoints. Their reported checkpoint averaging is
not the validation-minimum selection rule implemented here.

[Exploring the Limits of Transfer Learning with a Unified Text-to-Text Transformer](https://www.jmlr.org/papers/volume21/20-074/20-074.pdf)
explicitly describes validation-based checkpoint selection. Raffel and
colleagues save fine-tuning checkpoints at a fixed cadence, choose the one with
the best validation performance, and explicitly avoid using the test set for
model selection.

[Language Models are Few-Shot Learners](https://arxiv.org/pdf/2005.14165) shows
how these controls are used in large-scale decoder-only training. Brown and
colleagues carry Adam, scheduled learning rates, token-based batch scaling, and
global-gradient-norm clipping into decoder-only language-model training at GPT-3
scale.

Neural language-model work separated train, validation, and test
responsibilities; sequence and Transformer systems added mini-batches, explicit
schedules, clipping, and periodic candidates; later text-to-text work stated
that validation chooses a checkpoint so test data does not perform model
selection. Decoder-only LLM training repeatedly forms token batches,
differentiates the training objective, controls gradient magnitude, applies a
step schedule, and measures held-out validation candidates while reserving test
evidence for post-selection evaluation; this course's one-use count belongs to
one local evaluator instance. That count is not a repository-history claim.

These papers use different architectures and recipes. The course's fixed seed,
exact cadence, eight-step budget, and earliest-tie rule are local teaching
choices, not universal properties of LLM training. The small runnable contrast
shows why the selection signal matters: a training-only trace chooses its final
entry, while a held-out validation trace can choose an earlier one.

<RustSource
  path="rust/demos/ch33-training-selection/src/lib.rs"
  region="historical-selection-contrast"
  caption="Contrast selection by training loss with selection by a held-out validation trace"
  label="Rust source for the Chapter 33 LLM-training history contrast"
/>

{/* chapter-section:rust-implementation */}

## Represent the complete training plan explicitly in Rust

`LearningRateSchedule` stores exactly one finite positive rate per update.
`TrainerConfig` requires validation at $s=0$, strictly increasing checkpoint
steps, a final checkpoint after the last update, and one positive finite global
norm ceiling. The operation order is data, not an implication hidden inside a
loop.

<RustSource
  path="rust/crates/llm-from-scratch/src/training/trainer.rs"
  region="training-plan"
  caption="Validate the complete schedule, checkpoint cadence, clipping ceiling, and update order before execution"
  label="Rust source declaring the Chapter 33 training plan"
/>

Graph-free evaluation needs a real tape boundary. Calling `value` or `detach`
after a forward pass would be too late because the intermediate graph would
already exist. The thread-local `no_grad` scope suppresses parent edges as each
operation is created, supports nesting, and restores recording during unwinding.

<RustSource
  path="rust/crates/llm-from-scratch/src/autograd/tensor_core.rs"
  region="no-grad-scope"
  caption="Suppress reverse-mode parent edges in a nestable and panic-safe scope"
  label="Rust source implementing Chapter 33 graph-free evaluation support"
/>

`evaluate_no_grad` computes a token-weighted epoch mean and proves that every
result is untracked. It snapshots gradient bits before evaluation and rejects
the call if any bit changes.

<RustSource
  path="rust/crates/llm-from-scratch/src/training/trainer.rs"
  region="no-grad-evaluation"
  caption="Measure token-weighted loss without recording a graph or changing gradients"
  label="Rust source implementing Chapter 33 graph-free epoch evaluation"
/>

Before the first update, `train_decoder` must isolate its working decoder from
the model borrowed from its caller. `DecoderModelState::snapshot` therefore
copies every named parameter tensor once into graph-free state. The trainer
immediately calls `into_model` on that owned state: each name and tensor buffer
moves into the working decoder instead of being copied a second time. The
trainer also clones the caller's optimizer once. That working decoder and
optimizer then persist through all eight updates, so neither is reconstructed
for each mini-batch.

Later snapshots solve a different problem. When a validation checkpoint becomes
the new minimum, its parameter values must remain unchanged while later updates
continue to mutate the working decoder, so that minimum requires a deep snapshot.
At the end, `TrainingResult` keeps the selected graph-free state as the immutable
record of what validation chose and a separate decoder with the same values for
later evaluation. Because both must remain available,
`restore_independent_model` makes the one additional buffer copy.

<RustSource
  path="rust/crates/llm-from-scratch/src/training/trainer.rs"
  region="decoder-state-snapshot"
  caption="Copy buffers when two results must remain independently usable, then move buffers when owned state is consumed"
  label="Rust source defining the Chapter 33 decoder state-transfer boundary"
/>

After checking every raw gradient and computing one norm over all named
coordinates, the trainer derives one $\alpha_s$. It passes the working model's
existing parameter handles, $\eta_s$, and $\alpha_s$ to the same working
optimizer's `step_with_learning_rate_and_gradient_scale` method. The method
returns only the committed optimizer step number; Chapter 22's more detailed
trace is unnecessary inside this loop.

AdamW first validates the complete prospective update. Its first moments use
$\widetilde g_{s,p,i}$ and its second moments use
$\widetilde g_{s,p,i}^2$, so clipping is applied before the square. Decoupled
weight decay still uses the old parameter value and is not multiplied by
$\alpha_s$. AdamW prepares every next parameter tensor, both moment states, and
the step number before acquiring write access to all parameter values. A failure
during preparation or write acquisition leaves every parameter and optimizer
field unchanged. AdamW does hold each fully checked prospective tensor value
until the transaction can commit. Those prepared values are required optimizer
transaction state, not a replacement parameter vector owned by the trainer.

After all checks succeed, AdamW writes each prepared tensor into its existing
`TensorValue` node and commits the prepared optimizer state. Names, parameter
order, node identity, and the embedding/output tie do not change. The registry
and every decoder component already hold aliases of those nodes, so the next
forward pass observes the new values without rebuilding the decoder.

AdamW deliberately leaves each raw gradient tensor unchanged on its parameter
node. The trainer compares the returned optimizer step number with the planned
update index, calls `zero_grad()` on every live parameter, and verifies that
every gradient coordinate is zero before the next forward pass. This explicit
trainer operation prevents accumulation across mini-batches. No candidate
decoder, candidate optimizer, or replacement `Vec<NamedParameter>` is
constructed by the trainer for each update.

<RustSource
  path="rust/crates/llm-from-scratch/src/training/adamw.rs"
  region="adamw-execution-and-trace-api"
  caption="Apply the scheduled rate and one validated clipping factor without retaining a trace"
  label="Rust source defining the ordinary scaled AdamW update used in Chapter 33"
/>

`DecoderModel::from_parameters` remains a reconstruction boundary, not an
ordinary AdamW operation. `into_model` first moves each owned tensor buffer into
one new parameter leaf. `from_parameters` then borrows that stable-order leaf
list and applies the shared decoder-layout rules: valid configuration, exact
$2+9N$ tensor count, one required name at every list index, and the shape
required by each component. The layout check neither copies a tensor buffer nor
creates component handles. Passing it means that the leaf list has a valid
layout; it does not yet mean that live decoder aliases exist.

After validation, `from_parameters` gives the embedding, blocks, and final
RMSNorm shared handles to those leaves. Only this binding step re-establishes
one embedding node for both lookup and output projection. Cloning the handles
does not copy their tensor buffers. Reconstruction occurs when owned state
becomes a model; the trainer does not call this boundary after an ordinary
AdamW step.

<RustSource
  path="rust/crates/llm-from-scratch/src/models/decoder.rs"
  region="decoder-parameter-rebuild"
  caption="Bind moved parameter buffers into one decoder and re-establish every component alias"
  label="Rust source defining the Chapter 33 decoder parameter-binding boundary"
/>

`train_decoder` first rejects the wrong partitions and incompatible epochs. It
then creates the one isolated working model, executes all scheduled updates,
measures only declared checkpoints, and replaces the selected snapshot only on
strict loss improvement. After the final update, it returns the retained
selected snapshot together with its explicitly independent decoder. The
caller's model and optimizer remain unchanged even on failure.

<RustSource
  path="rust/crates/llm-from-scratch/src/training/trainer.rs"
  region="complete-training-loop"
  caption="Update one persistent decoder, snapshot strict validation improvements, and return the earliest minimum while this training execution rejects test data"
  label="Rust source implementing the complete Chapter 33 training loop"
/>

The deterministic fixture runs the complete plan twice, checks bitwise replay,
confirms clipping occurred, proves evaluation recorded no graphs, and verifies
that the selected model reproduces the saved parameter bits.

The exact-state checks need an owned vector of bit patterns, not owned copies of
the parameter tensors. `parameter_bits` temporarily reads each parameter tensor,
converts its scalar values to `u64`, and retains only the resulting bit vector
after the read ends.

<RustSource
  path="rust/demos/ch33-training-selection/src/lib.rs"
  region="learner-evidence"
  caption="Collect clipping, graph, ownership, selection, and replay evidence from two complete runs"
  label="Rust source collecting Chapter 33 learner evidence"
/>

<RustSource
  path="rust/demos/ch33-training-selection/src/main.rs"
  caption="Print only the frozen Chapter 33 learner report"
  label="Rust entry point for the Chapter 33 learner report"
/>

Run `cargo run --quiet --locked -p ch33-training-selection`. The report lists
all five measured checkpoints, the selected validation state, clipping,
preserved-node and explicit gradient-clear evidence, the rejected
test-partition probe, ownership checks, and bitwise replay.

{/* chapter-section:visualization */}

## Read measured checkpoints without inventing a curve

The recorded run contains the fixed configuration, the six-operation order
repeated for each of eight updates, four schedule segments, all eight update
records, an axis derived from the measured losses, five checkpoint pairs, one
selection record, and the verified boundaries. Together they show the
relationship between optimization and model selection without filling in
unmeasured steps.

<RustSource
  path="rust/demos/ch33-training-selection/src/diagram_trace.rs"
  region="training-selection-trace"
  caption="Record the measured updates, checkpoints, selection, and proof fields"
  label="Rust source producing the Chapter 33 training-selection evidence"
/>

<TrainingSelectionDiagram labels={diagramLabels} />

There are ten isolated markers: one training and one validation observation at
each of five checkpoints. No segment joins them. A line would imply values at
unmeasured steps, so the exact table remains the authoritative comparison.

At $s=0$, the losses are $2.095016$ and $1.918167$. At $s=8$, they are
$1.322897$ and $1.595297$. The double-underlined validation marker identifies
$s^*=8$ because $1.595297$ is the smallest measured validation loss. The
selection record also says `criterion=validation-only`, `snapshot=true`, and
`test_partition_rejected=true`.

Every update in this particular fixture has raw gradient norm above $0.35$ and
therefore reaches AdamW with norm $0.35$. That observation is fixture evidence,
not a requirement that all real LLM updates clip. The generic trainer also
accepts finite gradients already below the ceiling.

Each update record also says `nodes_preserved=true` and `cleared=true`. The
summary proof repeats the whole-run invariant as
`parameter_nodes_preserved=true` and `cleared_gradients=true`. The first field
means AdamW committed through the original parameter nodes; the second means the
trainer explicitly cleared their raw gradients afterward.

{/* chapter-section:exercises */}

## Test the information boundary and state ownership

1. Change the update epoch from `Train` to `Test`. Predict which preflight error
   occurs and why neither the caller's decoder nor optimizer may change.
2. Replace checkpoint steps $[0,2,4,6,8]$ with $[0,2,4,6]$. Explain why a
   final candidate is required after all planned updates.
3. Construct validation losses $[1.8,1.6,1.6]$. Predict which checkpoint strict
   comparison retains and relate it to the definition of $s^*$.
4. Average two batch means without token weights when their token counts differ.
   Compare that result with the token-weighted formula above.
5. Let AdamW commit new values into the existing parameter nodes. Explain why
   the registry, decoder components, and tied output projection all observe the
   update without a decoder rebuild.
6. Draw a line between the five validation markers. List the unmeasured claims
   that line introduces, then remove it and recover the discrete evidence.

<details>
  <summary>Check your reasoning</summary>

  1. `Test` is rejected before a forward pass because it cannot update or
     select.
  2. The checkpoint schedule must include the final planned update so every
     completed run has a final candidate.
  3. The first loss equal to the minimum wins because replacement uses strict
     less-than comparison.
  4. Batch means need weights proportional to their predicted-token counts.
  5. Those handles alias the same `TensorValue` nodes. Updating each node's value
     preserves the aliases, including the shared embedding/output node, so the
     next forward pass reads the committed values.
  6. Only the ten measured markers and exact table are justified; values
     between checkpoints remain unknown.
</details>

{/* chapter-section:decoder-connection */}

## Freeze the selected decoder before local test evaluation

The cumulative decoder can now execute a complete bounded training plan and
return a frozen validation-selected state. Chapter 34 will give one local
evaluator instance post-selection access to the test fixture and compare that
state fairly with the frozen baseline.

That next step must not tune the schedule, clipping ceiling, number of updates,
or checkpoint choice after seeing test results. Chapter 33 ends with model
selection complete and a verified rejection of `Test` at the training boundary.
Chapter 34's local evaluator will use its one access for evaluation rather than
selection. That count applies to one evaluator instance in one execution, not to
the repository's complete history of checking the fixture.
