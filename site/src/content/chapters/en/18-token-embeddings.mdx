---
{
  "chapter_id": "18-token-embeddings",
  "locale": "en",
  "content_revision": 8,
  "order": 18,
  "concept_id": "token-embeddings",
  "title": "Give token IDs trainable vectors",
  "description": "Build a trainable token table, validate public token IDs once, pass owned selectors through a private validated gather plan, and scatter-add repeated-token gradients.",
  "objective": "Gather trainable embedding rows for token IDs and scatter-add gradients for repeated IDs.",
  "worked_inputs": "Use the [4,2] table [[10,11],[20,21],[30,31],[40,41]] and token IDs [[2,1,2]]. Predict the [1,3,2] output, then seed its reverse pass with [[[1,0],[0,2],[3,4]]] and predict which table rows receive gradients.",
  "formula": {
    "latex": "X_{b,t,:}=E_{z_{b,t},:},\\quad \\bar{E}_{i,:}=\\sum_{(b,t):z_{b,t}=i}\\bar{X}_{b,t,:}",
    "symbols": [
      {
        "symbol": "E",
        "meaning": "the trainable token table with shape [V,d]"
      },
      {
        "symbol": "V",
        "meaning": "the vocabulary size and number of rows in E"
      },
      {
        "symbol": "d",
        "meaning": "the embedding width and number of features in each row"
      },
      {
        "symbol": "z_{b,t}",
        "meaning": "the integer token ID at batch index b and sequence position t"
      },
      {
        "symbol": "b",
        "meaning": "one leading batch index"
      },
      {
        "symbol": "t",
        "meaning": "one sequence-position index"
      },
      {
        "symbol": ":",
        "meaning": "every feature coordinate along the final axis"
      },
      {
        "symbol": "X_{b,t,:}",
        "meaning": "the selected width-d embedding vector at position (b,t)"
      },
      {
        "symbol": "\\bar{X}_{b,t,:}",
        "meaning": "the reverse-mode adjoint: the partial derivative of scalar loss L with respect to X at output position (b,t)"
      },
      {
        "symbol": "\\bar{E}_{i,:}",
        "meaning": "the reverse-mode adjoint: the partial derivative of scalar loss L with respect to every feature of table row i, accumulated across matching positions"
      },
      {
        "symbol": "i",
        "meaning": "one vocabulary-row index"
      },
      {
        "symbol": "\\sum_{(b,t):z_{b,t}=i}",
        "meaning": "the sum over every batch and sequence position whose token ID selects row i"
      }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "language-model",
      "limitation": "A sparse one-hot word representation assigns one coordinate to each vocabulary item but expresses no graded similarity between words; explicitly carrying that vocabulary-wide vector also wastes work when only one row is needed.",
      "later_advance": "Bengio et al. learn one dense word-feature matrix jointly with a neural next-word model and reuse that matrix across context positions; each word index still selects its corresponding row. The Transformer retains learned token embeddings for subword tokens, then adds positional information before its stacked attention and feed-forward computations.",
      "modern_llm_role": "One trainable vocabulary-by-feature matrix is reused at every batch item and sequence position. Each token ID selects its own vocabulary row: different IDs select different rows, while repeated occurrences of the same ID select the same row and accumulate gradients there. Reusing this input matrix across positions is separate from output-weight tying, which remains a later concern alongside positional information, embedding forward scaling, and attention.",
      "sources": [
        {
          "role": "earlier",
          "year": 2003,
          "name": "Bengio et al., A Neural Probabilistic Language Model",
          "source_url": "https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf",
          "claim": "Bengio et al. represent the mapping from a vocabulary word index to distributed features as a trainable matrix with one row per vocabulary item and one column per learned feature, share it across context positions, and learn it jointly with next-word prediction."
        },
        {
          "role": "later",
          "year": 2017,
          "name": "Vaswani et al., Attention Is All You Need",
          "source_url": "https://papers.nips.cc/paper_files/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf",
          "claim": "Vaswani et al. use learned embeddings whose width matches the model width for BPE or word-piece tokens and add positional encodings before the Transformer stack; their embedding forward scaling is separate from parameter initialization."
        }
      ]
    },
    "approach": "From sparse vocabulary-wide indicators to learned distributed token vectors and direct table lookup",
    "summary": "One-hot vectors make token identity explicit but carry a vocabulary-sized field of zeros. Learned dense word features let neural language models share statistical strength, and Transformers keep learned token embeddings as the numeric entrance to deeper sequence computation. One embedding matrix is reused across positions without merging its token rows: different IDs select different rows, while repeated occurrences of one ID reuse its row and add their gradients there.",
    "rust_source": "rust/demos/ch18-token-embeddings/src/lib.rs"
  },
  "rust_sources": [
    {
      "path": "rust/demos/ch18-token-embeddings/src/lib.rs",
      "region": "one-hot-baseline",
      "purpose": "Materialize the tiny one-hot baseline and multiply it by the table for an algebraic comparison."
    },
    {
      "path": "rust/demos/ch18-token-embeddings/src/lib.rs",
      "region": "known-token-lookup",
      "purpose": "Run the exact repeated-ID lookup and compare it with the one-hot baseline."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/nn/embedding.rs",
      "region": "embedding-errors",
      "purpose": "Declare deterministic construction, token-shape, count, bounds, conversion, and delegated failures."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/nn/embedding.rs",
      "region": "embedding-layer",
      "purpose": "Construct one named trainable table whose private shape invariants support embedding lookup."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/nn/embedding.rs",
      "region": "embedding-forward-boundary",
      "purpose": "Validate the public u32 token boundary once, convert selectors, and hand an owned sealed plan to the shared gather kernel."
    },
    {
      "path": "rust/demos/ch18-token-embeddings/src/lib.rs",
      "region": "repeated-token-gradient",
      "purpose": "Reverse the exact nonuniform output seed and retrieve the shared table gradient."
    },
    {
      "path": "rust/demos/ch18-token-embeddings/src/lib.rs",
      "region": "initialized-token-embedding",
      "purpose": "Compose the embedding layer with Chapter 17 initialization and verify reproducibility and clone identity."
    },
    {
      "path": "rust/demos/ch18-token-embeddings/src/lib.rs",
      "region": "embedding-edge-cases",
      "purpose": "Exercise an empty token shape and reject the first out-of-range token ID."
    },
    {
      "path": "rust/demos/ch18-token-embeddings/src/main.rs",
      "region": "learner-token-embeddings-output",
      "purpose": "Print the exact selected output and shared table gradient produced by the worked example."
    },
    {
      "path": "rust/demos/ch18-token-embeddings/src/diagram_trace.rs",
      "region": "token-embeddings-trace",
      "purpose": "Record every table row, one-hot indicator, selected output, upstream contribution, and accumulated row gradient."
    }
  ],
  "visualization": {
    "decision": "useful",
    "id": "token-embeddings",
    "rationale": "Aligning each one-hot indicator with its selected table row and output vector makes row reuse visible, while a separate reverse rail shows two position gradients converging on the same trainable row."
  },
  "decoder_connection": "The cumulative model can now turn token-ID tensors into differentiable feature tensors that append one embedding-width axis while keeping one shared named vocabulary-by-feature parameter. Chapter 19 treats that final embedding width as its input width and mixes features with a learned projection; lookup selects rows, while a linear layer combines coordinates."
}
---

import TokenEmbeddingsDiagram from '../../../components/chapters/TokenEmbeddingsDiagram.astro';

export const diagramLabels = {
  title: 'Follow different and repeated token IDs through one shared table',
  description:
    'Read the one-hot and direct-lookup equivalence as ID 1 selects row 1 and ID 2 selects row 2, then follow both occurrences of ID 2 as their reverse contributions add in that same row of one shared embedding table.',
  summary: {
    parameter: 'Named parameter',
    vocabulary: 'Vocabulary rows',
    width: 'Embedding width',
    tableShape: 'Table shape',
    idShape: 'Token-ID shape',
    outputShape: 'Output shape',
    gradientShape: 'Table-gradient shape',
  },
  stages: {
    ids: 'Start with integer token IDs',
    table: 'Keep one shared trainable table',
    lookup: 'Select rows without storing the zeros',
    gradients: 'Return contributions to their shared rows',
  },
  fields: {
    position: 'Position',
    tokenId: 'Token ID',
    row: 'Table row',
    uses: 'Selections',
    state: 'Row status',
    tableValues: 'Trainable vector',
    oneHot: 'One-hot indicator',
    operation: 'Equivalent product',
    selectedRow: 'Selected row',
    output: 'Output vector',
    upstream: 'Upstream gradient',
    positions: 'Contributing flat positions',
    contributions: 'Feature-wise contributions',
    rule: 'Reverse rule',
    accumulated: 'Stored row gradient',
  },
  states: {
    unused: 'Unused row',
    selectedOnce: 'Selected once',
    selectedRepeated: 'Shared by the repeated token ID',
    singleRow: 'Selects one singly used row',
    repeatedRow: 'Shares one row with another position',
    unusedZero: 'No selection; keep zero',
    singleCopy: 'One selection; copy its contribution',
    repeatedSum: 'Repeated selections; add both contributions',
    none: 'None',
  },
  notes: {
    oneHot:
      'The indicator exposes the algebra: one active coordinate selects one table row. The implementation performs direct lookup.',
    selectors:
      'Different IDs name different rows of one parameter matrix and stay outside the differentiation tape. Repeating an ID reuses that ID’s row.',
    accumulation:
      'Reverse mode returns each upstream feature vector to its selected row. Only the repeated row receives two vectors and sums them.',
  },
  symbols: {
    unused: '○',
    selectedOnce: '●',
    selectedRepeated: '◆',
  },
  scrollers: {
    ids: 'Scrollable token-ID positions',
    table: 'Scrollable embedding table',
    lookup: 'Scrollable one-hot and lookup evidence',
    gradients: 'Scrollable table-gradient accumulation evidence',
  },
};

{/* chapter-section:worked-example */}

## Predict three selected rows and one shared gradient

Use this trainable table, whose four vocabulary rows each have width $2$:

$$
E=\begin{bmatrix}
10 & 11 \\
20 & 21 \\
30 & 31 \\
40 & 41
\end{bmatrix}
$$

Before running anything, read the IDs $[[2,1,2]]$ from left to right. The first
and third positions both select row $2$; the middle position selects row $1$.
The predicted output is therefore $[[[30,31],[20,21],[30,31]]]$, with shape
$[1,3,2]$. Repetition copies a value into two positions, but it does not create
a second trainable row.

The same matrix $E$ serves all three positions. ID $1$ and ID $2$ select
different parameter rows within that matrix, while the two occurrences of ID
$2$ both select row $2$. Sharing the table across positions does not make the
rows for different token identities the same parameter coordinates or values.

One-hot notation makes the selection visible. For example, ID $2$ corresponds
to $[0,0,1,0]$. Multiplying that indicator by $E$ leaves $[30,31]$. The runnable
contrast materializes those zeros only for this tiny explanation:

<RustSource
  path="rust/demos/ch18-token-embeddings/src/lib.rs"
  region="one-hot-baseline"
  caption="Multiply explicit one-hot rows by the tiny table as an algebraic baseline"
  label="Rust source for the Chapter 18 one-hot multiplication contrast"
/>

The actual layer selects the row directly and verifies that both routes produce
the same exact values:

<RustSource
  path="rust/demos/ch18-token-embeddings/src/lib.rs"
  region="known-token-lookup"
  caption="Gather the repeated IDs and compare exact output values with the baseline"
  label="Rust source running the Chapter 18 known token-embedding lookup"
/>

{/* chapter-section:formula */}

## Select forward, accumulate backward

The complete lookup and reverse rule is:

$$
X_{b,t,:}=E_{z_{b,t},:},\quad \bar{E}_{i,:}=\sum_{(b,t):z_{b,t}=i}\bar{X}_{b,t,:}
$$

The forward half copies one selected row into each output position. Let $L$ be
the scalar loss. An overbar is reverse-mode shorthand:
$\bar{X}_{b,t,:}=\partial L/\partial X_{b,t,:}$ is the gradient arriving
at an output position, while
$\bar{E}_{i,:}=\partial L/\partial E_{i,:}$ is the gradient of table row
$i$. The reverse half follows the forward selections in the opposite direction.
If several positions selected the same row, their upstream vectors add feature
by feature into that one shared parameter row.

For the declared seed $[[[1,0],[0,2],[3,4]]]$, row $1$ receives $[0,2]$ and row
$2$ receives $[1,0]+[3,4]=[4,4]$. Unused rows $0$ and $3$ receive zero. Token IDs
receive no gradient because they are discrete selectors, not tape operands.

{/* chapter-section:symbol-glossary */}

## Keep vocabulary axes separate from feature axes

- $E$ is the trainable token table with shape $[V,d]$.
- $V$ is the vocabulary size and therefore the number of rows in $E$.
- $d$ is the embedding width and therefore the number of features in each row.
- $z_{b,t}$ is the integer token ID at batch index $b$ and sequence position $t$.
- $b$ identifies one batch item; $t$ identifies one position in its sequence.
- $:$ means every coordinate on the final feature axis.
- $X_{b,t,:}$ is the selected width-$d$ vector at position $(b,t)$.
- $\bar{X}_{b,t,:}=\partial L/\partial X_{b,t,:}$ is the upstream
  gradient vector arriving at that output position.
- $\bar{E}_{i,:}=\partial L/\partial E_{i,:}$ is the table-row gradient
  after contributions from every matching position have been accumulated.
- $i$ is one vocabulary-row index.
- The sum visits every $(b,t)$ whose $z_{b,t}$ equals $i$.

The numeric distance between IDs is not a semantic distance. IDs $1$ and $2$
are adjacent only in the tokenizer's numbering; training determines whether
their learned vectors become similar.

{/* chapter-section:history */}

## From sparse identity to the vector entrance of a Transformer

A sparse one-hot word representation assigns one coordinate to each vocabulary item but expresses no graded similarity between words; explicitly carrying that vocabulary-wide vector also wastes work when only one row is needed.

[Bengio et al., *A Neural Probabilistic Language Model*](https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf): Bengio et al. represent the mapping from a vocabulary word index to distributed features as a trainable matrix with one row per vocabulary item and one column per learned feature, share it across context positions, and learn it jointly with next-word prediction.

Bengio et al. learn one dense word-feature matrix jointly with a neural next-word model and reuse that matrix across context positions; each word index still selects its corresponding row. The Transformer retains learned token embeddings for subword tokens, then adds positional information before its stacked attention and feed-forward computations.

[Vaswani et al., *Attention Is All You Need*](https://papers.nips.cc/paper_files/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf): Vaswani et al. use learned embeddings whose width matches the model width for BPE or word-piece tokens and add positional encodings before the Transformer stack; their embedding forward scaling is separate from parameter initialization.

One trainable vocabulary-by-feature matrix is reused at every batch item and sequence position. Each token ID selects its own vocabulary row: different IDs select different rows, while repeated occurrences of the same ID select the same row and accumulate gradients there. Reusing this input matrix across positions is separate from output-weight tying, which remains a later concern alongside positional information, embedding forward scaling, and attention.

One-hot vectors make token identity explicit but carry a vocabulary-sized field
of zeros. Learned dense word features let neural language models share
statistical strength, and Transformers keep learned token embeddings as the
numeric entrance to deeper sequence computation. One embedding matrix is reused
across positions without merging its token rows: different IDs select different
rows, while repeated occurrences of one ID reuse its row and add their gradients
there.

The essential progression is from sparse identity codes to learned vectors.
Parameter sharing here means that every lookup site uses the same matrix, not
that distinct token IDs alias one row. Even if two rows temporarily contain
equal numbers, their token IDs still address different rows and parameter
coordinates. Repeated occurrences of one token reuse its row. A concrete implementation may choose
different integer types, storage layouts, initializers, and error conventions
without changing the lookup or reverse-mode equations. The one-hot product is
an explanatory identity, not a claim that either paper stores those sparse
vectors.

{/* chapter-section:rust-implementation */}

## Validate token IDs once, then reuse the gather rule

The layer reports table, token-shape, count, bounds, conversion-allocation, and
delegated autodiff failures without partial output:

<RustSource
  path="rust/crates/llm-from-scratch/src/nn/embedding.rs"
  region="embedding-errors"
  caption="Keep embedding construction and selector failures typed and deterministic"
  label="Rust source declaring Chapter 18 embedding errors"
/>

Public callers obtain an `Embedding` through `new`, `from_parameter`, or by
cloning an already validated layer. The constructors establish a nonempty
rank-two table, cloning preserves it, and private fields prevent callers from
replacing it. The construction code records the table's vocabulary size $V$
and embedding width $d$ once:

<RustSource
  path="rust/crates/llm-from-scratch/src/nn/embedding.rs"
  region="embedding-layer"
  caption="Construct one named rank-two table and retain its vocabulary and feature widths"
  label="Rust source constructing the Chapter 18 token-embedding layer"
/>

`Embedding::forward` is the public boundary for token IDs supplied as `u32`.
Because construction has already established the table shape $[V,d]$, the
method performs these checks in a fixed order:

1. compute the number of positions described by `token_shape`, rejecting an
   invalid or overflowing shape;
2. require `token_ids.len()` to equal that position count; and
3. scan the IDs in flat order, rejecting the first `u32` value that cannot name
   one of the table's $V$ rows.

Only after all IDs pass does the method reserve an owned `Vec<usize>` and convert
the selectors. After the tape establishes that the table operand is available,
the method creates Chapter 16's crate-private `RowGatherPlan`. The plan owns the
converted selectors and their logical shape and derives the output shape. Its
trusted constructor does not rescan table rank, selector count, or selector
bounds. The constructor-or-clone invariant supplies the table rank, while this
`forward` call has just established the selector shape, count, and bounds:

<RustSource
  path="rust/crates/llm-from-scratch/src/nn/embedding.rs"
  region="embedding-forward-boundary"
  caption="Check each raw token-ID fact once and pass owned selectors through the validated plan"
  label="Rust source implementing the Chapter 18 validated token-ID handoff"
/>

This trust is narrow. The public `TensorValue::gather_rows` method still checks
table rank, selector shape, selector count, and the first out-of-range selector
for callers that supply raw inputs. A valid plan also does not promise that
memory is already available: allocating the output buffer can still fail. Once
allocation succeeds, the shared kernel performs the row copies and saves the
same plan facts for reverse scatter-add. The embedding layer therefore avoids a
second selector-validation scan without duplicating either the lookup or the VJP.
Only ownership and reuse of checked facts change; output values and shapes,
failure precedence, the lookup equation, saved VJP facts, and repeated-row
gradient addition stay the same.

The exact nonuniform reverse seed makes repeated accumulation observable:

<RustSource
  path="rust/demos/ch18-token-embeddings/src/lib.rs"
  region="repeated-token-gradient"
  caption="Reverse through the repeated lookup and read the stored table gradient"
  label="Rust source proving repeated token-gradient accumulation"
/>

Construction composes with Chapter 17's shape-based matrix initializer by
passing $V$ and $d$ as the table's two dimensions. That is an explicit
initialization convention, not something derived from embedding lookup or
required by the Transformer paper. Another implementation can choose a different
initializer without changing the forward or reverse equations. The same seed
and request reproduce values, while separate constructions remain distinct
leaves. Cloning one layer deliberately preserves its parameter identity:

<RustSource
  path="rust/demos/ch18-token-embeddings/src/lib.rs"
  region="initialized-token-embedding"
  caption="Initialize the table reproducibly and check clone identity"
  label="Rust source connecting Chapter 17 initialization to Chapter 18 embeddings"
/>

The executable example also checks an empty ID shape and the first out-of-bounds ID:

<RustSource
  path="rust/demos/ch18-token-embeddings/src/lib.rs"
  region="embedding-edge-cases"
  caption="Accept an empty token shape and reject the first out-of-range ID"
  label="Rust source checking Chapter 18 empty input and bounds behavior"
/>

Its printed evidence contains the selected rows and accumulated table gradient.
A scalar ID shape $[]$ produces shape $[2]$; empty leading shapes append the
width without producing values. Shape and count errors are reported before the
first invalid selector. An all-coordinate finite-difference probe with step
$10^{-6}$ agrees with the analytic table gradient within absolute tolerance
$2\times10^{-6}$.

<RustSource
  path="rust/demos/ch18-token-embeddings/src/main.rs"
  region="learner-token-embeddings-output"
  caption="Print the selected vectors and the shared embedding-table gradient"
  label="Rust source displaying the Chapter 18 learner evidence"
/>

{/* chapter-section:visualization */}

## Follow every selection back to its shared row

The forward half aligns each position with its token ID, one-hot identity,
selected table row, and output vector. The reverse half aligns each upstream
vector with the row that receives it. Row $2$ appears at two positions, so the
diagram makes the difference visible: its value is copied to two outputs in the
forward pass, while both gradient contributions add into one parameter row in
the reverse pass.

<RustSource
  path="rust/demos/ch18-token-embeddings/src/diagram_trace.rs"
  region="token-embeddings-trace"
  caption="Collect the worked example's table rows, selections, and accumulated gradients"
  label="Rust source assembling the Chapter 18 token-embedding evidence"
/>

<TokenEmbeddingsDiagram labels={diagramLabels} />

Read the forward and reverse halves together. The repeated state is marked by
words, a diamond, and a double rule rather than color alone, so row sharing stays
visible independently of the color scheme.

{/* chapter-section:exercises */}

## Predict before checking the executable evidence

1. Predict all six output values for IDs $[[2,1,2]]$.
2. Write the length-four one-hot row for ID $2$ and multiply it by $E$.
3. Predict every table-gradient value for the declared upstream seed.
4. Explain why row $2$ receives a sum rather than two independent parameter gradients.
5. Predict the output shapes for ID shapes $[2,3]$, $[]$, and $[0]$ when $d=2$.
6. Put an invalid token shape, an ID-count mismatch, and an out-of-range ID in reporting order; then find the first invalid ID in $[1,4,9]$ for a four-row table.
7. Explain why `Embedding::forward` may hand its checked IDs to a trusted plan while the public generic gather method must validate raw selectors. What allocation can still fail after the plan exists?
8. Predict whether cloning an embedding layer creates a new trainable leaf.
9. Explain why IDs receive no gradient and why adjacent ID numbers imply no semantic similarity.
10. Decide whether Bengio et al. require an explicitly materialized one-hot implementation.
11. Decide whether a repeated occurrence owns a new embedding parameter.

<details>
  <summary>Check the predictions</summary>

  1. The output is $[[[30,31],[20,21],[30,31]]]$.
  2. ID $2$ is $[0,0,1,0]$, so multiplying it by $E$ yields $[30,31]$.
  3. The table gradient is $[[0,0],[0,2],[4,4],[0,0]]$.
  4. Both occurrences selected the same row-$2$ leaf, so $[1,0]$ and $[3,4]$ add into it.
  5. The output shapes are $[2,3,2]$, $[2]$, and $[0,2]$.
  6. The method checks the token shape first, the exact ID count second, and the IDs in flat order third. ID $4$ at flat position $1$ is the first invalid selector because valid rows are $0$ through $3$.
  7. Every `Embedding` value contains the rank-two table established by its public constructors, and `forward` has just checked shape, count, and bounds before converting the IDs. The trusted plan owns those facts. A generic public caller has established none of them, so `TensorValue::gather_rows` must perform its complete validation. Allocating the output tensor's value buffer can still fail after the plan exists.
  8. No. A clone is another handle to the same named table leaf.
  9. IDs are discrete selectors outside the tape, and their numeric assignment carries no geometry.
  10. No. One-hot multiplication is used here only as an algebraic explanation.
  11. No. Repetition reuses one table row; later position handling distinguishes occurrences without creating occurrence-specific embedding parameters.
</details>

{/* chapter-section:decoder-connection */}

## Hand the final feature axis to a learned projection

The cumulative model can now turn token-ID tensors into differentiable feature tensors that append one embedding-width axis while keeping one shared named vocabulary-by-feature parameter. Chapter 19 treats that final embedding width as its input width and mixes features with a learned projection; lookup selects rows, while a linear layer combines coordinates.

This is the decoder's numeric entrance, not yet a position-aware representation.
Reusing this input table at every position is not output-weight tying. Chapter
32 later adds that separate relationship by also using the embedding matrix for
the vocabulary projection.
Chapter 19 preserves all leading batch and sequence axes while applying a learned
matrix to the final feature axis. Later chapters make attention position-aware
by rotating projected queries and keys with RoPE; repeated occurrences still
share one embedding-table row.
