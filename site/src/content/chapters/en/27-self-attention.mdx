---
{
  "chapter_id": "27-self-attention",
  "locale": "en",
  "content_revision": 2,
  "order": 27,
  "concept_id": "scaled-dot-product-self-attention",
  "title": "Compute one unmasked self-attention head",
  "description": "Learn how one unmasked Transformer self-attention head scores queries against keys, normalizes each row, and mixes values with inspectable Rust evidence.",
  "objective": "Compute one unmasked attention head and explain every score, probability, and weighted value.",
  "worked_inputs": "Reuse Chapter 26's projected tensors $Q=[[[0,3],[2,-1]]]$, $K=[[[3,0],[-1,2]]]$, and $V=[[[3,-3],[1,3]]]$. Predict which value row each query will favor before running the worked example.",
  "formula": {
    "latex": "A=\\operatorname{softmax}\\left(\\frac{QK^\\top}{\\sqrt{d_k}}\\right),\\quad O=AV",
    "symbols": [
      { "symbol": "Q", "meaning": "the query tensor whose row at each token position asks what content to retrieve" },
      { "symbol": "K", "meaning": "the key tensor whose rows are compared with every query row" },
      { "symbol": "d_k", "meaning": "the shared query and key feature width used to scale their dot products" },
      { "symbol": "A", "meaning": "the attention-probability tensor obtained by normalizing each query row over key positions" },
      { "symbol": "V", "meaning": "the value tensor whose rows carry the content mixed by the attention probabilities" },
      { "symbol": "O", "meaning": "the output tensor containing one probability-weighted value mixture per query position" }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "neural-architecture",
      "limitation": "A basic recurrent encoder-decoder can force an entire source sentence through one fixed-size vector, while additive attention still advances recurrently and computes a new source alignment at each output step.",
      "later_advance": "The Transformer combines many queries into matrices and uses scaled dot-product self-attention to compare positions in the same available sequence, allowing one layer to form its full score grid with batched matrix operations.",
      "modern_llm_role": "Each decoder self-attention head turns learned queries and keys into row-normalized attention weights, then mixes learned values; a causal decoder additionally masks future key positions before normalization.",
      "sources": [
        {
          "role": "earlier",
          "year": 2014,
          "name": "Bahdanau, Cho, and Bengio, Neural Machine Translation by Jointly Learning to Align and Translate",
          "source_url": "https://arxiv.org/abs/1409.0473",
          "claim": "Bahdanau, Cho, and Bengio describe the possible fixed-length-vector bottleneck of a basic encoder-decoder and compute each new context as a softmax-weighted sum of encoder annotations scored against the previous decoder state."
        },
        {
          "role": "later",
          "year": 2017,
          "name": "Vaswani et al., Attention Is All You Need",
          "source_url": "https://arxiv.org/abs/1706.03762",
          "claim": "Vaswani et al. define scaled dot-product attention as a softmax-normalized matrix of scaled query-key dot products applied to values, combine simultaneous queries in a matrix, and define self-attention as relating positions within one sequence."
        }
      ]
    },
    "approach": "From fixed recurrent context through additive encoder-decoder alignment to scaled dot-product self-attention",
    "summary": "Additive attention replaced one fixed source summary with decoder-dependent retrieval from encoder annotations. Scaled dot-product self-attention lets every position issue a query while every available position can contribute a value, forms a matrix of compatibilities, normalizes each query row, and mixes the value rows. This chapter leaves every position visible; Chapter 28 adds causal visibility.",
    "rust_source": "rust/demos/ch27-self-attention/src/lib.rs"
  },
  "rust_sources": [
    {
      "path": "rust/demos/ch27-self-attention/src/lib.rs",
      "region": "historical-attention-contrast",
      "purpose": "Contrast separate decoder-to-encoder alignment sources with all query-key pairs drawn from one sequence."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/attention/self_attention.rs",
      "region": "self-attention-forward",
      "purpose": "Compose one unmasked scaled dot-product attention head from cumulative differentiable tensor operations."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/attention/self_attention.rs",
      "region": "self-attention-errors",
      "purpose": "Expose typed input and forward-stage boundaries with deterministic precedence."
    },
    {
      "path": "rust/demos/ch27-self-attention/src/lib.rs",
      "region": "self-attention-fixture",
      "purpose": "Build the worked forward, reverse, scale, shape, boundary, gradient-check, and replay evidence."
    },
    {
      "path": "rust/demos/ch27-self-attention/src/main.rs",
      "purpose": "Run the complete self-attention example and print its computed evidence."
    }
  ],
  "visualization": {
    "decision": "useful",
    "id": "self-attention",
    "rationale": "Following one query across the score row, normalized probabilities, weighted value terms, and output makes the retrieval calculation and its row-wise normalization visible."
  },
  "decoder_connection": "The cumulative decoder can now turn one projected query/key/value triplet into the output of an unmasked attention head. Chapter 28 will exclude future key positions before each score row is normalized."
}
---

import RustSource from '../../../components/RustSource.astro';
import SelfAttentionDiagram from '../../../components/chapters/SelfAttentionDiagram.astro';

export const diagramLabels = {
  title: 'Follow every score into a weighted value mixture',
  description:
    'Follow exact query, key, value, score, probability, mixture, gradient, shape, history, and rejected-boundary evidence through one unmasked attention head.',
  sections: {
    calculation: 'Trace one complete unmasked attention calculation',
    evidence: 'Check gradients, shapes, and rejected boundaries',
    history: 'Follow the neural-attention path toward modern LLMs',
  },
  stages: {
    inputs: 'Start with query, key, and value rows',
    dotProducts: 'Compare every query with every key',
    scaledScores: 'Scale the score grid',
    probabilities: 'Normalize each query row over keys',
    mixtures: 'Mix value rows into outputs',
  },
  fields: {
    shape: 'Shape',
    scale: 'Attention scale',
    gradientTolerance: 'Gradient-check tolerance',
    softmaxAxis: 'Normalization axis',
    mask: 'Visibility boundary',
    rowSum: 'Probability-row check',
    weightedTerms: 'Already-weighted value terms',
    output: 'Output row',
    backward: 'Reverse evidence',
    batchShape: 'Batch isolation and shapes',
    singleToken: 'One token has no competing key',
    errors: 'Rejected input boundaries',
    proof: 'Numerical checks',
    checkCount: 'Gradient coordinates checked',
    batchIsolation: 'Batch independence',
    queryGradient: 'Query gradient',
    keyGradient: 'Key gradient',
    replay: 'Deterministic replay',
    gradientCheck: 'Gradient check',
    errorKind: 'Error kind',
    errorEvidence: 'Rejected shape evidence',
    queryRowsKeyColumns: 'Query rows and key columns',
    tokenRowsFeatureColumns: 'Token rows and feature columns',
    earlier: 'Fixed recurrent context',
    bridge: 'Additive encoder-decoder alignment',
    transformer: 'Scaled dot-product self-attention',
  },
  roles: {
    query: 'Query rows: what should each position retrieve?',
    key: 'Key rows: how can each position be matched?',
    value: 'Value rows: what content can each position contribute?',
  },
  cues: {
    query: 'Solid query border',
    key: 'Dashed key border',
    value: 'Double value border',
    score: 'Dotted score cue',
    probability: 'Double-line probability cue',
    verified: 'Verified',
    rejected: 'Rejected',
    unmasked: 'Every key position is visible',
  },
  errorReasons: {
    queryRank: 'The query input must expose batch, token, and feature axes.',
    batchMismatch: 'Query, key, and value tensors must have the same batch size.',
    tokenMismatch: 'Query, key, and value tensors must have the same token count.',
    emptyTokens: 'Attention needs at least one key position to normalize each row.',
    queryKeyWidth: 'Query and key feature widths must match for their dot products.',
  },
  historyDetails: {
    earlier: 'One fixed-size source vector is reused across the recurrent decoder steps.',
    bridge: 'Each decoder step retrieves a new weighted context from encoder annotations.',
    transformer: 'One layer forms a score for every query-key position pair in the available sequence.',
    comparison: 'Every available key position is permitted for every query in this unmasked head.',
  },
  captions: {
    calculation: 'Rows and columns preserve the two-token calculation exactly; borders and text carry every distinction without color.',
    evidence: 'The gradients, shapes, and boundary cases show what the same attention operation preserves and rejects.',
    history: 'The comparison describes model structure, not programming-language history or a hardware benchmark.',
  },
  scrollers: {
    inputs: 'Query, key, and value input matrices',
    scores: 'Scores before and after scaling',
    probabilities: 'Attention probability rows',
    mixtures: 'Weighted value mixtures',
    gradients: 'Attention gradient table',
    history: 'Neural-attention history comparison',
  },
};

{/* chapter-section:worked-example */}

## Predict which value each query will retrieve

Continue directly from Chapter 26. Its three projections produced

$$
Q=\begin{bmatrix}0&3\\2&-1\end{bmatrix},\quad
K=\begin{bmatrix}3&0\\-1&2\end{bmatrix},\quad
V=\begin{bmatrix}3&-3\\1&3\end{bmatrix}.
$$

There is one batch with two token positions. The query/key feature width and
value width are both two, so $Q,K,V\in\mathbb{R}^{1\times2\times2}$.

Before calculating a softmax, predict which value each query will retrieve.
The first query produces the two dot products

$$
q_0K^\top=[0,6],
$$

so it should favor $v_1=[1,3]$. The second produces

$$
q_1K^\top=[6,-4],
$$

so it should strongly favor $v_0=[3,-3]$. The signs and relative gaps matter;
normalization will turn each score row into retrieval weights.

{/* chapter-section:formula */}

## Score, scale, normalize, and mix

One unmasked scaled dot-product attention head follows

$$
A=\operatorname{softmax}\left(\frac{QK^\top}{\sqrt{d_k}}\right),\quad O=AV
$$

For this worked example, the score matrices before and after scaling are

$$
QK^\top=\begin{bmatrix}0&6\\6&-4\end{bmatrix},\qquad
\frac{QK^\top}{\sqrt{2}}\approx
\begin{bmatrix}0&4.242641\\4.242641&-2.828427\end{bmatrix}.
$$

Softmax runs across key positions independently for each query:

$$
A\approx
\begin{bmatrix}
0.014166&0.985834\\
0.999151&0.000849
\end{bmatrix},\qquad
\sum_j A_{ij}=1.
$$

The first weighted value mixture is

$$
o_0
=0.014166[3,-3]+0.985834[1,3]
\approx[1.028332,2.915004].
$$

The second is

$$
o_1
=0.999151[3,-3]+0.000849[1,3]
\approx[2.998303,-2.994908].
$$

Thus

$$
O\approx
\begin{bmatrix}
1.028332&2.915004\\
2.998303&-2.994908
\end{bmatrix}.
$$

{/* chapter-section:symbol-glossary */}

## Keep query rows and key columns separate

- $Q$ contains one query row per token position: what should this position retrieve?
- $K$ contains the candidate key rows: how can each position be matched?
- $d_k$ is the shared query/key width.
- $A$ contains one normalized retrieval row for every query.
- $V$ contains the value rows whose content is mixed.
- $O$ contains one resulting mixture per query position.
- $B$ is the batch size, $T$ the token count, and $d_v$ the value width.

The complete shape rule is

$$
Q,K\in\mathbb{R}^{B\times T\times d_k},\quad
V\in\mathbb{R}^{B\times T\times d_v},\quad
A\in\mathbb{R}^{B\times T\times T},\quad
O\in\mathbb{R}^{B\times T\times d_v}.
$$

For fixed batch $b$ and query position $i$, the key position $j$ varies inside
one probability row:

$$
S_{bij}=\frac{q_{bi}\cdot k_{bj}}{\sqrt{d_k}},\qquad
A_{bij}=\frac{\exp(S_{bij})}{\sum_{r=0}^{T-1}\exp(S_{bir})}.
$$

That row sum makes $A_{bij}$ a retrieval weight. It is not a calibrated
probability that a token or claim is correct.

Vaswani et al. motivate the denominator under a specific assumption: if query
and key components are independent with mean zero and variance one, their dot
product has variance $d_k$. Under those assumptions, dividing by $\sqrt{d_k}$
keeps the variance of the softmax input from growing with $d_k$. This is not a
universal theorem, an overflow guarantee, or proof that the scale is an optimal
temperature.

A two-coordinate probe makes the effect concrete. For the scores $[1,0]$, the
favored weight without scaling is

$$
p_{\mathrm{unscaled}}=\frac{e^1}{e^1+e^0}\approx0.731059.
$$

With $d_k=2$, the same weight after square-root scaling is

$$
p_{\mathrm{scaled}}
=\frac{e^{1/\sqrt{2}}}{e^{1/\sqrt{2}}+e^0}
\approx0.669762.
$$

Scaling therefore softens this particular distribution; the assumptions above
explain why the denominator is useful, not how sharp every attention row must be.

{/* chapter-section:history */}

## From recurrent context to all-position retrieval

This is neural-attention history on the road to modern LLMs, not programming-
language history.

A basic recurrent encoder-decoder can force an entire source sentence through
one fixed-size vector, while additive attention still advances recurrently and
computes a new source alignment at each output step.

[Bahdanau, Cho, and Bengio, *Neural Machine Translation by Jointly Learning to Align and Translate*](https://arxiv.org/abs/1409.0473)
introduced the relevant bridge. Bahdanau, Cho, and Bengio describe the possible
fixed-length-vector bottleneck of a basic encoder-decoder and compute each new
context as a softmax-weighted sum of encoder annotations scored against the
previous decoder state.

Calling that learned scoring function *additive attention* follows the later
retrospective classification used by Vaswani and colleagues. The earlier paper
does not use this chapter's query, key, and value notation, scaled dot products,
or same-sequence score grid.

The Transformer combines many queries into matrices and uses scaled dot-product
self-attention to compare positions in the same available sequence, allowing one
layer to form its full score grid with batched matrix operations.

[Vaswani et al., *Attention Is All You Need*](https://arxiv.org/abs/1706.03762)
provide that later step. Vaswani et al. define scaled dot-product attention as a
softmax-normalized matrix of scaled query-key dot products applied to values,
combine simultaneous queries in a matrix, and define self-attention as relating
positions within one sequence.

Each decoder self-attention head turns learned queries and keys into
row-normalized attention weights, then mixes learned values; a causal decoder
additionally masks future key positions before normalization.

Forming that grid does not make total work constant, and it does not make
autoregressive token generation parallel. The important historical change is
structural: recurrent alignment computes a new context as the decoder advances,
while self-attention relates all available positions within one layer.

The executable contrast below isolates the change in which positions supply the
query side and the key/value side:

<RustSource
  path="rust/demos/ch27-self-attention/src/lib.rs"
  region="historical-attention-contrast"
  caption="Contrast encoder-decoder alignment sources with same-sequence query-key pairs"
  label="Rust source for the Chapter 27 LLM-centered attention history contrast"
/>

{/* chapter-section:rust-implementation */}

## Compose the head from differentiable tensor operations

`scaled_dot_product_self_attention` accepts three rank-three `TensorValue`
objects. It composes the existing transpose, batched `matmul`, scalar multiply,
stable `log_softmax`, `exp`, and second `matmul` operations. It exposes raw
scores, scaled scores, probabilities, output, scale, key width, and value width:

<RustSource
  path="rust/crates/llm-from-scratch/src/attention/self_attention.rs"
  region="self-attention-forward"
  caption="Compose one inspectable unmasked scaled dot-product attention head"
  label="Rust source implementing the cumulative Chapter 27 self-attention forward pass"
/>

Query, key, and value inputs must have rank three and matching batch and token
axes. The token axis and all feature axes must be nonempty. Query and key widths
must agree, but $d_v$ may differ from $d_k$. An empty batch is valid. Typed
errors preserve input-rank, batch, token, empty-token, feature-width, and
forward-stage context:

<RustSource
  path="rust/crates/llm-from-scratch/src/attention/self_attention.rs"
  region="self-attention-errors"
  caption="Reject invalid self-attention inputs before exposing a partial result"
  label="Rust source defining the Chapter 27 self-attention error boundary"
/>

For the reverse example, bars denote reverse-mode gradients:

$$
\bar X=\frac{\partial L}{\partial X}.
$$

Choose the upstream seed

$$
\bar O=\begin{bmatrix}1&0\\0&1\end{bmatrix},
$$

which defines the scalar objective

$$
L=\langle O,\bar O\rangle=O_{00}+O_{11}\approx-1.966576.
$$

First the output matrix multiplication gives

$$
\bar V_b=A_b^{\mathsf T}\bar O_b,\qquad
\bar A_b=\bar O_bV_b^{\mathsf T}.
$$

For each query row, the softmax backward rule is

$$
\bar S_{bij}
=A_{bij}\left(\bar A_{bij}-\sum_r A_{bir}\bar A_{bir}\right).
$$

The scaled dot products then send gradients to queries and keys:

$$
\bar Q_b=\frac{\bar S_bK_b}{\sqrt{d_k}},\qquad
\bar K_b=\frac{\bar S_b^{\mathsf T}Q_b}{\sqrt{d_k}}.
$$

Here $\mathsf T$ transposes the final two axes separately inside each batch.

For the worked inputs, these equations give

$$
\bar Q\approx
\begin{bmatrix}
0.079000&-0.039500\\
-0.014389&0.007195
\end{bmatrix},
$$

$$
\bar K\approx
\begin{bmatrix}
-0.007195&0.062847\\
0.007195&-0.062847
\end{bmatrix},
$$

$$
\bar V\approx
\begin{bmatrix}
0.014166&0.999151\\
0.985834&0.000849
\end{bmatrix}.
$$

All four coordinates of each input agree with central differences using step
$10^{-6}$ and tolerance $2\times10^{-6}$. Equal keys produce equal weights, a
single token gives its only value weight one, matching permutations of $Q$, $K$,
and $V$ produce the same output permutation, and separate batch elements remain
independent. An empty batch with $Q,K$ shaped $[0,2,2]$ and $V$ shaped
$[0,2,3]$ returns $A$ shaped $[0,2,2]$ and $O$ shaped $[0,2,3]$. The value
width may differ from the query/key width: values shaped $[1,2,1]$ produce an
output shaped $[1,2,1]$.

The complete evidence builder keeps these forward, reverse, scale, shape,
boundary, and replay checks together:

<RustSource
  path="rust/demos/ch27-self-attention/src/lib.rs"
  region="self-attention-fixture"
  caption="Build the worked self-attention evidence from the cumulative tensor operations"
  label="Rust source for the complete Chapter 27 self-attention example"
/>

<RustSource
  path="rust/demos/ch27-self-attention/src/main.rs"
  caption="Run the complete unmasked self-attention example"
  label="Rust entry point for the Chapter 27 self-attention example"
/>

Run `cargo run --quiet --locked -p ch27-self-attention` to inspect the same
scores, probabilities, mixtures, gradients, shapes, and boundary results.

{/* chapter-section:visualization */}

## Trace every attention row from scores to output

The diagram follows the exact inputs through raw and scaled scores, probability
rows, weighted value terms, outputs, gradients, shapes, rejected inputs, and the
historical transition to self-attention:

<SelfAttentionDiagram labels={diagramLabels} />

Read each probability row across key positions before following it into the
matching weighted value terms. Solid, dashed, double, and dotted borders keep
query, key, value, score, and probability roles distinct without relying on
color. Every position is still visible; Chapter 28 adds the missing causal
boundary.

{/* chapter-section:exercises */}

## Predict before reading the evidence

1. Compute all four entries of $QK^\top$ for the worked example.
2. Predict which key each query favors before applying softmax.
3. Explain why each row of $A$ sums to one instead of each column.
4. Predict the output shape for $Q,K\in\mathbb{R}^{4\times7\times3}$ and $V\in\mathbb{R}^{4\times7\times5}$.
5. Predict the attention probability and output when there is one token with value $[5,-2]$.
6. Decide what happens if the same two token positions are swapped in $Q$, $K$, and $V$.
7. Predict the probabilities when the two key rows are equal.
8. Identify which future access would leak target information during causal decoder training.
9. Contrast recurrent alignment with the score-grid structure of self-attention.

<details>
  <summary>Check the predictions</summary>

  1. The score rows are $[0,6]$ and $[6,-4]$.
  2. Query zero favors key one; query one favors key zero.
  3. Each query chooses among key positions, so softmax normalizes its key axis.
  4. The output shape is $[4,7,5]$.
  5. The only probability is $[1]$, and the output is $[5,-2]$.
  6. Jointly permuting the rows of $Q$, $K$, and $V$ applies the same permutation to the output rows. Position-dependent inputs or a mask must be analyzed separately.
  7. Equal scores produce the uniform row $[0.5,0.5]$.
  8. A query can use later target positions because this head is unmasked; that future access would leak target information during autoregressive training.
  9. Recurrent alignment computes a new context as the decoder advances; self-attention forms relationships among all available positions within one layer. This does not make total work constant or autoregressive generation parallel.
</details>

{/* chapter-section:decoder-connection */}

## Mask future keys next

The cumulative decoder can now turn one projected query/key/value triplet into
the output of an unmasked attention head. Chapter 28 will exclude future key
positions before each score row is normalized.

The head already preserves batch and query-position axes and returns both the
inspectable probability matrix and mixed values. It is not yet safe for
autoregressive decoding: every query can read every key. Causal masking is the
next boundary; positional information, multiple heads, output projection,
residual wrapping, and cached decoding remain later chapters.
