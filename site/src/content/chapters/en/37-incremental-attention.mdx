---
{
  "chapter_id": "37-incremental-attention",
  "locale": "en",
  "content_revision": 4,
  "order": 37,
  "concept_id": "incremental-attention",
  "title": "Keep the prefix, project only the new row",
  "description": "Learn how one layer-bound KV cache appends rotated keys and unrotated values while reproducing full-prefix attention at the newest position.",
  "objective": "Append one position's rotated keys and unrotated values to one attention-layer cache and reproduce the full-prefix attention result at that newest position.",
  "worked_inputs": "Feed three model-width-four rows into a two-head attention layer one row at a time, use cache lengths 0, 1, and 2 as the absolute RoPE positions, and compare each cached output with the corresponding full-prefix last row within 1e-12.",
  "formula": {
    "latex": "K^{(\\ell)}_{1:t}=[K^{(\\ell)}_{1:t-1};k^{(\\ell)}_t],\\quad V^{(\\ell)}_{1:t}=[V^{(\\ell)}_{1:t-1};v^{(\\ell)}_t]",
    "symbols": [
      {
        "symbol": "K^{(\\ell)}_{1:t}",
        "meaning": "the rotated key cache for attention layer ell after position t is appended"
      },
      {
        "symbol": "V^{(\\ell)}_{1:t}",
        "meaning": "the value cache for attention layer ell after position t is appended"
      },
      {
        "symbol": "\\ell",
        "meaning": "the decoder-block attention layer that owns this cache"
      },
      {
        "symbol": "t",
        "meaning": "the newest one-based position in this formula; the implementation uses zero-based RoPE position t-1"
      },
      {
        "symbol": "1:t",
        "meaning": "all retained positions from the first position through the newest position"
      },
      {
        "symbol": "K^{(\\ell)}_{1:t-1}",
        "meaning": "the keys already retained for earlier positions in layer ell"
      },
      {
        "symbol": "V^{(\\ell)}_{1:t-1}",
        "meaning": "the values already retained for earlier positions in layer ell"
      },
      {
        "symbol": "k^{(\\ell)}_t",
        "meaning": "the newest key after the layer's key projection, head split, and RoPE rotation at its absolute position"
      },
      {
        "symbol": "v^{(\\ell)}_t",
        "meaning": "the newest value after the layer's value projection and head split"
      },
      {
        "symbol": "[A;B]",
        "meaning": "concatenation along the sequence-position axis, with B appended after A"
      }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "inference-design",
      "limitation": "Causal Transformer attention lets each newest decoder position read the known prefix. Without retained projections, however, a generation loop can recompute unchanged earlier key and value rows at every step.",
      "later_advance": "Incremental decoding made one-step reuse explicit by retaining per-layer key/value tensors; later LLM serving systems treated the growing KV cache as a central memory-management object.",
      "modern_llm_role": "Modern cached generation keeps one compatible cache per decoder block: each new query reads the retained prefix while only the newest key and value are projected.",
      "sources": [
        {
          "role": "earlier",
          "year": 2017,
          "name": "Attention Is All You Need",
          "source_url": "https://arxiv.org/pdf/1706.03762",
          "claim": "Vaswani and colleagues define scaled dot-product attention, describe an autoregressive decoder that emits one element at a time, and mask decoder self-attention so a position can use only the known prefix; the paper does not specify KV caching or RoPE."
        },
        {
          "role": "later",
          "year": 2019,
          "name": "Fast Transformer Decoding: One Write-Head is All You Need",
          "source_url": "https://arxiv.org/pdf/1911.02150",
          "claim": "Shazeer's incremental multi-head self-attention takes previous key and value tensors, appends the current projected pair, and returns the enlarged tensors; its analysis identifies repeatedly loading those tensors as a memory-bandwidth bottleneck before proposing multi-query attention."
        },
        {
          "role": "later",
          "year": 2023,
          "name": "Efficient Memory Management for Large Language Model Serving with PagedAttention",
          "source_url": "https://arxiv.org/pdf/2309.06180",
          "claim": "Kwon and colleagues describe sequential LLM generation in which earlier key and value vectors are cached and only the newest pair is computed, then organize dynamically growing KV caches as logical blocks mapped to non-contiguous physical memory."
        }
      ]
    },
    "approach": "The transition is from recomputing every complete prefix to appending one layer-local key/value pair per step.",
    "summary": "The historical thread runs from causal masked attention, through explicit incremental KV reuse, to serving systems designed around a dynamically growing cache. Fixed capacity, exact layout, the RoPE offset rule, parameter-node and value-revision binding, reset behavior, and typed errors are this lesson's local correctness policies.",
    "rust_source": "rust/demos/ch37-incremental-attention/src/lib.rs"
  },
  "rust_sources": [
    {
      "path": "rust/crates/llm-from-scratch/src/attention/incremental.rs",
      "region": "layer-kv-cache",
      "purpose": "Own fixed key and value storage bound to one layer's parameter-node identities and captured value revisions, append one checked row transactionally, expose logical prefixes, and reset without reallocating or rebinding."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/attention/incremental.rs",
      "region": "incremental-attention",
      "purpose": "Project one row under no-gradient inference, apply absolute-position RoPE, attend across retained and candidate rows, and commit only after output projection succeeds."
    },
    {
      "path": "rust/demos/ch37-incremental-attention/src/lib.rs",
      "region": "historical-kv-contrast",
      "purpose": "Measure complete-prefix projection work against incremental reuse for the same three prefixes."
    },
    {
      "path": "rust/demos/ch37-incremental-attention/src/lib.rs",
      "region": "cache-errors",
      "purpose": "Check that full, shape, model, head, layer, RoPE, and nonfinite failures leave cache state unchanged."
    },
    {
      "path": "rust/demos/ch37-incremental-attention/src/lib.rs",
      "region": "cache-step",
      "purpose": "Assemble three cached-versus-reference matches, projection-row counts, allocation-preserving reset, exact replay, and error evidence."
    },
    {
      "path": "rust/demos/ch37-incremental-attention/src/main.rs",
      "purpose": "Print the exact Chapter 37 learner report."
    }
  ],
  "visualization": {
    "decision": "useful",
    "id": "incremental-attention",
    "rationale": "A three-step cache timeline makes the retained rows, one appended row, absolute RoPE position, growing attention span, full-prefix match, and avoided repeated projections visible together."
  },
  "decoder_connection": "One attention layer can now preserve graph-free rotated keys and unrotated values across decode steps and reproduce its full-prefix newest-position output. It rejects a different parameter-node identity, a changed parameter-value revision, or an incompatible configuration; reset clears logical state without reallocating or rebinding. Chapter 38 will compose one such cache per decoder block."
}
---

import RustSource from "../../../components/RustSource.astro";
import IncrementalAttentionDiagram from "../../../components/chapters/IncrementalAttentionDiagram.astro";

export const diagramLabels = {
  title: "Retain earlier key/value rows; append exactly one new pair",
  description: "The exact Rust trace follows three absolute positions, shows both head caches and attention weights, matches each newest output to a full-prefix reference, and records that reset plus rejected calls preserve storage.",
  sections: {
    timeline: "Advance the layer cache one position at a time",
    work: "Count projection rows, not hardware time",
    reset: "Reset or reject without corrupting retained state",
  },
  fields: {
    position: "Absolute position",
    cache: "Cache length",
    shape: "Logical cache shape",
    head: "Head",
    keys: "Rotated keys",
    values: "Unrotated values",
    weights: "Newest-query weights",
    cached: "Cached output",
    full: "Full-prefix reference",
    difference: "Maximum absolute difference",
  },
  cues: {
    retained: "retained earlier row - solid border",
    appended: "newly appended row - double border",
    match: "newest outputs match within tolerance",
    unchanged: "Reset keeps the allocation and stored values",
    replay: "The same rows replay identically",
    errors: "Rejected calls commit no append",
  },
  captions: {
    timeline: "The old logical length is the zero-based RoPE position. Every head retains its earlier rows, adds one key/value pair, and lets the newest query read the complete resulting prefix.",
    work: "For prefix lengths one through three, the reference projects six rows per branch. The cached path projects three new rows and reuses three earlier key rows plus three earlier value rows.",
    reset: "Logical reset reuses the same allocation. Full, incompatible, or invalid calls leave both length and storage unchanged.",
  },
  proofs: {
    full: "complete-prefix rows per query, key, or value projection",
    cached: "incremental rows per query, key, or value projection",
    reused: "earlier rows avoided across key and value projections",
  },
  lists: {
    cacheRows: "Cache rows in chronological order",
  },
};

{/* chapter-section:worked-example */}

## Predict the third result before running the example

The fixture has one batch, model width $D=4$, $H=2$ heads, head width $d_h=2$,
and cache capacity $C=4$. Before the first call the cache is empty. Each call
accepts one row shaped $[1,1,4]$, so the logical key/value shapes advance through
$[1,2,1,2]$, $[1,2,2,2]$, and $[1,2,3,2]$.

The first two cached outputs are $[1,0,1,0]$ and
$[0.213809009,0.786190991,0.770151153,-0.420735492]$. Before revealing the
third output, predict three facts:

1. The old length is $2$, so the new $Q$ and $K$ use absolute RoPE position $2$.
2. Each head's newest query receives $3$ key rows and $3$ value rows.
3. The cache appends one rotated key row and one unrotated value row; it does not
   retain the query.

The result is $[0.629044078,0.945303958,0.374718490,-0.583589471]$. An
independent full-prefix pass produces the same displayed values, and the
unrounded maximum absolute difference is at most $10^{-12}$.

{/* chapter-section:formula */}

## Append along the position axis

For attention layer $\ell$, the cache transition is

$$
K^{(\ell)}_{1:t}=[K^{(\ell)}_{1:t-1};k^{(\ell)}_t],\quad V^{(\ell)}_{1:t}=[V^{(\ell)}_{1:t-1};v^{(\ell)}_t]
$$

The semicolon means concatenate along the sequence-position axis. The new key
$k^{(\ell)}_t$ has already received RoPE for its absolute position. The new
value $v^{(\ell)}_t$ has been projected and split into heads but is not rotated.
Queries are needed only for the current calculation, so this implementation does
not store them in the cache.

The implementation uses a zero-based offset equal to the old cache length. The
formula uses conventional one-based prefix notation, so mathematical position
$t$ is one greater than that offset.

{/* chapter-section:symbol-glossary */}

## Keep layer, logical length, and capacity separate

- $K^{(\ell)}_{1:t}$ is layer $\ell$'s rotated key prefix after the append.
- $V^{(\ell)}_{1:t}$ is the same layer's value prefix after the append.
- $\ell$ identifies the decoder-block attention layer that owns the state.
- $t$ identifies the newest position in one-based mathematical prefix notation.
- $1:t$ includes every retained position through the newest one.
- $K^{(\ell)}_{1:t-1}$ and $V^{(\ell)}_{1:t-1}$ are the unchanged earlier rows.
- $k^{(\ell)}_t$ and $v^{(\ell)}_t$ are the one-row candidate pair.
- $[A;B]$ means append $B$ after $A$ along the position axis.

The physical buffers have shape $[B,H,C,d_h]$, where $B$ is batch size, $H$
is head count, $C$ is fixed capacity, and $d_h$ is head width. A logical
snapshot has shape $[B,H,t,d_h]$. Reset changes $t$ to $0$ without changing
$C$ or allocating replacement buffers. It also leaves the parameter-node
identities and value revisions captured at cache construction unchanged.

{/* chapter-section:history */}

## From causal attention to managed LLM inference state

Causal Transformer attention lets each newest decoder position read the known prefix. Without retained projections, however, a generation loop can recompute unchanged earlier key and value rows at every step.

[Attention Is All You Need](https://arxiv.org/pdf/1706.03762) describes the
full-prefix causal computation used as the reference here. Vaswani and colleagues define scaled dot-product attention, describe an autoregressive decoder that emits one element at a time, and mask decoder self-attention so a position can use only the known prefix; the paper does not specify KV caching or RoPE.

[Fast Transformer Decoding: One Write-Head is All You Need](https://arxiv.org/pdf/1911.02150)
makes the reuse boundary explicit. Shazeer's incremental multi-head self-attention takes previous key and value tensors, appends the current projected pair, and returns the enlarged tensors; its analysis identifies repeatedly loading those tensors as a memory-bandwidth bottleneck before proposing multi-query attention.

[Efficient Memory Management for Large Language Model Serving with PagedAttention](https://arxiv.org/pdf/2309.06180)
continues the road into modern serving. Kwon and colleagues describe sequential LLM generation in which earlier key and value vectors are cached and only the newest pair is computed, then organize dynamically growing KV caches as logical blocks mapped to non-contiguous physical memory.

Incremental decoding made one-step reuse explicit by retaining per-layer key/value tensors; later LLM serving systems treated the growing KV cache as a central memory-management object.

Modern cached generation keeps one compatible cache per decoder block: each new query reads the retained prefix while only the newest key and value are projected.

The historical thread runs from causal masked attention, through explicit
incremental KV reuse, to serving systems designed around a dynamically growing
cache. Fixed capacity, exact layout, the RoPE offset rule, parameter-node and
value-revision binding, reset behavior, and typed errors are this lesson's local
correctness policies.

<RustSource
  path="rust/demos/ch37-incremental-attention/src/lib.rs"
  region="historical-kv-contrast"
  caption="Measure newest-query span and projection reuse across three prefixes"
  label="Rust source for the Chapter 37 LLM-inference history contrast"
/>

{/* chapter-section:rust-implementation */}

## Bind the state, calculate completely, then commit

`LayerKvCache::new` takes the actual attention layer. It records the batch size,
model width, head count, cache capacity, and head width; owns fixed key/value
buffers; and, for each of the layer's four parameters, captures both the node
identity and its current value revision. It also records the exact RoPE
configuration.

A rebuilt layer contains different parameter nodes, so the cache returns
`CacheLayerMismatch`. A successful AdamW step updates the existing nodes in
place: their identities stay the same, but their value revisions advance. The
old cache is then stale and returns `CacheLayerRevisionMismatch`. This second
check matters because retained K/V rows were projected with the earlier weight
values; mixing those rows with projections from updated weights would describe
no single model. Construct a new cache after any weight update. Calling `reset`
on the old cache does not make it compatible: reset clears logical length but
does not refresh the captured revisions or bind the cache to another layer.

<RustSource
  path="rust/crates/llm-from-scratch/src/attention/incremental.rs"
  region="layer-kv-cache"
  caption="Own a fixed layer-bound cache, append one checked row, and reset logical length"
  label="Rust source implementing the Chapter 37 layer-local KV cache"
/>

`forward_incremental` accepts exactly $[B,1,D]$. Inside its own no-gradient
scope it projects the new row, splits heads, rotates $Q$ and $K$ at the old cache
length, computes stable weights shaped $[B,H,1,t+1]$, mixes the retained and
candidate values, merges heads, and applies the existing output projection.
Only a fully assembled result permits the key/value copy and length increment.

<RustSource
  path="rust/crates/llm-from-scratch/src/attention/incremental.rs"
  region="incremental-attention"
  caption="Evaluate one newest query across the retained rows plus the candidate key/value row before committing the append"
  label="Rust source implementing Chapter 37 incremental multi-head attention"
/>

Across prefix lengths $1$, $2$, and $3$, the executable history contrast records
that the newest queries read $[1,2,3]$ key rows. A full-prefix call visits
$1+2+3=6$ input rows in each $Q$, $K$, or $V$ projection. The incremental path
visits $1+1+1=3$ rows per projection. It therefore reuses $3$ earlier key rows
and $3$ earlier value rows. The newest query still reads the resulting prefix of
$t$ positions: $t-1$ retained positions plus the current candidate position.
This count therefore does not claim constant-time attention or a measured speedup.

Errors are transactional. The deterministic demo exercises two-token input, a
full cache, model or head mismatch, same-shaped rebuilt weights, a changed RoPE
base, the same base with a different position capacity, and finite input whose
projection becomes nonfinite. Every one of those calls returns before cached
values or logical length change. Separately, the revision comparison at the
start of `forward_incremental` rejects a layer updated in place before projection
or append. That mismatch remains an error after `reset`, because reset does not
rebind the cache. Raw key/value append is private to the bound attention
operation, so public callers cannot bypass those compatibility checks.

<RustSource
  path="rust/demos/ch37-incremental-attention/src/lib.rs"
  region="cache-errors"
  caption="Exercise incompatible and invalid calls against unchanged cache snapshots"
  label="Rust source checking that Chapter 37 cache errors are transactional"
/>

The fixture performs all three cached calls and all three independent reference
calls. It checks full logical key/value prefixes as well as outputs, resets to
length $0$ without replacing storage, and replays the same evidence exactly with
the unchanged layer. That replay proves allocation reuse; it does not turn reset
into a compatibility refresh after training.

<RustSource
  path="rust/demos/ch37-incremental-attention/src/lib.rs"
  region="cache-step"
  caption="Assemble cached matches, row counts, reset replay, and error evidence"
  label="Rust source assembling the Chapter 37 learner evidence"
/>

Run `cargo run --quiet --locked -p ch37-incremental-attention`. The central
evidence is emitted directly by the executable:

```text
step=position:0 cache:0->1 shape:[1,2,1,2] max_abs_diff:0.000000000000 output:[1.000000000,0.000000000,1.000000000,0.000000000]
step=position:1 cache:1->2 shape:[1,2,2,2] max_abs_diff:0.000000000000 output:[0.213809009,0.786190991,0.770151153,-0.420735492]
step=position:2 cache:2->3 shape:[1,2,3,2] max_abs_diff:0.000000000000 output:[0.629044078,0.945303958,0.374718490,-0.583589471]
work=full_rows_per_projection:6 incremental_rows_per_projection:3 reused_rows_per_kv_projection:3 avoided_rows_across_kv:6
reset=before:3 after:0 allocation_reused:true storage_unchanged:true replay_identical:true
errors=two_tokens:true full_cache:true model_mismatch:true head_mismatch:true layer_mismatch:true rope_mismatch:true rope_positions_mismatch:true nonfinite_projection:true unchanged:true
```

<RustSource
  path="rust/demos/ch37-incremental-attention/src/main.rs"
  caption="Print the exact Chapter 37 incremental-attention report"
  label="Rust source printing Chapter 37 cache evidence"
/>

{/* chapter-section:visualization */}

## Follow retained rows into the newest query

The figure follows the exact values produced by the three Rust calls. Read from
absolute position $0$ through position $2$. Solid-border boxes are retained rows;
the double box is the one new key/value pair. Each row includes its newest-query weight,
and each step ends with cached and full-prefix output values beside the recorded
maximum difference.

The work cards compare projection-row counts, not elapsed time. The final cards
show that reset reuses allocation and that rejected calls preserve state. A solid
border marks a retained row; a double border marks the newly appended pair.

<IncrementalAttentionDiagram labels={diagramLabels} />

{/* chapter-section:exercises */}

## Predict before checking the trace

1. Which RoPE offset is used when logical cache length is $2$?
2. Does the layer cache retain the newest query?
3. At the third call, how many keys can each newest query read?
4. How many rows does one full-prefix key projection visit across lengths $1$, $2$,
   and $3$?
5. How many rows does the incremental key projection visit?
6. What does reset change, and does it refresh the parameter binding?
7. Can a same-shaped cache from rebuilt weights be reused?
8. Can the old cache be reused after AdamW updates the same parameter nodes in
   place?
9. What happens to cache state when the capacity is already full?

<details>
<summary>Check the nine predictions</summary>

1. The offset is $2$, exactly the old logical length.
2. No. Only rotated $K$ and unrotated $V$ rows persist.
3. Each query reads $3$ keys and mixes $3$ values.
4. The reference visits $1+2+3=6$ key-projection rows.
5. The incremental path visits $1+1+1=3$ key-projection rows.
6. Reset changes logical length to $0$ and keeps the allocation and stored
   values. It does not refresh the captured parameter identities or value
   revisions, and it does not rebind the cache.
7. No. A rebuilt layer fails the parameter-node identity check even when shapes
   and numeric values agree.
8. No. An in-place AdamW update preserves the parameter nodes but advances their
   value revisions. The old cache returns `CacheLayerRevisionMismatch`; create a
   new cache from the updated layer.
9. The call returns a typed error before stored values or logical length change.

</details>

{/* chapter-section:decoder-connection */}

## Give every decoder block its own state next

One attention layer can now preserve graph-free rotated keys and unrotated values
across decode steps and reproduce its full-prefix newest-position output. It
rejects a different parameter-node identity, a changed parameter-value revision,
or an incompatible configuration. Reset clears logical state without
reallocating or rebinding. Chapter 38 will compose one such cache per decoder
block.

The current cumulative decoder still evaluates full prefixes. Chapter 38 will
create one cache from each block's actual attention layer, thread those caches
through prefill and one-token decode, and compare complete cached generation with
the uncached reference from Chapter 36.
