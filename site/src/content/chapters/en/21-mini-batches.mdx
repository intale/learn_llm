---
{
  "chapter_id": "21-mini-batches",
  "locale": "en",
  "content_revision": 3,
  "order": 21,
  "concept_id": "mini-batches",
  "title": "Count only the tokens that are really in the batch",
  "description": "Shuffle complete causal windows into mini-batches of fixed-length rows, keep the smaller final batch, and average loss and gradients over its actual target tokens.",
  "objective": "Shuffle complete causal windows reproducibly, stack fixed-size token rows without crossing boundaries, and average loss plus gradients over the target tokens actually present.",
  "worked_inputs": "Use context length T=2, maximum batch width 3, seed 7, and five complete windows from two separate training documents. Predict the two batch shapes after shuffling and decide whether the smaller final batch divides by the 6 target positions that a width-three batch could hold or by its 4 actual target-token occurrences.",
  "formula": {
    "latex": "\\mathcal{L}_B=\\frac{1}{|B|T}\\sum_{b\\in B}\\sum_{t=1}^{T}\\mathcal{L}_{b,t}",
    "symbols": [
      {
        "symbol": "B",
        "meaning": "the set of complete causal windows actually present in the current mini-batch"
      },
      {
        "symbol": "|B|",
        "meaning": "the current batch width, which may be smaller than the requested capacity in the final batch"
      },
      {
        "symbol": "T",
        "meaning": "the fixed number of input positions and shifted target positions in every admitted window"
      },
      {
        "symbol": "b",
        "meaning": "one window identity inside the current mini-batch"
      },
      {
        "symbol": "t",
        "meaning": "one target-token position from 1 through T within a window"
      },
      {
        "symbol": "\\mathcal{L}_{b,t}",
        "meaning": "the scalar negative log-likelihood contribution of target position t in window b"
      },
      {
        "symbol": "\\mathcal{L}_B",
        "meaning": "the mini-batch mean loss after every admitted target-token contribution is counted once"
      },
      {
        "symbol": "|B|T",
        "meaning": "the actual target-token denominator; no padding token or unused capacity slot is counted"
      },
      {
        "symbol": "j",
        "meaning": "one independently accumulated piece of the current batch"
      },
      {
        "symbol": "i",
        "meaning": "one target-token contribution within piece j"
      },
      {
        "symbol": "g_{j,i}",
        "meaning": "the parameter-gradient vector contributed by target occurrence i in piece j"
      },
      {
        "symbol": "S_j",
        "meaning": "the raw sum of gradient vectors in piece j before division"
      },
      {
        "symbol": "N_j",
        "meaning": "the actual number of target-token contributions in piece j"
      },
      {
        "symbol": "\\bar g",
        "meaning": "the token-weighted mean gradient after all pieces are merged"
      }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "training-practice",
      "limitation": "Bengio et al.'s early neural language model describes a stochastic parameter update after each training-corpus word-context example. That online endpoint exposes every example directly but offers no shared update across several examples; full-batch training moves to the opposite endpoint by waiting for the entire training set.",
      "later_advance": "The same paper discusses communicating every $K$ language-model examples as a mini-batch. Transformer training later grouped sentence pairs by approximate length and reported about $25{,}000$ source plus $25{,}000$ target tokens per batch, making token volume an explicit batching quantity.",
      "modern_llm_role": "GPT-3 reports batch size directly in tokens, from $0.5$ million to $3.2$ million across its model scales, with a $2{,}048$-token context. This course's decoder training preserves a smaller implementation invariant: each admitted target token contributes once, and loss plus gradients use the actual token count.",
      "sources": [
        {
          "role": "earlier",
          "year": 2003,
          "name": "Bengio et al., A Neural Probabilistic Language Model",
          "source_url": "https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf",
          "claim": "Bengio et al. define a stochastic update after presenting one training-corpus word and later discuss grouping $K$ examples before communication as a mini-batch."
        },
        {
          "role": "later",
          "year": 2017,
          "name": "Vaswani et al., Attention Is All You Need",
          "source_url": "https://papers.nips.cc/paper_files/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf",
          "claim": "Vaswani et al. batch sentence pairs by approximate length and report about $25{,}000$ source tokens and $25{,}000$ target tokens in each Transformer training batch."
        },
        {
          "role": "later",
          "year": 2020,
          "name": "Brown et al., Language Models are Few-Shot Learners",
          "source_url": "https://arxiv.org/pdf/2005.14165",
          "claim": "Brown et al. label GPT-3 batch size in tokens, report $0.5$ million through $3.2$ million tokens across model scales, and use a $2{,}048$-token context."
        }
      ]
    },
    "approach": "From one-example stochastic neural-language-model updates, through grouped language-model and Transformer token batches, to large autoregressive language-model batches measured directly in tokens",
    "summary": "Mini-batching is part of the road to modern LLM training because it combines multiple next-token examples into one update while keeping token accounting explicit. The papers establish that progression; this chapter's fixed windows, no-padding policy, shuffle, seed, exact widths, gradient fixture, and errors remain course choices.",
    "rust_source": "rust/demos/ch21-mini-batches/src/lib.rs"
  },
  "rust_sources": [
    {
      "path": "rust/demos/ch21-mini-batches/src/lib.rs",
      "region": "historical-update-grouping",
      "purpose": "Contrast one-example, three-example, and full-set update widths without turning the history into a language comparison."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/training/batch.rs",
      "region": "batch-configuration",
      "purpose": "Declare separate document provenance, requested width, and sequential or seeded order."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/training/batch.rs",
      "region": "mini-batch-epoch",
      "purpose": "Enumerate lightweight window descriptors, shuffle them, and copy selected source slices directly into final row-major batches."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/training/batch.rs",
      "region": "token-gradient-averaging",
      "purpose": "Preflight finite raw totals before committing loss, count, and gradient sums into the existing accumulator allocation."
    },
    {
      "path": "rust/demos/ch21-mini-batches/src/lib.rs",
      "region": "chapter-fixture",
      "purpose": "Build the frozen five-window epoch twice with the same seed and once with another seed."
    },
    {
      "path": "rust/demos/ch21-mini-batches/src/lib.rs",
      "region": "token-contributions",
      "purpose": "Attach one exact loss and two-coordinate gradient to every admitted target token."
    },
    {
      "path": "rust/demos/ch21-mini-batches/src/main.rs",
      "region": "learner-mini-batch-output",
      "purpose": "Print the complete deterministic learner report."
    },
    {
      "path": "rust/demos/ch21-mini-batches/src/diagram_trace.rs",
      "region": "mini-batches-trace",
      "purpose": "Emit exact shuffled provenance, batch means, and invariant proofs for the static diagram."
    }
  ],
  "visualization": {
    "decision": "useful",
    "id": "mini-batches",
    "rationale": "Two batch cards make shuffled provenance, row-major token axes, per-token loss contributions, and the smaller final denominator visible together; a capacity outline shows why unused slots must not enter the mean."
  },
  "decoder_connection": "The cumulative training path can now turn separate causal windows into reproducible mini-batches of fixed-length rows and produce token-mean loss plus gradient coordinates. Chapter 22 maps those averaged coordinates to stable named parameters and applies AdamW."
}
---

import RustSource from '../../../components/RustSource.astro';
import MiniBatchesDiagram from '../../../components/chapters/MiniBatchesDiagram.astro';

export const diagramLabels = {
  title: 'Follow five complete windows into two token-normalized batches',
  description:
    'Read the worked shuffle order, row-major input and target IDs, per-token losses, actual denominators, mean gradients, and coverage proofs for one reproducible epoch.',
  summary: {
    contextLength: 'Context length',
    requestedCapacity: 'Requested capacity',
    shuffleSeed: 'Shuffle seed',
    completeWindows: 'Complete windows',
    batchCount: 'Batches emitted',
  },
  stages: {
    shuffle: 'Shuffle complete window identities',
    batches: 'Stack actual rows and count target tokens',
    finalBatch: 'Exclude unused capacity from the final mean',
    proof: 'Check coverage, boundaries, replay, and accumulation',
  },
  fields: {
    slot: 'Shuffled slot',
    origin: 'Window origin',
    input: 'Input IDs',
    target: 'Target IDs',
    tokenLosses: 'One loss per target position',
    shape: 'Stacked shape',
    actualWidth: 'Actual final width',
    targetTokens: 'Target tokens',
    lossSum: 'Loss sum',
    denominator: 'Actual denominator',
    meanLoss: 'Mean loss',
    meanGradient: 'Mean gradient',
    capacitySlot: 'Possible third row',
    duplicates: 'Duplicate windows',
    padding: 'Padding IDs',
    crossPartition: 'Cross-partition windows',
    sameSeed: 'Same-seed replay',
    differentSeed: 'Different-seed order',
    rawAccumulation: 'Raw accumulation',
  },
  notes: {
    shuffle:
      'The seed permutes complete-window identities, each named by a document and start offset. It never shuffles a flattened stream of document tokens.',
    tokenMean:
      'Each row contributes exactly two target losses and two gradient vectors. The example supplies every value shown below.',
    finalBatch:
      'The requested capacity permits three rows, but the final batch creates only two. Its denominator is four target tokens, not six possible positions.',
    proof:
      'Changing the seed may change order, never membership. Merging raw sums before division reproduces one-pass averaging.',
  },
  symbols: {
    batch: 'Batch',
    window: 'Window',
    unused: 'Not created — contributes nothing',
    equal: 'Equal',
    same: 'Same',
    changed: 'Changed',
  },
  captions: {
    batchRows: 'Shuffled windows and their exact token contributions',
    proof: 'Rust-authored epoch invariants',
  },
  scrollers: {
    batchRows: 'Scrollable mini-batch token evidence for batch',
  },
};

{/* chapter-section:worked-example */}

## Predict the smaller final batch

Chapter 5 already turns each document into shifted windows without crossing a
boundary. Use context length $T=2$ on two separate training documents:

```text
train-a = [0, 10, 11, 12, 1]  ->  train-a@0, train-a@1, train-a@2
train-b = [0, 20, 21, 1]      ->  train-b@0, train-b@1
```

Seed `7` orders those five window identities as:

```text
train-b@1, train-a@1, train-b@0, train-a@0, train-a@2
```

With requested capacity $3$, predict the two stacked shapes. The first three
windows form $\left[3,2\right]$; the remaining two form
$\left[2,2\right]$. The final batch stays smaller. It is neither padded to
width $3$ nor dropped.

Now count targets. The first batch has $3\cdot2=6$ target occurrences. The
final batch has $2\cdot2=4$. The requested capacity would permit a third row,
but the final batch does not create one. Therefore no third-row token, loss, or
gradient exists to include in the mean.

The worked example constructs that epoch from separate documents and preserves the
same provenance shown above:

<RustSource
  path="rust/demos/ch21-mini-batches/src/lib.rs"
  region="chapter-fixture"
  caption="Build the same five-window shuffled epoch reproducibly"
  label="Rust source for the Chapter 21 deterministic worked epoch"
/>

{/* chapter-section:formula */}

## Divide by actual target tokens

For current batch $B$, average one scalar loss from every target position:

$$
\mathcal{L}_B=\frac{1}{|B|T}\sum_{b\in B}\sum_{t=1}^{T}\mathcal{L}_{b,t}
$$

The parameter gradient has the same denominator:

$$
\nabla_{\theta}\mathcal{L}_B=
\frac{1}{|B|T}
\sum_{b\in B}\sum_{t=1}^{T}
\nabla_{\theta}\mathcal{L}_{b,t}
$$

The assigned losses in the final batch are
$\left[0.125,0.25,0.625,0.75\right]$. Their sum is $1.75$, so

$$
\mathcal{L}_{B_1}=\frac{1.75}{2\cdot2}=0.4375
$$

Dividing by capacity would use $3\cdot2=6$ and produce about $0.291667$.
That is not a harmless reporting change: it multiplies every final-batch
gradient by $4/6$ as well.

If contributions arrive in pieces, keep raw sums $S_j$ and counts $N_j$:

$$
\bar g=
\frac{\sum_j S_j}{\sum_j N_j},
\qquad
S_j=\sum_{i=1}^{N_j}g_{j,i}.
$$

Here $j$ identifies one accumulated piece, $i$ identifies one target
contribution in that piece, $g_{j,i}$ is its parameter-gradient vector,
$S_j$ is the raw vector sum, $N_j$ is the actual contribution count, and
$\bar g$ is the token-weighted mean after all pieces are merged.

Do not average piece means without weighting by their token counts. A short
piece must not receive the same weight as a full piece.

{/* chapter-section:symbol-glossary */}

## Keep batch and sequence axes distinct

- $B$ is the set of complete windows actually present now.
- $|B|$ is current width, not maximum capacity.
- $T$ is the fixed input and target length of every admitted window.
- $b$ identifies one window in $B$.
- $t$ identifies one target position from $1$ through $T$.
- $\mathcal{L}_{b,t}$ is one target occurrence's negative log-likelihood.
- $\mathcal{L}_B$ is the mean after every admitted occurrence is counted once.
- $|B|T$ is the actual token denominator; absent rows and padding are excluded because they do not exist in the batch.
- $j$ identifies one independently accumulated piece, and $i$ identifies one target contribution within it.
- $g_{j,i}$ is target occurrence $i$'s gradient vector, $S_j$ is the piece's raw vector sum, and $N_j$ is its actual target-occurrence count.
- $\bar g$ is the token-weighted mean gradient after every piece is merged.

An overlapping corpus token may appear as a target in more than one causal
window. Those are separate training-example occurrences, and each occurrence
contributes once. “Count each target once” does not mean deduplicating equal
token IDs.

{/* chapter-section:history */}

## From one word update to token-sized LLM batches

Bengio et al.'s early neural language model describes a stochastic parameter update after each training-corpus word-context example. That online endpoint exposes every example directly but offers no shared update across several examples; full-batch training moves to the opposite endpoint by waiting for the entire training set.

[Bengio et al., *A Neural Probabilistic Language Model*](https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf): Bengio et al. define a stochastic update after presenting one training-corpus word and later discuss grouping $K$ examples before communication as a mini-batch.

Their one-example update is:

$$
\theta\leftarrow\theta+
\varepsilon\frac{\partial\log\widehat P(w_t\mid
w_{t-1},\ldots,w_{t-n+1})}{\partial\theta}.
$$

The same paper discusses communicating every $K$ language-model examples as a mini-batch. Transformer training later grouped sentence pairs by approximate length and reported about $25{,}000$ source plus $25{,}000$ target tokens per batch, making token volume an explicit batching quantity.

[Vaswani et al., *Attention Is All You Need*](https://papers.nips.cc/paper_files/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf): Vaswani et al. batch sentence pairs by approximate length and report about $25{,}000$ source tokens and $25{,}000$ target tokens in each Transformer training batch.

Those translation batches are not evidence for this course's fixed-window,
no-padding policy. They are evidence that Transformer training made token
volume an explicit unit of work.

GPT-3 reports batch size directly in tokens, from $0.5$ million to $3.2$ million across its model scales, with a $2{,}048$-token context. This course's decoder training preserves a smaller implementation invariant: each admitted target token contributes once, and loss plus gradients use the actual token count.

[Brown et al., *Language Models are Few-Shot Learners*](https://arxiv.org/pdf/2005.14165): Brown et al. label GPT-3 batch size in tokens, report $0.5$ million through $3.2$ million tokens across model scales, and use a $2{,}048$-token context.

This is the road from stochastic neural-language-model examples to modern LLM
token batches, not a programming-language history. The exact seed, shuffle,
widths, no-padding rule, losses, gradients, and trace remain course choices.

The historical executable contrast groups the same five example identities as
online widths $\left[1,1,1,1,1\right]$, mini-batch widths
$\left[3,2\right]$, or one full width $\left[5\right]$. Rust is only the
medium for running that language-independent contrast:

<RustSource
  path="rust/demos/ch21-mini-batches/src/lib.rs"
  region="historical-update-grouping"
  caption="Contrast online, mini-batch, and full-set update widths"
  label="Rust source for the Chapter 21 historical batching contrast"
/>

{/* chapter-section:rust-implementation */}

## Shuffle windows, then merge raw sums

The public configuration distinguishes document provenance, requested capacity,
and sequential or seeded order:

<RustSource
  path="rust/crates/llm-from-scratch/src/training/batch.rs"
  region="batch-configuration"
  caption="Keep each document and epoch-order policy explicit"
  label="Rust source declaring Chapter 21 batching inputs"
/>

The epoch builder validates one partition, then records each complete window as
a `WindowDescriptor`. It contains only the source document index and start
offset, stored in `document_index` and `start`. Because the source documents
stay borrowed during construction, those two values are enough to find the
window's $T+1$ source tokens later. Fisher-Yates shuffles only these lightweight
descriptors.
For each final batch, the builder creates input and target buffers for that
batch's actual logical shape, then copies the selected input and target
occurrences directly from the borrowed document into their final row-major
destinations. No token-owning staging window is created:

<RustSource
  path="rust/crates/llm-from-scratch/src/training/batch.rs"
  region="mini-batch-epoch"
  caption="Shuffle window descriptors and write final batch rows directly"
  label="Rust source implementing the Chapter 21 mini-batch epoch"
/>

Loss and gradient accumulation use an all-or-nothing preflight. `add_token` and
`merge` first check the prospective token count, loss sum, and every gradient
coordinate. Only after every check succeeds do they update the existing
gradient-sum vector in place, retaining its allocation. A failure at a later
coordinate therefore cannot leave an earlier coordinate changed:

<RustSource
  path="rust/crates/llm-from-scratch/src/training/batch.rs"
  region="token-gradient-averaging"
  caption="Preflight raw totals before updating gradient storage in place"
  label="Rust source implementing Chapter 21 token-mean gradients"
/>

The worked example assigns one deterministic finite loss and two gradient
coordinates to each flattened target position, then proves that merging two raw
accumulators equals one-pass averaging. These values are stand-ins chosen to
isolate token averaging; this example does not recompute logits or negative
log-likelihood:

<RustSource
  path="rust/demos/ch21-mini-batches/src/lib.rs"
  region="token-contributions"
  caption="Average one finite contribution per actual target token"
  label="Rust source proving the Chapter 21 mean loss and gradients"
/>

The learner executable prints the batch rows, means, historical grouping widths,
replay result, coverage, padding count, and partition evidence:

<RustSource
  path="rust/demos/ch21-mini-batches/src/main.rs"
  region="learner-mini-batch-output"
  caption="Print the deterministic Chapter 21 learner report"
  label="Rust source for the Chapter 21 learner output"
/>

Run `cargo run --quiet --locked -p ch21-mini-batches` to follow the worked epoch
from the shuffle seed through both batch means. The smaller final batch makes the
contrast between requested capacity and actual target count visible in the
report.

{/* chapter-section:visualization */}

## Trace every shuffled window and denominator

The figure follows five shuffled windows into two batch summaries, then compares
the final batch's requested capacity with its actual denominator:

<RustSource
  path="rust/demos/ch21-mini-batches/src/diagram_trace.rs"
  region="mini-batches-trace"
  caption="Emit exact batch provenance and token-normalization evidence"
  label="Rust source producing the Chapter 21 mini-batch diagram trace"
/>

<MiniBatchesDiagram labels={diagramLabels} />

Read the shuffled origins in order, then inspect the input IDs, target IDs, and
two loss contributions in every admitted row. The first batch contains three
rows and divides by six targets. The second contains two rows and divides by
four; a dashed hypothetical third row shows what the requested capacity would
permit, but that row is not stored and contributes nothing. The
final proof records complete coverage, no duplicates, no padding, no partition
crossing, reproducible same-seed order, changed order for the other seed, and
equivalence with merging raw accumulators before division.

{/* chapter-section:exercises */}

## Predict before checking the exact epoch

1. List every complete window origin before shuffling.
2. With requested capacity $3$, predict both batch widths and shapes.
3. Count the first batch's actual target-token denominator.
4. Count the final batch's actual denominator and compare it with capacity.
5. Predict the scale error caused by dividing the final sum by $6$ instead of $4$.
6. Explain why raw accumulator merging is valid but an unweighted mean of means is not.
7. State what same and different seeds may change, and what they must preserve.
8. Match Bengio, Vaswani, and Brown to one-example updates, Transformer token batches, and large language-model batch sizes in tokens.

<details>
  <summary>Check the predictions</summary>

  1. The origins are `train-a@0`, `train-a@1`, `train-a@2`, `train-b@0`, and `train-b@1`.
  2. Widths are $3$ and $2$; shapes are $\left[3,2\right]$ and $\left[2,2\right]$.
  3. The first denominator is $3\cdot2=6$ target occurrences.
  4. The final denominator is $2\cdot2=4$, not capacity $3\cdot2=6$.
  5. The wrong denominator multiplies the correct final loss and gradient by $4/6$.
  6. Raw sums retain their token counts; unweighted piece means erase those weights.
  7. The same seed replays order. Another seed may change order, but coverage, shapes by chunk width, boundaries, and one contribution per target remain.
  8. Bengio supplies the online and $K$-example language-model context, Vaswani the Transformer token batches, and Brown the later LLM batch sizes in tokens.
</details>

{/* chapter-section:decoder-connection */}

## Hand token-mean gradients to AdamW

The cumulative training path can now turn separate causal windows into reproducible mini-batches of fixed-length rows and produce token-mean loss plus gradient coordinates. Chapter 22 maps those averaged coordinates to stable named parameters and applies AdamW.

Batching changes which examples share an update; it does not yet change a
parameter. Chapter 22 gives each stable named parameter first- and second-moment
state, applies bias correction, and keeps weight decay separate from the
token-mean gradient learned here.
