---
{
  "chapter_id": "29-rope",
  "locale": "en",
  "content_revision": 3,
  "order": 29,
  "concept_id": "rotary-position-embedding",
  "title": "Turn query and key pairs with RoPE",
  "description": "Learn how rotary position embeddings turn query and key feature pairs by absolute position so relative offsets appear in attention dot products, with a tested Rust implementation.",
  "objective": "Rotate query and key feature pairs by absolute position, then observe signed relative positions in their dot products without changing the causal visibility boundary.",
  "worked_inputs": "First predict one pair with $q=[1,0]$, $k=[0,1]$, and $\\theta_0=1$: compare $(m,n)=(0,0)$, $(1,0)$, and the equal shift $(3,2)$. Then inspect the Rust example, which repeats $[1,0,1,0]$ at positions $0,1,2$ with $b=100$.",
  "formula": {
    "latex": "\\left(\\operatorname{RoPE}(x_m)\\right)_{2k:2k+2}=R(m\\theta_k)(x_m)_{2k:2k+2}",
    "symbols": [
      { "symbol": "\\operatorname{RoPE}", "meaning": "the pairwise rotary position transformation applied independently to a query or key" },
      { "symbol": "x_m", "meaning": "one query or key feature vector assigned to absolute position m" },
      { "symbol": "m", "meaning": "the query or input absolute position, including a caller-supplied sequence offset" },
      { "symbol": "n", "meaning": "the key absolute position" },
      { "symbol": "k", "meaning": "the feature-pair and frequency index from zero through d/2 minus one" },
      { "symbol": "2k:2k+2", "meaning": "the half-open slice selecting adjacent coordinates 2k and 2k+1" },
      { "symbol": "R", "meaning": "the counterclockwise two-dimensional rotation matrix" },
      { "symbol": "\\phi", "meaning": "an arbitrary rotation angle in radians" },
      { "symbol": "\\theta_k", "meaning": "the radians advanced per position by feature pair k" },
      { "symbol": "b", "meaning": "the positive finite frequency base; the teaching example uses 100" },
      { "symbol": "d", "meaning": "the nonzero even query or key feature width" },
      { "symbol": "q_m^{(k)}", "meaning": "the kth two-coordinate query pair at position m before rotation" },
      { "symbol": "k_n^{(k)}", "meaning": "the kth two-coordinate key pair at position n before rotation" },
      { "symbol": "n-m", "meaning": "the signed key-minus-query position difference, not an unsigned distance" },
      { "symbol": "I", "meaning": "the two-dimensional identity matrix" },
      { "symbol": "y", "meaning": "the rotated output corresponding to input x" },
      { "symbol": "\\bar{x}", "meaning": "the derivative of scalar loss L with respect to the input x" },
      { "symbol": "\\bar{y}", "meaning": "the derivative of scalar loss L with respect to the rotated output y" }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "neural-architecture",
      "limitation": "Recurrent neural language models carried order through a state updated one token at a time; unmasked self-attention without a position signal is instead equivariant to a consistent permutation of its content rows.",
      "later_advance": "Unlike the original Transformer's additive input encoding, RoFormer's query-key rotations introduce an inner-product term that depends on the signed position difference.",
      "modern_llm_role": "In the original LLaMA, RoPE shapes query-key score geometry at each Transformer layer while the causal mask separately controls visibility.",
      "sources": [
        { "role": "earlier", "year": 2017, "name": "Vaswani et al., Attention Is All You Need", "source_url": "https://arxiv.org/abs/1706.03762", "claim": "Vaswani et al. remove recurrence and convolution, add positional encodings to input embeddings, and compare fixed sinusoidal and learned position representations." },
        { "role": "later", "year": 2021, "name": "Su et al., RoFormer: Enhanced Transformer with Rotary Position Embedding", "source_url": "https://arxiv.org/abs/2104.09864", "claim": "Su et al. encode absolute position through rotations of query and key subspaces and derive a self-attention inner product whose positional term uses their position difference." },
        { "role": "later", "year": 2023, "name": "Touvron et al., LLaMA: Open and Efficient Foundation Language Models", "source_url": "https://arxiv.org/abs/2302.13971", "claim": "Touvron et al. document that the original LLaMA removes absolute positional embeddings and applies RoPE at each Transformer layer." }
      ]
    },
    "approach": "From order carried by recurrent state, through position vectors added to embeddings, to multiplicative query-key rotations",
    "summary": "Before masking, content-only self-attention has no relative-position geometry. Absolute rotary angles add that geometry, while the shared rotation identity makes a fixed query-key dot product depend on the signed difference n-m as well as contents and pair frequencies.",
    "rust_source": "rust/demos/ch29-rope/src/lib.rs"
  },
  "rust_sources": [
    { "path": "rust/demos/ch29-rope/src/lib.rs", "region": "historical-position-contrast", "purpose": "Show how the original Transformer-style sinusoidal method adds a position vector to an embedding, in contrast with later query-key rotations." },
    { "path": "rust/crates/llm-from-scratch/src/attention/rope.rs", "region": "rope-tables", "purpose": "Validate the complete deterministic table specification, then allocate and retain its inverse frequencies, sines, and cosines exactly once during RoPE construction." },
    { "path": "rust/crates/llm-from-scratch/src/attention/rope.rs", "region": "rope-rotation", "purpose": "Validate the final feature width and checked absolute interval before rotating the penultimate token axis." },
    { "path": "rust/crates/llm-from-scratch/src/autograd/model_ops.rs", "region": "rotary-pairs-forward", "purpose": "Apply adjacent rotations in linear time and reuse their transpose for the local reverse rule." },
    { "path": "rust/demos/ch29-rope/src/main.rs", "purpose": "Run the complete Chapter 29 executable example." }
  ],
  "visualization": {
    "decision": "useful",
    "id": "rotary-position-pairs",
    "rationale": "Position rows make the two rotation rates inspectable, while repeated fixed content makes equal signed offsets visible along dot-matrix diagonals and lets two equally shifted position ranges demonstrate the local invariant."
  },
  "decoder_connection": "The cumulative single-head path can rotate query and key rows before causal scoring. Chapter 30 reshapes projected queries and keys into heads, applies RoPE along each head's final feature axis, runs attention per head, concatenates the head outputs, and projects them."
}
---

import RustSource from '../../../components/RustSource.astro';
import RopeDiagram from '../../../components/chapters/RopeDiagram.astro';

export const diagramLabels = {
  title: 'Watch absolute rotations reveal relative query-key positions',
  description:
    'Compare two pair frequencies, their rotations, a query-key dot matrix, equal-shift evidence, reverse-mode values, valid shapes, rejected boundaries, and the path to modern decoder LLMs.',
  sections: {
    rotations: 'Follow two feature pairs through three absolute positions',
    dots: 'Read signed relative positions in the query-key dot matrix',
    evidence: 'Check norms, shapes, boundaries, and reverse mode',
    history: 'Follow position information toward modern decoder LLMs',
  },
  fields: {
    position: 'Absolute position',
    pair: 'Coordinate pair',
    featureCoordinates: 'Feature coordinates',
    frequency: 'Radians per position',
    angle: 'Absolute angle',
    beforeRotation: 'Before rotation',
    afterRotation: 'After rotation',
    queryPosition: 'Query position',
    keyPosition: 'Key position',
    relativeOffset: 'Key position relative to query',
    dot: 'Rotated dot product',
    originalPositions: 'Original absolute positions',
    shiftedPositions: 'Equally shifted positions',
    originalGrid: 'Dot matrix at positions zero through two',
    shiftedGrid: 'Dot matrix at positions three through five',
    norm: 'Norm preservation',
    shape: 'Preserved tensor shapes',
    errors: 'Rejected boundaries',
    tensor: 'Input tensor',
    outputAdjoint: 'Output adjoint',
    inputAdjoint: 'Input adjoint',
    reverseMode: 'Transpose rotation in reverse mode',
    commonShiftCheck: 'Fixed-content equal-shift check',
    gradientCheck: 'All-coordinate gradient check',
    earlier: 'Order carried by recurrent state',
    transformer: 'Position vectors added to embeddings',
    rotary: 'Query-key pair rotations',
    modern: 'RoPE in an influential decoder LLM',
    causalBoundary: 'Visibility remains a separate decision',
  },
  cues: {
    fastPair: 'Fast pair: solid cue',
    slowPair: 'Slow pair: dashed cue',
    zeroOffset: 'Equal positions: double cue',
    positiveOffset: 'Key lies later: solid cue',
    negativeOffset: 'Key lies earlier: dashed cue',
    verified: 'Checked',
    rejected: 'Rejected',
  },
  captions: {
    legend: 'Legend for pair frequencies and signed relative positions',
    rotations: 'The first pair advances one radian per position; the second advances one tenth of a radian.',
    dots: 'Because this example repeats the same content, cells with the same signed relative position repeat along diagonals.',
    evidence: 'Orthogonal rotations preserve norms and shapes; the transpose carries output adjoints back to the inputs.',
    history: 'Position information moved from sequential state to explicit vectors and then into query-key score geometry.',
  },
  scrollers: {
    rotations: 'Scrollable rotary feature-pair table',
    dots: 'Scrollable relative-position dot-product table',
    originalShift: 'Scrollable dot matrix for original positions zero through two',
    shiftedShift: 'Scrollable dot matrix for shifted positions three through five',
    gradients: 'Scrollable rotary reverse-mode values',
    history: 'Scrollable history of position information in LLMs',
  },
  shapeCases: {
    rank3: 'Batch, token, and feature axes',
    rank4: 'Batch, head, token, and feature axes',
    emptyLeading: 'Empty leading axis',
    emptyTokens: 'Empty token axis at table capacity',
  },
  historyDetails: {
    earlier: 'A recurrent state changed once per token, carrying order through sequential computation.',
    transformer: 'The original Transformer added explicit position vectors to token embeddings.',
    rotary: 'RoPE rotates query and key pairs by absolute position so their dot product contains a signed relative-position term.',
    modern: 'The original LLaMA applies RoPE at every Transformer layer.',
    causalBoundary: 'RoPE changes score geometry. The causal mask still decides which keys a query may see.',
  },
  errorCases: {
    oddWidth: 'Odd final feature width',
    inputRank: 'Input below rank two',
    widthMismatch: 'Configured and input widths differ',
    positionRange: 'Position interval exceeds the table',
    offsetOverflow: 'Position interval overflows',
    releasedInput: 'Released autodiff operand',
  },
};

{/* chapter-section:worked-example */}

## Predict one pair before looking at the table

Use one two-coordinate query/key pair and the first frequency:

$$
q=[1,0],\qquad k=[0,1],\qquad \theta_0=1.
$$

At equal positions $m=n=0$, neither vector rotates, so

$$
q^\top k=0.
$$

Now put the query at $m=1$ while the key stays at $n=0$:

$$
R(1)q\approx[0.540302,0.841471],\qquad R(0)k=[0,1].
$$

Their dot product is approximately $0.841471$. Shift both absolute positions
by $2$: $(m,n)=(3,2)$ still gives $n-m=-1$, so the dot product for these
unchanged vectors remains approximately $0.841471$. Both rotated coordinates
change, but their relative rotation does not.

The executable example expands this idea to $d=4$. It repeats $[1,0,1,0]$ at
positions $0$, $1$, and $2$, uses $b=100$, and therefore has frequencies

$$
[\theta_0,\theta_1]=[1,0.1].
$$

Before running it, predict which pair turns faster, what happens at position
$0$, and which cells should match after every query and key position is shifted
by $3$ while their contents stay fixed.

{/* chapter-section:formula */}

## Rotate adjacent coordinates by absolute position

For each adjacent feature pair,

$$
\left(\operatorname{RoPE}(x_m)\right)_{2k:2k+2}=R(m\theta_k)(x_m)_{2k:2k+2}
$$

The rotation matrix and frequency schedule are

$$
R(\phi)=
\begin{bmatrix}
\cos\phi&-\sin\phi\\
\sin\phi&\cos\phi
\end{bmatrix},
\qquad
\theta_k=b^{-2k/d}.
$$

This implementation pairs coordinates $2k$ and $2k+1$. Other implementations
may arrange the pairs differently. Here $d$ must be nonzero and even so every
coordinate has a partner.

Position $m=0$ gives a zero angle for every pair, and each rotation is
orthogonal:

$$
R(0)=I,
\qquad
R(\phi)^\top R(\phi)=I,
\qquad
\lVert R(\phi)x\rVert_2=\lVert x\rVert_2.
$$

When a query at $m$ meets a key at $n$,

$$
\bigl(R(m\theta_k)q_m^{(k)}\bigr)^\top
\bigl(R(n\theta_k)k_n^{(k)}\bigr)
=(q_m^{(k)})^\top R((n-m)\theta_k)k_n^{(k)}.
$$

This follows from

$$
R(\alpha)^\top R(\beta)=R(\beta-\alpha).
$$

The rotations receive absolute positions. The signed difference $n-m$ appears
only when the two rotations combine in the dot product. The score still depends
on the query/key contents and every $\theta_k$; it is not a function of position
difference alone.

Let $L$ be a scalar loss, $\bar{y}=\partial L/\partial y$ the output adjoint,
and $\bar{x}=\partial L/\partial x$ the input adjoint. Reverse mode applies the
transposed rotation:

$$
\begin{bmatrix}\bar{x}_{2k}\\\bar{x}_{2k+1}\end{bmatrix}
=R(m\theta_k)^\top
\begin{bmatrix}\bar{y}_{2k}\\\bar{y}_{2k+1}\end{bmatrix}.
$$

{/* chapter-section:symbol-glossary */}

## Keep position, pair, and visibility roles distinct

- $q_m^{(k)}$ and $k_n^{(k)}$ are the two-coordinate query and key pairs at
  absolute positions $m$ and $n$ before rotation.
- $k$ selects one adjacent coordinate pair and its frequency $\theta_k$.
- $\phi$ is any rotation angle, and $I$ is the identity matrix.
- $n-m$ is the signed position of the key relative to the query, not an
  unsigned distance.
- A bar denotes a derivative of $L$: $\bar{y}$ arrives from later operations,
  and $\bar{x}$ is sent to earlier ones.

Without a position signal and before masking, content-only self-attention is
equivariant to a consistent permutation of its rows: permuting queries, keys,
and values together merely permutes the outputs. RoPE adds relative-position
geometry to query-key scores. It does not decide visibility. Chapter 28's causal
mask still blocks future keys, and values $V$ are not rotated here.

{/* chapter-section:history */}

## From recurrent order to rotary score geometry

Recurrent neural language models carried order through a state updated one
token at a time; unmasked self-attention without a position signal is instead
equivariant to a consistent permutation of its content rows.

[Vaswani et al. describe the original Transformer](https://arxiv.org/abs/1706.03762).
Vaswani et al. remove recurrence and convolution, add positional encodings to
input embeddings, and compare fixed sinusoidal and learned position
representations. Their sinusoidal choice was
motivated by a hypothesis about learning fixed offsets, not a guarantee of
extrapolation to every longer context.

The additive approach changes an embedding before it becomes a query, key, or
value. This small Rust function shows that operation directly:

<RustSource
  path="rust/demos/ch29-rope/src/lib.rs"
  region="historical-position-contrast"
  caption="Add a sinusoidal position vector to one embedding"
  label="Rust source contrasting additive sinusoidal position encoding with RoPE"
/>

[Su et al. introduce RoFormer](https://arxiv.org/abs/2104.09864). Su et al.
encode absolute position through rotations of query and key subspaces and
derive a self-attention inner product whose positional term uses their position
difference. Unlike the original Transformer's additive input encoding,
RoFormer's query-key rotations introduce an inner-product term that depends on
the signed position difference.

[Touvron et al. describe the original LLaMA](https://arxiv.org/abs/2302.13971).
Touvron et al. document that the original LLaMA removes absolute positional
embeddings and applies RoPE at each Transformer layer. In the original LLaMA,
RoPE shapes query-key score geometry at each Transformer layer while the causal
mask separately controls visibility. This example does not make RoPE universal,
guarantee length extrapolation, or prove that an entire decoder is invariant to
a shifted input.

{/* chapter-section:rust-implementation */}

## Precompute once, rotate each query and key row

`RotaryEmbedding::new` validates a nonzero even width, a nonzero position
capacity, and a positive finite base. The complete table specification also
checks that the number of position-pair cells fits in `usize`, that each array
length is a representable `Vec<f64>` capacity, and that every inverse frequency
is finite and nonnegative. For one pair, multiplying that frequency by larger
nonnegative positions cannot produce a smaller angle. A finite angle also has a
finite sine and cosine. The storage-free check can therefore prove that every
table cell is finite by testing the last position for each pair. If that last
angle is not finite, binary search finds the pair's first failing position; the
smallest resulting position-pair index preserves row-major error order.

This bounded check grows with the number of coordinate pairs and, only for a
failing pair, the logarithm of the position capacity—not with the number of
table cells. It allocates no table storage and does not test whether the
allocator can satisfy an otherwise representable request; only construction can
report that failure. Construction then reserves the three arrays, visits every
cell in row-major order, and retains one inverse frequency per pair and one
sine/cosine row per absolute position. A decoder can thus validate the logical
RoPE table implied by an existing attention layout without building a temporary
table, while the actual attention component still owns the tables it constructs.

<RustSource
  path="rust/crates/llm-from-scratch/src/attention/rope.rs"
  region="rope-tables"
  caption="Prove every rotary table value is finite, then materialize the table only during RoPE construction"
  label="Rust source building inverse-frequency and trigonometric tables for RoPE"
/>

`RotaryEmbedding::rotate` interprets the penultimate axis as tokens and the
final axis as features. It accepts $[T,d]$, $[B,T,d]$, $[B,H,T,d]$, and
independent leading axes. For supplied offset $o$, it checks the interval

$$
[o,o+T).
$$

An empty token interval may start at the table capacity; a nonempty interval
may not cross it.

<RustSource
  path="rust/crates/llm-from-scratch/src/attention/rope.rs"
  region="rope-rotation"
  caption="Validate shapes and select the absolute-position interval"
  label="Rust source applying the rotary table to a tensor"
/>

The differentiable operation visits each adjacent pair once, without building
a dense $d\times d$ matrix. Its inverse branch applies the transpose for the
vector-Jacobian product:

<RustSource
  path="rust/crates/llm-from-scratch/src/autograd/model_ops.rs"
  region="rotary-pairs-forward"
  caption="Rotate adjacent pairs and reverse them with the transpose"
  label="Rust source implementing differentiable pair rotation"
/>

The example calls the same operation independently for $Q$ and $K$. Central
differences check all $12$ coordinates of each input with

$$
h=10^{-6},\qquad \varepsilon_g=4\times10^{-6}.
$$

It also checks signs, norms, every dot-matrix cell, an equal position shift,
rank-two through rank-four layouts, empty axes, invalid configurations,
position-range errors, finite saved values, and deterministic replay. Run
`cargo run --quiet --locked -p ch29-rope` to inspect the complete values. The
example executable constructs and prints that deterministic report:

<RustSource
  path="rust/demos/ch29-rope/src/main.rs"
  caption="Run the complete RoPE example"
  label="Rust entry point for the Chapter 29 RoPE example"
/>

{/* chapter-section:visualization */}

## Read the pair rotations and relative diagonals

<RopeDiagram labels={diagramLabels} />

The matrix before the equal shift is

$$
\begin{bmatrix}
2&1.535306&0.563920\\
1.535306&2&1.535306\\
0.563920&1.535306&2
\end{bmatrix}.
$$

Because this example repeats the same query and key content at every position,
equal signed offsets repeat along diagonals. Moving the unchanged content from
absolute positions $0,1,2$ to $3,4,5$ changes the rotated coordinates but keeps
all nine dot products equal within $10^{-12}$. This numerical comparison checks
one instance of the algebraic identity; the identity itself is established by
the rotation equation above.

{/* chapter-section:exercises */}

## Predict, then check the invariant

1. Prove that position $m=0$ is the identity for every pair.
2. Compute the single-pair result for $(m,n)=(1,0)$.
3. Predict what changes after adding the same offset to both positions while
   holding query and key contents fixed.
4. Explain why shifting only $m$ generally changes the dot product.
5. Derive norm preservation from $R(\phi)^\top R(\phi)=I$.
6. Decide whether $V$ or the causal mask should be rotated.
7. Explain why this full-width adjacent-pair implementation rejects $d=3$.
8. Distinguish the absolute inputs from the signed difference in the dot
   product.
9. Explain why the local fixed-content identity does not prove shift invariance
   for an entire decoder.

<details>
  <summary>Check the key distinctions</summary>

  1. Position $m=0$ gives angle $0$ for every pair, so $R(0)=I$.
  2. At $(m,n)=(1,0)$, the dot product is approximately $0.841471$.
  3. Adding $c$ to both positions keeps $(n+c)-(m+c)=n-m$, so the dot product
     for unchanged contents stays fixed although both absolute rotations change.
  4. Shifting only $m$ changes $n-m$ and therefore generally changes the result;
     special contents or periodic angles can still produce equality.
  5. $R(\phi)^\top R(\phi)=I$ preserves squared norm and therefore norm.
  6. This chapter rotates $Q$ and $K$, not $V$; the causal mask independently
     decides which keys are visible.
  7. Width $3$ leaves one coordinate without a partner.
  8. Absolute indices set local angles; the signed relative position appears
     only after the query and key rotations combine.
  9. A whole-model invariance claim would require checking every component and
     boundary and preserving the same content and context. This local identity
     alone is insufficient.
</details>

Misconception: RoPE receives relative indices.

Correction: it receives absolute positions. Shared rotation algebra makes a
fixed query-key dot product depend on their signed relative difference. RoPE
does not replace causal masking, and the score still depends on content and
frequency.

{/* chapter-section:decoder-connection */}

## Hand the position-aware axis to multiple heads

The current single-head path rotates each query and key row before Chapter 28's
causal score computation. Chapter 30 first reshapes projected queries and keys
into heads, applies RoPE along each head's final feature axis, performs causal
attention independently per head, concatenates the head outputs, and applies
the learned output projection $W_O$.

Partial rotary dimensions, alternate pair layouts, key/value caches, prefill,
and long-context frequency scaling remain outside this chapter.
