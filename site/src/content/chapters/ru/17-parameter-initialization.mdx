---
{
  "chapter_id": "17-parameter-initialization",
  "locale": "ru",
  "content_revision": 3,
  "order": 17,
  "concept_id": "parameter-initialization",
  "title": "Воспроизводимо инициализируйте обучаемые веса",
  "description": "Воспроизводимо инициализируйте матрицы весов с учётом ширины, сравните нулевую инициализацию, равномерную выборку с удвоенной границей и масштаб по схеме Ксавье, а затем проследите ожидаемую дисперсию по глубине.",
  "objective": "Воспроизводимо создавать именованные обучаемые матрицы весов с масштабом, учитывающим входную и выходную ширину, и различать отдельные правила начальных значений для смещений, коэффициентов нормализации и таблиц токенов.",
  "worked_inputs": "Возьмите начальное значение генератора 17 и инициализируйте проекцию [2,2] с входной и выходной шириной 2. Предскажите целевую дисперсию 1/2, стандартное отклонение 1/sqrt(2), границу равномерного распределения sqrt(3/2), точное воспроизведение того же запроса, наблюдаемое отличие для начального значения 18 и неодинаковые столбцы выбранной матрицы. Сопоставьте результат с нулевым слоем SiLU 2-на-2, в котором одинаково обрабатываемые скрытые нейроны получают одинаковые столбцы градиента.",
  "formula": {
    "latex": "\\operatorname{Var}(W_{ij})=\\frac{2}{\\operatorname{fan}_{in}+\\operatorname{fan}_{out}}",
    "symbols": [
      {
        "symbol": "W",
        "meaning": "одна матрица весов до обучения"
      },
      {
        "symbol": "i",
        "meaning": "индекс входной координаты одного веса"
      },
      {
        "symbol": "j",
        "meaning": "индекс выходной координаты одного веса"
      },
      {
        "symbol": "W_{ij}",
        "meaning": "вес, соединяющий входную координату i с выходной координатой j"
      },
      {
        "symbol": "\\operatorname{Var}(W_{ij})",
        "meaning": "целевая дисперсия распределения инициализации, а не измеренная дисперсия одной конечной матрицы"
      },
      {
        "symbol": "\\operatorname{fan}_{in}",
        "meaning": "число входных значений, суммируемых в одном выходе"
      },
      {
        "symbol": "\\operatorname{fan}_{out}",
        "meaning": "число выходов, в которые поступает каждое входное значение"
      },
      {
        "symbol": "2",
        "meaning": "компромисс между условиями сохранения дисперсии в прямом проходе по входной ширине и в обратном проходе по выходной ширине"
      }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "language-model",
      "limitation": "Bengio и соавторы совместно обучают признаки слов и матрицы нейросети для предсказания следующего слова и сообщают, что признаки слов инициализируются случайно, подобно весам нейросети. В работе не задано ни учитывающее размерности, ни воспроизводимое правило инициализации; при последовательном применении обучаемых преобразований через много слоёв произвольный масштаб становится всё более существенным.",
      "later_advance": "Glorot и Bengio выводят компромисс для нормированной дисперсии глубокой сети прямого распространения при явно сформулированных допущениях о почти линейном режиме и независимости. Позже Vaswani и соавторы размещают обучаемые эмбеддинги на границах модели, а проекции внимания, выходную проекцию внимания и матрицы сети прямого распространения повторяют в слоях Transformer, поэтому в одной языковой модели появляется множество обучаемых матриц, масштаб которых зависит от ширины.",
      "modern_llm_role": "В этой главе матрицы весов будущего декодера получают значения из воспроизводимой выборки, стабильные имена и явно заданные целевые дисперсии, учитывающие ширину. В создаваемом здесь декодере матрицы весов инициализируются равномерно по схеме Ксавье, необязательные смещения — нулями, коэффициенты RMSNorm — единицами, а для таблицы токенов принято отдельное правило на основе её формы. Это явные правила реализации, а не утверждение, что исходная работа о Transformer предписывает их или что дисперсия любого сигнала сохраняется точно.",
      "sources": [
        {
          "role": "earlier",
          "year": 2003,
          "name": "Bengio et al., A Neural Probabilistic Language Model",
          "source_url": "https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf",
          "claim": "Bengio и соавторы задают обучаемую матрицу признаков слов и матрицы параметров нейросети для предсказания следующего слова, оптимизируют их совместно и сообщают, что признаки слов инициализируются случайно, подобно весам нейросети."
        },
        {
          "role": "later",
          "year": 2010,
          "name": "Glorot and Bengio, Understanding the difficulty of training deep feedforward neural networks",
          "source_url": "https://proceedings.mlr.press/v9/glorot10a/glorot10a.pdf",
          "claim": "Glorot и Bengio при сформулированных упрощающих допущениях уравновешивают условия по дисперсии для входной и выходной ширины. Получается целевая дисперсия 2, делённая на сумму этих ширин, и нормированная равномерная инициализация, симметричная относительно нуля."
        },
        {
          "role": "later",
          "year": 2017,
          "name": "Vaswani et al., Attention Is All You Need",
          "source_url": "https://papers.nips.cc/paper_files/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf",
          "claim": "Vaswani и соавторы используют обучаемые эмбеддинги на границах модели, а в слоях Transformer повторяют проекции запросов, ключей и значений, выходную проекцию внимания и две проекции сети прямого распространения; работа не предписывает инициализатор параметров."
        }
      ]
    },
    "approach": "От случайно инициализированных признаков нейросетевой языковой модели к начальному масштабу, учитывающему ширину последовательных обучаемых преобразований",
    "summary": "Ранние нейросетевые модели следующего слова показали практичность обучаемых векторов слов и матриц нейросети и случайно инициализировали признаки слов. Позже инициализация с учётом дисперсии стала ограничивать многократное изменение масштаба по глубине, а в Transformer множество обучаемых проекций повторяется в слоях. Разобранный пример показывает один случай сохранения симметрии при нулевых весах, выбранные значения для начального значения генератора 17 и стабильный порядок именованных параметров, не приписывая этим работам генератор, имена, ошибки или общие правила создаваемого декодера.",
    "rust_source": "rust/demos/ch17-parameter-initialization/src/lib.rs"
  },
  "rust_sources": [
    {
      "path": "rust/demos/ch17-parameter-initialization/src/lib.rs",
      "region": "zero-symmetry-probe",
      "purpose": "Показать одинаковые столбцы градиента скрытых нейронов в одном пути SiLU с нулевыми начальными весами."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/nn/init.rs",
      "region": "parameter-init-errors",
      "purpose": "Объявить типизированные ошибки имён, ширины, формы, выделения памяти, тензоров и повторяющихся параметров."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/nn/init.rs",
      "region": "deterministic-prng",
      "purpose": "Задать точный поток SplitMix64 без внешних зависимостей и его отображение в binary64."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/nn/init.rs",
      "region": "xavier-initialization",
      "purpose": "Проверить обе ширины, вычислить масштаб по схеме Ксавье и сгенерировать значения в построчном порядке внутри заданной границы."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/nn/init.rs",
      "region": "named-parameters",
      "purpose": "Проверить стабильные имена из разделённых точками сегментов, сохранить порядок объявления и отклонить повторы."
    },
    {
      "path": "rust/demos/ch17-parameter-initialization/src/lib.rs",
      "region": "fixed-seed-parameter",
      "purpose": "Построить проекцию для начального значения 17 и сопоставить повторный запрос с запросом для начального значения 18."
    },
    {
      "path": "rust/demos/ch17-parameter-initialization/src/lib.rs",
      "region": "named-parameter-enumeration",
      "purpose": "Перечислить два стабильных имени параметров и сравнить идентичность клона с независимо построенным тензором."
    },
    {
      "path": "rust/demos/ch17-parameter-initialization/src/lib.rs",
      "region": "initialization-errors-example",
      "purpose": "Показать порядок проверок и убедиться, что отклонённые запросы не изменяют состояние генератора."
    },
    {
      "path": "rust/demos/ch17-parameter-initialization/src/main.rs",
      "region": "learner-parameter-initialization-output",
      "purpose": "Вывести выбранный масштаб, значения, проверку симметрии и идентичности, ошибки и переход к следующей главе."
    },
    {
      "path": "rust/demos/ch17-parameter-initialization/src/diagram_trace.rs",
      "region": "parameter-initialization-trace",
      "purpose": "Вычислить точные гистограммы, статистики конечной выборки, теоретическое распространение дисперсии и свидетельства воспроизводимости."
    }
  ],
  "visualization": {
    "decision": "useful",
    "id": "parameter-initialization",
    "rationale": "Сопоставление распределений при одном исходном состоянии и теоретической дисперсии линейных слоёв наглядно показывает схлопывание при нулевых весах, разброс при удвоенной границе и распространение масштаба, учитывающего ширину; по одним итоговым значениям параметров или текстовому списку эти связи не видны."
  },
  "decoder_connection": "Теперь совокупная реализация умеет воспроизводимо создавать именованные обучаемые матрицы с явно заданным масштабом, учитывающим входную и выходную ширину. В главе 18 матрица станет таблицей эмбеддингов: ID токенов выбирают строки, повторяющиеся ID обращаются к одной строке, а их градиентные вклады суммируются. Для этой таблицы матричный инициализатор получает размер словаря и ширину признаков как два размера формы — это отдельное принятое правило, а не следствие дисперсии операции выбора строки."
}
---

import ParameterInitializationDiagram from '../../../components/chapters/ParameterInitializationDiagram.astro';

export const diagramLabels = {
  title: 'Сопоставьте нулевые веса с двумя связанными масштабами',
  description:
    'Сравните конечные выборки нулевой инициализации, равномерной инициализации с удвоенной границей и схемы Ксавье; затем проследите теоретическую дисперсию через четыре независимых линейных слоя.',
  summary: {
    seed: 'Общее начальное значение',
    width: 'Форма матрицы',
    samples: 'Число весов',
    fanIn: 'Входная ширина',
    fanOut: 'Выходная ширина',
    inputVariance: 'Дисперсия входа',
    generator: 'Генератор и отображение',
    statistic: 'Статистика',
    assumption: 'Дисперсия совокупности, рассчитанная в два прохода',
  },
  sections: {
    distributions: 'Сопоставьте распределения весов',
    propagation: 'Проследите ожидаемую дисперсию по глубине',
    reproducibility: 'Проверьте границы воспроизводимости',
  },
  fields: {
    range: 'Интервал',
    count: 'Количество',
    share: 'Доля выборки',
    strategy: 'Правило',
    seed: 'Начальное значение',
    limit: 'Граница распределения',
    minimum: 'Минимум выборки',
    maximum: 'Максимум выборки',
    mean: 'Среднее выборки',
    variance: 'Дисперсия совокупности',
    layer: 'Глубина',
    limitRatio: 'Отношение границ',
  },
  strategies: {
    zero: 'Нулевые веса',
    oversized: 'Равномерные веса с удвоенной границей',
    xavier: 'Равномерные веса по схеме Ксавье',
  },
  states: {
    noSeed: 'Без выборки',
    sameStream: 'Общие исходные значения.',
    sameSeedEqual: 'Тот же запрос при том же начальном значении даёт точное совпадение',
    alternateSeedDifferent: 'Другое выбранное начальное значение даёт иной результат',
  },
  symbols: {
    zero: '0',
    oversized: '2×',
    xavier: 'X',
    same: '=',
    different: '≠',
  },
  binClosure:
    'Левая граница входит во все интервалы, правая — только в последний.',
  propagationAssumption:
    'Это ожидаемые дисперсии для независимых линейных слоёв с единичной дисперсией входа, а не измеренные гарантии для нелинейного декодера с остаточными связями.',
  pairing: 'Сравнение на общей выборке',
  accessibility: {
    histogramTable: 'Измеренные гистограммы конечных выборок для трёх правил начальных значений',
    histograms: {
      zero: 'Строка гистограммы для нулевых весов',
      oversized: 'Строка гистограммы для весов с удвоенной границей равномерного распределения',
      xavier: 'Строка гистограммы для равномерно инициализированных по схеме Ксавье весов',
    },
    binTemplate: 'Интервал {range}; количество {count}; доля {share}',
    propagationTable: 'Теоретическая ожидаемая дисперсия по глубине линейных слоёв',
  },
};

{/* chapter-section:worked-example */}

## Предскажите масштаб при заданном начальном значении и проявление симметрии

Инициализируйте проекцию формы `[2,2]`, задав начальное значение генератора `17`,
входную ширину $2$ и выходную ширину $2$. До запуска примера предскажите целевую
дисперсию $1/2$, стандартное отклонение $1/\sqrt{2}$ и границу равномерного
распределения с центром в нуле, равную $\sqrt{3/2}$. После округления до двенадцати
знаков генератор выдаёт в построчном порядке такие значения:

```text
 0.004950883736  -0.265932089217
-0.420504358848  -0.676313443233
```

Если совпадают начальное значение генератора, форма, входная и выходная ширина и
порядок создания, тот же тензор должен воспроизводиться побитно. В выбранном примере начальное значение `18`
даёт другой тензор. Эти свойства можно проверить, хотя вручную вычислять
последовательность псевдослучайных чисел учащемуся не требуется.

Теперь сопоставьте неодинаковые столбцы выбранной матрицы с вычислительным путём
из двух нейронов, начальные веса которых равны нулю. Пусть $x=[1,-1]$, входная матрица
формы `[2,2]` нулевая, после неё применяется SiLU, а выходные веса равны `[1,1]`.
Скалярный выход равен нулю. Производная SiLU в нуле равна $1/2$, поэтому оба
столбца градиента входных весов равны $[0.5,-0.5]^\top$. Одинаковое обновление
сохраняет равенство скрытых нейронов.

<RustSource
  path="rust/demos/ch17-parameter-initialization/src/lib.rs"
  region="zero-symmetry-probe"
  caption="Дифференцировать заданный путь SiLU с нулевыми весами и показать одинаковые столбцы градиента"
  label="Исходный код на Rust с доказательством симметрии скрытых нейронов при нулевой инициализации из главы 17"
/>

Это пример одного конкретного случая сохранения симметрии, а не правило, запрещающее любые нулевые
начальные значения. Нулевые смещения и единичные коэффициенты нормализации могут
быть осознанным выбором: они не создают двух одинаково обрабатываемых обучаемых
столбцов признаков.

{/* chapter-section:formula */}

## Задайте целевое распределение, а не точную статистику одной выборки

Основная формула главы:

$$
\operatorname{Var}(W_{ij})=\frac{2}{\operatorname{fan}_{in}+\operatorname{fan}_{out}}
$$

Для равномерного распределения на интервале $[-a,a)$ с центром в нуле дисперсия
равна $a^2/3$. Приравняв её к значению из основной формулы, получаем
$a=\sqrt{6/(\operatorname{fan}_{in}+\operatorname{fan}_{out})}$.

Слово «целевая» здесь существенно. Четыре выбранных числа не обязаны иметь
дисперсию, в точности равную теоретической дисперсии распределения. Правило также
не гарантирует сохранения масштаба сигнала: при его выводе используются
упрощающие допущения о независимости и почти линейном режиме, тогда
как будущий декодер содержит нелинейные функции, нормализацию, остаточные пути,
данные и оптимизацию.

Точнее, при таком уравновешивании предполагаются независимые веса плотных
слоёв, одинаковая дисперсия входных признаков и симметричная функция активации
вблизи линейного режима с единичным наклоном. SiLU не вполне удовлетворяет
последнему допущению. В создаваемом здесь декодере для обучаемых матриц
используется инициализация по схеме Ксавье, необязательные смещения начинаются с
нуля, а коэффициенты RMSNorm — с единицы. Позже матричный инициализатор будет
использован для таблицы токенов с размером словаря и шириной признаков в качестве
двух размеров формы. Это принятое правило реализации, а не следствие дисперсии
операции выбора строки.

{/* chapter-section:symbol-glossary */}

## Обозначьте вес и обе ширины

| Символ | Практический смысл |
| :--- | :--- |
| $W$ | Одна матрица весов до обучения. |
| $i$ | Индекс входной координаты одного веса. |
| $j$ | Индекс выходной координаты одного веса. |
| $W_{ij}$ | Вес, соединяющий входную координату $i$ с выходной координатой $j$. |
| $\operatorname{Var}(W_{ij})$ | Целевая дисперсия распределения инициализации, а не измеренная дисперсия одной конечной матрицы. |
| $\operatorname{fan}_{in}$ | Число входных значений, суммируемых в одном выходе. |
| $\operatorname{fan}_{out}$ | Число выходов, в которые поступает каждое входное значение. |
| $2$ | Компромисс между условиями сохранения дисперсии в прямом проходе по входной ширине и в обратном проходе по выходной ширине. |

При входной ширине $2$ и выходной ширине $2$ целевое стандартное отклонение
равно $0.707106781187$, а граница равномерного распределения — $1.224744871392$.
Если увеличить входную ширину до $4$, оставив выходную ширину равной $2$, эти
величины уменьшатся до $0.577350269190$ и $1.000000000000$. Ширина входит в
запрос на инициализацию: её нельзя определять задним числом после выбора
значений.

{/* chapter-section:history */}

## От признаков слов в нейросетевой модели к параметрам декодера с учётом ширины

Bengio и соавторы совместно обучают признаки слов и матрицы нейросети для
предсказания следующего слова и сообщают, что признаки слов инициализируются
случайно, подобно весам нейросети. В работе не задано ни учитывающее размерности,
ни воспроизводимое правило инициализации; при последовательном применении
обучаемых преобразований через много слоёв произвольный масштаб становится всё
более существенным.

Начальный этап развития нейросетевых языковых моделей представлен работой
[Bengio et al., *A Neural Probabilistic Language Model*](https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf).
Bengio и соавторы задают обучаемую матрицу признаков слов и матрицы параметров
нейросети для предсказания следующего слова, оптимизируют их совместно и
сообщают, что признаки слов инициализируются случайно, подобно весам нейросети.

Эта работа подтверждает использование случайных начальных значений для
обучаемых признаков и матриц языковой модели. Она не задаёт масштаб с учётом
размерностей, точное распределение, начальное значение генератора, сам генератор,
стабильные имена или порядок проверок.

Glorot и Bengio выводят компромисс для нормированной дисперсии глубокой сети
прямого распространения при явно сформулированных допущениях о почти линейном
режиме и независимости. Позже Vaswani и соавторы размещают обучаемые эмбеддинги
на границах модели, а проекции внимания, выходную проекцию внимания и матрицы
сети прямого распространения повторяют в слоях Transformer, поэтому в одной
языковой модели появляется множество обучаемых матриц, масштаб которых зависит
от ширины.

Переход к масштабу с учётом ширины описан в работе
[Glorot and Bengio, *Understanding the difficulty of training deep feedforward neural networks*](https://proceedings.mlr.press/v9/glorot10a/glorot10a.pdf).
Glorot и Bengio при сформулированных упрощающих допущениях уравновешивают
условия по дисперсии для входной и выходной ширины. Получается целевая дисперсия
2, делённая на сумму этих ширин, и нормированная равномерная инициализация,
симметричная относительно нуля. Эти допущения обосновывают выбор масштаба, но не
описывают точно декодер с SiLU, RMSNorm и остаточными связями.

Повторение обучаемых проекций в слоях Transformer описано в работе
[Vaswani et al., *Attention Is All You Need*](https://papers.nips.cc/paper_files/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf).
Vaswani и соавторы используют обучаемые эмбеддинги на границах модели, а в
слоях Transformer повторяют проекции запросов, ключей и значений, выходную
проекцию внимания и две проекции сети прямого распространения; работа не
предписывает инициализатор параметров. Масштабирование оценок внимания и
эмбеддингов относится к прямому вычислению, а не служит свидетельством
инициализации по схеме Ксавье.

В этой главе матрицы весов будущего декодера получают значения из воспроизводимой
выборки, стабильные имена и явно заданные целевые дисперсии, учитывающие ширину.
В создаваемом здесь декодере матрицы весов инициализируются равномерно по схеме
Ксавье, необязательные смещения — нулями, коэффициенты RMSNorm — единицами, а
для таблицы токенов принято отдельное правило на основе её формы. Это явные
правила реализации, а не утверждение, что исходная работа о Transformer
предписывает их или что дисперсия любого сигнала сохраняется точно.

Так случайно инициализированные признаки слов в ранних нейросетевых моделях
связываются с учитывающими дисперсию глубокими преобразованиями, а затем — с
повторяющимися проекциями Transformer.

{/* chapter-section:rust-implementation */}

## Создавайте и именуйте параметры без частичного изменения состояния

В реализации используется компактный и полностью определённый алгоритм
SplitMix64 без внешних зависимостей. Начальное значение — это исходное состояние
до первого приращения; ноль допустим. При каждом обращении старшие 53
перемешанных бита отображаются в число формата binary64, после чего выполняется
аффинное преобразование к нужному равномерному распределению. Это
детерминированная псевдослучайная выборка, а не криптографическая случайность.

<RustSource
  path="rust/crates/llm-from-scratch/src/nn/init.rs"
  region="parameter-init-errors"
  caption="Объявить стабильные ошибки недопустимых имён, ширины, формы, выделения памяти, тензоров и повторов"
  label="Исходный код на Rust с ошибками инициализации параметров из главы 17"
/>

<RustSource
  path="rust/crates/llm-from-scratch/src/nn/init.rs"
  region="deterministic-prng"
  caption="Продвинуть состояние SplitMix64 и отобразить старшие 53 бита в одно число формата binary64 на единичном интервале"
  label="Исходный код на Rust с детерминированным генератором псевдослучайных чисел из главы 17"
/>

<RustSource
  path="rust/crates/llm-from-scratch/src/nn/init.rs"
  region="xavier-initialization"
  caption="Проверить обе ширины, вывести масштаб по схеме Ксавье и сгенерировать значения в построчном порядке"
  label="Исходный код на Rust с масштабом и правилом выборки по схеме Ксавье из главы 17"
/>

<RustSource
  path="rust/crates/llm-from-scratch/src/nn/init.rs"
  region="named-parameters"
  caption="Проверить неизменяемые имена из разделённых точками сегментов и сохранить порядок объявления"
  label="Исходный код на Rust с коллекцией именованных параметров из главы 17"
/>

Имена состоят из непустых сегментов, разделённых точками. В каждом сегменте
допустимы строчные латинские буквы ASCII, цифры и знаки подчёркивания. В заданном
порядке проверяются имя, нулевая входная ширина, нулевая выходная ширина,
переполнение суммы ширин, переполнение произведения размеров формы, выделение
памяти и создание тензора. Для выборки используется клон генератора, а его
новое состояние переносится в исходный генератор только после успешного
создания обучаемого листового узла-параметра. Поэтому любая возвращённая ошибка оставляет состояние
генератора вызывающего кода неизменным.

Имя служит стабильным внешним идентификатором. Клон `NamedParameter` сохраняет
тот же узел ленты операций, тогда как независимо созданный равный
тензор получает другой узел. Числовые ID параметров, абстракции слоёв, группы
оптимизатора и формат контрольной точки в этой главе не вводятся.

<RustSource
  path="rust/demos/ch17-parameter-initialization/src/lib.rs"
  region="fixed-seed-parameter"
  caption="Построить выбранную проекцию и сопоставить начальное значение 17 с ним самим и со значением 18"
  label="Исходный код на Rust со сравнением запросов на инициализацию проекции из главы 17"
/>

<RustSource
  path="rust/demos/ch17-parameter-initialization/src/lib.rs"
  region="named-parameter-enumeration"
  caption="Сохранить порядок объявления и отличить стабильные имена от идентичности узлов времени выполнения"
  label="Исходный код на Rust с перечислением именованных параметров из главы 17"
/>

<RustSource
  path="rust/demos/ch17-parameter-initialization/src/lib.rs"
  region="initialization-errors-example"
  caption="Отклонить недопустимые запросы, не продвигая генератор вызывающего кода"
  label="Исходный код на Rust с ошибками создания параметров без частичного изменения состояния из главы 17"
/>

<RustSource
  path="rust/demos/ch17-parameter-initialization/src/main.rs"
  region="learner-parameter-initialization-output"
  caption="Подготовить точный масштаб, выбранные значения, проверку симметрии, имена, идентичность, ошибки и переход к следующей главе"
  label="Исходный код на Rust с подготовкой учебного вывода из главы 17"
/>

Запустите полный пример из корня репозитория:

```sh
./course run cargo run --quiet --locked -p ch17-parameter-initialization
```

В выводе видны теоретический масштаб, выбранные значения, сопоставление
симметрии, стабильные имена параметров и ошибки, при которых состояние
генератора остаётся прежним.

{/* chapter-section:visualization */}

## Сопоставьте распределения при общем начальном значении и ожидаемую дисперсию

В диагностическом примере форма увеличена до `[64,64]`, поэтому для каждой
стратегии получается `4096` весов. При нулевой инициализации генератор не
используется. Равномерные выборки с удвоенной границей и с границей по схеме
Ксавье используют одни и те же исходные значения генератора с начальным
состоянием `17`; первая граница ровно вдвое больше второй. Такое сопоставление
позволяет отделить влияние масштаба: программа записывает интервалы гистограммы,
количество и отображаемую долю значений в них, а также двухпроходные статистики
совокупности.

Вторая часть схемы действует только при сформулированных допущениях. Начиная с
единичной дисперсии входа, она показывает ожидаемую дисперсию после четырёх
независимых линейных слоёв: нулевые веса обращают её в ноль, удвоенная граница
умножает дисперсию на четыре в каждом слое, а при масштабе Ксавье она остаётся
равной единице. Это ожидаемые значения при допущениях о линейности и
независимости, а не измерения или гарантии для нелинейного декодера с
остаточными связями.

<RustSource
  path="rust/demos/ch17-parameter-initialization/src/diagram_trace.rs"
  region="parameter-initialization-trace"
  caption="Вычислить показанные ниже связанные распределения, гистограммы, теоретическое распространение дисперсии и сравнения начальных значений"
  label="Исходный код на Rust с вычислением сопоставления способов инициализации параметров из главы 17"
/>

<ParameterInitializationDiagram labels={diagramLabels} />

Обе равномерные выборки используют одинаковые исходные значения, поэтому их
гистограммы и статистики различаются только из-за удвоенного масштаба. Дисперсии на
гистограммах измерены по $4096$ значениям; таблица глубины, напротив, следует
теоретическим множителям целевой дисперсии при сформулированных допущениях о
линейности.

{/* chapter-section:exercises */}

## Предскажите результат до запуска Rust

1. При входной ширине $2$ и выходной ширине $2$ вычислите целевую дисперсию, стандартное отклонение и границу равномерного распределения.
2. Вычислите эти три величины заново, если входная ширина станет равной $4$, а выходная останется равной $2$.
3. Выведите два столбца градиента входных весов для примера с нулевыми весами и SiLU.
4. Какие значения должны совпасть, если одинаковы начальное значение генератора, форма, обе ширины и порядок создания?
5. Почему запрос с выбранным начальным значением `18` даёт другой результат, но это не доказывает различие для любой пары возможных начальных значений?
6. Почему измеренная дисперсия одного конечного тензора `[2,2]` не обязана в точности равняться $1/2$?
7. В каком порядке перечисляются `decoder.block.0.attention.query.weight` и `token_embedding.weight`?
8. Продвигают ли генератор отклонённое недопустимое имя или нулевая ширина и о каком повторе сообщает коллекция?
9. Предписывают ли Vaswani и соавторы инициализацию Transformer по схеме Ксавье?
10. Гарантирует ли формула точную измеренную дисперсию или сохранение масштаба любого сигнала?

<details>
  <summary>Проверьте предсказания</summary>

  1. Целевая дисперсия равна $0.5$, стандартное отклонение — $0.707106781187$, а граница — $1.224744871392$.
  2. Новые значения равны соответственно $1/3$, $0.577350269190$ и $1.000000000000$.
  3. Оба столбца равны $[0.5,-0.5]^\top$: из-за одинаковых выходных весов и производной SiLU $1/2$ оба скрытых нейрона получают один и тот же множитель обратного прохода.
  4. Для такого полностью одинакового запроса совпадают поток генератора и биты итогового тензора.
  5. В этом примере запрос с начальным значением `18` даёт другой тензор. Генератор псевдослучайных чисел детерминирован, но наблюдаемое различие не является математической гарантией уникальности для любого начального значения и запроса.
  6. Формула описывает распределение. Статистика небольшой конечной выборки подвержена выборочному разбросу.
  7. Сохраняется порядок объявления: первой идёт проекция запросов, за ней — таблица токенов.
  8. Ни одна из этих ошибок не продвигает генератор. Коллекция сообщает первое повторяющееся имя вместе с индексами первого и повторного вхождения.
  9. Нет. В работе описаны обучаемые параметры Transformer, но способ их инициализации не предписан.
  10. Нет. Схема Ксавье задаёт целевую дисперсию распределения при упрощающих допущениях; конечная выборка, нелинейные функции, нормализация, остаточные пути, глубина, данные и оптимизация по-прежнему влияют на распространение сигнала.
</details>

Главное заблуждение — считать целевую дисперсию распределения точной измеренной
дисперсией одной матрицы или обещанием, что масштаб активаций никогда не затухнет
и не возрастёт чрезмерно. Инициализация по схеме Ксавье — ясная исходная точка
с ограниченным диапазоном, но такой гарантии она не даёт.

{/* chapter-section:decoder-connection */}

## Придайте инициализированной матрице смысл таблицы токенов

Теперь совокупная реализация умеет воспроизводимо создавать именованные
обучаемые матрицы с явно заданным масштабом, учитывающим входную и выходную
ширину. В главе 18 матрица станет таблицей эмбеддингов: ID токенов выбирают
строки, повторяющиеся ID обращаются к одной строке, а их градиентные вклады
суммируются. Для этой таблицы матричный инициализатор получает размер словаря и
ширину признаков как два размера формы — это отдельное принятое правило, а не
следствие дисперсии операции выбора строки.

Если несколько параметров используют общий поток генератора, порядок их
создания влияет на результат: вставка более раннего параметра изменит все
последующие выбранные значения. Поэтому порядок следует задавать осознанно и
проверять. Глава 18 добавит семантику выбора токенов, в главе 21 перемешивание
данных получит отдельный поток генератора, глава 22 введёт состояние
оптимизатора, а глава 35 сохранит значения параметров и сведения о происхождении
состояния обучения.
