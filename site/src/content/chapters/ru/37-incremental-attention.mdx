---
{
  "chapter_id": "37-incremental-attention",
  "locale": "ru",
  "content_revision": 4,
  "order": 37,
  "concept_id": "incremental-attention",
  "title": "Сохраняйте префикс, проецируйте только новую строку",
  "description": "Разберитесь, как привязанный к слою KV-кэш (кэш ключей и значений) добавляет повёрнутые ключи и значения без поворота, сохраняя для новой позиции результат расчёта внимания по полному префиксу.",
  "objective": "Добавьте в кэш одного слоя внимания повёрнутый ключ и значение без поворота для новой позиции и получите в этой позиции тот же результат, что и при расчёте внимания по полному префиксу.",
  "worked_inputs": "Поочерёдно подайте три строки ширины модели 4 в слой внимания с двумя головами, используйте длины кэша 0, 1 и 2 как абсолютные позиции RoPE и после каждого шага убедитесь с допуском 1e-12, что выход с кэшем совпадает с последней строкой результата для соответствующего полного префикса.",
  "formula": {
    "latex": "K^{(\\ell)}_{1:t}=[K^{(\\ell)}_{1:t-1};k^{(\\ell)}_t],\\quad V^{(\\ell)}_{1:t}=[V^{(\\ell)}_{1:t-1};v^{(\\ell)}_t]",
    "symbols": [
      {
        "symbol": "K^{(\\ell)}_{1:t}",
        "meaning": "кэш повёрнутых ключей слоя внимания ell после добавления позиции t"
      },
      {
        "symbol": "V^{(\\ell)}_{1:t}",
        "meaning": "кэш значений слоя внимания ell после добавления позиции t"
      },
      {
        "symbol": "\\ell",
        "meaning": "слой внимания блока декодера, которому принадлежит этот кэш"
      },
      {
        "symbol": "t",
        "meaning": "номер последней позиции с единицы в этой формуле; соответствующая позиция RoPE с нумерацией от нуля равна t-1"
      },
      {
        "symbol": "1:t",
        "meaning": "все сохранённые позиции от первой до последней включительно"
      },
      {
        "symbol": "K^{(\\ell)}_{1:t-1}",
        "meaning": "ключи предыдущих позиций, уже сохранённые в слое ell"
      },
      {
        "symbol": "V^{(\\ell)}_{1:t-1}",
        "meaning": "значения предыдущих позиций, уже сохранённые в слое ell"
      },
      {
        "symbol": "k^{(\\ell)}_t",
        "meaning": "ключ последней позиции после проекции ключа в слое, разделения на головы и поворота RoPE для его абсолютной позиции"
      },
      {
        "symbol": "v^{(\\ell)}_t",
        "meaning": "значение последней позиции после проекции значения в слое и разделения на головы"
      },
      {
        "symbol": "[A;B]",
        "meaning": "конкатенация по оси позиций последовательности, при которой B добавляется после A"
      }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "inference-design",
      "limitation": "Каузальное внимание Transformer позволяет каждой новой позиции декодера обращаться к известному префиксу. Однако без сохранённых проекций цикл генерации может на каждом шаге заново вычислять неизменившиеся строки ключей и значений предыдущих позиций.",
      "later_advance": "При инкрементальном декодировании повторное использование на каждом шаге стало явным: тензоры ключей и значений сохраняются отдельно для каждого слоя. Позднее системы обслуживания LLM стали рассматривать растущий KV-кэш как центральный объект управления памятью.",
      "modern_llm_role": "При современной генерации с кэшированием каждый блок декодера хранит собственный совместимый кэш: новый запрос обращается к сохранённому префиксу, а проецируются только последние ключ и значение.",
      "sources": [
        {
          "role": "earlier",
          "year": 2017,
          "name": "Attention Is All You Need",
          "source_url": "https://arxiv.org/pdf/1706.03762",
          "claim": "Васвани и соавторы задают внимание на основе масштабированного скалярного произведения, описывают авторегрессионный декодер, который выдаёт по одному элементу, и маскируют самовнимание декодера так, чтобы позиция могла использовать только известный префикс; статья не описывает ни KV-кэширование, ни RoPE."
        },
        {
          "role": "later",
          "year": 2019,
          "name": "Fast Transformer Decoding: One Write-Head is All You Need",
          "source_url": "https://arxiv.org/pdf/1911.02150",
          "claim": "Инкрементальное многоголовое самовнимание Шейзира принимает предыдущие тензоры ключей и значений, добавляет текущую спроецированную пару и возвращает увеличенные тензоры; прежде чем предложить многозапросное внимание (multi-query attention), автор указывает, что повторная загрузка этих тензоров становится узким местом по пропускной способности памяти."
        },
        {
          "role": "later",
          "year": 2023,
          "name": "Efficient Memory Management for Large Language Model Serving with PagedAttention",
          "source_url": "https://arxiv.org/pdf/2309.06180",
          "claim": "Квон и соавторы описывают последовательную генерацию в LLM, при которой прежние векторы ключей и значений кэшируются, а вычисляется только последняя пара, после чего организуют динамически растущие KV-кэши как логические блоки, отображаемые на несмежные участки физической памяти."
        }
      ]
    },
    "approach": "Переход от повторного вычисления каждого полного префикса к добавлению на каждом шаге одной локальной для слоя пары ключа и значения.",
    "summary": "Исторический путь идёт от каузального внимания с маской через явное инкрементальное повторное использование тензоров K/V к системам обслуживания, рассчитанным на динамически растущий кэш. Фиксированная ёмкость, точное расположение данных, правило смещения RoPE, привязка к идентичностям узлов параметров и версиям их значений, поведение при сбросе и типизированные ошибки — локальные правила корректности этой главы.",
    "rust_source": "rust/demos/ch37-incremental-attention/src/lib.rs"
  },
  "rust_sources": [
    {
      "path": "rust/crates/llm-from-scratch/src/attention/incremental.rs",
      "region": "layer-kv-cache",
      "purpose": "Хранить фиксированные буферы ключей и значений, привязанные к идентичностям узлов параметров слоя и зафиксированным версиям их значений, атомарно добавлять одну проверенную строку, предоставлять логические префиксы и сбрасывать длину без нового выделения памяти и без изменения привязки."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/attention/incremental.rs",
      "region": "incremental-attention",
      "purpose": "Спроецировать одну строку в режиме вывода без графа вычислений, применить RoPE по абсолютной позиции, вычислить внимание по сохранённым и добавляемым строкам и обновить кэш только после успешной выходной проекции."
    },
    {
      "path": "rust/demos/ch37-incremental-attention/src/lib.rs",
      "region": "historical-kv-contrast",
      "purpose": "Сопоставить число проецируемых строк при обработке полных префиксов и при инкрементальном повторном использовании для тех же трёх префиксов."
    },
    {
      "path": "rust/demos/ch37-incremental-attention/src/lib.rs",
      "region": "cache-errors",
      "purpose": "Проверить, что ошибки заполненного кэша, формы, модели, числа голов, слоя, RoPE и неконечных значений не изменяют состояние кэша."
    },
    {
      "path": "rust/demos/ch37-incremental-attention/src/lib.rs",
      "region": "cache-step",
      "purpose": "Собрать результаты трёх сравнений выходов с кэшем и эталонным расчётом, число строк проекций, повторную проверку после сброса и результаты проверок ошибок."
    },
    {
      "path": "rust/demos/ch37-incremental-attention/src/main.rs",
      "purpose": "Напечатать точный учебный отчёт главы 37."
    }
  ],
  "visualization": {
    "decision": "useful",
    "id": "incremental-attention",
    "rationale": "Временная шкала из трёх шагов одновременно показывает сохранённые строки, одну добавленную строку, абсолютную позицию RoPE, растущий охват внимания, совпадение с эталоном по полному префиксу и исключённые повторные проекции."
  },
  "decoder_connection": "Теперь один слой внимания может сохранять между шагами декодирования повёрнутые ключи и значения без поворота вне графа вычислений и воспроизводить для новой позиции результат расчёта по полному префиксу. Слой отклоняет вызов, если изменилась идентичность узла параметра, версия его значения или конфигурация; сброс очищает логическое состояние без нового выделения памяти и без изменения привязки. В главе 38 такой кэш появится в каждом блоке декодера."
}
---

import RustSource from "../../../components/RustSource.astro";
import IncrementalAttentionDiagram from "../../../components/chapters/IncrementalAttentionDiagram.astro";

export const diagramLabels = {
  title:
    "Сохранять предыдущие строки ключей и значений; добавлять ровно одну новую пару",
  description:
    "Точная трассировка программы на Rust охватывает три абсолютные позиции, показывает кэши обеих голов внимания и веса, сопоставляет выход последней позиции на каждом шаге с эталонным расчётом по полному префиксу и подтверждает сохранность хранилища после сброса и отклонённых вызовов.",
  sections: {
    timeline: "Продвигать кэш слоя по одной позиции",
    work: "Считать строки проекций, а не время работы",
    reset: "Сбрасывать или отклонять вызов без повреждения состояния",
  },
  fields: {
    position: "Абсолютная позиция",
    cache: "Длина кэша",
    shape: "Логическая форма кэша",
    head: "Голова внимания",
    keys: "Повёрнутые ключи",
    values: "Значения без поворота",
    weights: "Веса последнего запроса",
    cached: "Выход с кэшем",
    full: "Эталонный расчёт по полному префиксу",
    difference: "Максимальная абсолютная разность",
  },
  cues: {
    retained: "сохранённая строка — сплошная рамка",
    appended: "добавленная строка — двойная рамка",
    match: "последние выходы совпадают в пределах допуска",
    unchanged: "сброс сохраняет выделенную память и хранимые значения",
    replay: "те же строки воспроизводятся в точности",
    errors: "отклонённый вызов ничего не добавляет",
  },
  captions: {
    timeline:
      "Старая логическая длина равна позиции RoPE с нумерацией от нуля. Каждая голова сохраняет прежние строки, добавляет одну пару ключа и значения, а последний запрос читает весь получившийся префикс.",
    work: "Для префиксов длины от одного до трёх эталонный путь проецирует по шесть строк в каждой ветви. Путь с кэшем проецирует три новые строки и повторно использует три прежние строки ключей и три строки значений.",
    reset:
      "Логический сброс повторно использует ту же выделенную память. Вызовы при заполненном кэше, несовместимости или недопустимом входе не изменяют ни длину, ни хранимые значения.",
  },
  proofs: {
    full: "строк полного префикса в каждой проекции запроса, ключа или значения",
    cached:
      "новых строк в каждой инкрементальной проекции запроса, ключа или значения",
    reused: "прежних строк без повторной проекции в ветвях ключей и значений",
  },
  lists: {
    cacheRows: "Строки кэша в хронологическом порядке",
  },
};

{/* chapter-section:worked-example */}

## Предскажите результат третьего вызова до запуска примера

В примере размер пакета равен единице, ширина модели $D=4$, число голов $H=2$,
ширина головы $d_h=2$, а ёмкость кэша $C=4$. Перед первым вызовом кэш пуст.
Каждый вызов принимает одну строку формы $[1,1,4]$, поэтому логические формы
ключей и значений последовательно становятся $[1,2,1,2]$, $[1,2,2,2]$ и
$[1,2,3,2]$.

Первые два выхода с кэшем равны $[1,0,1,0]$ и
$[0.213809009,0.786190991,0.770151153,-0.420735492]$. Прежде чем посмотреть
третий выход, предскажите три факта:

1. Старая длина равна $2$, поэтому новые $Q$ и $K$ используют абсолютную
   позицию RoPE $2$.
2. Последнему запросу каждой головы доступны $3$ строки ключей и $3$ строки
   значений.
3. Кэш добавляет одну строку повёрнутых ключей и одну строку значений без
   поворота, но не сохраняет запрос.

Результат равен $[0.629044078,0.945303958,0.374718490,-0.583589471]$.
Независимый эталонный расчёт по полному префиксу даёт те же показанные значения, а
максимальная абсолютная разность без округления не превышает $10^{-12}$.

{/* chapter-section:formula */}

## Добавляйте строки вдоль оси позиций

Для слоя внимания $\ell$ переход кэша имеет вид

$$
K^{(\ell)}_{1:t}=[K^{(\ell)}_{1:t-1};k^{(\ell)}_t],\quad V^{(\ell)}_{1:t}=[V^{(\ell)}_{1:t-1};v^{(\ell)}_t]
$$

Точка с запятой означает конкатенацию вдоль оси позиций последовательности.
К новому ключу $k^{(\ell)}_t$ уже применён RoPE для его абсолютной позиции.
Новое значение $v^{(\ell)}_t$ уже спроецировано и разделено на головы, но не
повёрнуто. Запрос нужен только для текущего вычисления, поэтому эта реализация не
хранит запросы в кэше.

Реализация использует смещение с нумерацией от нуля, равное старой длине кэша.
В формуле принята обычная запись префикса с нумерацией от единицы, поэтому
математическая позиция $t$ на единицу больше этого смещения.

{/* chapter-section:symbol-glossary */}

## Различайте слой, логическую длину и ёмкость

- $K^{(\ell)}_{1:t}$ — префикс повёрнутых ключей слоя $\ell$ после добавления
  новой строки.
- $V^{(\ell)}_{1:t}$ — префикс значений того же слоя после добавления.
- $\ell$ обозначает слой внимания блока декодера, которому принадлежит состояние.
- $t$ обозначает последнюю позицию в математической записи префикса с нумерацией
  от единицы.
- $1:t$ включает все сохранённые позиции вплоть до последней.
- $K^{(\ell)}_{1:t-1}$ и $V^{(\ell)}_{1:t-1}$ — неизменившиеся строки
  предыдущих позиций.
- $k^{(\ell)}_t$ и $v^{(\ell)}_t$ — добавляемая пара из одной строки.
- $[A;B]$ означает добавление $B$ после $A$ вдоль оси позиций.

Физические буферы имеют форму $[B,H,C,d_h]$, где $B$ — размер пакета, $H$ —
число голов, $C$ — фиксированная ёмкость, а $d_h$ — ширина головы. Логический
снимок имеет форму $[B,H,t,d_h]$. Сброс переводит $t$ в $0$, не меняя $C$ и
не выделяя новые буферы. При этом также не меняются идентичности узлов параметров
и версии их значений, зафиксированные при создании кэша.

{/* chapter-section:history */}

## От каузального внимания к управлению состоянием LLM при генерации

Каузальное внимание Transformer позволяет каждой новой позиции декодера
обращаться к известному префиксу. Однако без сохранённых проекций цикл генерации
может на каждом шаге заново вычислять неизменившиеся строки ключей и значений
предыдущих позиций.

[Attention Is All You Need](https://arxiv.org/pdf/1706.03762) описывает
каузальное вычисление по полному префиксу, которое служит здесь эталоном. Васвани
и соавторы задают внимание на основе масштабированного скалярного
произведения, описывают авторегрессионный декодер, который выдаёт по одному
элементу, и маскируют самовнимание декодера так, чтобы позиция могла использовать
только известный префикс; статья не описывает ни KV-кэширование, ни RoPE.

[Fast Transformer Decoding: One Write-Head is All You Need](https://arxiv.org/pdf/1911.02150)
явно показывает, какие данные переиспользуются. Инкрементальное многоголовое
самовнимание Шейзира принимает предыдущие тензоры ключей и значений, добавляет
текущую спроецированную пару и возвращает увеличенные тензоры; прежде чем
предложить многозапросное внимание (multi-query attention), автор указывает, что
повторная загрузка этих тензоров становится узким местом по пропускной способности
памяти.

[Efficient Memory Management for Large Language Model Serving with PagedAttention](https://arxiv.org/pdf/2309.06180)
продолжает этот путь в современных системах обслуживания. Квон и соавторы
описывают последовательную генерацию в LLM, при которой прежние векторы ключей и
значений кэшируются, а вычисляется только последняя пара, после чего организуют
динамически растущие KV-кэши как логические блоки, отображаемые на несмежные
участки физической памяти.

При инкрементальном декодировании повторное использование на каждом шаге стало
явным: тензоры ключей и значений сохраняются отдельно для каждого слоя. Позднее
системы обслуживания LLM стали рассматривать растущий KV-кэш как центральный
объект управления памятью.

При современной генерации с кэшированием каждый блок декодера хранит собственный
совместимый кэш: новый запрос обращается к сохранённому префиксу, а проецируются
только последние ключ и значение.

Исторический путь идёт от каузального внимания с маской через явное
инкрементальное повторное использование тензоров K/V к системам обслуживания,
рассчитанным на динамически растущий кэш. Фиксированная ёмкость, точное расположение данных,
правило смещения RoPE, привязка к идентичностям узлов параметров и версиям их
значений, поведение при сбросе и типизированные ошибки — локальные правила
корректности этой главы.

<RustSource
  path="rust/demos/ch37-incremental-attention/src/lib.rs"
  region="historical-kv-contrast"
  caption="Измерить охват последнего запроса и повторное использование проекций на трёх префиксах"
  label="Исходный код на Rust для исторического сравнения подходов к генерации LLM в главе 37"
/>

{/* chapter-section:rust-implementation */}

## Привяжите состояние к слою, полностью вычислите результат и лишь затем обновите кэш

`LayerKvCache::new` принимает сам слой внимания. Метод запоминает размер пакета,
ширину модели, число голов, ёмкость кэша и ширину головы, владеет фиксированными
буферами ключей и значений, а для каждого из четырёх параметров слоя запоминает как
идентичность узла, так и текущую версию его значения. Кроме того, кэш сохраняет
точную конфигурацию RoPE.

В заново созданном слое параметры находятся в других узлах, поэтому кэш
возвращает `CacheLayerMismatch`. Успешный шаг AdamW записывает новые значения в
прежние узлы: их идентичности сохраняются, но версии значений увеличиваются.
Старый кэш после этого становится устаревшим и возвращает
`CacheLayerRevisionMismatch`. Эта вторая проверка необходима, потому что
сохранённые строки K/V были вычислены со
старыми весами. Если добавить к ним строки, полученные с обновлёнными весами,
кэш уже не будет соответствовать ни одной определённой версии модели. После
любого обновления весов создайте новый кэш. Вызов `reset` для старого кэша не
восстанавливает совместимость: сброс очищает логическую длину, но не обновляет
зафиксированные версии и не привязывает кэш к другому слою.

<RustSource
  path="rust/crates/llm-from-scratch/src/attention/incremental.rs"
  region="layer-kv-cache"
  caption="Хранить фиксированный привязанный к слою кэш, добавлять одну проверенную строку и сбрасывать логическую длину"
  label="Исходный код на Rust с реализацией локального для слоя KV-кэша главы 37"
/>

`forward_incremental` принимает только тензор формы $[B,1,D]$. В области, где
не записываются градиенты и граф вычислений, метод проецирует новую строку,
разделяет головы, поворачивает $Q$ и $K$ для позиции, равной старой длине кэша,
вычисляет
устойчивые веса формы $[B,H,1,t+1]$, смешивает сохранённые и добавляемые
значения, объединяет головы и применяет существующую выходную проекцию. Ключ и
значение копируются, а длина увеличивается, только когда весь результат готов.

<RustSource
  path="rust/crates/llm-from-scratch/src/attention/incremental.rs"
  region="incremental-attention"
  caption="Вычислить последний запрос по сохранённым строкам и добавляемой строке ключа и значения до фиксации новой пары"
  label="Исходный код на Rust с реализацией инкрементального многоголового внимания главы 37"
/>

Для префиксов длины $1$, $2$ и $3$ программа из этой главы сравнивает два
подхода и показывает, что последние запросы обращаются соответственно к
$[1,2,3]$ строкам ключей.
Вызовы по полному префиксу обрабатывают $1+2+3=6$ входных строк в каждой проекции
$Q$, $K$ или $V$. Инкрементальный путь обрабатывает в каждой проекции
$1+1+1=3$ строки. Значит, он повторно использует $3$ прежние строки ключей и
$3$ прежние строки значений. Однако последний запрос по-прежнему читает весь
получившийся префикс из $t$ позиций: $t-1$ сохранённых позиций и текущую
добавляемую позицию. Поэтому эти подсчёты не доказывают ни постоянного времени
внимания, ни измеренного ускорения.

Изменение кэша атомарно: при ошибке частично обновлённого состояния не остаётся.
Демонстрационная программа главы 37 проверяет вход из двух токенов, заполненный кэш,
несовпадение ширины модели или числа голов, заново созданные веса той же формы,
другое основание RoPE, то же основание с другой ёмкостью позиций и конечный вход,
при проекции которого получается неконечное значение. В каждом из этих случаев
вызов завершается до изменения сохранённых значений или логической длины.
Отдельная проверка версии в начале `forward_incremental` отклоняет слой,
параметры которого AdamW обновил на месте, ещё до проекции или добавления строки.
Несовпадение версий остаётся ошибкой и после `reset`, потому что сброс не меняет
привязку кэша.
Непосредственное добавление ключей и значений закрыто внутри привязанной к слою
операции внимания, поэтому внешний вызов не может обойти эти проверки
совместимости.

<RustSource
  path="rust/demos/ch37-incremental-attention/src/lib.rs"
  region="cache-errors"
  caption="Проверить несовместимые и недопустимые вызовы по неизменившимся снимкам кэша"
  label="Исходный код на Rust, проверяющий атомарность ошибок кэша в главе 37"
/>

Пример выполняет все три вызова с кэшем и все три независимых эталонных вызова.
Он проверяет не только выходы, но и полные логические префиксы ключей и значений,
сбрасывает длину в $0$ без замены хранилища и повторяет те же проверки с теми же
результатами для неизменившегося слоя. Это подтверждает повторное использование
выделенной памяти, но не означает, что сброс обновляет совместимость кэша после
обучения.

<RustSource
  path="rust/demos/ch37-incremental-attention/src/lib.rs"
  region="cache-step"
  caption="Собрать результаты сравнений с кэшем, число строк, повторную проверку после сброса и результаты проверок ошибок"
  label="Исходный код на Rust, собирающий результаты проверок главы 37"
/>

Выполните `cargo run --quiet --locked -p ch37-incremental-attention`. Основные
результаты проверки печатает сама программа:

```text
step=position:0 cache:0->1 shape:[1,2,1,2] max_abs_diff:0.000000000000 output:[1.000000000,0.000000000,1.000000000,0.000000000]
step=position:1 cache:1->2 shape:[1,2,2,2] max_abs_diff:0.000000000000 output:[0.213809009,0.786190991,0.770151153,-0.420735492]
step=position:2 cache:2->3 shape:[1,2,3,2] max_abs_diff:0.000000000000 output:[0.629044078,0.945303958,0.374718490,-0.583589471]
work=full_rows_per_projection:6 incremental_rows_per_projection:3 reused_rows_per_kv_projection:3 avoided_rows_across_kv:6
reset=before:3 after:0 allocation_reused:true storage_unchanged:true replay_identical:true
errors=two_tokens:true full_cache:true model_mismatch:true head_mismatch:true layer_mismatch:true rope_mismatch:true rope_positions_mismatch:true nonfinite_projection:true unchanged:true
```

<RustSource
  path="rust/demos/ch37-incremental-attention/src/main.rs"
  caption="Напечатать точный отчёт главы 37 об инкрементальном внимании"
  label="Исходный код на Rust, печатающий результаты проверок кэша в главе 37"
/>

{/* chapter-section:visualization */}

## Проследите путь сохранённых строк к последнему запросу

Схема показывает точные значения, полученные при трёх вызовах программы на Rust.
Читайте её от
абсолютной позиции $0$ до позиции $2$. Прямоугольники со сплошной рамкой — это
сохранённые строки, а прямоугольник с двойной рамкой — одна новая пара ключа и
значения. Для каждой строки указан её вес для последнего запроса, а каждый шаг
завершается выходами с кэшем и по полному префиксу рядом с зафиксированной
максимальной разностью.

Карточки вычислительной работы сопоставляют число строк проекций, а не прошедшее
время. Последние карточки показывают, что при сбросе повторно используется та же
выделенная память, а отклонённые вызовы сохраняют состояние. Сплошная рамка
обозначает сохранённую строку, двойная — добавленную пару.

<IncrementalAttentionDiagram labels={diagramLabels} />

{/* chapter-section:exercises */}

## Сначала предскажите, затем сверьтесь с трассировкой

1. Какое смещение RoPE используется при логической длине кэша $2$?
2. Сохраняет ли кэш слоя последний запрос?
3. Сколько ключей может прочитать последний запрос каждой головы при третьем
   вызове?
4. Сколько строк обрабатывает одна проекция ключей по полному префиксу для длин
   $1$, $2$ и $3$ вместе?
5. Сколько строк обрабатывает инкрементальная проекция ключей?
6. Что изменяет сброс и обновляет ли он привязку к параметрам?
7. Можно ли повторно использовать кэш той же формы с заново созданными весами?
8. Можно ли повторно использовать старый кэш после того, как AdamW обновил
   значения в тех же узлах параметров?
9. Что происходит с состоянием кэша, если его ёмкость уже исчерпана?

<details>
<summary>Проверьте девять прогнозов</summary>

1. Смещение равно $2$ — в точности старой логической длине.
2. Нет. Сохраняются только повёрнутые строки $K$ и строки $V$ без поворота.
3. Каждый запрос читает $3$ ключа и смешивает $3$ значения.
4. Эталон обрабатывает $1+2+3=6$ строк проекции ключей.
5. Инкрементальный путь обрабатывает $1+1+1=3$ строки проекции ключей.
6. Сброс переводит логическую длину в $0$, сохраняя выделенную память и
   хранимые значения. Он не обновляет зафиксированные идентичности узлов и версии
   значений параметров и не меняет привязку кэша.
7. Нет. В заново созданном слое не совпадут идентичности узлов параметров, даже
   если формы и числовые значения весов те же.
8. Нет. Обновление AdamW на месте сохраняет узлы параметров, но увеличивает
   версии их значений. Старый кэш вернёт `CacheLayerRevisionMismatch`; для
   обновлённого слоя нужно создать новый кэш.
9. Вызов возвращает типизированную ошибку до изменения хранимых значений или
   логической длины.

</details>

{/* chapter-section:decoder-connection */}

## В следующей главе дайте каждому блоку декодера собственное состояние

Теперь один слой внимания может сохранять между шагами декодирования повёрнутые
ключи и значения без поворота вне графа вычислений и воспроизводить для новой
позиции результат расчёта по полному префиксу. Слой отклоняет вызов, если
изменилась идентичность узла параметра, версия его значения или конфигурация.
Сброс очищает логическое состояние без нового выделения памяти и без изменения
привязки. В главе 38 такой кэш появится в каждом блоке декодера.

Пока полный декодер курса по-прежнему вычисляет весь префикс. В главе 38 для
конкретного слоя внимания каждого блока будет создан отдельный кэш; эти кэши
будут последовательно передаваться при предварительной обработке промпта и
декодировании по одному токену, а полная генерация с кэшем будет сопоставлена с
эталоном без кэша из главы 36.
