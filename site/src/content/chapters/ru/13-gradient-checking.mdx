---
{
  "chapter_id": "13-gradient-checking",
  "locale": "ru",
  "content_revision": 6,
  "order": 13,
  "concept_id": "gradient-checking",
  "title": "Проверяйте градиенты, прежде чем доверять обратному распространению",
  "description": "Сверяйте выбранные производные для обучения LLM по фактически представимым точкам: учитывайте требование локальной гладкости и погрешность с учётом масштаба, а координаты тензора выбирайте детерминированно в Rust.",
  "objective": "Приближённо вычислять производные локально гладких функций по фактически представимым точкам и сравнивать выбранные аналитические значения с численными оценками, используя погрешность с учётом масштаба.",
  "worked_inputs": "Для q(theta)=theta^2 при theta=3 и запрошенном h=0.1 различите представимые точки вычисления и фактические расстояния до них слева и справа, предскажите производную 6 и объясните, почему значения 6 и 5.5 проходят и не проходят проверку. Затем проверьте на f(x)=x случай с неравными расстояниями после округления, недифференцируемую точку f(x)=abs(x) при нуле, шесть запрошенных шагов для g(theta)=theta^3-2theta при theta=1.5 и четыре детерминированно выбранные координаты тензора среднего NLL из главы 12.",
  "formula": {
    "latex": "f'(\\theta)\\approx\\frac{h_+}{h_-+h_+}\\frac{f(\\theta)-f(\\theta_-)}{h_-}+\\frac{h_-}{h_-+h_+}\\frac{f(\\theta_+)-f(\\theta)}{h_+}",
    "symbols": [
      {
        "symbol": "f",
        "meaning": "детерминированная скалярная функция потерь, производная которой проверяется"
      },
      {
        "symbol": "\\theta",
        "meaning": "конечный скалярный параметр или одна проверяемая координата тензора"
      },
      {
        "symbol": "h",
        "meaning": "положительная конечная запрошенная величина шага, по которой строятся обе точки вычисления в формате с плавающей запятой"
      },
      {
        "symbol": "\\theta_-,\\theta_+",
        "meaning": "фактически представимые точки вычисления fl(theta-h) и fl(theta+h)"
      },
      {
        "symbol": "h_-,h_+",
        "meaning": "фактические положительные расстояния theta-theta_minus и theta_plus-theta слева и справа"
      },
      {
        "symbol": "f'(\\theta)",
        "meaning": "производная в локально гладкой точке, приближённая трёхточечной формулой для неравных расстояний"
      }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "training-practice",
      "limitation": "Нейронная языковая модель Бенжио и соавторов максимизирует логарифмическое правдоподобие следующего слова и явно выполняет этап обратного распространения и обновления параметров выходного и скрытого слоёв, а также обучаемых векторных представлений слов. Распространяемые назад производные позволяют многократно обновлять параметры, но реализованный путь их вычисления не служит независимой проверкой самого себя.",
      "later_advance": "Transformer с повторяющимися слоями внимания и полносвязными блоками обучают градиентным методом, выполняя 100 000 шагов Adam для базовой модели и 300 000 для большой. Байдин и соавторы различают численное дифференцирование и автоматическое дифференцирование в обратном режиме: конечные разности оценивают одну локальную производную по нескольким вычислениям функции, а обратный режим эффективно получает градиент скалярной цели по множеству параметров.",
      "modern_llm_role": "В этой главе трёхточечные конечные разности служат лишь медленной выборочной численной сверкой для локально гладких целевых функций, в том числе для выбранных производных среднего NLL по индексам из главы 12, прежде чем в главе 14 появится обратный режим. Успешная сверка свидетельствует только о выбранных координатах, точках вычисления, шаге, допуске и конкретном примере; она не доказывает правильность всего градиента или локальную дифференцируемость. Такая проверка не обучает и не запускает декодер, а её правила относятся к данной реализации курса.",
      "sources": [
        {
          "role": "earlier",
          "year": 2003,
          "name": "Bengio et al., A Neural Probabilistic Language Model",
          "source_url": "https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf",
          "claim": "Бенжио и соавторы максимизируют логарифмическое правдоподобие следующего слова и описывают обратное распространение с обновлением параметров, при котором градиенты проходят через выходные элементы, веса скрытого слоя и обучаемые векторные представления слов."
        },
        {
          "role": "later",
          "year": 2017,
          "name": "Vaswani et al., Attention Is All You Need",
          "source_url": "https://papers.nips.cc/paper_files/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf",
          "claim": "Васвани и соавторы обучают базовые модели Transformer 100 000 шагов, а большие — 300 000 шагов, применяя Adam с явно заданным расписанием скорости обучения."
        },
        {
          "role": "later",
          "year": 2018,
          "name": "Baydin et al., Automatic Differentiation in Machine Learning: a Survey",
          "source_url": "https://arxiv.org/abs/1502.05767",
          "claim": "Байдин и соавторы описывают центральные конечные разности, компромисс между погрешностями усечения и округления при выборе шага, плохую масштабируемость полного численного градиента и эффективность обратного режима для скалярной цели с множеством параметров."
        }
      ]
    },
    "approach": "От обратного распространения для правдоподобия следующего слова к выборочной сверке производных при обучении Transformer",
    "summary": "Бенжио и соавторы публикуют вычисления обратного прохода и обновления параметров для нейронной модели следующего слова. Позднее Васвани и соавторы обучают повторяющиеся блоки Transformer с помощью Adam в течение 100 000 или 300 000 шагов. Байдин и соавторы объясняют, почему конечные разности просты, но чувствительны к погрешностям усечения и округления и плохо подходят для полного градиента, тогда как обратный режим удобен для скалярной функции потерь с множеством параметров. Поэтому пример на Rust проверяет лишь выбранные производные и не подменяет численным дифференцированием обучение LLM.",
    "rust_source": "rust/demos/ch13-gradient-checking/src/lib.rs"
  },
  "rust_sources": [
    {
      "path": "rust/demos/ch13-gradient-checking/src/lib.rs",
      "region": "quadratic-gradient-prediction",
      "purpose": "Задать квадратичную функцию для предварительного расчёта и сравнить два аналитических значения: верное и заведомо ошибочное."
    },
    {
      "path": "rust/demos/ch13-gradient-checking/src/lib.rs",
      "region": "step-size-scan",
      "purpose": "Проверить одну и ту же производную кубической функции при шести величинах шага и сопоставить погрешности усечения и округления."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/autograd/gradcheck.rs",
      "region": "gradcheck-errors",
      "purpose": "Различать ошибки конфигурации, возмущения, вычисления, формы, выбора координат, выделения памяти, представления и индекса."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/autograd/gradcheck.rs",
      "region": "central-difference",
      "purpose": "Вычислить фактические расстояния до проверенных представимых точек слева и справа и применить трёхточечную формулу для неравных расстояний."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/autograd/gradcheck.rs",
      "region": "scale-aware-comparison",
      "purpose": "Сравнить конечные аналитическое и численное значения по одному явно заданному допуску нормированной погрешности."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/autograd/gradcheck.rs",
      "region": "sample-tensor-coordinates",
      "purpose": "Детерминированно выбрать уникальные координаты тензора в построчном порядке без начального значения генератора случайных чисел."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/autograd/gradcheck.rs",
      "region": "sampled-tensor-gradient-check",
      "purpose": "Проверить выбранные координаты, временно изменяя по одному значению в собственном хранилище и восстанавливая его перед каждым обычным возвратом."
    },
    {
      "path": "rust/demos/ch13-gradient-checking/src/lib.rs",
      "region": "hand-derived-nll-gradient",
      "purpose": "Вычислить аналитическое значение производной среднего NLL непосредственно по исходным строкам логитов, не вызывая основные реализации softmax или NLL по индексам."
    },
    {
      "path": "rust/demos/ch13-gradient-checking/src/lib.rs",
      "region": "sampled-nll-gradient-check",
      "purpose": "Сопоставить отдельное аналитическое вычисление с целевой функцией NLL по индексам в четырёх детерминированно выбранных координатах логитов и подтвердить точное восстановление значений."
    },
    {
      "path": "rust/demos/ch13-gradient-checking/src/main.rs",
      "region": "learner-gradient-check-output",
      "purpose": "Подготовить квадратичный пример, перебор шагов, координаты NLL, свидетельство восстановления и ошибку шага, который после округления не изменил точку."
    }
  ],
  "visualization": {
    "decision": "useful",
    "id": "gradient-checking",
    "rationale": "Запрошенная величина шага и фактические расстояния до точек вычисления, проверка линейной функции после округления, известная недифференцируемая точка, шесть шагов, нормированные погрешности, верное и ошибочное аналитические значения, отдельные пути NLL с общими предпосылками и выбранные координаты функции потерь вместе показывают надёжную область для гладкой функции, последующее ухудшение из-за округления и ограниченность выводов по выборочной проверке."
  },
  "decoder_connection": "Теперь проект может сопоставлять выбранные производные по логитам словаря, вычисленные отдельным локальным аналитическим путём, с результатами основной реализации среднего NLL по индексам при изменённых входах. Оба пути всё ещё используют одну и ту же арифметику f64, одно и то же хранилище Tensor, входные данные примера и соглашения об индексах, поэтому совпадение служит полезным выборочным свидетельством, а не доказательством. В главе 14 та же граница применится к скалярным производным в обратном режиме."
}
---

import GradientCheckingDiagram from '../../../components/chapters/GradientCheckingDiagram.astro';

export const diagramLabels = {
  title: 'Сопоставьте фактические расстояния, прежде чем доверять выборочной сверке',
  description:
    'Схема сопоставляет запрошенную величину шага с фактическими расстояниями и показывает проверку линейной функции после округления, известную недифференцируемую точку, отдельные пути NLL с общими предпосылками, выбранные координаты функции потерь, точное восстановление и недопустимые входные данные.',
  summary: {
    quadratic: 'Численная производная квадратичной функции',
    scanPoint: 'Точка для кубической функции',
    tensorLoss: 'Среднее NLL по индексам',
  },
  display: {
    summaryQuadratic: 'Численная производная',
    numerical: 'Численный градиент',
    analytic: 'Аналитическое значение',
    scaledError: 'Нормированная погрешность',
  },
  sections: {
    quadratic: 'Проверьте фактические расстояния при значении параметра три',
    scan: 'Переберите шесть величин шага при значении параметра полтора',
    candidates: 'Сопоставьте два конечных значения градиента',
    tensor: 'Сверка четырёх выбранных координат NLL',
    boundaries: 'Что не доказывает успешная сверка',
    errors: 'Недопустимые запросы',
  },
  fields: {
    requestedStep: 'Запрошенная величина шага',
    minusProbe: 'Фактическая точка слева',
    minusSpacing: 'Фактическое расстояние слева',
    center: 'Центр',
    plusProbe: 'Фактическая точка справа',
    plusSpacing: 'Фактическое расстояние справа',
    leftSlope: 'Односторонний наклон слева',
    rightSlope: 'Односторонний наклон справа',
    stencil: 'Схема расположения точек',
    numerical: 'Численный градиент',
    analytic: 'Аналитическое значение',
    scaledError: 'Нормированная погрешность',
    tolerance: 'Допуск',
    phase: 'Режим погрешности',
    restored: 'Биты восстановлены',
  },
  phases: {
    truncation: 'усечение',
    converging: 'сходимость',
    trusted: 'надёжная область',
    rounding: 'округление',
  },
  statuses: {
    pass: 'в допуске',
    fail: 'выше допуска',
    restored: 'да, точно',
  },
  sides: {
    minus: 'слева',
  },
  errors: {
    'invalid-step': 'нулевой шаг',
    'collapsed-perturbation': 'точка не изменилась',
    'non-finite-evaluation': 'неконечное значение',
    'shape-mismatch': 'формы не совпадают',
  },
  notes: {
    tensor:
      'Каждая запись свидетельствует только о выбранной координате, целевой функции, точках вычисления, шаге, допуске и конкретном примере.',
    boundaries:
      'При неравных расстояниях после округления нужно использовать фактические расстояния; известный излом показывает ложное успешное сравнение, а выборочная сверка не является доказательством.',
    oracle:
      'Аналитический путь NLL реализован локально и не вызывает основные функции вероятностей или NLL по индексам; оба пути по-прежнему используют общие входные значения и цели, арифметику IEEE f64 и её базовую экспоненту, хранилище Tensor и соглашения о построчной индексации.',
    errors:
      'Недопустимый запрос отклоняется до принятия результата за производную.',
  },
  symbols: {
    truncation: '△',
    converging: '→',
    trusted: '✓',
    rounding: '≈',
    pass: '✓',
    fail: '!',
    rejected: '×',
  },
};

{/* chapter-section:worked-example */}

## Предскажите одну производную квадратичной функции

Начнём с небольшой скалярной проверки градиента:

$$
q(\theta)=\theta^2,\qquad \theta=3,\qquad h=0.1
$$

Запрошенный шаг задаёт две точки, которые в ручном расчёте записаны как $2.9$ и
$3.1$. Однако в `f64` хранятся соседние представимые значения
$\theta_- = \operatorname{fl}(3-0.1)$ и
$\theta_+ = \operatorname{fl}(3+0.1)$. Поэтому реализация вычисляет фактические
расстояния $h_-=3-\theta_-$ и $h_+=\theta_+-3$, а не считает каждое из них
равным запрошенному $0.1$. При обычном округлении для ручного расчёта получаем
$q(2.9)=8.41$ и $q(3.1)=9.61$, а трёхточечная формула даёт численную оценку
производной $6$.

Этот результат проверяет аналитическое значение, а не создаёт его. Производная
$\theta^2$, выведенная вручную, даёт значение $6$, которое должно уложиться в
допуск. Значение $5.5$ тоже является вполне конечным числом, но сравнение с ним
должно завершиться с `pass=false`. Математическое несовпадение отличается от
недопустимого шага или неконечного значения целевой функции.

<RustSource
  path="rust/demos/ch13-gradient-checking/src/lib.rs"
  region="quadratic-gradient-prediction"
  caption="Задать квадратичный пример и оба аналитических значения"
  label="Исходный код Rust с предварительно рассчитанной проверкой производной квадратичной функции в главе 13"
/>

{/* chapter-section:formula */}

## Используйте расстояния до значений, представимых компьютером

Пользователь задаёт положительную величину шага $h$, а сложение и вычитание в
формате с плавающей запятой дают фактически представимые точки вычисления

$$
\theta_- = \operatorname{fl}(\theta-h),\qquad
\theta_+ = \operatorname{fl}(\theta+h).
$$

Обозначим положительные фактические расстояния от проверяемой точки через

$$
h_- = \theta-\theta_-,\qquad h_+ = \theta_+-\theta.
$$

Трёхточечная формула для неравных расстояний объединяет два односторонних
наклона:

$$
f'(\theta)\approx\frac{h_+}{h_-+h_+}\frac{f(\theta)-f(\theta_-)}{h_-}+\frac{h_-}{h_-+h_+}\frac{f(\theta_+)-f(\theta)}{h_+}
$$

Только при равных фактических расстояниях
$h_-=h_+=\widehat h$ вклад значения в центре сокращается, и формула принимает
вид

$$
f'(\theta)\approx\frac{f(\theta_+)-f(\theta_-)}{2\widehat h}.
$$

В знаменатель входит фактическое расстояние $\widehat h$, а не просто
запрошенное $h$.

Реализация вычисляет веса, не складывая сначала два потенциально огромных
расстояния. При $m=\max(h_-,h_+)$ она находит $u_-=h_-/m$ и $u_+=h_+/m$, а
затем вес левого наклона $u_+/(u_-+u_+)$ и правого —
$u_-/(u_-+u_+)$. Эти отношения сохраняют коэффициенты формулы для неравных
расстояний и не допускают переполнения суммы $h_-+h_+$.

Если функция достаточно гладкая в окрестности точки — например,
её третья производная непрерывна на всём отрезке от $\theta_-$ до $\theta_+$,
— погрешность усечения трёхточечной интерполяции имеет порядок
$O(\max(h_-,h_+)^2)$. Уменьшение запрошенного шага помогает лишь до тех пор,
пока главной причиной ошибки не станут округление точек вычисления и потеря
точности при вычитании почти равных значений функции.

Линейная функция $f(x)=x$ позволяет обнаружить ошибку в работе с округлением.
Выберите конечную точку и шаг, для которых две операции с плавающей запятой дают
разные ненулевые расстояния. Оба односторонних наклона в формуле по-прежнему
равны $1$, поэтому итоговая оценка должна совпасть с $1$ в пределах допуска.
Деление на запрошенное $2h$ может не пройти такую проверку: этот знаменатель не
описывает фактические точки.

Локальная гладкость — предварительное условие, которое нельзя доказать одним
совпадением. Для $f(x)=|x|$ при $x=0$ симметричные точки дают численную оценку
$0$, и аналитическое значение $0$ может пройти проверку, хотя в точке излома
производная не существует. Это ложное успешное сравнение за пределами области
применимости метода; процедура не умеет безошибочно находить все изломы и другие
негладкие точки.

`compare_one_sided_slopes` вычисляет расхождение с учётом масштаба между
записанными наклонами. Расхождение может указывать на излом, слишком большой для
локальной кривизны шаг или погрешность округления; совпадение само по себе не
доказывает дифференцируемость.

Реализация требует конечного $\theta$, положительного конечного $h$, конечного
диапазона запрошенного шага, двух конечных точек, строго расположенных по разные
стороны от $\theta$, положительных конечных фактических расстояний и конечных
значений функции во всех трёх точках. Если после округления одна из точек
совпала с исходной, запрос отклоняется, а не выдаётся нулевая производная.

<RustSource
  path="rust/crates/llm-from-scratch/src/autograd/gradcheck.rs"
  region="central-difference"
  caption="Вычислить фактические расстояния и применить формулу для неравных расстояний"
  label="Исходный код Rust с трёхточечной оценкой производной по фактическим представимым точкам в главе 13"
/>

{/* chapter-section:symbol-glossary */}

## Назовите величины, используемые при проверке производной

| Символ | Практический смысл |
| :--- | :--- |
| $f$ | Детерминированная скалярная функция потерь, производная которой проверяется. |
| $\theta$ | Конечный скалярный параметр или выбранная координата тензора. |
| $h$ | Положительная конечная запрошенная величина шага, по которой строятся обе точки вычисления в формате с плавающей запятой. |
| $\theta_-,\theta_+$ | Фактически представимые точки вычисления $\operatorname{fl}(\theta-h)$ и $\operatorname{fl}(\theta+h)$. |
| $h_-,h_+$ | Фактические положительные расстояния $\theta-\theta_-$ и $\theta_+-\theta$ слева и справа. |
| $f'(\theta)$ | Производная в локально гладкой точке, приближённая трёхточечной формулой для неравных расстояний. |

Для аналитического значения $a$ и численной оценки $n$ процедура выбирает
$s=\max(1,|a|,|n|)$ и записывает
$e=|a/s-n/s|$. При малых градиентах нижняя граница масштаба $1$ делает
погрешность абсолютной; при больших она становится относительной. Аналитическое
значение укладывается в допуск тогда и только тогда, когда
нормированная погрешность не превышает заданный конечный неотрицательный допуск.

<RustSource
  path="rust/crates/llm-from-scratch/src/autograd/gradcheck.rs"
  region="scale-aware-comparison"
  caption="Нормировать расхождение конечных производных по большей величине или единице"
  label="Исходный код Rust с правилом сравнения градиентов с учётом масштаба в главе 13"
/>

{/* chapter-section:history */}

## От обратного распространения для следующего слова к проверке производных при обучении Transformer

Нейронная языковая модель Бенжио и соавторов максимизирует логарифмическое
правдоподобие следующего слова и явно выполняет этап обратного распространения
и обновления параметров выходного и скрытого слоёв, а также обучаемых векторных
представлений слов.
Распространяемые назад производные позволяют многократно обновлять параметры,
но реализованный путь их вычисления не служит независимой проверкой самого себя.

Эта процедура обучения описана в работе
[Bengio et al., *A Neural Probabilistic Language Model*](https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf).
Бенжио и соавторы максимизируют логарифмическое правдоподобие следующего слова и
описывают обратное распространение с обновлением параметров, при котором
градиенты проходят через выходные элементы, веса скрытого слоя и обучаемые
векторные представления слов.

Transformer с повторяющимися слоями внимания и полносвязными блоками обучают
градиентным методом, выполняя 100 000 шагов Adam для базовой модели и 300 000
для большой. Байдин и соавторы различают численное дифференцирование и
автоматическое дифференцирование в обратном режиме: конечные разности оценивают
одну локальную производную по нескольким вычислениям функции, а обратный режим
эффективно получает градиент скалярной цели по множеству параметров. Здесь
отдельный путь численного вычисления помогает обнаружить локальную ошибку в
проверяемой производной.

Сведения о более поздней модели взяты из работы
[Vaswani et al., *Attention Is All You Need*](https://papers.nips.cc/paper_files/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf).
Васвани и соавторы обучают базовые модели Transformer 100 000 шагов, а большие —
300 000 шагов, применяя Adam с явно заданным расписанием скорости обучения.

Различие между этими способами вычисления производных объясняется в обзоре
[Baydin et al., *Automatic Differentiation in Machine Learning: a Survey*](https://arxiv.org/abs/1502.05767).
Байдин и соавторы описывают центральные конечные разности, компромисс между
погрешностями усечения и округления при выборе шага, плохую масштабируемость
полного численного градиента и эффективность обратного режима для скалярной
цели с множеством параметров.

В этой главе трёхточечные конечные разности служат лишь медленной выборочной численной сверкой для локально гладких целевых функций, в том числе для выбранных производных среднего NLL по индексам из главы 12, прежде чем в главе 14 появится обратный режим. Успешная сверка свидетельствует только о выбранных координатах, точках вычисления, шаге, допуске и конкретном примере; она не доказывает правильность всего градиента или локальную дифференцируемость. Такая проверка не обучает и не запускает декодер, а её правила относятся к данной реализации курса.

Успешная сверка также не доказывает вывод формулы, полную правильность обеих
реализаций или их работу в соседних точках и на других входных данных.

Ни одна из двух работ о языковых моделях не утверждает, что использует эту
процедуру проверки. Конечные разности — не позднее изобретение для LLM, не
замена алгоритма обучения и не часть работы декодера при генерации. Здесь они
нужны только для одного: сопоставить результат будущего обратного прохода с
выборочными прямыми вычислениями функции потерь по отдельному пути до обновления
параметров языковой модели.

{/* chapter-section:rust-implementation */}

## Реализуйте выборочную численную сверку

Типизированные ошибки отделяют неверную конфигурацию от корректно завершившегося
сравнения, которое не уложилось в допуск. В сообщении об ошибке указано, на какой
стороне возникла проблема, а к ошибкам тензора добавлена соответствующая координата.
Весь набор выбранных координат проверяется до первого вызова целевой функции.
Поэтому даже неконечное аналитическое значение в конце списка обнаруживается
заранее и частичный результат не возвращается.

<RustSource
  path="rust/crates/llm-from-scratch/src/autograd/gradcheck.rs"
  region="gradcheck-errors"
  caption="Различать ошибки шагов, точек, значений, форм, координат и представлений"
  label="Исходный код Rust с типами ошибок численной проверки градиента в главе 13"
/>

Пусть $N$ — число элементов непустого тензора, $R$ — максимальное запрошенное
число координат, а $S=\min(R,N)$ — фактическое число выбранных координат.
Аргумент Rust `max_samples` задаёт $R$. Нулевой запрос и пустой тензор
отклоняются, поэтому $S\geq1$.

При $S>1$ алгоритм вычисляет плоское смещение
$\left\lfloor k(N-1)/(S-1)\right\rfloor$ для каждого целого
$k\in\{0,1,\ldots,S-1\}$. Для формы `[2,3]` имеем $N=6$; при $R=4$ получаем
$S=4$, а $k=0,1,2,3$ даёт смещения
`[0,1,3,5]`. Им соответствуют координаты `[[0,0],[0,1],[1,0],[1,2]]`.
Генератор случайных чисел и скрытое состояние не нужны. При $S=1$ знаменатель
формулы был бы равен нулю, поэтому отдельная ветвь выбирает
$\lfloor N/2\rfloor$; для чётного $N$ это большее из двух центральных плоских
смещений. Общий алгоритм использует промежуточное значение без риска
переполнения и те же произведения последующих размерностей, что и накопительное
тензорное хранилище.

<RustSource
  path="rust/crates/llm-from-scratch/src/autograd/gradcheck.rs"
  region="sample-tensor-coordinates"
  caption="Выбрать уникальные упорядоченные координаты тензора без случайности"
  label="Исходный код Rust с детерминированным выбором координат тензора в главе 13"
/>

Для каждой выбранной координаты процедура вычисляет одну и ту же целевую
функцию сначала в фактической точке слева $\theta_-$, затем при исходном
значении $\theta$ и после этого в фактической точке справа $\theta_+$.
После каждого временного изменения она сразу восстанавливает точные исходные
биты — до анализа результата и до любого обычного возврата с ошибкой. Если
переданная целевая функция паникует, ограниченная гарантия восстановления из
этого учебного примера не действует.

<RustSource
  path="rust/crates/llm-from-scratch/src/autograd/gradcheck.rs"
  region="sampled-tensor-gradient-check"
  caption="Проверить выбранные координаты тензора и восстановить каждое временное изменение"
  label="Исходный код Rust с выборочной проверкой градиента по координатам тензора"
/>

В численной целевой функции используется среднее NLL по индексам из главы 12.
Логиты формы `[2,3]` равны `[0,1,-1,2,0,-2]`, цели — `[0,2]`, а прямое
вычисление средней функции потерь даёт `2.775268796472`. Отдельная аналитическая
реализация начинает с исходных логитов каждой строки. Для строки $r$ она
вычисляет

$$
m_r=\max_j \ell_{r,j},\qquad
p_{r,c}=\frac{\exp(\ell_{r,c}-m_r)}{\sum_j\exp(\ell_{r,j}-m_r)}.
$$

Для класса словаря $c$ и цели $t_r$ вручную выведенная производная имеет вид

$$
\frac{\partial\mathcal L}{\partial \ell_{r,c}}
=\frac{p_{r,c}-\mathbf 1[c=t_r]}{2}.
$$

В позиции целевого логита из вычисленной здесь вероятности вычитается единица,
после чего градиент каждой строки делится на число строк с целями, равное двум.
Этот аналитический путь не вызывает основные функции `softmax` или
`indexed_mean_nll`, а численная целевая функция вызывает `indexed_mean_nll`.
Такое разделение существенных вычислений помогает обнаружить ошибку, которая не
повторена в обоих путях, но не делает их полностью независимыми: у них общие
исходные логиты и цели, элементарная арифметика `f64`, хранилище `Tensor` и
соглашение о построчных индексах.

<RustSource
  path="rust/demos/ch13-gradient-checking/src/lib.rs"
  region="hand-derived-nll-gradient"
  caption="Вычислить аналитическое значение NLL непосредственно по исходным логитам"
  label="Исходный код Rust с аналитической производной функции потерь без основных функций softmax и NLL по индексам в главе 13"
/>

<RustSource
  path="rust/demos/ch13-gradient-checking/src/lib.rs"
  region="sampled-nll-gradient-check"
  caption="Сопоставить четыре аналитических значения с изменёнными входами NLL по индексам"
  label="Исходный код Rust с выборочной проверкой производной среднего NLL из главы 12"
/>

Пример подготавливает оба скалярных аналитических значения, перебор шести шагов, все четыре
координаты NLL, точное восстановление и одну ошибку совпавшей после округления
точки:

<RustSource
  path="rust/demos/ch13-gradient-checking/src/main.rs"
  region="learner-gradient-check-output"
  caption="Подготовить полный детерминированный результат перед печатью"
  label="Исходный код Rust с запуском учебного примера численной проверки градиента в главе 13"
/>

```bash
./course run cargo run --quiet --locked -p ch13-gradient-checking
```

```text
quadratic: theta=3.000000000000 requested_h=1.00000000000000006e-1 actual_h_minus=1.00000000000000089e-1 actual_h_plus=1.00000000000000089e-1 f_minus=8.410000000000 f_center=9.000000000000 f_plus=9.610000000000 left_slope=5.900000000000 right_slope=6.100000000000 left_weight=5.00000000000000000e-1 right_weight=5.00000000000000000e-1 numerical=6.000000000000
wrong candidate: analytic=5.500000000000 scaled_error=8.333333333333e-2 tolerance=1.000000000000e-6 pass=false
nll logits: shape=[2, 3] values=[0.0, 1.0, -1.0, 2.0, 0.0, -2.0] targets=[0, 2] loss=2.775268796472
sampled coordinates: [[0, 0], [0, 1], [1, 0], [1, 2]]
tensor restored exactly: true
```

{/* chapter-section:visualization */}

## Проследите, как уменьшение шага сначала помогает, а затем мешает

Для $g(\theta)=\theta^3-2\theta$ при $\theta=1.5$ аналитическая производная
равна $4.75$. Перебор в Rust начинается с запрошенного $h=1$, улучшается при
$10^{-1}$ и $10^{-3}$ и достигает наименьшей показанной нормированной
погрешности при запрошенном $h=10^{-5}$: численная оценка равна
$4.750000000100$, нормированная погрешность —
$2.103583973678\times10^{-11}$, и результат укладывается в допуск. Затем из-за
округления при запрошенном $h=10^{-13}$ получается $4.751111111111$ с
нормированной погрешностью $2.338634237605\times10^{-4}$, поэтому результат не
проходит проверку. При запрошенном $h=10^{-15}$ численная оценка равна
$4.800000000000$, а нормированная погрешность —
$1.041666666667\times10^{-2}$; этот результат также не укладывается в допуск.
Глава не объявляет $10^{-5}$ универсально лучшим шагом: это надёжная область
только для данного примера.

Схема сопоставляет запрошенную величину шага с фактическими расстояниями до
точек вычисления и показывает проверку линейной функции после округления,
известную недифференцируемую точку $|x|$ при $x=0$, отдельные аналитический и
численный пути NLL с их общими предпосылками, все режимы погрешности, результаты
сравнения, выбранные координаты и отклонённые запросы. Оценки и нормированные
погрешности в схеме округлены только для удобства чтения; полный вывод
исполняемого примера сохраняет их точные значения вместе с точками вычисления и
значениями функции. Каждое показанное $h$ — запрошенный шаг, а производная
вычисляется по двум фактическим расстояниям. Перебор показывает полезную область
для гладкой функции между погрешностями усечения и округления, а координаты
функции потерь — ограниченность выводов по успешной выборочной сверке.

<RustSource
  path="rust/demos/ch13-gradient-checking/src/lib.rs"
  region="step-size-scan"
  caption="Проверить производную кубической функции при всех шести величинах шага"
  label="Исходный код Rust с перебором шагов от погрешности усечения к погрешности округления в главе 13"
/>

<GradientCheckingDiagram labels={diagramLabels} instanceId="lesson" />

{/* chapter-section:exercises */}

## Сделайте предсказания до запуска Rust

1. Вычислите $q(2.9)$ и $q(3.1)$, а затем объясните, почему реализация должна получить $h_-$ и $h_+$, а не делить на запрошенное $2h$.
2. Для $f(x)=x$ и разных ненулевых фактических расстояний предскажите оба односторонних наклона и результат взвешенной трёхточечной формулы.
3. Предскажите результаты сравнения для аналитических значений $6$ и $5.5$ при допуске $10^{-6}$.
4. Выберите результат с наименьшей погрешностью в этом переборе шести шагов, не объявляя соответствующий запрошенный шаг универсально лучшим.
5. Объясните, почему запрошенные шаги $h=10^{-13}$ и $h=10^{-15}$ не проходят проверку после успешного результата с более крупным шагом из надёжной области.
6. До ответа вычислите масштаб и нормированную погрешность для ошибочного аналитического значения.
7. В тензоре $N=6$ элементов, а значение `max_samples` равно $R=4$. Вычислите $S=\min(R,N)$, затем для каждого $k\in\{0,1,2,3\}$ найдите плоское смещение $\left\lfloor k(N-1)/(S-1)\right\rfloor$ и сопоставьте четыре смещения с координатами формы `[2,3]`.
8. Предскажите поведение при нулевом шаге, точке, которая после округления не изменилась, и обычной ошибке после временного изменения тензора.
9. Для $f(x)=|x|$ при $x=0$ объясните, почему численная оценка $0$ может совпасть с аналитическим значением $0$, но не установить существование производной.
10. Проверьте понимание: что именно подтверждают четыре успешные сверки NLL, а какие общие предпосылки и вычисления остаются непроверенными?

<details>
  <summary>Проверьте предсказания</summary>

1. При округлении для ручного расчёта $q(2.9)=8.41$ и $q(3.1)=9.61$. Операции с плавающей запятой могут расположить точки на разных расстояниях, поэтому в формулу должны входить эти расстояния, а не запрошенный $h$.
2. Оба отношения равны $1$: $(f(x)-f(x_-))/h_-=1$ и $(f(x_+)-f(x))/h_+=1$. Сумма весов равна единице, поэтому даже при $h_-\ne h_+$ результат совпадает с $1$ в пределах погрешности вычислений.
3. Аналитическое значение $6$ укладывается в допуск. Для значения $5.5$ возвращается конечный результат сравнения с `pass=false`.
4. В этом фиксированном переборе запрошенный шаг $h=10^{-5}$ даёт наименьшую показанную нормированную погрешность: $2.103583973678\times10^{-11}$ при численной оценке $4.750000000100$. Для другой функции, точки, числового типа или пары фактических точек надёжная область может отличаться.
5. При запрошенном $h=10^{-13}$ численная оценка $4.751111111111$ имеет нормированную погрешность $2.338634237605\times10^{-4}$ и не проходит проверку. При $h=10^{-15}$ фактическое расстояние с каждой стороны равно лишь $1.11022302462515654\times10^{-15}$; односторонние наклоны становятся равны $4.4$ и $5.2$, поэтому их взвешенное значение $4.800000000000$ имеет нормированную погрешность $1.041666666667\times10^{-2}$ и также не проходит проверку. После округления соседние значения уже не сохраняют достаточно младших разрядов для надёжной оценки наклона.
6. В точной арифметике $\max(1,5.5,6)=6$ и $|5.5/6-6/6|=1/12$; напечатанный результат `f64` равен `8.333333333333e-2` и всё равно намного превышает $10^{-6}$.
7. Здесь $S=\min(4,6)=4$. Для $k=0,1,2,3$ выражение $\lfloor k(6-1)/(4-1)\rfloor=\lfloor 5k/3\rfloor$ даёт плоские смещения `[0,1,3,5]`, то есть координаты `[[0,0],[0,1],[1,0],[1,2]]`.
8. Нулевой $h$ недопустим; при $h=10^{-20}$ и исходной точке $1$ смещённая влево точка после округления совпадает с исходной. При любой обычной ошибке временное значение тензора восстанавливается до возврата.
9. Наклон симметричной секущей равен $0$, но наклон слева равен $-1$, а справа — $1$. В точке излома производная не существует, поэтому совпадение с нулём — ложное успешное сравнение за пределами требования локальной гладкости.
10. Результаты свидетельствуют только о четырёх выбранных координатах для этой целевой функции, входных данных, фактических точек, запрошенного шага и допуска. Они не доказывают весь градиент, не обучают модель и не относятся к генерации. Кроме того, оба пути используют общие входы, `f64`, `Tensor` и соглашения об индексах, поэтому эти предпосылки не проверяются сравнением.

</details>

После предсказаний запустите пример:

```bash
./course run cargo run --quiet --locked -p ch13-gradient-checking
```

{/* chapter-section:decoder-connection */}

## Подготовьте автоматическое дифференцирование в обратном режиме

Теперь проект может сопоставлять выбранные производные по логитам словаря,
вычисленные отдельным локальным аналитическим путём, с результатами основной
реализации среднего NLL по индексам при изменённых входах. Аналитический путь не
вызывает основные функции `softmax` или `indexed_mean_nll`, но оба пути всё ещё
используют одни и те же исходные данные, арифметику `f64`, хранилище `Tensor` и
соглашения об индексах. Поэтому совпадение служит полезным выборочным
свидетельством, а не доказательством полной правильности обеих реализаций. В
главе 14 та же граница применится к скалярным производным в обратном режиме.

В следующей главе узлы скалярных вычислений будут хранить значения и локальные
зависимости, обратный проход обработает их в обратном топологическом порядке, а
для повторно использованных значений вклады суммируются. Сам по себе
завершившийся обратный проход ничего не доказывает: его результаты по-прежнему
нужно выборочно сопоставлять с трёхточечной оценкой из этой главы в локально
гладких точках.
