---
{
  "chapter_id": "35-checkpoints",
  "locale": "ru",
  "content_revision": 2,
  "order": 35,
  "concept_id": "checkpoints",
  "title": "Сохраните всё состояние и продолжите без расхождений",
  "description": "Разберитесь, как контрольная точка LLM с версией формата сохраняет токенизатор и конфигурацию декодера, параметры, моменты AdamW и состояние генератора псевдослучайных чисел, отклоняет повреждённые данные, а после загрузки воспроизводит логиты и следующее обновление без расхождений.",
  "objective": "Сохранить и загрузить контрольную точку декодера с версией формата так, чтобы точно восстановить токенизатор и конфигурацию, параметры и состояние оптимизатора, состояние генератора псевдослучайных чисел, логиты и одно последующее обновление.",
  "worked_inputs": "Сохранить выбранное в главе 33 состояние декодера со словарём из пяти токенов на шаге 8: пять однобайтовых меток явно заданных токенов, 11 тензоров параметров, 22 тензора моментов AdamW и одно состояние SplitMix64; затем загрузить его и выполнить то же обновление, переходящее к шагу 9.",
  "formula": {
    "latex": "o_{k+1}=o_k+b_k\\prod_i n_i^{(k)},\\quad o_0=h",
    "symbols": [
      {
        "symbol": "o_{k+1}",
        "meaning": "абсолютное смещение в файле, с которого начинается следующая упорядоченная запись данных"
      },
      {
        "symbol": "o_k",
        "meaning": "абсолютное смещение в файле, с которого начинается упорядоченная запись данных k"
      },
      {
        "symbol": "b_k",
        "meaning": "размер одного элемента записи k в байтах: например, 1 для u8, 4 для u32 или 8 для f64"
      },
      {
        "symbol": "n_i^{(k)}",
        "meaning": "длина оси i в объявленной форме записи k"
      },
      {
        "symbol": "i",
        "meaning": "индекс одной оси в форме текущей записи"
      },
      {
        "symbol": "k",
        "meaning": "индекс записи в каноническом порядке данных токенизатора, параметров и оптимизатора"
      },
      {
        "symbol": "h",
        "meaning": "полный размер заголовка в байтах, совпадающий со смещением первой записи данных"
      }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "model-building-practice",
      "limitation": "Изолированный массив значений параметров не сообщает, какой токенизатор, конфигурация модели, формы тензоров, моменты оптимизатора и поток псевдослучайных чисел придают этим байтам смысл, а раздельно хранимые файлы могут перестать соответствовать друг другу.",
      "later_advance": "Выпуски нейронных языковых моделей стали согласованно распространять токенизатор, конфигурацию и контрольные точки; при обучении крупных моделей состояние оптимизатора превратилось в значительную часть сохраняемого состояния, а более поздние тензорные контейнеры начали указывать тип данных, форму и смещения до загрузки значений.",
      "modern_llm_role": "Воспроизводимая контрольная точка LLM связывает тензорный формат, который хранит типы данных, формы и смещения, со схемой приложения для токенизатора, конфигурации декодера, состояния оптимизатора, необходимого для продолжения обучения, состояния генератора псевдослучайных чисел, проверки данных и сохранения файла.",
      "sources": [
        {
          "role": "earlier",
          "year": 2019,
          "name": "OpenAI GPT-2 model downloader",
          "source_url": "https://github.com/openai/gpt-2/blob/master/download_model.py",
          "claim": "Загрузчик OpenAI получает данные, индекс и метаданные контрольной точки GPT-2 вместе с указателем на контрольную точку, гиперпараметрами, данными кодировщика и словарём BPE. Это показывает, что выпущенная языковая модель представляла собой согласованный комплект файлов, а не один изолированный файл весов."
        },
        {
          "role": "later",
          "year": 2020,
          "name": "ZeRO: Memory Optimizations Toward Training Trillion Parameter Models",
          "source_url": "https://arxiv.org/pdf/1910.02054",
          "claim": "Раджбхандари и соавторы относят к состоянию обучения крупной модели параметры, градиенты и состояние оптимизатора, например импульс и дисперсию Adam, а затем оценивают и распределяют эти составляющие по мере роста языковых моделей."
        },
        {
          "role": "later",
          "year": 2022,
          "name": "Safetensors format specification",
          "source_url": "https://github.com/huggingface/safetensors/blob/main/README.md",
          "claim": "Формат safetensors описывает длину заголовка с порядком байтов от младшего к старшему, тип данных и форму каждого тензора, пары смещений, задающие полуоткрытые диапазоны байтов, и полностью индексированный буфер данных с построчным расположением без промежутков."
        }
      ]
    },
    "approach": "Перейти от изолированных байтов весов и раздельно согласуемых файлов модели к проверяемой контрольной точке с версией формата, которая связывает всё состояние, необходимое для интерпретации одной LLM и продолжения её вычислений.",
    "summary": "Путь к воспроизводимым современным LLM объединяет согласованные файлы токенизатора и конфигурации, состояние оптимизатора и метаданные типов, форм и смещений тензоров. Контрольная сумма и правила атомарной замены в этой главе относятся именно к учебному формату; они не приписываются GPT-2, ZeRO или safetensors.",
    "rust_source": "rust/demos/ch35-checkpoints/src/lib.rs"
  },
  "rust_sources": [
    {
      "path": "rust/crates/llm-from-scratch/src/checkpoint.rs",
      "region": "versioned-checkpoint-encoding",
      "purpose": "Кодировать один канонический заголовок с порядком байтов от младшего к старшему, таблицу дескрипторов, записи данных разного размера и контрольную сумму всего файла для обнаружения случайных повреждений."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/checkpoint.rs",
      "region": "validated-checkpoint-loading",
      "purpose": "Отклонять расхождения в размере файла, контрольной сумме, дескрипторах, токенизаторе, модели, оптимизаторе и каноническом повторном кодировании, не выдавая некорректное восстановленное состояние."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/checkpoint.rs",
      "region": "atomic-checkpoint-save",
      "purpose": "Сохранять синхронизированный полный файл посредством атомарной замены файла из того же каталога в поддерживаемом сценарии Unix."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/training/adamw.rs",
      "region": "adamw-persistence-state",
      "purpose": "Проверять и сохранять именованные тензоры моментов, номер шага, группы параметров, конфигурацию и точные накопленные степени коэффициентов бета."
    },
    {
      "path": "rust/demos/ch35-checkpoints/src/lib.rs",
      "region": "historical-checkpoint-contrast",
      "purpose": "Получить из выбранной модели массив, содержащий только байты параметров, и сопоставить его измеренные свойства с полной контрольной точкой для продолжения вычислений."
    },
    {
      "path": "rust/demos/ch35-checkpoints/src/lib.rs",
      "region": "learner-evidence",
      "purpose": "Сериализовать выбранное в главе 33 состояние, независимо загрузить его, сравнить точные логиты и продолжение генератора псевдослучайных чисел, а затем выполнить одно и то же следующее обновление."
    },
    {
      "path": "rust/demos/ch35-checkpoints/src/main.rs",
      "purpose": "Вывести точные данные о схеме, расположении байтов, воспроизведении, проверке повреждённых файлов, атомарной замене и сопоставлении с массивом параметров."
    }
  ],
  "visualization": {
    "decision": "not-useful",
    "id": null,
    "rationale": "Смысловая таблица расположения байтов, короткий точный шестнадцатеричный префикс и результаты проверки повреждённых файлов показывают порядок записей и смещения точнее, чем схема."
  },
  "decoder_connection": "Теперь совокупный декодер можно сохранить как одну проверенную контрольную точку выбранного состояния и загрузить с теми же логитами, состоянием оптимизатора и продолжением генератора псевдослучайных чисел; глава 36 загрузит её перед преобразованием логитов в выбор токена."
}
---

import InlineMath from "../../../components/InlineMath.astro";
import RustSource from "../../../components/RustSource.astro";

{/* chapter-section:worked-example */}

## Зафиксируйте всё состояние, а не только веса

В главе 33 был выбран декодер на шаге $8$. В этом фиксированном примере он
совпадает с последним шагом обучения, поэтому параметры и состояние AdamW
описывают одну и ту же границу вычислений. Перед сохранением глава 35 проверяет
это равенство: модель, выбранная на шаге $6$, вместе с оптимизатором на шаге $8$
будет отклонена и не станет контрольной точкой.

Полное состояние включает:

1. версии схемы, формата данных токенизатора и алгоритма генератора
   псевдослучайных чисел;
2. пять упорядоченных байтовых представлений явно заданных токенов и все поля
   конфигурации декодера;
3. $11$ именованных тензоров параметров в стабильном порядке;
4. $22$ именованных тензора моментов AdamW, группы параметров, номер шага и
   точные накопленные степени $\beta_1$ и $\beta_2$;
5. выбранный шаг и одно значение состояния SplitMix64 для продолжения потока.

На этой границе сразу после обновления градиенты уже не нужны для сохранения.
Тестовые документы и итоговая проверка из главы 34 не открываются заново и не
включаются в файл. Кэш внимания тоже отсутствует: состоянием кэша займётся
глава 38.

Пять ID токенов от $0$ до $4$ в этом примере соответствуют однобайтовым меткам
`0`—`4`. Вместе с записями модели и оптимизатора они дают
$5+11+22=38$ упорядоченных записей данных. Два кодирования одного состояния
дают одинаковые $6330$ байт и одно и то же значение контрольной суммы
`fnv1a64:2b8b6097eaed6a91`.

Гарантия намеренно узкая: в одной и той же арифметической среде загрузка
воспроизводит точные биты. При смене оборудования или арифметических ядер
поведение чисел с плавающей точкой может измениться, даже если контрольная
точка корректна. FNV-1a обнаруживает случайные повреждения, но не подтверждает
подлинность файла и не защищает от злоумышленника.

{/* chapter-section:formula */}

## Вычисляйте следующее смещение по форме и размеру элемента

Для канонической записи данных $k$ следующее абсолютное смещение в байтах равно

$$
o_{k+1}=o_k+b_k\prod_i n_i^{(k)},\quad o_0=h
$$

Заголовок занимает $h=2869$ байт. Пять однобайтовых записей токенов занимают
$[2869,2874)$, поэтому первый параметр начинается со смещения $2874$. Это
`token_embedding.weight` формы $[5,4]$; каждый его элемент хранится как
восьмибайтовое значение `f64`. Поэтому не включённая правая граница равна

$$
2874+8(5\cdot4)=3034.
$$

Выравнивающие промежутки не добавляются. Запись `f64` на диске не обязана
начинаться с границы, подходящей для выравнивания значения в памяти: загрузчик
копирует восемь байтов и декодирует их в порядке от младшего к старшему
(little-endian), а не преобразует указатель на файл в указатель на `f64`.

Проверяются произведение размеров осей, умножение на размер элемента, сложение
смещений и преобразование дискового `u64` в `usize` текущего процесса. Начало
каждой записи должно совпадать с концом предыдущей, а последняя запись должна
заканчиваться на байте $6330$. Промежуток, перекрытие, арифметическое
переполнение, усечение файла или лишний байт считаются ошибкой.

{/* chapter-section:symbol-glossary */}

## Не смешивайте порядок записей, форму и представление значений

- $o_k$ — абсолютное смещение начала записи $k$.
- $o_{k+1}$ — абсолютное смещение начала следующей записи.
- $b_k$ — размер одного элемента записи $k$ в байтах: $1$ для `u8`, $4$ для
  `u32` или $8$ для `f64`.
- $n_i^{(k)}$ — длина оси $i$ записи $k$.
- $\prod_i n_i^{(k)}$ — число элементов в записи.
- $i$ выбирает одну ось формы.
- $k$ следует стабильному порядку данных токенизатора, параметров и состояния
  оптимизатора.
- $h$ — полный размер заголовка и смещение первой записи данных, поэтому
  $o_0=h$.

Дескриптор хранит назначение, имя, тип данных, форму, абсолютное смещение и
число байтов. Эти поля решают разные задачи. Форма определяет, как значения
образуют тензор; тип данных — сколько байтов представляет каждое значение;
канонический порядок — какому компоненту принадлежат значения. Совпадение
длины файла не компенсирует изменённое имя или форму.

Пять сгруппированных диапазонов ниже охватывают все $38$ дескрипторов без
промежутков. Сводные строки объединяют соседние дескрипторы, но загрузчик всё
равно проверяет каждый из них отдельно.

<table tabindex="0">
  <caption>Сгруппированные смежные диапазоны байтов в контрольной точке главы 35</caption>
  <thead>
    <tr>
      <th scope="col">Запись или группа</th>
      <th scope="col">Назначение</th>
      <th scope="col">Тип данных</th>
      <th scope="col">Форма или число элементов</th>
      <th scope="col">Размер элемента, байт</th>
      <th scope="col">Полуоткрытый диапазон байтов</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>literal-token-0</code></td>
      <td>явно заданный токен</td>
      <td><code>u8</code></td>
      <td><InlineMath latex="1" /></td>
      <td><InlineMath latex="1" /></td>
      <td><InlineMath latex="[2869,2870)" /></td>
    </tr>
    <tr>
      <td>от <code>literal-token-1</code> до <code>literal-token-3</code></td>
      <td>явно заданные токены</td>
      <td><code>u8</code></td>
      <td><InlineMath latex="3" /> однобайтовые записи</td>
      <td><InlineMath latex="1" /></td>
      <td><InlineMath latex="[2870,2873)" /></td>
    </tr>
    <tr>
      <td><code>literal-token-4</code></td>
      <td>явно заданный токен</td>
      <td><code>u8</code></td>
      <td><InlineMath latex="1" /></td>
      <td><InlineMath latex="1" /></td>
      <td><InlineMath latex="[2873,2874)" /></td>
    </tr>
    <tr>
      <td><code>token_embedding.weight</code></td>
      <td>параметр модели</td>
      <td><code>f64</code></td>
      <td><InlineMath latex="[5,4]" /></td>
      <td><InlineMath latex="8" /></td>
      <td><InlineMath latex="[2874,3034)" /></td>
    </tr>
    <tr>
      <td>остальные параметры и моменты</td>
      <td>состояние модели и AdamW</td>
      <td><code>f64</code></td>
      <td><InlineMath latex="32" /> тензорные записи / <InlineMath latex="412" /> скалярных значений</td>
      <td><InlineMath latex="8" /></td>
      <td><InlineMath latex="[3034,6330)" /></td>
    </tr>
  </tbody>
</table>

Последовательность из первых $16$ байт достаточно коротка для побайтовой проверки:

```text
4c 4c 4d 43 50 33 35 00 01 00 04 03 02 01 35 0b
```

Сначала идёт сигнатура `LLMCP35`, затем версия схемы $1$, маркер порядка байтов
и начало закодированной длины заголовка. Расположение каждой записи данных
определяет таблица, а не правила выравнивания в памяти.

{/* chapter-section:history */}

## От комплектов файлов модели к проверяемым контрольным точкам LLM

Эта последовательность показывает, как сохранялись смысл языковой модели и
возможность продолжить её вычисления.

Изолированный массив значений параметров не сообщает, какой токенизатор,
конфигурация модели, формы тензоров, моменты оптимизатора и поток
псевдослучайных чисел придают этим байтам смысл, а раздельно хранимые файлы
могут перестать соответствовать друг другу.

[Загрузчик модели GPT-2 от OpenAI](https://github.com/openai/gpt-2/blob/master/download_model.py)
показывает конкретную границу распространения языковой модели в 2019 году.
Загрузчик OpenAI получает данные, индекс и метаданные контрольной точки GPT-2
вместе с указателем на контрольную точку, гиперпараметрами, данными кодировщика
и словарём BPE. Это показывает, что выпущенная языковая модель представляла
собой согласованный комплект файлов, а не один изолированный файл весов. Этот
скрипт не задаёт единый автономный файл, точное продолжение обучения,
восстановление оптимизатора или генератора псевдослучайных чисел, контрольные
суммы либо формат этой главы.

[ZeRO](https://arxiv.org/pdf/1910.02054) показывает, почему объём полного
состояния становится существенным. Раджбхандари и соавторы относят к состоянию
обучения крупной модели параметры, градиенты и состояние оптимизатора, например
импульс и дисперсию Adam, а затем оценивают и распределяют эти составляющие по
мере роста языковых моделей. Из такого учёта следует, что одних весов недостаточно, чтобы
определить следующее адаптивное обновление. ZeRO исследует распределение данных
в памяти; контрольные точки активаций в этой работе означают повторное
вычисление активаций. Работа не задаёт формат долговременного хранения и не
требует сохранять градиенты на чистой границе сразу после обновления.

Более поздняя [спецификация формата safetensors](https://github.com/huggingface/safetensors/blob/main/README.md)
задаёт тензорный формат с метаданными типов, форм и смещений. Формат safetensors
описывает длину заголовка с порядком байтов от младшего к старшему, тип данных и
форму каждого тензора, пары смещений, задающие полуоткрытые диапазоны байтов, и
полностью индексированный буфер данных с построчным расположением без
промежутков. Сам по себе он не задаёт
токенизатор, конфигурацию декодера, оптимизатор, генератор псевдослучайных чисел,
контрольную сумму или правила атомарной замены. Его цель безопасности нельзя
смешивать с проверкой случайных повреждений в этой главе.

Выпуски нейронных языковых моделей стали согласованно распространять
токенизатор, конфигурацию и контрольные точки; при обучении крупных моделей
состояние оптимизатора превратилось в значительную часть сохраняемого состояния,
а более поздние тензорные контейнеры начали указывать тип данных, форму и
смещения до загрузки значений.

Воспроизводимая контрольная точка LLM связывает тензорный формат, который хранит
типы данных, формы и смещения, со схемой приложения для токенизатора,
конфигурации декодера, состояния оптимизатора, необходимого для продолжения
обучения, состояния генератора псевдослучайных чисел, проверки данных и
сохранения файла.

Путь к воспроизводимым современным LLM объединяет согласованные файлы
токенизатора и конфигурации, состояние оптимизатора и метаданные типов, форм и
смещений тензоров. Контрольная сумма и правила атомарной замены в этой главе
относятся именно к учебному формату; они не приписываются GPT-2, ZeRO или
safetensors. Локальное сопоставление на Rust получает $1152$ байт из $144$
значений параметров `f64` выбранной модели. Затем оно фиксирует, что полный файл
размером $6330$ байт также содержит пять записей токенизатора, $11$ именованных
записей параметров, $22$ записи моментов оптимизатора и состояние генератора
псевдослучайных чисел. Это измеряемое сопоставление не описывает GPT-2 или
safetensors как дамп памяти. Учебный формат не совместим с safetensors и не
предлагается как универсальный стандарт.

<RustSource
  path="rust/demos/ch35-checkpoints/src/lib.rs"
  region="historical-checkpoint-contrast"
  caption="Получить массив только из значений параметров и сопоставить его с полным состоянием контрольной точки"
  label="Исходный код на Rust: сопоставление массива параметров и полной контрольной точки LLM в главе 35"
/>

{/* chapter-section:rust-implementation */}

## Кодируйте и проверяйте данные, затем заменяйте файл атомарно

`AdamWStateEntry` хранит имя и форму одного параметра, конечный первый момент и
конечный неотрицательный второй момент. `AdamWState` добавляет конфигурацию,
необязательное разбиение параметров на группы с затуханием весов и без него,
номер шага, точные накопленные степени $\beta_1$ и $\beta_2$, а также таблицу,
ключами в которой служат стабильные имена. На шаге
$0$ степени должны быть в точности равны $1$, а моментов быть не должно; на
последующих шагах степени должны принадлежать $[0,1)$, а набор моментов — быть
непустым. При загрузке эти степени восстанавливаются напрямую, а не вычисляются
заново по потенциально отличающемуся арифметическому пути.

<RustSource
  path="rust/crates/llm-from-scratch/src/training/adamw.rs"
  region="adamw-persistence-state"
  caption="Проверить каждый именованный тензор моментов AdamW и сохранить точную арифметику продолжения"
  label="Исходный код на Rust: сохранение состояния AdamW в главе 35"
/>

`CheckpointTokenizer` хранит либо непустые уникальные байтовые представления
явно заданных токенов, либо упорядоченные пары слияния BPE на уровне байтов в пространстве
обучения. В файле записаны версии формата токенизатора и алгоритма SplitMix64.
Для восстановления BPE вызывается существующий конструктор токенизатора,
который выполняет проверку, а поток псевдослучайных чисел восстанавливается из
сохранённого значения состояния. Это поток для последующей генерации, а не начальное
значение перемешивания пакетов из главы 33.

Сначала кодировщик строит записи `u8`, `u32` и `f64`, затем измеряет заголовок и
назначает абсолютные смещения. Каждое примитивное значение явно записывается в
порядке байтов от младшего к старшему. FNV-1a охватывает весь канонический файл;
во время вычисления поле самой контрольной суммы считается нулевым.

<RustSource
  path="rust/crates/llm-from-scratch/src/checkpoint.rs"
  region="versioned-checkpoint-encoding"
  caption="Измерить и закодировать каноническую таблицу дескрипторов и записи данных разного размера"
  label="Исходный код на Rust: кодирование контрольной точки с версией формата в главе 35"
/>

До создания компонентов загрузчик проверяет фиксированную часть заголовка,
полный размер файла, контрольную сумму, известные назначения и типы данных, а
также диапазоны дескрипторов. Затем он проверяет инварианты восстановленных
токенизатора, модели и оптимизатора и точное каноническое повторное кодирование.
Ошибка на любом этапе не возвращает `Checkpoint`.

<RustSource
  path="rust/crates/llm-from-scratch/src/checkpoint.rs"
  region="validated-checkpoint-loading"
  caption="Отклонить повреждённые или смыслово несогласованные данные, не выдавая некорректную контрольную точку"
  label="Исходный код на Rust: проверяемая загрузка контрольной точки в главе 35"
/>

При сохранении в каталоге назначения через `create_new` создаётся уникальный
временный файл. В него полностью записываются и синхронизируются данные, после
чего он переименовывается с заменой целевого файла, а каталог синхронизируется. В
поддерживаемом сценарии Unix это атомарная замена в пределах одной файловой
системы. На других платформах возвращается явная ошибка о неподдерживаемой
операции, а предыдущая контрольная точка не удаляется.

<RustSource
  path="rust/crates/llm-from-scratch/src/checkpoint.rs"
  region="atomic-checkpoint-save"
  caption="Синхронизировать полный временный файл из того же каталога перед атомарной заменой"
  label="Исходный код на Rust: атомарное сохранение контрольной точки в главе 35"
/>

Исполняемый пример независимо восстанавливает исходную и загруженную модели.
Обе вычисляют логиты без записи графа для входа $[0,1]$, выполняют обратное
распространение для целей $[1,2]$, а затем одинаковое обновление с явно заданной
скоростью обучения $0.006$. Сравниваются все биты параметров, точное состояние
оптимизатора, логиты после обновления и следующее значение SplitMix64.

<RustSource
  path="rust/demos/ch35-checkpoints/src/lib.rs"
  region="learner-evidence"
  caption="Проверить детерминированные байты, точную загрузку, отклонение повреждённых данных, одинаковое следующее обновление и атомарную замену"
  label="Исходный код на Rust: сбор измеряемых результатов главы 35"
/>

Исполняемый отчёт фиксирует четыре измеряемых результата:

```text
roundtrip=bytes_deterministic:true loaded_bytes_identical:true logits_bits_identical:true logits_fingerprint:fnv1a64:6029064fe7cd162d rng_next_identical:true rng_next:0x9a8c505971939232
resume=learning_rate:0.006000 next_step:9 parameter_bits_identical:true optimizer_state_identical:true logits_bits_identical:true logits_fingerprint:fnv1a64:0b875a0c9f380d8f
reject=version:true vocabulary_mismatch:true truncation:true checksum:true
atomic=replaced_complete_file:true loaded_rng_state:0x9e3779b97f4a7c39 temporary_files:0 unix_same_directory:true
```

Полный исполняемый пример также проверяет BPE на уровне байтов, разные размеры
элементов, неподдерживаемые версии и порядок байтов, повреждение метаданных,
лишние байты, несоответствия токенизатора и модели, отрицательные вторые моменты
и детерминированное повторное кодирование.

<RustSource
  path="rust/demos/ch35-checkpoints/src/main.rs"
  caption="Вывести точные данные о схеме, расположении байтов, воспроизведении, проверке повреждённых файлов и атомарной замене"
  label="Исходный код на Rust: точка входа для отчёта главы 35"
/>

{/* chapter-section:visualization */}

## Проверьте расположение байтов, прежде чем доверять данным

Загрузчику контрольной точки нужны точные порядок записей, размер элемента,
форма и диапазон байтов. Блок-схема отбросила бы эти детали, а таблица
дескрипторов сохраняет их для непосредственной проверки.

Читайте таблицу сверху вниз. Каждая строка должна начинаться точно там, где
заканчивается предыдущая; её длина должна соответствовать типу данных и форме;
последняя не включённая правая граница должна совпадать с объявленной длиной
файла. Фиксированный префикс из $16$ байт позволяет сопоставить сигнатуру,
версию схемы, маркер порядка байтов и начало длины заголовка с теми же байтами,
которые получает загрузчик.

Строки отклонения охватывают разные границы ошибок: неизвестную версию,
несовместимый словарь, усечённый диапазон и изменение проверяемого байта. Вместе
с точным циклом сохранения-загрузки и одинаковым следующим обновлением они
показывают, что было сохранено и какие несоответствия не принимаются.

Интерпретируйте результаты сдержанно. `logits_bits_identical:true` устанавливает
точное воспроизведение для этого примера. `checksum:true` показывает, что один
изменённый проверяемый байт отклоняется, но не превращает FNV-1a в
криптографическую подпись.

{/* chapter-section:exercises */}

## Предскажите смещения, затем проверьте повреждённые файлы

1. Начните со смещения $2874$ и тензора `f64` формы $[5,4]$. Предскажите его
   не включённую правую границу.
2. Тензор пар BPE формы $[3,2]$ хранит значения `u32`. Сколько байтов он занимает?
3. Метка одного явно заданного токена растёт с одного байта до четырёх. Какие
   последующие смещения изменятся?
4. Значения параметров не изменились, но изменилась форма в дескрипторе. Должна
   ли загрузка пройти, если контрольная сумма охватывает и метаданные, и значения?
5. Веса загрузились точно, но моменты AdamW сброшены. Совпадёт ли следующее
   обновление?
6. Выбранное состояние переносится на шаг $6$, а оптимизатор остаётся на шаге
   $8$. Какой инвариант создания отклонит эту пару?
7. Процесс останавливается после синхронизации временного файла, но до
   переименования. Какой полный файл назначения остаётся видимым?
8. Доказывает ли совпадение FNV-1a, что злоумышленник не менял файл?

<details>
  <summary>Проверьте ход рассуждений</summary>

  1. $3034$, потому что $2874+8(5\cdot4)=3034$.
  2. $4(3\cdot2)=24$ байта.
  3. Конец изменённой метки и все последующие абсолютные смещения сдвигаются на
     три байта; каноническое повторное кодирование также меняет проверяемые
     метаданные.
  4. Нет. Контрольная сумма метаданных изменится, а семантическая проверка формы
     отклонит и несогласованный дескриптор с заново вычисленной контрольной суммой.
  5. Нет. Следующее адаптивное направление AdamW зависит от первого и второго
     моментов и накопленных степеней $\beta_1$ и $\beta_2$.
  6. При создании контрольной точки выбранный шаг должен совпадать с шагом
     оптимизатора.
  7. Видимым остаётся предыдущий полный файл назначения; неопубликованный
     временный файл можно удалить.
  8. Нет. FNV-1a обнаруживает случайные повреждения, но не подтверждает
     подлинность.
</details>

Главное заблуждение — считать контрольную точку только весами. Веса
воспроизводят функцию лишь вместе с токенизатором и конфигурацией; для точного
продолжения обучения дополнительно нужны состояние оптимизатора и состояние
генератора псевдослучайных чисел с того же шага.

{/* chapter-section:decoder-connection */}

## Перед выбором токена загрузите то же состояние

Теперь совокупный декодер можно сохранить как одну проверенную контрольную
точку выбранного состояния и загрузить с теми же логитами, состоянием
оптимизатора и продолжением генератора псевдослучайных чисел. Загрузчик
восстанавливает смысл модели до того, как значения получит любой потребитель.

Глава 36 загрузит эту контрольную точку перед преобразованием логитов в выбор
токена с помощью температуры и top-$k$. В следующей главе можно будет менять
правила случайного выбора токена, не подменяя токенизатор, конфигурацию модели,
веса, состояние оптимизатора, сведения о шаге, с которого оно получено, или
продолжаемый поток псевдослучайных чисел.
