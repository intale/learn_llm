---
{
  "chapter_id": "30-multi-head-attention",
  "locale": "ru",
  "content_revision": 2,
  "order": 30,
  "concept_id": "multi-head-causal-self-attention",
  "title": "Вычислите каузальное внимание в нескольких головах, затем смешайте их выходы",
  "description": "Разберитесь, как полноразмерные проекции запросов, ключей и значений разделяются на головы, в каждой из которых независимо применяются RoPE и каузальное внимание, а затем выходы конкатенируются и проходят через обучаемую выходную проекцию.",
  "objective": "Разделите спроецированные признаки на головы каузального внимания, отдельно вычислите выход каждой головы, конкатенируйте результаты и смешайте их обучаемой выходной проекцией.",
  "worked_inputs": "Возьмите один вход из трёх токенов при $d_{\\mathrm{model}}=4$ и $h=2$. Единичные матрицы запросов, ключей и значений позволяют явно увидеть две группы признаков: первая компенсирует поворот RoPE и даёт равномерные каузальные строки, а вторая — неравномерные. Затем $W_O$ меняет местами два блока выходных признаков голов. Сначала предскажите все промежуточные формы и укажите, на каком этапе впервые могут смешаться признаки, полученные разными головами.",
  "formula": {
    "latex": "\\operatorname{MHA}(X)=\\operatorname{Concat}(H_1,\\ldots,H_h)W_O",
    "symbols": [
      { "symbol": "\\operatorname{MHA}", "meaning": "полный слой многоголового каузального самовнимания" },
      { "symbol": "X", "meaning": "входные скрытые состояния с осями батча, токенов и ширины модели" },
      { "symbol": "B", "meaning": "размер батча" },
      { "symbol": "T", "meaning": "число позиций токенов в каждой последовательности" },
      { "symbol": "d_{\\mathrm{model}}", "meaning": "ширина входных и выходных признаков полного слоя" },
      { "symbol": "h", "meaning": "ненулевое число голов внимания" },
      { "symbol": "i", "meaning": "индекс головы от единицы до h" },
      { "symbol": "d_h", "meaning": "ширина одной головы, равная отношению ширины модели к числу голов" },
      { "symbol": "W_Q,W_K,W_V", "meaning": "обучаемые упакованные матрицы проекций запросов, ключей и значений без смещений" },
      { "symbol": "Q,K,V", "meaning": "полноразмерные тензоры спроецированных запросов, ключей и значений до разделения на головы" },
      { "symbol": "W_i^Q,W_i^K,W_i^V", "meaning": "столбцы матриц проекций, относящиеся к голове i" },
      { "symbol": "Q_i,K_i,V_i", "meaning": "строки спроецированных запросов, ключей и значений головы i" },
      { "symbol": "R(t)", "meaning": "двумерный поворот на t радиан, используемый первой головой в примере" },
      { "symbol": "t", "meaning": "позиция токена в примере с индексацией от нуля" },
      { "symbol": "\\widetilde Q_i,\\widetilde K_i", "meaning": "строки запросов и ключей головы i после ротационного позиционного кодирования" },
      { "symbol": "M", "meaning": "каузальная маска с разрешённой диагональю, закрывающая будущие позиции ключей" },
      { "symbol": "j", "meaning": "индекс позиции ключа, по которому нормализуется каждая строка внимания" },
      { "symbol": "A_i", "meaning": "построчно нормализованная каузальная таблица внимания головы i" },
      { "symbol": "H_i", "meaning": "взвешенная с учётом каузальности смесь значений, вычисленная головой i" },
      { "symbol": "\\operatorname{Concat}", "meaning": "конкатенация выходов голов вдоль последней оси признаков" },
      { "symbol": "W_O", "meaning": "обучаемая матрица выходной проекции без смещения, смешивающая конкатенированные признаки голов" },
      { "symbol": "N_\\theta", "meaning": "число обучаемых скалярных параметров слоя" },
      { "symbol": "\\delta", "meaning": "шаг центральной разностной формулы при проверке градиента" },
      { "symbol": "\\varepsilon_g", "meaning": "максимально допустимая погрешность центральной разности" },
      { "symbol": "\\varepsilon", "meaning": "допуск для проверок правильности объединения этапов и инвариантов" }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "neural-architecture",
      "limitation": "Бахданау и соавторы заменяют один фиксированный вектор исходного предложения контекстным вектором, зависящим от шага декодирования, однако рекуррентный декодер по-прежнему за один шаг строит одно распределение выравнивания и один контекстный вектор, а не несколько параллельных спроецированных голов самовнимания.",
      "later_advance": "Васвани и соавторы несколько раз проецируют запросы, ключи и значения, параллельно вычисляют внимание для этих проекций, конкатенируют выходы и проецируют полученный результат.",
      "modern_llm_role": "Туврон и соавторы описывают модель на основе Transformer с несколькими головами внимания, RoPE в каждом слое и оптимизированной реализацией каузального многоголового внимания.",
      "sources": [
        {
          "role": "earlier",
          "year": 2014,
          "name": "Bahdanau, Cho, and Bengio, Neural Machine Translation by Jointly Learning to Align and Translate",
          "source_url": "https://arxiv.org/abs/1409.0473",
          "claim": "Бахданау и соавторы заменяют один фиксированный вектор исходного предложения контекстным вектором, зависящим от шага декодирования, однако рекуррентный декодер по-прежнему за один шаг строит одно распределение выравнивания и один контекстный вектор, а не несколько параллельных спроецированных голов самовнимания."
        },
        {
          "role": "later",
          "year": 2017,
          "name": "Vaswani et al., Attention Is All You Need",
          "source_url": "https://arxiv.org/abs/1706.03762",
          "claim": "Васвани и соавторы несколько раз проецируют запросы, ключи и значения, параллельно вычисляют внимание для этих проекций, конкатенируют выходы и проецируют полученный результат."
        },
        {
          "role": "later",
          "year": 2023,
          "name": "Touvron et al., LLaMA: Open and Efficient Foundation Language Models",
          "source_url": "https://arxiv.org/abs/2302.13971",
          "claim": "Туврон и соавторы описывают модель на основе Transformer с несколькими головами внимания, RoPE в каждом слое и оптимизированной реализацией каузального многоголового внимания."
        }
      ]
    },
    "approach": "От одного зависящего от шага распределения внимания к нескольким независимо спроецированным подпространствам каузального внимания и обучаемому смешиванию их выходов",
    "summary": "Несколько голов не повторяют одно полноразмерное вычисление. Обучаемые матрицы запросов, ключей и значений создают несколько представлений меньшей ширины; каждая голова вычисляет собственные каузальные строки вероятностей и смеси значений; конкатенация восстанавливает ширину модели, а выходная матрица учится смешивать признаки голов.",
    "rust_source": "rust/demos/ch30-multi-head-attention/src/lib.rs"
  },
  "rust_sources": [
    {
      "path": "rust/crates/llm-from-scratch/src/attention/multi_head.rs",
      "region": "head-layout",
      "purpose": "Разделить тензор на головы и снова объединить их дифференцируемыми операциями изменения формы и перестановки осей."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/attention/multi_head.rs",
      "region": "multi-head-errors",
      "purpose": "Явно представить ошибки конфигурации, расположения осей, позиций и вложенных операций."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/attention/multi_head.rs",
      "region": "multi-head-layer",
      "purpose": "Собрать полноразмерные проекции запросов, ключей и значений, RoPE и каузальное внимание по головам, объединение и выходную проекцию."
    },
    {
      "path": "rust/demos/ch30-multi-head-attention/src/lib.rs",
      "region": "historical-multi-head-contrast",
      "purpose": "Показать ограниченный переход от рекуррентного выравнивания исходной последовательности к спроецированным головам Transformer."
    },
    {
      "path": "rust/demos/ch30-multi-head-attention/src/lib.rs",
      "region": "learner-report",
      "purpose": "Собрать точные значения примера, инварианты, градиенты, граничные случаи и переход к следующей главе."
    },
    {
      "path": "rust/demos/ch30-multi-head-attention/src/main.rs",
      "purpose": "Запустить полный пример главы 30."
    },
    {
      "path": "rust/demos/ch30-multi-head-attention/src/diagram_trace.rs",
      "region": "multi-head-trace",
      "purpose": "Сериализовать точные значения и инварианты примера, используемые в схеме."
    }
  ],
  "visualization": {
    "decision": "useful",
    "id": "multi-head-attention-flow",
    "rationale": "Две видимые группы признаков, две независимо нормализованные нижнетреугольные таблицы внимания, конкатенированная строка и строка после выходной проекции показывают, где головы остаются раздельными и где их признаки наконец могут смешаться."
  },
  "decoder_connection": "Теперь в собираемом декодере есть полный слой многоголового каузального внимания без смещений и с учётом позиций. В главе 31 его вход будет нормализован, сам слой окажется в остаточной ветви, а рядом появится остаточная ветвь сети прямого распространения."
}
---

import RustSource from '../../../components/RustSource.astro';
import MultiHeadAttentionDiagram from '../../../components/chapters/MultiHeadAttentionDiagram.astro';

export const diagramLabels = {
  title: 'Две головы внимания остаются раздельными до выходной проекции',
  description:
    'Проследите путь спроецированных признаков через разделение на головы, RoPE, две каузальные таблицы вероятностей, смеси значений, конкатенацию и выходную проекцию.',
  sections: {
    split: 'Спроецируйте признаки и разделите их по головам',
    heads: 'В каждой голове отдельно нормируйте каузальную таблицу вероятностей',
    merge: 'Конкатенируйте выходы голов и примените обучаемую выходную проекцию',
    proof: 'Проверьте неизменность выходов префикса и инварианты сборки',
  },
  stages: {
    projected: 'После проекции',
    rotary: 'После RoPE',
    causalWeights: 'Каузальные вероятности внимания',
    headOutputs: 'Смеси значений по головам',
    concatenation: 'Конкатенированные выходы голов',
    outputProjection: 'Строки до и после выходной проекции',
  },
  shapeStages: {
    input: 'Вход',
    split: 'После разделения на головы',
    rotated: 'После RoPE',
    weights: 'Вероятности внимания',
    headOutput: 'Выходы голов',
    merged: 'После конкатенации',
    outputWeight: 'Матрица выходной проекции',
    output: 'Выход слоя',
  },
  fields: {
    shape: 'Форма тензора',
    features: 'Голова и координаты спроецированных признаков',
    tokenPosition: 'Позиция токена',
    queryPosition: 'Позиция запроса',
    keyPosition: 'Позиция ключа',
    visibility: 'Доступность ключа',
    probability: 'Вероятность внимания',
    rowSum: 'Сумма строки',
    headOutput: 'Выход головы',
    mergedRow: 'Конкатенированная строка',
    outputWeight: 'Строка матрицы выходной проекции',
    finalOutput: 'Итоговая строка выхода',
    prefixProof: 'Результат после изменения последнего токена',
  },
  cues: {
    headZero: 'Голова 0',
    headOne: 'Голова 1',
    headZeroCue: 'Сплошная рамка обозначает голову 0',
    headOneCue: 'Штриховая рамка обозначает голову 1',
    allowed: 'Доступный ключ',
    blocked: 'Закрытый ключ',
    diagonal: 'Разрешённая диагональ',
    allowedCue: 'Доступный ключ: сплошное подчёркивание',
    blockedCue: 'Закрытый ключ: штриховое подчёркивание',
    diagonalCue: 'Разрешённая диагональ: двойная рамка',
    concatenated: 'До выходной проекции',
    projected: 'После выходной проекции',
    unchanged: 'Без изменений',
    changed: 'Изменилось',
    verified: 'Подтверждено расчётом',
  },
  captions: {
    split: 'Общие проекции могут использовать весь вход; единичные веса лишь показывают две группы выхода.',
    heads: 'У каждой головы своя матрица оценок, свой softmax по строкам и своя смесь значений, но граница каузальной видимости у них одинакова.',
    merge: 'Конкатенация сохраняет обе группы признаков; выбранная выходная матрица затем меняет их местами, поэтому эти два шага нельзя спутать.',
    proof: 'Если изменить только последнюю входную строку, строки выхода 0 и 1 останутся побитово прежними в обеих головах и после выходной проекции.',
  },
  proofChecks: {
    splitMerge: 'Разделение с последующим объединением',
    splitMergeResult: 'Точно восстанавливает тензор',
    headIsolation: 'Влияние выходов голов друг на друга',
    headIsolationResult: 'Отсутствует до выходной проекции',
    futureProbabilities: 'Вероятности будущих ключей',
    futureProbabilitiesResult: 'Точно равны нулю',
    commonOffset: 'Общий сдвиг позиций при неизменном содержимом',
    commonOffsetResult: 'Сохраняет вероятности внимания',
    parameters: 'Обучаемые скалярные параметры',
    gradients: 'Координаты, проверенные центральными разностями',
  },
  scrollers: {
    splitFormula: 'Формула разделения признаков по головам',
    headFormula: 'Формулы каузального внимания в отдельной голове',
    mergeFormula: 'Формула конкатенации и выходной проекции',
    partitions: 'Прокручиваемая таблица проекций и поворотов признаков по головам',
    heads: 'Прокручиваемая таблица каузальных вероятностей и выходов головы',
    concatenation: 'Прокручиваемая таблица конкатенации выходов голов',
    outputMap: 'Прокручиваемая таблица матрицы выходной проекции',
    finalOutput: 'Прокручиваемая таблица строк до и после выходной проекции',
  },
};

{/* chapter-section:worked-example */}

## Сначала определите границы этапов, затем вычисляйте вероятности

Возьмём один батч с тремя строками токенов, ширину модели
$d_{\mathrm{model}}=4$ и число голов $h=2$. Ширина одной головы равна

$$
d_h=\frac{d_{\mathrm{model}}}{h}=2.
$$

В этой главе RoPE применяется ко всей голове, поэтому ширина $d_h$ должна быть
ещё и чётной. Именно поэтому $h=4$ здесь недопустимо, хотя $h=4$ делит
$d_{\mathrm{model}}=4$ без остатка: в каждой голове осталась бы одна координата
без пары.

Зафиксируем вход

$$
X=
\begin{bmatrix}
1&0&1&0\\
\cos(1)&-\sin(1)&0&1\\
\cos(2)&-\sin(2)&1&1
\end{bmatrix}.
$$

Только в этом примере $W_Q=W_K=W_V=I$. Благодаря этому группы признаков после
проекции легко проследить. Для многоголового внимания это не ограничение: в
обученном слое столбцы, относящиеся к любой голове, могут использовать все
входные признаки.

Прежде чем смотреть на вывод программы на Rust, предскажите последовательность
форм:

$$
\begin{aligned}
X:[1,3,4]
&\to(Q,K,V):[1,3,4]
\to\operatorname{split}(Q,K,V):[1,2,3,2],\\
\operatorname{split}(Q,K,V):[1,2,3,2]
&\to A:[1,2,3,3]
\to H:[1,2,3,2],\\
H:[1,2,3,2]
&\to\operatorname{Concat}(H):[1,3,4]
\xrightarrow{W_O}\operatorname{MHA}(X):[1,3,4].
\end{aligned}
$$

Первые две входные координаты намеренно компенсируют поворот, задаваемый
позицией. В позиции токена $t$ они равны $R(-t)[1,0]$, поэтому поворот из главы
29 возвращает

$$
R(t)R(-t)[1,0]=[1,0].
$$

Таким образом, голова $0$ сравнивает три одинаковые строки запросов и ключей.
До запуска программы предскажите её каузальные вероятности:

$$
A^{(0)}=
\begin{bmatrix}
1&0&0\\
\tfrac12&\tfrac12&0\\
\tfrac13&\tfrac13&\tfrac13
\end{bmatrix}.
$$

До поворота голова $1$ получает строки $[1,0]$, $[0,1]$ и $[1,1]$, поэтому её
разрешённые оценки не равны друг другу. При этом в обеих головах вероятность
выбора ключа выше диагонали точно равна нулю.

Теперь проследим позицию токена $1$ в обеих головах. Голова $0$ усредняет первые
две строки значений до поворота, а не повёрнутые строки запросов:

$$
H^{(0)}_1
=\tfrac12[1,0]+\tfrac12[0.540302,-0.841471]
=[0.770151,-0.420735].
$$

Для головы $1$ две видимые масштабированные оценки и их softmax равны

$$
[s^{(1)}_{1,0},s^{(1)}_{1,1}]
=[-0.595010,0.707107]
\xrightarrow{\operatorname{softmax}}
[0.213809,0.786191].
$$

Её строки значений до поворота равны $[1,0]$ и $[0,1]$, поэтому
$H^{(1)}_1=[0.213809,0.786191]$. После конкатенации двух выходов получаем
$[0.770151,-0.420735,0.213809,0.786191]$.

Наконец, предскажите, на каком этапе признаки двух голов впервые могут повлиять
на одну и ту же обучаемую выходную координату. Разделение и оба вычисления
внимания сохраняют головы раздельными, а конкатенация только размещает их
координаты рядом. Первая возможность обучаемого смешивания голов после их
вычислений внимания появляется при умножении на $W_O$. Зафиксированная $W_O$ меняет две
группы местами и тем самым показывает обучаемую маршрутизацию; в упражнении 5
внеблочный элемент создаёт настоящую сумму признаков разных голов. При этом
предшествующие плотные проекции запросов, ключей и значений уже могут использовать
каждую координату общей входной строки.

{/* chapter-section:formula */}

## Сначала спроецируйте признаки, затем разделите их на головы и вычислите оценки

Реализация хранит три упакованные матрицы без смещений:

$$
Q=XW_Q,\qquad K=XW_K,\qquad V=XW_V,
$$

где $Q$, $K$ и $V$ имеют форму $[B,T,d_{\mathrm{model}}]$. Изменение формы с
последующей перестановкой осей даёт $[B,h,T,d_h]$.

Упакованная запись — лишь удобная организация столбцов отдельных голов из
исходной статьи. Для запросов

$$
W_Q=[W_1^Q\;W_2^Q\;\cdots\;W_h^Q],
\qquad Q_i=XW_i^Q,
$$

и то же соотношение выполняется для ключей и значений. Порядок операций важен:
если сначала разделить исходный $X$, каждая голова будет ограничена фиксированной
частью входа. Если сначала выполнить проекцию, то все $W_i^Q$, $W_i^K$ и $W_i^V$
могут использовать всю входную строку, а уже их выходы попадают в отдельные
ветви голов.

Для головы $i$ примените RoPE только к строкам запросов и ключей:

$$
\widetilde Q_i=\operatorname{RoPE}(Q_i),\qquad
\widetilde K_i=\operatorname{RoPE}(K_i).
$$

Затем используйте операцию каузального внимания из главы 28:

$$
A_i=\operatorname{softmax}_{j}\!\left(
\frac{\widetilde Q_i\widetilde K_i^\top}{\sqrt{d_h}}+M
\right),
\qquad
H_i=A_iV_i.
$$

$M$ закрывает будущие ключи, а softmax выполняется отдельно по позициям ключей
в каждой голове. Одно полноразмерное вычисление внимания дало бы одну
нормализованную таблицу $[T,T]$, общую для всех признаков значений.
Многоголовое внимание вместо этого строит $h$ отдельно нормализованных таблиц
$[T,T]$. Они могут различаться из-за разных проекций запросов и ключей, но при
некоторых входах и параметрах их численные значения могут и совпасть.

Каждый $A_i$ имеет форму $[B,T,T]$, а каждый $H_i$ — форму $[B,T,d_h]$.
Восстановите ширину модели конкатенацией по последней оси признаков, затем
примените одну обучаемую выходную матрицу:

$$
\operatorname{MHA}(X)=\operatorname{Concat}(H_1,\ldots,H_h)W_O
$$

где

$$
W_O\in\mathbb{R}^{d_{\mathrm{model}}\times d_{\mathrm{model}}}.
$$

У конкатенации нет параметров, и она ничего не усредняет. $W_O$ может сохранять,
менять местами, складывать и иначе комбинировать координаты, полученные разными
головами. В целевом слое четыре матрицы ширины модели без смещений, поэтому

$$
N_\theta=4d_{\mathrm{model}}^2.
$$

При $d_{\mathrm{model}}=4$ это $64$ скалярных параметра.

{/* chapter-section:symbol-glossary */}

## Не путайте оси модели, голов и токенов

- $\operatorname{MHA}$ — полный слой многоголового каузального самовнимания.
- $X$ — батч входных строк скрытых состояний.
- $\operatorname{Concat}$ объединяет выходы голов вдоль оси признаков в каждой
  позиции токена; она не нормализует и не смешивает их.
- $B$ — размер батча.
- $T$ — число позиций токенов в каждой последовательности.
- $d_{\mathrm{model}}$ — ширина входа и выхода полного слоя.
- $h$ — число отдельно нормализуемых голов внимания.
- $d_h=d_{\mathrm{model}}/h$ — ширина одной головы; здесь она чётная, потому что
  RoPE поворачивает соседние пары координат.
- $t$ — позиция токена в примере с индексацией от нуля, а $R(t)$ — двумерный
  поворот на $t$ радиан, используемый первой головой примера.
- $W_Q$, $W_K$ и $W_V$ — упакованные матрицы проекций ширины модели. Столбцы,
  относящиеся к голове $i$, образуют $W_i^Q$, $W_i^K$ и $W_i^V$.
- $Q_i$, $K_i$ и $V_i$ — спроецированные строки запросов, ключей и значений
  головы $i$.
- $\widetilde Q_i$ и $\widetilde K_i$ — эти же запросы и ключи после RoPE;
  значения не поворачиваются.
- $M$ — каузальная маска, закрывающая ключи справа от позиции запроса.
- $j$ — индекс позиции ключа и ось, по которой нормализуется каждая строка
  softmax.
- $A_i$ — построчно нормализованная каузальная таблица внимания головы $i$.
- $H_i=A_iV_i$ — взвешенная смесь значений головы $i$.
- $W_O$ — обучаемая выходная матрица, применяемая после конкатенации всех строк
  $H_i$.
- $N_\theta$ — число обучаемых скалярных параметров. $\delta$ — приращение
  центральной разности, $\varepsilon_g$ — допустимая погрешность этой проверки,
  а $\varepsilon$ — допуск для проверок сборки и инвариантов.

В формулах используется принятое в статье соглашение
$i\in\{1,\ldots,h\}$. В программе на Rust и зафиксированных данных главы
индексы голов начинаются с нуля: $0,\ldots,h-1$. Поэтому головы $0$ и $1$ в
примере соответствуют $H_1$ и $H_2$ в формулах.

Оси голов и токенов выполняют разные задачи. Каждая голова получает своё
распределение вероятностей по позициям токенов, а конкатенация объединяет
признаки голов в одной и той же позиции токена. Между головами вероятности не
нормализуются.

{/* chapter-section:history */}

## От одного рекуррентного выравнивания к параллельным проекциям внимания

В [статье о нейронном машинном переводе](https://arxiv.org/abs/1409.0473)
Бахданау и соавторы заменяют один фиксированный вектор исходного предложения
контекстным вектором, зависящим от шага декодирования, однако рекуррентный
декодер по-прежнему за один шаг строит одно распределение выравнивания и один
контекстный вектор, а не несколько параллельных спроецированных голов
самовнимания. Каждый контекст — это взвешенная сумма аннотаций энкодера, оценки
которых зависят от предыдущего состояния декодера. Это важный предшественник
обучаемого внимания, но ещё не самовнимание Transformer: в статье нет запросов,
ключей, значений, каузальной маски и нескольких параллельных голов.

Одна голова внимания на основе масштабированного скалярного произведения,
построенная в главе 27,
даёт каждой строке запроса одно пространство сравнения, одно распределение
вероятностей по позициям ключей и одну смесь значений. В статье
[*Attention Is All You Need*](https://arxiv.org/abs/1706.03762) Васвани и
соавторы несколько раз проецируют запросы, ключи и значения, параллельно
вычисляют внимание для этих проекций, конкатенируют выходы и проецируют
полученный результат. Авторы объясняют, что такая архитектура позволяет
одновременно учитывать информацию из разных подпространств представления и
разных позиций.

Однако это возможность, а не гарантия специализации. Головы получают общий
$X$, обучаются совместно и снова связываются матрицей $W_O$. В конкретной
обученной модели их поведение может повторяться, смешиваться или плохо
поддаваться интерпретации. Этот пример доказывает только то, что один допустимый
набор параметров создаёт две разные каузальные таблицы и сохраняет раздельность
их выходов до $W_O$.

[Статья о LLaMA](https://arxiv.org/abs/2302.13971) приводит конкретный современный
пример каузальной языковой модели. Туврон и соавторы описывают модель на основе
Transformer с несколькими головами внимания, RoPE в каждом слое и
оптимизированной реализацией каузального многоголового внимания. Эта реализация
не хранит веса внимания. В приведённом ниже плотном вычислении веса остаются
видимыми для изучения; оптимизированное ядро здесь не воспроизводится.

Исполняемое сравнение не пытается воспроизвести весь рекуррентный декодер. Оно
для одного зафиксированного распределения выравнивания вычисляет взвешенную
сумму аннотаций энкодера — контекст исходной последовательности, запускает существующую одноголовую
каузальную операцию на том же входе из трёх токенов и сравнивает её единственный
тензор вероятностей $[1,3,3]$ с тензором многоголового примера $[1,2,3,3]$.
В обоих случаях строки нормализованы, но второй тензор содержит две отдельно
нормализованные таблицы. Сравнение не приписывает статьям выбранную в примере
упаковку матриц, правило чётной ширины, отсутствие смещений, имена и значения:

<RustSource
  path="rust/demos/ch30-multi-head-attention/src/lib.rs"
  region="historical-multi-head-contrast"
  caption="Вычислить один взвешенный контекст исходной последовательности, одну полноразмерную каузальную таблицу и две таблицы отдельных голов"
  label="Исходный код на Rust для сравнения этапов развития внимания в главе 30"
/>

{/* chapter-section:rust-implementation */}

## Соберите на Rust полностью дифференцируемый и проверяемый путь вычислений

Открытая функция `split_heads` сначала меняет форму тензора, а затем переставляет
оси:

$$
[B,T,d_{\mathrm{model}}]
\to[B,T,h,d_h]
\to[B,h,T,d_h].
$$

`merge_heads` выполняет обратную перестановку осей и возвращает исходную форму.
Обе операции остаются на ленте автоматического дифференцирования; отдельного
алгоритма ручного копирования нет.

<RustSource
  path="rust/crates/llm-from-scratch/src/attention/multi_head.rs"
  region="head-layout"
  caption="Разделить тензор на головы и объединить их операциями изменения формы и перестановки осей на ленте"
  label="Исходный код на Rust для расположения осей голов в главе 30"
/>

`MultiHeadAttention::new` до изменения случайного состояния проверяет ненулевую
ширину модели, ненулевое число голов, точную делимость, чётную ширину головы,
ёмкость позиций и основание RoPE. Матрицы запросов, ключей, значений и выхода
инициализируются в устойчивом порядке через пробный генератор.
`MultiHeadAttention::from_parameters` создаёт тот же проверенный слой с точно
заданными матрицами для примера и будущей загрузки контрольных точек.

Вход проверяется в намеренно заданном порядке: ранг, последняя размерность,
непустая ось токенов, затем полный интервал абсолютных позиций. Границы позиций
проверяются до операций на ленте, поэтому недопустимый интервал сообщает одну и
ту же ошибку, даже если вход уже освобождён. Ошибки расположения осей, проекций,
RoPE и каузального внимания сохраняют название неудавшегося этапа.

<RustSource
  path="rust/crates/llm-from-scratch/src/attention/multi_head.rs"
  region="multi-head-errors"
  caption="Сохранить типизированные ошибки конфигурации, входа, позиций и вложенных этапов"
  label="Исходный код на Rust для граничных условий многоголового внимания в главе 30"
/>

Прямой проход выполняет проекции до полной ширины модели, разделяет все три
тензора на головы, поворачивает только $Q$ и $K$, объединяет оси батча и голов в
независимые ветви, повторно использует проверенную одноголовую каузальную
операцию, восстанавливает ось голов, объединяет признаки и применяет выходную
проекцию. Возвращаемые промежуточные значения показывают границы всех этапов, а
итоговый выход сохраняет форму $[B,T,d_{\mathrm{model}}]$.

<RustSource
  path="rust/crates/llm-from-scratch/src/attention/multi_head.rs"
  region="multi-head-layer"
  caption="Собрать полноразмерные проекции, RoPE и каузальное внимание по головам, объединение и выходную проекцию"
  label="Исходный код на Rust с реализацией многоголового слоя главы 30"
/>

Неоднородная начальная сопряжённая величина на выходе даёт конечные тензоры градиентов для входа и
всех четырёх матриц. Центральные разности проверяют все
$12+4\cdot16=76$ координат при

$$
\delta=10^{-6},\qquad \varepsilon_g=8\times10^{-6}.
$$

Тесты также подтверждают побитовую обратимость разделения и объединения,
равномерную таблицу одной головы и неравномерную таблицу другой, точные нули для
будущих ключей, отсутствие влияния голов друг на друга до выходной проекции,
побитовую неизменность префикса при изменении последнего токена, сохранение
вероятностей RoPE при общем сдвиге позиций неизменного содержимого,
дифференцирование пустого батча, границу с одним токеном, устойчивые имена и
идентичность параметров, типизированные ошибки конфигурации и формы, сохранённый
на ленте контекст, содержащий только конечные значения, и побитовое повторение результата.

Пример собирает эти значения и проверки в один воспроизводимый отчёт:

<RustSource
  path="rust/demos/ch30-multi-head-attention/src/lib.rs"
  region="learner-report"
  caption="Собрать точные значения примера, инварианты, градиенты, граничные случаи и переход к следующей главе"
  label="Исходный код на Rust, формирующий вывод примера главы 30"
/>

Исполняемый файл печатает этот отчёт:

<RustSource
  path="rust/demos/ch30-multi-head-attention/src/main.rs"
  caption="Запустить полный пример многоголового внимания"
  label="Точка входа программы на Rust для примера главы 30"
/>

Выполните `cargo run --quiet --locked -p ch30-multi-head-attention`. Стандартный
вывод побайтно совпадёт с `rust/demos/ch30-multi-head-attention/expected.txt`,
включая завершающий перевод строки.

{/* chapter-section:visualization */}

## Проследите, где отдельные головы объединяются в строку ширины модели

Схема использует значения из того же примера: строки до и после поворота, все
шесть каузальных строк вероятностей, каждый выход головы, все три строки после
конкатенации, четыре строки выходной матрицы, три итоговые строки и проверку
префикса.

<RustSource
  path="rust/demos/ch30-multi-head-attention/src/diagram_trace.rs"
  region="multi-head-trace"
  caption="Получить значения многоголового внимания, используемые в схеме"
  label="Исходный код на Rust для значений схемы главы 30"
/>

<MultiHeadAttentionDiagram labels={diagramLabels} />

Сплошные и штриховые рамки различают две головы. Сплошные и штриховые
подчёркивания отделяют видимые ключи от закрытых каузальной маской, а двойная
рамка отмечает разрешённую диагональ. Две таблицы вероятностей нормализуются
независимо, а проверки форм и префикса показывают, что их выходы остаются
раздельными до выходной проекции.

Внимательно рассмотрите позицию токена $1$. До $W_O$ конкатенированная строка
равна

$$
[0.770151,-0.420735,0.213809,0.786191].
$$

Выбранная выходная матрица меняет местами две группы по две координаты и даёт

$$
[0.213809,0.786191,0.770151,-0.420735].
$$

Это видимое изменение объясняет, почему конкатенацию и выходную проекцию нужно
считать двумя разными этапами.

{/* chapter-section:exercises */}

## Проверяйте формы, видимость и смешивание, а не приписывайте головам роли

1. Для $d_{\mathrm{model}}=6$ классифицируйте $h=1$, $h=2$, $h=3$, $h=4$ и
   $h=6$ по правилу этой главы: делимость плюс чётная ширина головы для RoPE.
2. Проследите все формы при $B=2$, $T=5$, $d_{\mathrm{model}}=12$ и $h=3$.
3. Заполните треугольник каузальной видимости из трёх токенов для обеих голов.
4. Объясните, почему одно полноразмерное вычисление внимания не равносильно двум
   вычислениям внутри отдельных голов.
5. Примените меняющую группы местами матрицу $W_O$ из примера к символическим
   строкам $H^{(0)}=[a,b]$ и $H^{(1)}=[c,d]$. Затем возьмите единичную $W_O$,
   дополнительно задайте $(W_O)_{0,2}=1$ и выведите новую координату выхода
   $y_2$.
6. Измените только токен в позиции $2$. Предскажите, какие строки выхода могут
   измениться, а какие обязаны остаться побитово прежними.
7. Объясните, где заканчивается разделение групп в примере и почему общая
   плотная проекция не изолирует фиксированные части исходного входа.
8. Вычислите число параметров целевого слоя без смещений при
   $d_{\mathrm{model}}=128$.

<details>
  <summary>Проверьте структурные ответы</summary>

  1. При $h=1$ ширина $d_h=6$ чётная; при $h=3$ ширина $d_h=2$ тоже чётная.
     Оба варианта допустимы. При $h=2$ получаем нечётную $d_h=3$, а при $h=6$
     — нечётную $d_h=1$; оба нарушают правило RoPE для полной головы. $h=4$ не
     делит $6$.
  2. Формы $Q$, $K$ и $V$ равны $[2,5,12]$; после разделения — $[2,3,5,4]$;
     вероятности каждой головы имеют форму $[2,5,5]$, а вместе — $[2,3,5,5]$;
     выходы голов равны $[2,3,5,4]$; после объединения и на выходе —
     $[2,5,12]$.
  3. Каждая голова сохраняет один, затем два, затем три ключа. Треугольники
     видимости совпадают, хотя вероятности в разрешённых ячейках могут различаться.
  4. Одно полноразмерное вычисление создаёт одну нормализованную таблицу оценок
     $[5,5]$, общую для всех признаков значений. Две головы создают две отдельно
     нормализованные таблицы $[5,5]$, каждая со своим знаменателем для каждой
     строки запроса; при некоторых параметрах их численные значения могут
     совпасть.
  5. Конкатенация даёт $[a,b,c,d]$, а зафиксированная выходная матрица —
     $[c,d,a,b]$. Если начать с единичной матрицы и добавить
     $(W_O)_{0,2}=1$, третья выходная координата станет $y_2=a+c$ и получит
     вклады обеих голов.
  6. Строки $0$ и $1$ не видят токен $2$, поэтому останутся побитово прежними.
     Строка $2$ может измениться в обеих головах и после $W_O$.
  7. Единичные матрицы запросов, ключей и значений делают группы примера
     видимыми. Столбцы общих упакованных проекций могут использовать все входные
     признаки; отдельные ветви получают лишь их спроецированные выходы. $W_O$
     снова может смешать эти ветви.
  8. Число параметров равно $4\cdot128^2=65{,}536$.
</details>

Заблуждение: многоголовое внимание разбивает входной тензор — или уже готовый
результат одной головы — на части.

Исправление: обучаемые проекции выполняются до разделения. Затем каждая голова
по своей группе спроецированных признаков отдельно вычисляет оценки запросов и ключей после
RoPE, каузальный softmax и смесь значений. Конкатенируются только готовые выходы
голов, после чего $W_O$ может смешивать их. Различие двух таблиц в примере
показывает один допустимый набор параметров, но не гарантирует устойчивой
семантической роли каждой головы в любой обученной модели.

{/* chapter-section:decoder-connection */}

## Поместите преобразование в остаточную ветвь с предварительной нормализацией

Теперь в собираемом декодере есть полное каузальное преобразование
внимания с учётом позиций. Оно принимает и возвращает
$[B,T,d_{\mathrm{model}}]$, но пока не нормализует вход и не прибавляет результат
к остаточному потоку.

В главе 31 оно войдёт в первую остаточную ветвь с предварительной нормализацией,

$$
x'=x+\operatorname{MHA}(\operatorname{RMSNorm}(x)),
$$

после которой появится остаточная ветвь сети прямого распространения. Владение
кэшем ключей и значений и инкрементальное декодирование остаются отложенными до
главы 37.
