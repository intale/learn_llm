---
{
  "chapter_id": "29-rope",
  "locale": "ru",
  "content_revision": 3,
  "order": 29,
  "concept_id": "rotary-position-embedding",
  "title": "Поворачивайте пары координат запросов и ключей с помощью RoPE",
  "description": "Разберитесь, как ротационное позиционное кодирование поворачивает пары координат запросов и ключей в зависимости от абсолютной позиции, чтобы в скалярных произведениях внимания учитывалось относительное положение, и реализуйте его на Rust.",
  "objective": "Поверните пары координат запросов и ключей в зависимости от абсолютной позиции и проследите, как в их скалярных произведениях возникает знаковая разность позиций, не меняя каузальную границу видимости.",
  "worked_inputs": "Сначала предскажите результат для одной пары при $q=[1,0]$, $k=[0,1]$ и $\\theta_0=1$: сравните $(m,n)=(0,0)$, $(1,0)$ и одинаковый сдвиг $(3,2)$. Затем изучите пример на Rust, в котором строка $[1,0,1,0]$ повторяется в позициях $0,1,2$ при $b=100$.",
  "formula": {
    "latex": "\\left(\\operatorname{RoPE}(x_m)\\right)_{2k:2k+2}=R(m\\theta_k)(x_m)_{2k:2k+2}",
    "symbols": [
      { "symbol": "\\operatorname{RoPE}", "meaning": "попарное ротационное позиционное преобразование, независимо применяемое к запросу или ключу" },
      { "symbol": "x_m", "meaning": "вектор признаков одного запроса или ключа, которому назначена абсолютная позиция m" },
      { "symbol": "m", "meaning": "абсолютная позиция запроса или входного вектора с учётом заданного вызывающим кодом смещения последовательности" },
      { "symbol": "n", "meaning": "абсолютная позиция ключа" },
      { "symbol": "k", "meaning": "индекс пары координат и её частоты от нуля до d/2 минус один" },
      { "symbol": "2k:2k+2", "meaning": "полуинтервал, выбирающий соседние координаты 2k и 2k+1" },
      { "symbol": "R", "meaning": "двумерная матрица поворота против часовой стрелки" },
      { "symbol": "\\phi", "meaning": "произвольный угол поворота в радианах" },
      { "symbol": "\\theta_k", "meaning": "приращение угла в радианах на одну позицию для пары k" },
      { "symbol": "b", "meaning": "положительное конечное основание частот; в учебном примере оно равно 100" },
      { "symbol": "d", "meaning": "ненулевая чётная ширина признаков запроса или ключа" },
      { "symbol": "q_m^{(k)}", "meaning": "k-я двухкоординатная пара запроса в позиции m до поворота" },
      { "symbol": "k_n^{(k)}", "meaning": "k-я двухкоординатная пара ключа в позиции n до поворота" },
      { "symbol": "n-m", "meaning": "знаковая разность позиции ключа и позиции запроса, а не расстояние без знака" },
      { "symbol": "I", "meaning": "двумерная единичная матрица" },
      { "symbol": "y", "meaning": "повёрнутый выход, соответствующий входу x" },
      { "symbol": "\\bar{x}", "meaning": "производная скалярной функции потерь L по входу x" },
      { "symbol": "\\bar{y}", "meaning": "производная скалярной функции потерь L по повёрнутому выходу y" }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "neural-architecture",
      "limitation": "В рекуррентных нейронных языковых моделях порядок переносился через состояние, обновляемое по одному токену; самовнимание без маски и позиционного сигнала вместо этого эквивариантно относительно согласованной перестановки строк содержимого.",
      "later_advance": "В отличие от аддитивного кодирования на входе исходного Transformer, повороты запросов и ключей в RoFormer добавляют в скалярное произведение член, зависящий от знаковой разности позиций.",
      "modern_llm_role": "В исходной LLaMA RoPE меняет геометрию оценок запрос–ключ в каждом слое Transformer, а каузальная маска отдельно управляет видимостью.",
      "sources": [
        { "role": "earlier", "year": 2017, "name": "Vaswani et al., Attention Is All You Need", "source_url": "https://arxiv.org/abs/1706.03762", "claim": "Vaswani и соавторы отказываются от рекуррентности и свёрток, добавляют позиционные кодирования к входным эмбеддингам и сравнивают фиксированное синусоидальное представление позиций с обучаемым." },
        { "role": "later", "year": 2021, "name": "Su et al., RoFormer: Enhanced Transformer with Rotary Position Embedding", "source_url": "https://arxiv.org/abs/2104.09864", "claim": "Su и соавторы кодируют абсолютную позицию поворотами подпространств запросов и ключей и выводят скалярное произведение самовнимания, позиционный член которого использует разность их позиций." },
        { "role": "later", "year": 2023, "name": "Touvron et al., LLaMA: Open and Efficient Foundation Language Models", "source_url": "https://arxiv.org/abs/2302.13971", "claim": "Touvron и соавторы указывают, что в исходной LLaMA абсолютные позиционные эмбеддинги удалены, а RoPE применяется в каждом слое Transformer." }
      ]
    },
    "approach": "От порядка в рекуррентном состоянии через добавляемые к эмбеддингам векторы позиций к мультипликативным поворотам запросов и ключей",
    "summary": "До наложения маски у самовнимания, зависящего только от содержимого, нет геометрии относительных позиций. Абсолютные углы RoPE добавляют эту геометрию, а тождество поворотов делает скалярное произведение фиксированных запроса и ключа зависимым от знаковой разности n-m, их содержимого и частот пар.",
    "rust_source": "rust/demos/ch29-rope/src/lib.rs"
  },
  "rust_sources": [
    { "path": "rust/demos/ch29-rope/src/lib.rs", "region": "historical-position-contrast", "purpose": "Показать, как синусоидальный метод исходного Transformer добавляет вектор позиции к эмбеддингу, в отличие от более поздних поворотов запросов и ключей." },
    { "path": "rust/crates/llm-from-scratch/src/attention/rope.rs", "region": "rope-tables", "purpose": "Проверить размерности и все вычисляемые значения таблиц, а затем ровно один раз выделить память и сохранить обратные частоты, синусы и косинусы при создании RoPE." },
    { "path": "rust/crates/llm-from-scratch/src/attention/rope.rs", "region": "rope-rotation", "purpose": "Проверить последнюю ось признаков и допустимый интервал абсолютных позиций перед поворотом предпоследней оси токенов." },
    { "path": "rust/crates/llm-from-scratch/src/autograd/model_ops.rs", "region": "rotary-pairs-forward", "purpose": "Повернуть соседние пары за линейное время и повторно использовать транспонированный поворот в локальном правиле обратного прохода." },
    { "path": "rust/demos/ch29-rope/src/main.rs", "purpose": "Запустить полный исполняемый пример главы 29." }
  ],
  "visualization": {
    "decision": "useful",
    "id": "rotary-position-pairs",
    "rationale": "Строки позиций позволяют сравнить две скорости поворота, а повторяющееся фиксированное содержимое показывает одинаковые знаковые разности на диагоналях матрицы скалярных произведений и позволяет проверить локальное свойство на двух диапазонах позиций с одинаковым сдвигом."
  },
  "decoder_connection": "Накопленный одноголовый путь теперь может поворачивать строки запросов и ключей до вычисления каузальных оценок. В главе 30 проекции запросов и ключей сначала преобразуются в отдельные головы, затем RoPE применяется вдоль последней оси признаков каждой головы, внимание вычисляется независимо по головам, их выходы объединяются и проецируются."
}
---

import RustSource from '../../../components/RustSource.astro';
import RopeDiagram from '../../../components/chapters/RopeDiagram.astro';

export const diagramLabels = {
  title: 'Проследите, как абсолютные повороты выявляют взаимное положение запроса и ключа',
  description:
    'Сравните частоты двух пар, их повороты, матрицу скалярных произведений запросов и ключей, проверку одинакового сдвига, величины обратного прохода, допустимые формы, недопустимые граничные случаи и путь к современным декодерным LLM.',
  sections: {
    rotations: 'Проследите две пары координат в трёх абсолютных позициях',
    dots: 'Найдите знаковые разности позиций в матрице запрос–ключ',
    evidence: 'Проверьте нормы, формы, границы и обратный проход',
    history: 'Проследите развитие позиционной информации в декодерных LLM',
  },
  fields: {
    position: 'Абсолютная позиция',
    pair: 'Пара координат',
    featureCoordinates: 'Координаты признаков',
    frequency: 'Радиан на позицию',
    angle: 'Абсолютный угол',
    beforeRotation: 'До поворота',
    afterRotation: 'После поворота',
    queryPosition: 'Позиция запроса',
    keyPosition: 'Позиция ключа',
    relativeOffset: 'Положение ключа относительно запроса',
    dot: 'Скалярное произведение после поворота',
    originalPositions: 'Исходные абсолютные позиции',
    shiftedPositions: 'Позиции после одинакового сдвига',
    originalGrid: 'Матрица скалярных произведений для позиций от нуля до двух',
    shiftedGrid: 'Матрица скалярных произведений для позиций от трёх до пяти',
    norm: 'Сохранение нормы',
    shape: 'Сохранение форм тензоров',
    errors: 'Недопустимые граничные случаи',
    tensor: 'Входной тензор',
    outputAdjoint: 'Выходная сопряжённая величина',
    inputAdjoint: 'Сопряжённая величина входа',
    reverseMode: 'Транспонированный поворот в обратном режиме',
    commonShiftCheck: 'Проверка одинакового сдвига при фиксированном содержимом',
    gradientCheck: 'Проверка градиента по всем координатам',
    earlier: 'Порядок в рекуррентном состоянии',
    transformer: 'Векторы позиций в эмбеддингах',
    rotary: 'Повороты пар координат запросов и ключей',
    modern: 'RoPE во влиятельной декодерной LLM',
    causalBoundary: 'Видимость задаётся отдельно',
  },
  cues: {
    fastPair: 'Быстрая пара: сплошная рамка',
    slowPair: 'Медленная пара: штриховая рамка',
    zeroOffset: 'Одинаковые позиции: двойная рамка',
    positiveOffset: 'Ключ расположен позже: сплошная рамка',
    negativeOffset: 'Ключ расположен раньше: штриховая рамка',
    verified: 'Проверено',
    rejected: 'Отклонено',
  },
  captions: {
    legend: 'Обозначения частот пар и знаковых разностей позиций',
    rotations: 'Первая пара поворачивается на один радиан за позицию, вторая — на одну десятую радиана.',
    dots: 'Поскольку содержимое во всех позициях повторяется, значения с одинаковой знаковой разностью позиций повторяются по диагоналям.',
    evidence: 'Ортогональные повороты сохраняют нормы и формы, а транспонированная матрица переносит выходные сопряжённые величины ко входам.',
    history: 'Позиционная информация перешла от последовательного состояния к явным векторам, а затем — в геометрию оценок запрос–ключ.',
  },
  scrollers: {
    rotations: 'Прокручиваемая таблица поворотов пар координат',
    dots: 'Прокручиваемая таблица скалярных произведений по относительным позициям',
    originalShift: 'Прокручиваемая матрица скалярных произведений для исходных позиций от нуля до двух',
    shiftedShift: 'Прокручиваемая матрица скалярных произведений для сдвинутых позиций от трёх до пяти',
    gradients: 'Прокручиваемые величины обратного прохода через RoPE',
    history: 'Прокручиваемая история позиционной информации в LLM',
  },
  shapeCases: {
    rank3: 'Оси пакета, токенов и признаков',
    rank4: 'Оси пакета, голов, токенов и признаков',
    emptyLeading: 'Пустая ведущая ось',
    emptyTokens: 'Пустая ось токенов у границы таблицы',
  },
  historyDetails: {
    earlier: 'Рекуррентное состояние обновлялось для каждого токена и переносило порядок через последовательные вычисления.',
    transformer: 'В исходном Transformer к эмбеддингам токенов добавлялись явные векторы позиций.',
    rotary: 'RoPE поворачивает пары координат запросов и ключей по абсолютной позиции, поэтому их скалярное произведение зависит от знаковой разности позиций.',
    modern: 'В исходной LLaMA RoPE применяется в каждом слое Transformer.',
    causalBoundary: 'RoPE меняет геометрию оценок. Каузальная маска по-прежнему решает, какие ключи доступны запросу.',
  },
  errorCases: {
    oddWidth: 'Нечётная ширина последней оси признаков',
    inputRank: 'Ранг входа меньше двух',
    widthMismatch: 'Настроенная ширина не совпадает с шириной входа',
    positionRange: 'Интервал позиций выходит за таблицу',
    offsetOverflow: 'Переполнение интервала позиций',
    releasedInput: 'Освобождённый операнд автоматического дифференцирования',
  },
};

{/* chapter-section:worked-example */}

## Сначала предскажите поворот одной пары

Возьмём одну двухкоординатную пару запроса и ключа с первой частотой:

$$
q=[1,0],\qquad k=[0,1],\qquad \theta_0=1.
$$

При одинаковых позициях $m=n=0$ ни один вектор не поворачивается, поэтому

$$
q^\top k=0.
$$

Теперь поместим запрос в позицию $m=1$, а ключ оставим в позиции $n=0$:

$$
R(1)q\approx[0.540302,0.841471],\qquad R(0)k=[0,1].
$$

Их скалярное произведение приблизительно равно $0.841471$. Сдвинем обе
абсолютные позиции на $2$: для $(m,n)=(3,2)$ по-прежнему выполняется
$n-m=-1$, поэтому при тех же векторах скалярное произведение останется
приблизительно равным $0.841471$. Обе повёрнутые пары изменятся, но их
относительный поворот — нет.

Исполняемый пример расширяет этот расчёт до $d=4$. Строка $[1,0,1,0]$
повторяется в позициях $0$, $1$ и $2$, а при $b=100$ получаются частоты

$$
[\theta_0,\theta_1]=[1,0.1].
$$

До запуска предскажите, какая пара поворачивается быстрее, что произойдёт в
позиции $0$ и какие ячейки совпадут после сдвига всех позиций запросов и ключей
на $3$ при неизменном содержимом.

{/* chapter-section:formula */}

## Поворачивайте соседние координаты по абсолютной позиции

Для каждой соседней пары координат

$$
\left(\operatorname{RoPE}(x_m)\right)_{2k:2k+2}=R(m\theta_k)(x_m)_{2k:2k+2}
$$

Матрица поворота и расписание частот имеют вид

$$
R(\phi)=
\begin{bmatrix}
\cos\phi&-\sin\phi\\
\sin\phi&\cos\phi
\end{bmatrix},
\qquad
\theta_k=b^{-2k/d}.
$$

В этой реализации образуют пары координаты $2k$ и $2k+1$. В других
реализациях пары могут располагаться иначе. Здесь $d$ должно быть ненулевым и
чётным, чтобы у каждой координаты был партнёр.

В позиции $m=0$ угол любой пары равен нулю, а каждый поворот ортогонален:

$$
R(0)=I,
\qquad
R(\phi)^\top R(\phi)=I,
\qquad
\lVert R(\phi)x\rVert_2=\lVert x\rVert_2.
$$

Когда запрос в позиции $m$ сопоставляется с ключом в позиции $n$,

$$
\bigl(R(m\theta_k)q_m^{(k)}\bigr)^\top
\bigl(R(n\theta_k)k_n^{(k)}\bigr)
=(q_m^{(k)})^\top R((n-m)\theta_k)k_n^{(k)}.
$$

Это следует из тождества

$$
R(\alpha)^\top R(\beta)=R(\beta-\alpha).
$$

На вход поворотов поступают абсолютные позиции. Знаковая разность $n-m$
возникает лишь при объединении двух поворотов в скалярном произведении. Оценка
по-прежнему зависит от содержимого запроса и ключа и от всех $\theta_k$, а не
только от разности позиций.

Пусть $L$ — скалярная функция потерь, $\bar{y}=\partial L/\partial y$ —
выходная сопряжённая величина, а $\bar{x}=\partial L/\partial x$ — сопряжённая
величина входа. В обратном режиме применяется транспонированный поворот:

$$
\begin{bmatrix}\bar{x}_{2k}\\\bar{x}_{2k+1}\end{bmatrix}
=R(m\theta_k)^\top
\begin{bmatrix}\bar{y}_{2k}\\\bar{y}_{2k+1}\end{bmatrix}.
$$

{/* chapter-section:symbol-glossary */}

## Разделяйте роли позиции, пары и видимости

- $q_m^{(k)}$ и $k_n^{(k)}$ — двухкоординатные пары запроса и ключа в
  абсолютных позициях $m$ и $n$ до поворота.
- $k$ выбирает соседнюю пару координат и её частоту $\theta_k$.
- $\phi$ — произвольный угол поворота, а $I$ — единичная матрица.
- $n-m$ — знаковая разность позиции ключа и позиции запроса, а не расстояние без
  знака.
- Черта обозначает производную $L$: $\bar{y}$ приходит от последующих
  операций, а $\bar{x}$ передаётся предыдущим.

До наложения маски самовнимание без позиционного сигнала эквивариантно
относительно согласованной перестановки строк содержимого: если вместе
переставить запросы, ключи и значения, выходы переставятся так же. RoPE
добавляет в оценки запрос–ключ геометрию относительных позиций, но не управляет
видимостью. Каузальная маска из главы 28 по-прежнему закрывает будущие ключи, а
значения $V$ здесь не поворачиваются.

{/* chapter-section:history */}

## От порядка в рекуррентном состоянии к геометрии RoPE

В рекуррентных нейронных языковых моделях порядок переносился через состояние,
обновляемое по одному токену; самовнимание без маски и позиционного сигнала
вместо этого эквивариантно относительно согласованной перестановки строк
содержимого.

[Vaswani и соавторы описывают исходный Transformer](https://arxiv.org/abs/1706.03762).
Vaswani и соавторы отказываются от рекуррентности и свёрток, добавляют
позиционные кодирования к входным эмбеддингам и сравнивают фиксированное
синусоидальное представление позиций с обучаемым. Синусоидальный вариант был
мотивирован гипотезой о возможности обучить модель учитывать фиксированные
смещения, но это не гарантирует перенос на любую более длинную
последовательность.

Аддитивный подход изменяет эмбеддинг ещё до получения запроса, ключа или
значения. Небольшая функция на Rust показывает эту операцию непосредственно:

<RustSource
  path="rust/demos/ch29-rope/src/lib.rs"
  region="historical-position-contrast"
  caption="Добавьте синусоидальный вектор позиции к одному эмбеддингу"
  label="Исходный код на Rust, сопоставляющий аддитивное синусоидальное позиционное кодирование с RoPE"
/>

[Su и соавторы представляют RoFormer](https://arxiv.org/abs/2104.09864). Su и
соавторы кодируют абсолютную позицию поворотами подпространств запросов и ключей
и выводят скалярное произведение самовнимания, позиционный член которого
использует разность их позиций. В отличие от аддитивного кодирования на входе
исходного Transformer, повороты запросов и ключей в RoFormer добавляют в
скалярное произведение член, зависящий от знаковой разности позиций.

[Touvron и соавторы описывают исходную LLaMA](https://arxiv.org/abs/2302.13971).
Touvron и соавторы указывают, что в исходной LLaMA абсолютные позиционные
эмбеддинги удалены, а RoPE применяется в каждом слое Transformer. В исходной
LLaMA RoPE меняет геометрию оценок запрос–ключ в каждом слое Transformer, а
каузальная маска отдельно управляет видимостью. Из этого не следует, что RoPE
используют все LLM, что оно гарантирует перенос на любую длину или что целый
декодер инвариантен к сдвигу входа.

{/* chapter-section:rust-implementation */}

## Вычислите таблицы один раз и поворачивайте каждую строку

`RotaryEmbedding::new` проверяет ненулевую чётную ширину, ненулевую ёмкость по
позициям и положительное конечное основание. Кроме того, проверяется, что число
ячеек «позиция–пара» помещается в `usize`, длина каждого массива допустима для
`Vec<f64>`, а каждая обратная частота конечна и неотрицательна. Для одной пары
при умножении такой частоты на возрастающие неотрицательные позиции угол не
может уменьшиться. Синус и косинус конечного угла также конечны. Поэтому можно
доказать конечность всех ячеек таблицы, проверив для каждой пары только последнюю
позицию. Если последний угол не конечен, двоичный поиск находит первую позицию с
ошибкой, а наименьшая из найденных пар «позиция–пара» сохраняет порядок ошибок
при построчном обходе.

Объём этой проверки растёт с числом пар координат, а для пары с ошибкой — ещё и
с логарифмом ёмкости по позициям, но не с числом ячеек таблицы. Память под
таблицы при этом не выделяется, и проверка не выясняет, сможет ли распределитель
памяти удовлетворить допустимый по размеру запрос: об этой ошибке может сообщить
только конструктор. Затем конструктор выделяет память под три массива, обходит
все ячейки построчно и сохраняет одну обратную частоту для каждой пары и по одной
строке синусов и косинусов для каждой абсолютной позиции. Таким образом,
декодер может проверить логическую таблицу RoPE, заданную шириной головы и
максимальным числом позиций, не создавая временную таблицу. Сами таблицы
по-прежнему хранятся только в создавшем их компоненте внимания.

<RustSource
  path="rust/crates/llm-from-scratch/src/attention/rope.rs"
  region="rope-tables"
  caption="Докажите конечность всех значений таблиц RoPE и создайте сами таблицы только в конструкторе"
  label="Исходный код на Rust, создающий таблицы обратных частот, синусов и косинусов для RoPE"
/>

`RotaryEmbedding::rotate` считает предпоследнюю ось осью токенов, а последнюю
— осью признаков. Поддерживаются формы $[T,d]$, $[B,T,d]$, $[B,H,T,d]$ и
независимые ведущие оси. Для заданного смещения $o$ проверяется интервал

$$
[o,o+T).
$$

Пустой интервал токенов может начинаться на границе ёмкости таблицы, а непустой
не может выходить за неё.

<RustSource
  path="rust/crates/llm-from-scratch/src/attention/rope.rs"
  region="rope-rotation"
  caption="Проверьте формы и выберите интервал абсолютных позиций"
  label="Исходный код на Rust, применяющий таблицу RoPE к тензору"
/>

Дифференцируемая операция посещает каждую соседнюю пару один раз и не строит
плотную матрицу размера $d\times d$. В обратной ветви для произведения вектора
на якобиан применяется транспонированный поворот:

<RustSource
  path="rust/crates/llm-from-scratch/src/autograd/model_ops.rs"
  region="rotary-pairs-forward"
  caption="Поверните соседние пары и выполните обратный проход транспонированным поворотом"
  label="Исходный код на Rust, реализующий дифференцируемый поворот пар"
/>

Одна и та же операция независимо вызывается для $Q$ и $K$. Центральные
разности проверяют все $12$ координат каждого входа при

$$
h=10^{-6},\qquad \varepsilon_g=4\times10^{-6}.
$$

Пример также проверяет знаки, нормы, все ячейки матрицы скалярных произведений,
одинаковый сдвиг позиций, формы от второго до четвёртого ранга, пустые оси,
недопустимые конфигурации, выход за диапазон позиций, конечность сохранённых
значений и воспроизводимость результата. Выполните
`cargo run --quiet --locked -p ch29-rope`, чтобы изучить все значения.
Исполняемый пример строит и печатает этот детерминированный отчёт:

<RustSource
  path="rust/demos/ch29-rope/src/main.rs"
  caption="Запустите полный пример RoPE"
  label="Точка входа на Rust для примера RoPE из главы 29"
/>

{/* chapter-section:visualization */}

## Сопоставьте повороты пар с диагоналями матрицы

<RopeDiagram labels={diagramLabels} />

До одинакового сдвига получается матрица

$$
\begin{bmatrix}
2&1.535306&0.563920\\
1.535306&2&1.535306\\
0.563920&1.535306&2
\end{bmatrix}.
$$

В этом примере содержимое запросов и ключей повторяется во всех позициях,
поэтому одинаковые знаковые разности позиций дают одинаковые значения на
диагоналях. Перенос неизменного содержимого из абсолютных позиций $0,1,2$ в
$3,4,5$ меняет повёрнутые координаты, но сохраняет все девять скалярных
произведений с точностью $10^{-12}$. Численное сравнение проверяет один пример;
общее свойство следует из приведённого выше тождества матриц поворота.

{/* chapter-section:exercises */}

## Сначала предскажите, затем проверьте свойство

1. Докажите, что позиция $m=0$ задаёт единичный поворот для каждой пары.
2. Вычислите результат для одной пары при $(m,n)=(1,0)$.
3. Предскажите изменения после одинакового сдвига обеих позиций при
   фиксированном содержимом запроса и ключа.
4. Объясните, почему сдвиг только $m$ обычно меняет скалярное произведение.
5. Выведите сохранение нормы из $R(\phi)^\top R(\phi)=I$.
6. Решите, нужно ли поворачивать $V$ или каузальную маску.
7. Объясните, почему эта реализация с полным попарным поворотом отклоняет $d=3$.
8. Разграничьте абсолютные входные позиции и знаковую разность в скалярном
   произведении.
9. Объясните, почему локальное свойство при фиксированном содержимом не
   доказывает инвариантность целого декодера к сдвигу.

<details>
  <summary>Проверьте ключевые различия</summary>

  1. При $m=0$ угол каждой пары равен $0$, поэтому $R(0)=I$.
  2. При $(m,n)=(1,0)$ скалярное произведение приблизительно равно $0.841471$.
  3. При добавлении $c$ к обеим позициям сохраняется
     $(n+c)-(m+c)=n-m$, поэтому для неизменного содержимого скалярное
     произведение не меняется, хотя оба абсолютных поворота становятся другими.
  4. Сдвиг только $m$ меняет $n-m$ и потому обычно меняет результат; особое
     содержимое или периодичность углов всё же могут дать прежнее значение.
  5. Тождество $R(\phi)^\top R(\phi)=I$ сохраняет квадрат нормы, а значит и
     саму норму.
  6. Здесь поворачиваются $Q$ и $K$, но не $V$; каузальная маска независимо
     определяет видимые ключи.
  7. При ширине $3$ у одной координаты не остаётся пары.
  8. Абсолютные индексы задают локальные углы, а знаковая разность появляется
     лишь после объединения поворотов запроса и ключа.
  9. Для утверждения о целой модели пришлось бы проверить каждый компонент и
     каждую границу, сохранив те же содержимое и контекст. Одного локального
     тождества недостаточно.
</details>

Заблуждение: RoPE получает на вход относительные индексы.

Исправление: на вход поступают абсолютные позиции. Благодаря алгебре поворотов
скалярное произведение фиксированных запроса и ключа зависит от знаковой
разности их позиций. RoPE не заменяет каузальную маску, а оценка по-прежнему
зависит от содержимого и частоты.

{/* chapter-section:decoder-connection */}

## Передайте ось с позиционной информацией нескольким головам

В текущем одноголовом пути каждая строка запроса и ключа поворачивается перед
вычислением каузальных оценок из главы 28. В главе 30 проекции запросов и ключей
сначала преобразуются в отдельные головы, затем RoPE применяется вдоль
последней оси признаков каждой головы, каузальное внимание вычисляется
независимо по головам, их выходы объединяются и к результату применяется
обучаемая выходная проекция $W_O$.

Частичный поворот признаков, другие схемы образования пар, кэши ключей и
значений, предварительная обработка контекста и масштабирование частот для
длинного контекста остаются за пределами главы.
