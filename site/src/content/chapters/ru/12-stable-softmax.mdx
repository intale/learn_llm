---
{
  "chapter_id": "12-stable-softmax",
  "locale": "ru",
  "content_revision": 5,
  "order": 12,
  "concept_id": "stable-softmax",
  "title": "Преобразуйте экстремальные логиты в устойчивые вероятности",
  "description": "Преобразуйте логиты словаря и внимания в устойчивые вероятности, логарифмы вероятностей, значения log-sum-exp и среднее NLL по целевым индексам с помощью Rust без сторонних зависимостей.",
  "objective": "Преобразовывать логиты в нормированные вероятности и их логарифмы, а также оценивать целевые классы по индексам, избегая устранимого переполнения и округления слишком малых экспонент до нуля.",
  "worked_inputs": "Нормируйте по оси 1 логиты формы [3,2] со строками [0,1], [1000,1001] и [-1001,-1000]. Предскажите, что после вычитания максимума каждая строка станет [-1,0], поэтому вероятности во всех трёх строках будут равны [0.268941421370,0.731058578630]. Затем вычислите потери для целевых классов [1,0,1].",
  "formula": {
    "latex": "p_i=\\frac{\\exp(\\ell_i-m)}{\\sum_j\\exp(\\ell_j-m)}, \\quad m=\\max_j\\ell_j",
    "symbols": [
      {
        "symbol": "p_i",
        "meaning": "нормированная вероятность, присвоенная классу i"
      },
      {
        "symbol": "\\ell_i",
        "meaning": "конечный входной логит класса i"
      },
      {
        "symbol": "m",
        "meaning": "наибольший логит в выбранной группе нормализации"
      },
      {
        "symbol": "i",
        "meaning": "класс, вероятность которого вычисляется"
      },
      {
        "symbol": "j",
        "meaning": "индекс, перебирающий все классы в группе нормализации"
      }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "language-model",
      "limitation": "Нейронная языковая модель Бенжио и соавторов преобразует оценки словаря в положительные вероятности следующего слова с помощью выходного softmax; сумма вероятностей равна единице. При вычислениях с конечной точностью прямое возведение экспоненты от больших несдвинутых логитов может привести к переполнению, а от достаточно отрицательных — округлить все слагаемые до нуля.",
      "later_advance": "В Transformer softmax применяется и к масштабированным оценкам «запрос — ключ» внутри внимания, и при предсказании следующего токена. В опубликованном исходном коде GPT-2 показано устойчивое вычисление для внимания: перед возведением в экспоненту из оценок по последней оси вычитают максимум, затем складывают сдвинутые экспоненты и нормируют их до взвешивания значений.",
      "modern_llm_role": "В точной арифметике добавление одной и той же константы ко всем логитам не меняет softmax. Вычитание максимума сохраняет это распределение и устраняет сбои прямого вычисления экспонент для рассматриваемых строк; log-sum-exp, log-softmax и объединённое среднее NLL по индексам целевых классов сохраняют сведения для обучения в логарифмической шкале, даже когда обычная вероятность округляется до нуля. Поддержка произвольной оси, требование конечных входов, расположение целей, правила выделения памяти и порядок ошибок — локальные решения этой реализации.",
      "sources": [
        {
          "role": "earlier",
          "year": 2003,
          "name": "Bengio et al., A Neural Probabilistic Language Model",
          "source_url": "https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf",
          "claim": "Бенжио и соавторы описывают выходной softmax с положительными значениями, сумма которых равна единице, а его входы трактуют как ненормированные логарифмы вероятностей следующего слова."
        },
        {
          "role": "later",
          "year": 2017,
          "name": "Vaswani et al., Attention Is All You Need",
          "source_url": "https://papers.nips.cc/paper_files/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf",
          "claim": "Васвани и соавторы определяют внимание на основе масштабированного скалярного произведения: к масштабированным произведениям запросов и ключей применяют softmax, после чего полученными весами взвешивают значения. Для получения вероятностей следующего токена к выходам декодера применяют обучаемое линейное преобразование и softmax."
        },
        {
          "role": "later",
          "year": 2019,
          "name": "OpenAI GPT-2 model.py",
          "source_url": "https://github.com/openai/gpt-2/blob/master/src/model.py",
          "claim": "В исходном коде GPT-2 от OpenAI softmax по последней оси вычисляется вычитанием максимума с сохранением размерности, возведением в экспоненту и делением на сумму с сохранением размерности; в механизме внимания эта функция применяется к масштабированным и замаскированным оценкам до объединения значений."
        }
      ]
    },
    "approach": "От выходного softmax по словарю к устойчивой нормализации для внимания Transformer и предсказания следующего токена",
    "summary": "Бенжио и соавторы применяют выходной softmax, чтобы преобразовать оценки следующего слова в распределение вероятностей. Transformer использует softmax и для весов внимания, и для предсказания токенов, а опубликованный код внимания GPT-2 показывает вычитание максимума перед возведением в экспоненту. В этой главе реализуется связующий численный приём и вычисление потерь в логарифмической шкале; локальные правила работы с тензорами и ошибками не приписываются этим источникам.",
    "rust_source": "rust/demos/ch12-stable-softmax/src/lib.rs"
  },
  "rust_sources": [
    {
      "path": "rust/demos/ch12-stable-softmax/src/lib.rs",
      "region": "direct-output-softmax",
      "purpose": "Показать, как нормализация непосредственно вычисленных экспонент ведёт себя для обычных и экстремальных конечных логитов, не приписывая этот вариант реализации какой-либо из рассмотренных моделей."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/nn/probability.rs",
      "region": "probability-errors",
      "purpose": "Явно различать недопустимые оси, пустые оси классов, ошибки выходного тензора, неконечные логиты и ошибки целевых индексов."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/nn/probability.rs",
      "region": "stable-probability-operations",
      "purpose": "Вычислять log-sum-exp с вычитанием максимума, вероятности, их логарифмы и среднее NLL по индексам с основным накопителем полных потерь и запасным накопителем, слагаемые которого делятся на число целей до сложения."
    },
    {
      "path": "rust/demos/ch12-stable-softmax/src/lib.rs",
      "region": "tiny-stable-softmax-example",
      "purpose": "Создать три строки примера, отличающиеся общим сдвигом, и применить к ним все накопленные операции с вероятностями."
    },
    {
      "path": "rust/demos/ch12-stable-softmax/src/main.rs",
      "region": "learner-stable-softmax-output",
      "purpose": "Подготовить воспроизводимые нормированные значения, признаки сбоев прямого вычисления, подтверждение инвариантности к сдвигу, потери по индексам и типизированные ошибки."
    }
  ],
  "visualization": {
    "decision": "useful",
    "id": "stable-softmax",
    "rationale": "Три строки с одинаковыми относительными логитами позволяют одновременно увидеть переполнение и округление экспонент до нуля при прямом вычислении, общий сдвиг с вычитанием максимума, совпадающие вероятности и потери целевых классов — один итоговый вектор вероятностей этих связей не показывает."
  },
  "decoder_connection": "Теперь тензорное ядро может по любой явно указанной оси преобразовывать конечные логиты из представления с произвольными шагами в тензоры с собственным хранилищем: вероятности, логарифмы вероятностей и значения log-sum-exp; оно также вычисляет объединённое среднее NLL по индексам целевых классов. Эти операции нормируют оценки словаря и внимания и дают значение прямого прохода, производные которого глава 13 проверит независимым способом."
}
---

import StableSoftmaxDiagram from '../../../components/chapters/StableSoftmaxDiagram.astro';

export const diagramLabels = {
  title: 'Вычитание максимума: устойчивое вычисление',
  description:
    'Сравните три сдвинутых ряда логитов, их нормализацию, потери и четыре причины отказа.',
  summary: {
    shape: 'Форма логитов',
    axis: 'Ось классов',
    meanNll: 'Среднее NLL',
  },
  sections: {
    shift: 'Вычтите максимум до экспоненцирования',
    targets: 'Выберите логарифм вероятности цели',
    errors: 'Отклоните неверные оси, логиты и цели',
  },
  fields: {
    row: 'Строка',
    rawLogits: 'Исходные логиты',
    maximum: 'Максимум',
    shifted: 'Сдвинутые логиты',
    exponentials: 'Экспоненты',
    denominator: 'Знаменатель',
    probabilities: 'Вероятности',
    logProbabilities: 'Логарифм вероятности',
    naivePath: 'Прямое экспоненцирование',
    targetClass: 'Класс',
    targetLoss: 'Потеря NLL',
    group: 'Группа',
    classes: 'Классы',
    rank: 'Ранг',
  },
  statuses: {
    finite: 'результат конечен',
    overflowUndefined: 'не определено после переполнения',
    underflowUndefined: 'не определено после округления до нуля',
    probabilitiesMatch: 'Во всех трёх строках записаны одинаковые вероятности.',
  },
  notes: {
    shift:
      'Абсолютный масштаб строк различается. После вычитания максимума таблица показывает одинаковые сдвинутые значения, экспоненты и нормированные вероятности.',
    targets:
      'Индекс цели выбирает в строке логарифм вероятности целевого класса. Смена знака даёт потерю; выше показано среднее.',
    errors:
      'Карточки со штриховой рамкой показывают ось, группу, класс или цель, из-за которых запрос отклонён.',
  },
  symbols: {
    finite: '=',
    stable: '✓',
    overflow: '↑',
    underflow: '↓',
    rejected: '×',
  },
};

{/* chapter-section:worked-example */}

## Предскажите результат сдвига трёх строк

Начнём с трёх строк, в каждой из которых находятся оценки двух классов:

```text
shape [3,2]
[[    0,     1],
 [ 1000,  1001],
 [-1001, -1000]]
```

Это логиты — оценки до нормализации, а не вероятности. В каждой строке второе
значение ровно на единицу больше первого. Прежде чем брать калькулятор,
предскажите результат вычитания максимума каждой строки:

$$
\begin{aligned}
[0,1]-1 &= [-1,0], \\
[1000,1001]-1001 &= [-1,0], \\
[-1001,-1000]-(-1000) &= [-1,0].
\end{aligned}
$$

После экспоненцирования каждая строка превращается в `[0.367879441171,1]`.
Деление на сумму `1.367879441171` каждый раз даёт одну и ту же пару:

```text
[0.268941421370, 0.731058578630]
```

Совпадение связано не с округлением при выводе: программа на Rust одинаково
вычисляет каждую сдвинутую строку. Вычитание максимума удаляет общую константу,
но сохраняет разницу в единицу, от которой зависят относительные вероятности.

Для целей `[1,0,1]` предскажите, какой элемент будет выбран в каждой строке.
Отрицательные логарифмы выбранных вероятностей равны
`[0.313261687518,1.313261687518,0.313261687518]`, а их среднее —
`0.646595020852` ната на целевой элемент.

{/* chapter-section:formula */}

## Нормируйте после вычитания максимума

Формула численно устойчивого softmax:

$$
p_i=\frac{\exp(\ell_i-m)}{\sum_j\exp(\ell_j-m)}, \quad m=\max_j\ell_j
$$

Наибольший сдвинутый логит в точности равен нулю. Поэтому хотя бы одна
экспонента равна единице, а остальные не превышают её. Так мы устраняем
переполнение при непосредственном вычислении $\exp(1000)$ и не допускаем
нулевого знаменателя для строки с большими по модулю отрицательными значениями.

В точной арифметике одна и та же константа, прибавленная ко всем логитам,
прибавляется и к максимуму, поэтому разности не меняются. В строках нашего
примера эти разности точно представимы в `f64`; однако произвольный сдвиг в
арифметике с плавающей точкой может округлить малую разницу ещё до вычисления
softmax. Максимум сам по себе не является вероятностью, а одно вычитание ничего
не нормирует: всё ещё нужны экспоненцирование и деление на сумму по всей группе.

Те же сдвинутые значения позволяют вычислить три величины в логарифмической
шкале:

$$
\operatorname{LSE}(\ell_1,\ldots,\ell_n)
=m+\ln\sum_j\exp(\ell_j-m)
$$

$$
\log p_i=(\ell_i-m)-\ln\sum_j\exp(\ell_j-m)
$$

$$
\mathcal{L}_t=(m-\ell_t)+\ln\sum_j\exp(\ell_j-m)
$$

При вычислении log-sum-exp максимум возвращается после взятия натурального
логарифма суммы сдвинутых экспонент. В log-softmax из сдвинутой разности
вычитается этот логарифм. Для NLL по индексу выбирается одна цель, а её потеря
вычисляется без предварительного округления обычной вероятности.

{/* chapter-section:symbol-glossary */}

## Разберите обозначения вероятностей

| Символ | Смысл в вычислении |
| :--- | :--- |
| $p_i$ | Нормированная вероятность, присвоенная классу $i$. |
| $\ell_i$ | Конечный входной логит класса $i$. |
| $m$ | Наибольший логит в выбранной группе нормализации. |
| $i$ | Класс, для которого вычисляется вероятность. |
| $j$ | Индекс класса, перебирающий весь знаменатель. |

Ось делит тензор на независимые группы нормализации. Для формы `[3,2]` и оси
`1` получаются три группы по два класса. После удаления оси классов форма групп
равна `[3]`, поэтому плоские цели `[1,0,1]` соответствуют строкам ноль, один и
два именно в таком порядке.

Операция log-sum-exp может удалить выбранную ось или сохранить её с размером
один. Softmax и log-softmax сохраняют исходную форму. Любой успешно вычисленный
тензор результата получает собственный непрерывный буфер в построчном порядке,
даже если входные данные были представлены срезом или транспонированным представлением.

{/* chapter-section:history */}

## От softmax по словарю к вероятностям Transformer

Нейронная языковая модель Бенжио и соавторов преобразует оценки словаря в положительные вероятности следующего слова с помощью выходного softmax; сумма вероятностей равна единице. При вычислениях с конечной точностью прямое возведение экспоненты от больших несдвинутых логитов может привести к переполнению, а от достаточно отрицательных — округлить все слагаемые до нуля.

Первая историческая точка — работа
[Бенжио и соавторов *A Neural Probabilistic Language Model*](https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf).
Бенжио и соавторы описывают выходной softmax с положительными значениями, сумма которых равна единице, а его входы трактуют как ненормированные логарифмы вероятностей следующего слова.

Значимый для нас исторический шаг состоит именно в появлении выходного
распределения: обученные нейронной сетью оценки превращаются в вероятности по
словарю. В статье не утверждается, что её реализация использовала прямой вариант
на Rust из этой главы, интерфейс с произвольной осью, отклонение неконечных
значений или такой же порядок ошибок.

В Transformer softmax применяется и к масштабированным оценкам «запрос — ключ» внутри внимания, и при предсказании следующего токена. В опубликованном исходном коде GPT-2 показано устойчивое вычисление для внимания: перед возведением в экспоненту из оценок по последней оси вычитают максимум, затем складывают сдвинутые экспоненты и нормируют их до взвешивания значений.

Более поздние источники —
[работа Васвани и соавторов *Attention Is All You Need*](https://papers.nips.cc/paper_files/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf)
и
[опубликованный OpenAI файл GPT-2 `model.py`](https://github.com/openai/gpt-2/blob/master/src/model.py).
Васвани и соавторы определяют внимание на основе масштабированного скалярного произведения: к масштабированным произведениям запросов и ключей применяют softmax, после чего полученными весами взвешивают значения. Для получения вероятностей следующего токена к выходам декодера применяют обучаемое линейное преобразование и softmax. В исходном коде GPT-2 от OpenAI softmax по последней оси вычисляется вычитанием максимума с сохранением размерности, возведением в экспоненту и делением на сумму с сохранением размерности; в механизме внимания эта функция применяется к масштабированным и замаскированным оценкам до объединения значений. В коде эти операции обозначены как `reduce_max` и `reduce_sum`.

В точной арифметике добавление одной и той же константы ко всем логитам не меняет softmax. Вычитание максимума сохраняет это распределение и устраняет сбои прямого вычисления экспонент для рассматриваемых строк; log-sum-exp, log-softmax и объединённое среднее NLL по индексам целевых классов сохраняют сведения для обучения в логарифмической шкале, даже когда обычная вероятность округляется до нуля. Поддержка произвольной оси, требование конечных входов, расположение целей, правила выделения памяти и порядок ошибок — локальные решения этой реализации.

Небольшой пример на Rust начинается с непосредственной реализации формулы. Для `[0,1]`
вычисление завершается успешно; на больших положительных значениях получается
деление бесконечности на бесконечность, а на больших по модулю отрицательных —
деление нуля на ноль. Так проявляется численная проблема, которую на пути к
современным LLM решает устойчивое вычисление. Она связана с арифметикой с
плавающей точкой, а не с конкретным языком программирования: реализации на
разных языках сталкиваются с теми же переполнением и округлением до нуля, хотя
их интерфейсы и обработка ошибок могут различаться.

<RustSource
  path="rust/demos/ch12-stable-softmax/src/lib.rs"
  region="direct-output-softmax"
  caption="Показать прямую нормализацию экспонент для одной обычной и двух экстремальных строк с конечными значениями"
  label="Код на Rust с прямым вариантом softmax, который показывает переполнение и округление до нуля при вычислениях с конечной точностью"
/>

{/* chapter-section:rust-implementation */}

## Реализуйте операции с проверкой входных данных в логарифмической шкале

Порядок проверок делает причины отказа предсказуемыми. Сначала проверяются
границы оси. Затем softmax, log-softmax и NLL по индексам отклоняют пустую ось
классов. Размещение результата и резервирование памяти проверяются до чтения
тензора. Для NLL также заранее проверяются число целей, наличие хотя бы одной
цели и границы каждого целевого индекса. Лишь после этого сообщается о первом
NaN или положительной либо отрицательной бесконечности: сначала перебираются группы, а внутри
каждой группы — классы.

<RustSource
  path="rust/crates/llm-from-scratch/src/nn/probability.rs"
  region="probability-errors"
  caption="Различать ошибки осей, пустых классов, результата, неконечных логитов и целевых индексов"
  label="Код на Rust, определяющий ошибки устойчивых вероятностных операций и отрицательного логарифмического правдоподобия по индексам в главе 12"
/>

Для каждой непустой группы сначала находится максимум с помощью
`TensorView::get`. Во втором проходе один член, соответствующий максимуму, не
включается в остаток, а остальные сдвинутые экспоненты складываются и передаются в
`ln_1p(tail)`. Так сохраняется представимая субнормальная поправка в
логарифмической шкале, которую обычное $\ln(1+\mathrm{tail})$ могло бы потерять из-за
округления ещё до взятия логарифма.

Softmax делит сдвинутые экспоненты на $1+\mathrm{tail}$. Log-softmax вычитает
`ln_1p(tail)` из сдвинутого логита. Для $T$ целей объединённое среднее NLL по
индексам одновременно ведёт два накопителя. Основной накопитель `total`
складывает полные неотрицательные потери строк. Если каждая потеря и текущая
сумма остаются конечными, функция один раз делит `total` на $T$ и возвращает
среднее в натах на целевой элемент; одно деление в конце позволяет сохранить
представимое субнормальное среднее.

Параллельно запасной накопитель `scaled_mean` складывает две части каждой
потери после того, как каждая часть поделена на $T$: разность между максимумом и
целевым логитом $(m-\ell_{t_r})/T$, где $t_r$ — целевой класс строки $r$, и
логарифм нормирующей суммы $\ln(1+\mathrm{tail})/T$. Если сама разность
$m-\ell_{t_r}$ переполняется, запасной путь вместо неё вычисляет
$m/T-\ell_{t_r}/T$. Функция возвращает `scaled_mean` только тогда, когда полная
потеря строки или текущее значение `total` переполняется; в остальных случаях
она делит `total` на $T$ и возвращает полученное частное. При построении
координаты индекс класса вставляется в координату группы, перебираемую в
построчном порядке. Поэтому подходит любая логическая ось, а значения результата
занимают положения в обычном построчном порядке.

<RustSource
  path="rust/crates/llm-from-scratch/src/nn/probability.rs"
  region="stable-probability-operations"
  caption="Нормировать конечные логиты с произвольными шагами и оценивать плоские целевые индексы без устранимого переполнения"
  label="Код на Rust, реализующий log-sum-exp, softmax, log-softmax и объединённое среднее отрицательное логарифмическое правдоподобие по произвольной оси"
/>

Вспомогательная функция примера один раз создаёт три строки, отличающиеся общим
сдвигом, и вызывает накопленный API. Второй реализации устойчивой формулы в ней
нет.

<RustSource
  path="rust/demos/ch12-stable-softmax/src/lib.rs"
  region="tiny-stable-softmax-example"
  caption="Выполнить все проверяемые вероятностные операции над общим примером из трёх строк"
  label="Код на Rust, создающий полный пример устойчивого softmax для главы 12"
/>

Для пустой выбранной оси в логарифмической шкале существует полезный нейтральный
элемент: log-sum-exp возвращает отрицательную бесконечность для каждой группы по
оставшимся осям. Нормированного распределения в этом случае нет, поэтому
остальные операции завершаются ошибкой. Нулевой размер другой оси даёт корректный
пустой тензор без чтения значений. Точный ноль приводится к положительному нулю,
поэтому log-softmax и NLL для единственного класса также в точности равны
положительному нулю.

Вычитание максимума устраняет предотвратимые сбои, но не ограничения `f64`.
Вероятность крайне маловероятного класса с конечным логитом всё ещё может
округлиться до нуля, тогда как её логарифм и объединённая потеря для цели останутся
конечными. Если математическая разность между логитами классов превышает возможности
`f64`, результат всё ещё может стать знаковой бесконечностью, а log-sum-exp у
верхней границы может округлиться до `f64::MAX`. В главе 7 уже округлённая до нуля
вероятность наблюдаемой цели правильно даёт бесконечное NLL; объединённое
вычисление потери непосредственно из логитов позволяет избежать этого округления,
если ответ в логарифмической шкале представим.

Программа из примера выводит обычные и экстремальные результаты, а также четыре
ошибки:

<RustSource
  path="rust/demos/ch12-stable-softmax/src/main.rs"
  region="learner-stable-softmax-output"
  caption="Подготовить устойчивые результаты, признаки сбоев прямого вычисления, потерю цели, инвариантность и типизированные ошибки"
  label="Код на Rust, запускающий учебный пример главы 12 перед выводом точного воспроизводимого результата"
/>

```bash
./course run cargo run --quiet --locked -p ch12-stable-softmax
```

```text
stable softmax: shape=[3, 2] values=[0.268941421370, 0.731058578630, 0.268941421370, 0.731058578630, 0.268941421370, 0.731058578630]
log-sum-exp: shape=[3] values=[1.313261687518, 1001.313261687518, -999.686738312482]
targets: [1, 0, 1] losses=[0.313261687518, 1.313261687518, 0.313261687518] mean_nll=0.646595020852
naive overflow [1000, 1001]: undefined=true
naive underflow [-1001, -1000]: undefined=true
```

В полном выводе также приведены логиты, log-softmax, суммы вероятностей,
обычный прямой результат, подтверждение инвариантности к сдвигу, точные ошибки
и переход к главе 13. Вместе эти записи позволяют отличить округлённые десятичные
значения от точных форм тензоров, вариантов ошибок, бесконечностей и поведения
положительного нуля.

{/* chapter-section:visualization */}

## Сравните прямое и устойчивое вычисление softmax

В таблице состояние прямого пути сопоставлено с этапами после вычитания
максимума для той же строки. В ней видны максимум, сдвинутые значения,
знаменатель, вероятности, выбранная цель и потеря — результаты, уже вычисленные
программой на Rust. Разные символы и типы рамок отделяют конечное и устойчивое
состояния от переполнения, округления до нуля и отклонённого запроса без опоры
только на цвет.

<StableSoftmaxDiagram labels={diagramLabels} />

{/* chapter-section:exercises */}

## Сделайте прогноз перед запуском Rust

1. Предскажите сдвинутые значения для `[1000,1001]`.
2. Изменится ли какая-либо вероятность, если прибавить `-1001` к `[0,1]`?
3. Объясните, почему прямая нормализация `[1000,1001]` не определена в `f64`.
4. Предскажите обе вероятности для одинаковых логитов `[7,7]`.
5. Найдите потерю для целевого класса `0` в строке `[1000,1001]`.
6. Предскажите формы результата log-sum-exp для входа `[2,3,4]` и оси `1` при выключенном и включённом `keep_dim`.
7. У какой операции над пустой осью классов определён нейтральный элемент?
8. Проверка заблуждения: превращает ли само вычитание максимума логиты в вероятности?

<details>
  <summary>Проверить прогнозы</summary>

1. Максимум равен 1001, поэтому сдвинутые значения в точности равны `[-1,0]`.
2. Нет. Общая прибавленная константа на ту же величину изменяет максимум, поэтому обе разности после вычитания остаются прежними.
3. Обе исходные экспоненты переполняются до бесконечности, поэтому вместо вероятностей получаются отношения бесконечности к бесконечности.
4. У одинаковых сдвинутых логитов равны экспоненты, поэтому обе вероятности равны `0.5`.
5. Логарифм вероятности класса ноль равен `-1.313261687518`, поэтому NLL равно `1.313261687518`.
6. После удаления оси с индексом `1` получается `[2,4]`, а после её сохранения — `[2,1,4]`.
7. Log-sum-exp возвращает нейтральный элемент логарифмического сложения — отрицательную бесконечность. Для распределений softmax и log-softmax, а также для потери по целевому индексу нужен хотя бы один класс.
8. Нет. Сдвиг лишь делает вычисление относительных логитов устойчивым. Вероятности появляются после экспоненцирования и деления на полную сумму сдвинутых экспонент.

</details>

После прогноза запустите пример:

```bash
./course run cargo run --quiet --locked -p ch12-stable-softmax
```

{/* chapter-section:decoder-connection */}

## Подготовьте независимую проверку градиентов

Теперь накопленное тензорное ядро может по любой явно заданной оси
преобразовывать конечные логиты из представлений с произвольными шагами в
собственные тензоры вероятностей, логарифмов вероятностей и значений log-sum-exp,
а также вычислять объединённое среднее NLL по индексам. Эти операции будут
нормировать оценки по словарю и в механизме внимания и дадут прямое значение
функции потерь, производные которого глава 13 проверит независимо.

Одних значений прямого прохода недостаточно, чтобы подтвердить правильность
будущей реализации обратного прохода. Поэтому в главе 13 будет построена
медленная проверка конечными разностями: она по очереди изменяет одно скалярное
входное значение и сравнивает предполагаемые производные до того, как курс
начнёт полагаться на автоматическое дифференцирование.
