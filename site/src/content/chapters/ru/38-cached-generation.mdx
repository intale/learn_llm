---
{
  "chapter_id": "38-cached-generation",
  "locale": "ru",
  "content_revision": 4,
  "order": 38,
  "concept_id": "cached-generation",
  "title": "Один раз заполните кэши, затем декодируйте по одному токену",
  "description": "Разберитесь, как отдельный KV-кэш (кэш ключей и значений) для каждого блока декодера позволяет один раз обработать промпт, затем согласованно декодировать по одному токену и сравнивать логиты последней позиции и решения при генерации с эталонным расчётом по полному префиксу.",
  "objective": "Выделите каждому блоку декодера собственный KV-кэш и один раз заполните кэши по промпту. В заданных примерах согласованно обновляйте кэши всех блоков и проверяйте, что логиты последней позиции и выбранные при генерации токены совпадают с результатами эталонного расчёта по полному префиксу.",
  "worked_inputs": "Обработать идентификаторы токенов 0 и 1 декодером с двумя слоями и двумя головами, затем декодировать токен 2, на обоих этапах сравнить логиты последней позиции с эталонными расчётами по полному префиксу в пределах 2e-12 и сопоставить генерацию с KV-кэшем при заданном начальном состоянии с циклом без кэша из главы 36.",
  "formula": {
    "latex": "\\sum_{t=1}^{T}t^2\\in\\Theta(T^3),\\quad \\sum_{t=1}^{T}t\\in\\Theta(T^2)\\,.",
    "symbols": [
      {
        "symbol": "t",
        "meaning": "текущая длина сохранённого префикса, а значит, и число оценок внимания, которые вычисляет один новый запрос с KV-кэшем при обращении ко всем сохранённым ключам"
      },
      {
        "symbol": "T",
        "meaning": "конечная длина сохранённого префикса, охваченная сравнением"
      },
      {
        "symbol": "t^2",
        "meaning": "плотная каузальная матрица оценок внимания, которую заново строит один повторный расчёт декодера по полному префиксу длины t"
      },
      {
        "symbol": "\\sum_{t=1}^{T}",
        "meaning": "суммарная работа по вычислению оценок внимания для сохранённых длин от единицы до T"
      },
      {
        "symbol": "\\Theta(T^3)",
        "meaning": "кубический класс роста повторно вычисляемых матриц оценок внимания по полному префиксу при фиксированных размере пакета, числе слоёв и голов и ширине головы"
      },
      {
        "symbol": "\\Theta(T^2)",
        "meaning": "квадратичный класс роста строк оценок внимания для новых запросов с KV-кэшем при тех же фиксированных множителях"
      }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "inference-design",
      "limitation": "Каузальный декодер Transformer может генерировать по одному токену, каждый раз повторно обрабатывая весь известный префикс. Однако такой интерфейс без состояния при каждом следующем вызове заново строит матрицы оценок внимания и проекции ключей и значений для прежних позиций.",
      "later_advance": "При инкрементальном декодировании прежние тензоры ключей и значений стали явным состоянием. Позднее в системах обслуживания LLM обработку промпта отделили от последовательной генерации, сохраняя состояние ключей и значений во всех слоях и головах декодера.",
      "modern_llm_role": "При генерации с KV-кэшем модель один раз заполняет отдельный кэш каждого блока декодера, а затем обновляет состояние всех блоков лишь тогда, когда нужны следующие логиты. В заданных примерах логиты последней позиции совпадают с эталонными расчётами по полному префиксу в пределах допуска, а после восстановления контрольной точки совпадают выбранные токены, псевдослучайные значения для сэмплирования, конечное состояние генератора псевдослучайных чисел и причина остановки.",
      "sources": [
        {
          "role": "earlier",
          "year": 2017,
          "name": "Attention Is All You Need",
          "source_url": "https://arxiv.org/pdf/1706.03762",
          "claim": "Васвани и соавторы описывают стек авторегрессионных слоёв декодера Transformer, в котором маскированное самовнимание не позволяет позиции обращаться к последующим позициям; их архитектура «кодировщик — декодер» также содержит перекрёстное внимание, но не задаёт API для KV-кэша."
        },
        {
          "role": "later",
          "year": 2019,
          "name": "Fast Transformer Decoding: One Write-Head is All You Need",
          "source_url": "https://arxiv.org/pdf/1911.02150",
          "claim": "Инкрементальное самовнимание Шейзира получает прежние тензоры ключей и значений, добавляет текущие спроецированные ключ и значение и возвращает обновлённое состояние; вклад статьи состоит в многозапросном внимании, а не в заявлении об изобретении KV-кэширования."
        },
        {
          "role": "later",
          "year": 2023,
          "name": "Efficient Memory Management for Large Language Model Serving with PagedAttention",
          "source_url": "https://arxiv.org/pdf/2309.06180",
          "claim": "Квон и соавторы отделяют обработку промпта от последовательной генерации, описывают повторное использование сохранённых ключей и значений при вычислении только новой пары на последующих итерациях и учитывают состояние KV-кэша во всех слоях и головах Transformer."
        }
      ]
    },
    "approach": "Перейти от повторного расчёта всего каузального префикса декодера перед каждым выбором к однократному заполнению кэшей по промпту и последующему декодированию по одному токену с сохранением состояния.",
    "summary": "Исходная архитектура Transformer задаёт стек каузальных слоёв декодера. Интерфейсы, которым явно передаются сохранённые K/V, позволяют повторно использовать состояние при выводе, а современные системы обслуживания LLM отделяют обработку промпта от последовательного декодирования. Привязка к конфигурации модели, идентичностям узлов параметров и зафиксированным версиям их значений, а также согласованное обновление всех кэшей, сброс, счётчики, обработка ошибок и последовательная обработка промпта в этом курсе — локальные правила корректности, а не требования этих статей.",
    "rust_source": "rust/demos/ch38-cached-generation/src/lib.rs"
  },
  "rust_sources": [
    {
      "path": "rust/crates/llm-from-scratch/src/attention/incremental.rs",
      "region": "incremental-attention",
      "purpose": "Подготовить добавляемую строку инкрементального внимания одного слоя, не меняя состояние кэша, а затем применить уже проверенное изменение."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/generation/kv_cache.rs",
      "region": "decoder-kv-cache",
      "purpose": "Хранить отдельный кэш слоя для каждого блока декодера, привязать общее состояние к точной конфигурации декодера, идентичностям узлов всех параметров и зафиксированным версиям их значений, согласованно обрабатывать промпт и новые токены, считать работу и сбрасывать логическое состояние без изменения привязки."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/generation/kv_cache.rs",
      "region": "cached-generation",
      "purpose": "Объединить состояние KV-кэшей всей модели с существующим сэмплером, сохранив правила для EOS, ограничения числа токенов и контекста, а также поведение генератора псевдослучайных чисел."
    },
    {
      "path": "rust/demos/ch38-cached-generation/src/lib.rs",
      "region": "historical-cache-contrast",
      "purpose": "Сопоставить длины сохранённого префикса и число значений в тензорах оценок внимания для пути с KV-кэшем и эталонных вызовов по полному префиксу."
    },
    {
      "path": "rust/demos/ch38-cached-generation/src/lib.rs",
      "region": "learner-evidence",
      "purpose": "Собрать результаты сравнения в пределах допуска при обработке промпта и нового токена, подсчёт значений в тензорах оценок внимания, решения при генерации из восстановленной контрольной точки, результаты сброса и проверки изоляции хранилищ и ошибок."
    },
    {
      "path": "rust/demos/ch38-cached-generation/src/main.rs",
      "purpose": "Напечатать точный учебный отчёт главы 38."
    }
  ],
  "visualization": {
    "decision": "useful",
    "id": "cached-generation",
    "rationale": "Временная шкала от промпта до декодирования через два отдельных кэша блоков декодера одновременно показывает, как кэши принадлежат всей модели, как согласованно меняется их длина, как совпадают логиты последней позиции и как различается число значений оценок внимания."
  },
  "decoder_connection": "Теперь полный декодер может сохранять совместимое состояние K/V без графа вычислений во всех блоках, заполнять кэши по промпту и декодировать выбранные токены лишь тогда, когда нужны следующие логиты. Он может сбрасывать логическое состояние для повторной работы с той же неизменившейся моделью, отклонять вызовы при изменении узлов параметров или версий их значений и в заданном примере выбирать те же токены, что и эталонный расчёт по полному префиксу. После любого обновления весов необходимо создать новый кэш. Глава 39 соединит этот путь вывода с полным процессом обучения, оценки, сохранения, загрузки и генерации."
}
---

import RustSource from "../../../components/RustSource.astro";
import CachedGenerationDiagram from "../../../components/chapters/CachedGenerationDiagram.astro";

export const diagramLabels = {
  title: "Заполните кэш каждого слоя по промпту; затем декодируйте все слои согласованно",
  description:
    "Точная трассировка программы на Rust проводит промпт из двух токенов и один следующий токен через два отдельных кэша блоков декодера, проверяет логиты последней позиции в пределах допуска и сравнивает измеренное число оценок внимания, причины остановки и поведение при сбросе.",
  sections: {
    timeline: "От заполнения кэшей по промпту к декодированию одного токена",
    work: "Считайте только значения оценок внимания",
    generation: "Сопоставьте сэмплирование и остановки после восстановления контрольной точки",
    reset: "Сбросьте или отклоните вызов, не повредив состояние всей модели",
  },
  fields: {
    prompt: "Идентификаторы токенов промпта",
    token: "Выбранный токен",
    position: "Абсолютная позиция",
    cache: "Логическая длина кэша",
    layer: "Блок декодера",
    shape: "Форма кэша слоя",
    cached: "Путь с KV-кэшем",
    completePrefix: "Эталонный расчёт по полному префиксу",
    difference: "Максимальная абсолютная разность",
    scoreCells: "Значения оценок внимания",
    generated: "Идентификаторы и текст сгенерированных токенов",
    stop: "Причина остановки",
  },
  cues: {
    prefill: "заполнение по промпту — сплошная рамка",
    decode: "новый шаг декодирования одного токена — двойная рамка",
    distinct: "владеет отдельным хранилищем K/V",
    match: "логиты последней позиции совпадают в пределах допуска",
    reset: "логическое состояние возвращается к нулю",
    unchanged: "отклонённые вызовы не изменяют состояние",
  },
  captions: {
    timeline:
      "Промпт заполняет кэши обоих блоков в позициях ноль и один. Затем токен два поступает в позицию два, и длины обоих блоков согласованно увеличиваются.",
    work:
      "В примере считаются значения, образованные внутри механизма внимания. По этим числам нельзя судить о полном времени работы; это не аппаратные измерения.",
    generation:
      "После восстановления контрольной точки в обоих путях совпадают выбранные токены и псевдослучайные значения для сэмплирования. Оба пути завершаются с одинаковым состоянием генератора псевдослучайных чисел и одинаково останавливаются на границе контекста и по EOS.",
    reset:
      "Сброс сохраняет выделенную память, но очищает логическое состояние и счётчики работы. Недопустимые запросы для текущей фазы, ёмкости, модели или конфигурации не меняют состояние, записанное после последней успешной операции.",
  },
  proofs: {
    cache: "Четыре комбинации «слой — голова» образуют по одной строке оценок для каждой сохранённой длины.",
    reference: "Два эталонных вызова заново строят квадратные матрицы оценок при длине промпта два и длине три после декодирования.",
    loaded: "Восстановленная контрольная точка: совпадающие решения и точная граница контекста",
    eos: "EOS: выберите токен и остановитесь до ненужного декодирования",
  },
};

{/* chapter-section:worked-example */}

## Предскажите длины обоих кэшей до запуска

В примере используются $L=2$ блока декодера, $H=2$ головы внимания в каждом
блоке, ширина модели $D=4$, ширина головы $d_h=2$ и ёмкость контекста $C=4$.
Сначала состояние KV-кэшей всей модели пусто, а промпт состоит из токенов с
идентификаторами $[0,1]$.

До просмотра трассировки предскажите четыре факта:

1. При заполнении кэшей по промпту обе его позиции проходят через оба блока,
   поэтому кэш каждого блока достигает логической длины $2$ и формы
   $[1,2,2,2]$.
2. Следующим поступает токен с идентификатором $2$ в абсолютной позиции $2$.
3. При успешном декодировании оба кэша переходят от длины $2$ к длине $3$:
   один блок не может отстать от другого.
4. Выбранный токен EOS возвращается вызывающей стороне, но не проходит через
   декодер, если последующие логиты не нужны.

Последние логиты промпта равны
$[1.768374438,0.208825256,1.056205728,-0.451857108,0.388467944]$. После токена
$2$ следующие логиты равны
$[0.032908910,-0.679583624,1.408381841,0.525525421,-0.588014095]$. После обработки
промпта и после декодирования токена $2$ максимальная абсолютная разность до
округления между результатами с KV-кэшем и по полному префиксу не превышает
$2\times10^{-12}$; на экране она равна $0.000000000000$.

В модели, восстановленной из контрольной точки, ёмкость контекста равна $2$.
Генерация начинается с промпта $[0]$ и состояния генератора псевдослучайных чисел
`0x9e3779b97f4a7c38`. Оба пути выбирают $[4,4]$. При преобразовании
сгенерированных ID токенов обратно в текст получается строка `44`. Оба пути
используют одинаковые псевдослучайные значения для сэмплирования, завершаются
при одинаковом состоянии генератора псевдослучайных чисел и останавливаются на
границе контекста.
Заполнение по промпту доводит длину кэша до $1$; первый токен $4$ проходит через
декодер и увеличивает длину кэша до $2$; второй токен $4$ выбирается из
полученных логитов и возвращается, после чего ограничение контекста останавливает
генерацию до его декодирования. Если токен $4$ задан как EOS, оба пути
останавливаются после выбора $[4]$ и не выполняют ни одного последующего вызова
декодера для этого токена.

{/* chapter-section:formula */}

## Считайте значения оценок внимания, а не полное время работы

Зафиксируем размер пакета, число слоёв и голов и ширину головы. При конечной
длине сохранённого префикса $T$ два суммарных числа значений оценок внимания
растут как

$$
\sum_{t=1}^{T}t^2\in\Theta(T^3),\quad \sum_{t=1}^{T}t\in\Theta(T^2)\,.
$$

При сохранённой длине $t$ повторный расчёт по полному префиксу заново строит
плотную каузальную матрицу из $t^2$ оценок для каждой фиксированной комбинации
элемента пакета, слоя и головы. Шаг с KV-кэшем формирует только строку нового
запроса: $t$ оценок для всех сохранённых ключей. Сумма таких величин на каждом
шаге от $t=1$ до $T$ даёт два указанных класса роста.

Область этого сравнения намеренно узка. Внимание с KV-кэшем не работает за
постоянное время: новый запрос по-прежнему просматривает префикс, растущий вместе
с $t$. Формула не учитывает вычисления проекций или MLP, обращения к памяти,
выделение памяти, полное время работы или ускорение по фактическому времени.

В заданном примере размер пакета $1$, $2$ блока и $2$ головы дают фиксированный
множитель $4$. Последовательное заполнение по промпту и декодирование образуют
$4(1+2+3)=24$ значения оценок внимания с KV-кэшем. Два независимых эталонных
вызова при длинах $2$ и $3$ образуют $4(2^2+3^2)=52$ значения.

Последовательности вызовов в примере намеренно различаются. Путь с KV-кэшем
обрабатывает префиксы длины $1$, $2$ и $3$, а глава выполняет лишь две независимые
проверки по полному префиксу — при длинах $2$ и $3$. Поэтому $24$ и $52$ —
измеренные числа для фактически выполненных вызовов, а не две асимптотические
суммы, вычисленные при $T=3$.

{/* chapter-section:symbol-glossary */}

## Не смешивайте сохранённую и конечную длины

- $t$ — текущая длина сохранённого префикса. Столько же ключей читает один новый
  запрос с KV-кэшем.
- $T$ — конечная длина сохранённого префикса, охваченная сравнением.
- $t^2$ — матрица оценок по полному префиксу, заново построенная при длине $t$
  для каждой фиксированной комбинации элемента пакета, слоя и головы.
- $\sum_{t=1}^{T}$ суммирует работу по вычислению оценок внимания от сохранённой
  длины $1$ до длины $T$.
- $\Theta(T^3)$ — класс роста повторно вычисляемых матриц оценок по полному
  префиксу, когда опущенные множители остаются фиксированными.
- $\Theta(T^2)$ — класс роста строк оценок нового запроса с KV-кэшем при тех же
  фиксированных множителях.

Сам кэш по-прежнему хранит в каждом блоке один логический префикс K/V формы
$[B,H,t,d_h]$. Одинаковая форма не делает два кэша взаимозаменяемыми: их строки
получены на разной глубине стека декодера. Содержащий их `DecoderKvCache` также
привязан к одной точной конфигурации декодера, узлам всех параметров модели и
зафиксированным версиям их значений. Каждый вложенный кэш слоя отдельно привязан
к четырём параметрам внимания своего блока. После обновления весов ни форма, ни
совпадение узлов сами по себе не доказывают совместимость, потому что при
обновлении на месте меняются версии значений без замены узлов.

{/* chapter-section:history */}

## От стека каузальных слоёв к обработке промпта и последовательному декодированию

Каузальный декодер Transformer может генерировать по одному токену, каждый раз
повторно обрабатывая весь известный префикс. Однако такой интерфейс без состояния
при каждом следующем вызове заново строит матрицы оценок внимания и проекции
ключей и значений для прежних позиций.

[Attention Is All You Need](https://arxiv.org/pdf/1706.03762) задаёт каузальный
стек. Васвани и соавторы описывают стек авторегрессионных слоёв декодера
Transformer, в котором маскированное самовнимание не позволяет позиции
обращаться к последующим позициям; их архитектура «кодировщик — декодер» также
содержит перекрёстное внимание, но не задаёт API для KV-кэша.

[Fast Transformer Decoding: One Write-Head is All You Need](https://arxiv.org/pdf/1911.02150)
делает предыдущее состояние явным. Инкрементальное самовнимание Шейзира получает
прежние тензоры ключей и значений, добавляет текущие спроецированные ключ и
значение и возвращает обновлённое состояние; вклад статьи состоит в
многозапросном внимании, а не в заявлении об изобретении KV-кэширования.

[Efficient Memory Management for Large Language Model Serving with PagedAttention](https://arxiv.org/pdf/2309.06180)
переходит к современным системам вывода LLM. Квон и соавторы отделяют обработку
промпта от последовательной генерации, описывают повторное использование
сохранённых ключей и значений при вычислении только новой пары на последующих
итерациях и учитывают состояние KV-кэша во всех слоях и головах Transformer.

При инкрементальном декодировании прежние тензоры ключей и значений стали явным
состоянием. Позднее в системах обслуживания LLM обработку промпта отделили от
последовательной генерации, сохраняя состояние ключей и значений во всех слоях и
головах декодера.

При генерации с KV-кэшем модель один раз заполняет отдельный кэш каждого блока
декодера, а затем обновляет состояние всех блоков лишь тогда, когда нужны
следующие логиты. В заданных примерах логиты последней позиции совпадают с эталонными
расчётами по полному префиксу в пределах допуска, а после восстановления
контрольной точки совпадают выбранные токены, псевдослучайные значения для сэмплирования,
конечное состояние генератора псевдослучайных чисел и причина остановки.

Программа из этой главы связывает этот путь с подсчётом работы внимания. В четырёх
комбинациях «элемент пакета — слой — голова» последовательные строки с KV-кэшем
при сохранённых длинах $[1,2,3]$ содержат $24$ значения оценок внимания. Два
вызова по полному префиксу при длинах $[2,3]$ содержат $52$ значения, поэтому в
этой заданной последовательности вызовов не вычисляются $28$ значений. Это числа
элементов тензоров, а не измерение страничной организации памяти или времени
работы.

Привязка к конфигурации модели, идентичностям узлов параметров и зафиксированным
версиям их значений, а также согласованное обновление всех кэшей, сброс,
счётчики, обработка ошибок и последовательная обработка промпта в этом курсе —
локальные правила корректности, а не требования цитируемых статей.

Страничная организация, совместное использование, вытеснение и управление
памятью в промышленных системах остаются за пределами главы. Используемый здесь
кэш с непрерывным размещением данных позволяет отдельно разобрать требования к
корректности перед изучением этих задач обслуживания моделей.

<RustSource
  path="rust/demos/ch38-cached-generation/src/lib.rs"
  region="historical-cache-contrast"
  caption="Измерить тензоры оценок для путей с KV-кэшем и по полному префиксу при двух последовательностях вызовов из примера"
  label="Исходный код на Rust для исторического сравнения подходов к выводу LLM в главе 38"
/>

{/* chapter-section:rust-implementation */}

## Подготовьте каждый блок, затем одновременно обновите все кэши

Операция одного слоя из главы 37 теперь разделена на два этапа. Сначала она
вычисляет новый выход и строку K/V, не меняя логическое состояние. Обычный
публичный вызов сразу добавляет подготовленную строку. Код для всей модели вместо
этого сохраняет по одному подготовленному результату для каждого блока и
добавляет строки только после успешного вычисления всех блоков и итоговой
проекции на словарь.

<RustSource
  path="rust/crates/llm-from-scratch/src/attention/incremental.rs"
  region="incremental-attention"
  caption="Подготовить добавляемую строку слоя и записать её только после успешного вычисления всей строки декодера"
  label="Исходный код на Rust с подготовкой инкрементального внимания для главы 38"
/>

`DecoderKvCache::new` принимает конкретный декодер. Он выделяет по одному
`LayerKvCache` фиксированной ёмкости для каждого блока и записывает точную
конфигурацию декодера. Для каждого параметра модели кэш запоминает как
идентичность узла, так и текущую версию его значения. В заново созданной модели
параметры находятся в других узлах, поэтому возвращается
`ModelParameterMismatch`. Успешный шаг AdamW на месте записывает новые значения
в прежние узлы: их идентичности совпадают, но версии значений увеличиваются.
Старый кэш в этом случае возвращает `ModelParameterRevisionMismatch`.

Обе проверки защищают смысл сохранённых строк. Сохранённые значения K/V были
вычислены, пока идентичности узлов и версии значений параметров модели совпадали
с зафиксированными при создании кэша. Если добавить к ним строки, полученные с
обновлёнными весами, в одном префиксе смешаются две версии модели. После любого
обновления весов создайте новый `DecoderKvCache`, даже если старый кэш уже был
сброшен.

`prefill` принимает проверенный непустой промпт, только пока состояние пусто. В
этой учебной реализации строки промпта последовательно проходят через один и
тот же путь для одной строки. Так переход состояния остаётся наглядным; это не
оптимизированное ядро параллельной обработки промпта. `decode` принимает ровно
один токен из словаря лишь после заполнения по промпту и до исчерпания ёмкости.

Для каждой строки декодер вычисляет эмбеддинг, затем по порядку применяет в
каждом блоке подслои внимания и сети прямого распространения с предварительной
нормализацией и остаточными связями, после чего выполняет итоговую RMSNorm и
проекцию на словарь с общими весами матрицы эмбеддингов. Каждый блок подготавливает
добавляемую строку, не меняя кэш. Кэши слоёв, общая длина, счётчики фаз и число
значений оценок внимания обновляются вместе только после успешного вычисления
оставшихся блоков, итоговой нормализации и проекции на словарь.

<RustSource
  path="rust/crates/llm-from-scratch/src/generation/kv_cache.rs"
  region="decoder-kv-cache"
  caption="Хранить отдельный кэш каждого блока для конкретного экземпляра модели и согласованно обновлять состояние декодера"
  label="Исходный код на Rust с реализацией состояния KV-кэшей всей модели в главе 38"
/>

`generate_cached` повторно использует правило выбора из главы 36 и проверяет
условия остановки в порядке EOS, ограничение числа токенов, затем ограничение
контекста. Только если ни одно из них не выполнено, выбранный токен передаётся в
`decode`, чтобы получить следующие логиты. Поэтому второй токен $4$ в
восстановленном примере возвращается на границе контекста, а полный кэш остаётся
на длине $2$.

<RustSource
  path="rust/crates/llm-from-scratch/src/generation/kv_cache.rs"
  region="cached-generation"
  caption="Генерировать из заполненного состояния всей модели с прежними правилами сэмплирования и остановки"
  label="Исходный код на Rust с реализацией генерации с KV-кэшем в главе 38"
/>

Сброс опустошает логический префикс K/V: длина и состояние фазы заполнения
возвращаются к начальным значениям, а счётчики работы обнуляются. При этом
выделенная память и записанные значения K/V за пределами нового пустого
логического префикса сохраняются. Сброс не обновляет зафиксированные версии
параметров и не привязывает кэш к другой
модели. Декодирование до обработки промпта, заполнение непустого состояния,
переполнение, заново построенная модель, та же модель после обновления параметров
на месте или изменённая конфигурация возвращают соответствующую типизированную
ошибку, не меняя состояние KV-кэшей всей модели, записанное после последней
успешной операции. В частности, сброс после шага AdamW не устраняет
`ModelParameterRevisionMismatch`.

Демонстрационная программа главы 38 проверяет логиты последней позиции при обработке промпта и
декодировании в пределах допуска, отмечает раздельность двух буферов слоёв,
подсчитывает значения в тензорах оценок внимания с KV-кэшем и в эталонном
расчёте, сравнивает решения при генерации из восстановленной контрольной точки и
поведение EOS, выполняет сброс с повторным запуском и проверяет, что отклонённые
операции не меняют снимки состояния.

<RustSource
  path="rust/demos/ch38-cached-generation/src/lib.rs"
  region="learner-evidence"
  caption="Собрать проверки совпадения в пределах допуска, подсчёта работы внимания, генерации, сброса и точных вариантов ошибок для всей модели"
  label="Исходный код на Rust, собирающий результаты проверок главы 38"
/>

Выполните `cargo run --quiet --locked -p ch38-cached-generation`. Команда
напечатает точный учебный отчёт:

```text
chapter=38-cached-generation
config=layers:2 heads:2 model_width:4 context:4 tolerance:0.000000000002
prefill=prompt:[0,1] cache:0->2 layer_lengths:[2,2] shape:[1,2,2,2] cached_scores:12 complete_prefix_scores:16 max_abs_diff:0.000000000000 logits:[1.768374438,0.208825256,1.056205728,-0.451857108,0.388467944]
decode=token:2 position:2 cache:2->3 layer_lengths:[3,3] shape:[1,2,3,2] cached_scores:12 complete_prefix_scores:36 max_abs_diff:0.000000000000 logits:[0.032908910,-0.679583624,1.408381841,0.525525421,-0.588014095]
work=prefill_tokens:2 decode_tokens:1 layer_caches:2 cache_appends:6 qkv_rows:18 cached_scores:24 complete_prefix_scores:52 layer_storage_distinct:true
loaded=checkpoint_bytes:6330 context_capacity:2 rng_state:0x9e3779b97f4a7c38 prompt:[0] generated:[4,4] text:44 prefixes:[1,2] stop:context-limit final_cache:2 prefill_tokens:1 decode_tokens:1 cached_scores:6 complete_prefix_scores:10 tokens_match:true rng_match:true
eos=token:4 generated:[4] stop:eos final_cache:1 decode_tokens:0 tokens_match:true rng_match:true
reset=before:3 after:0 allocation_reused:true storage_unchanged:true work_zeroed:true replay_identical:true
errors=decode_before_prefill:true prefill_nonempty:true overflow:true rebuilt_model:true changed_config:true unchanged:true
history=lanes:4 cached_lengths:[1,2,3] cached_scores:24 complete_prefix_lengths:[2,3] complete_prefix_scores:52 avoided_scores:28
next=assemble the complete end-to-end LLM pipeline
```

<RustSource
  path="rust/demos/ch38-cached-generation/src/main.rs"
  caption="Напечатать точный отчёт главы 38 о генерации с KV-кэшем"
  label="Исходный код на Rust для точки входа программы из примера главы 38"
/>

{/* chapter-section:visualization */}

## Проследите путь от промпта до декодирования одного токена

Сначала схема показывает позиции промпта $0$ и $1$ в двух отдельных кэшах
блоков, а затем показывает добавление выбранного токена $2$ в абсолютную позицию $2$. Сводки
слоёв на этапе заполнения имеют сплошную рамку, а на этапе декодирования —
двойную, поэтому фазы различимы без цвета. Каждая координата логитов с KV-кэшем
расположена рядом с соответствующей координатой эталонного расчёта по полному
префиксу, и сравнение с допуском видно непосредственно.

Карточки работы сопоставляют $24$ значения оценок внимания с KV-кэшем и $52$
эталонных значения. Далее отдельно показаны две причины остановки. В запуске
после восстановления контрольной точки заполнение по
промпту доводит длину кэша до $1$, первый выбранный токен $4$ декодируется до
длины $2$, а второй выбранный токен $4$ возвращается до остановки по ограничению
контекста без ещё одного прохода декодера. В запуске с EOS первый выбранный
токен также возвращается, а следующий проход декодера не выполняется.

<CachedGenerationDiagram labels={diagramLabels} />

{/* chapter-section:exercises */}

## Сначала предскажите, затем сверьтесь с результатами

1. Сколько кэшей слоёв принадлежит декодеру из двух блоков?
2. Какова логическая длина каждого кэша слоя после обработки промпта из двух
   токенов?
3. Какую абсолютную позицию использует первый декодируемый токен?
4. Почему нельзя повторно использовать кэш с заново построенными весами тех же
   значений?
5. Почему одной проверки идентичности узлов недостаточно после обновления AdamW
   на месте?
6. Сколько добавлений в кэш происходит для двух строк промпта и одной строки
   декодирования?
7. Почему в примере образуются $24$ значения оценок внимания с KV-кэшем?
8. Делает ли KV-кэш обработку нового запроса внимания операцией постоянного
   времени?
9. Нужно ли декодировать первый выбранный токен, если это EOS?
10. Что меняет сброс, что он сохраняет и меняет ли он привязку кэша?

Заблуждение: кэши одинаковой формы можно совместно использовать в разных
блоках. На самом деле каждая сохранённая строка зависит от скрытого состояния на
входе своего блока, поэтому блок $1$ не может повторно использовать строки K/V
блока $0$. Содержащий их `DecoderKvCache` привязывает состояние всей модели к
точной конфигурации декодера, всем узлам параметров и зафиксированным версиям их
значений. Каждый вложенный кэш слоя также привязан к параметрам внимания своего
блока.

<details>
<summary>Сверить десять ответов</summary>

1. Декодеру принадлежат $2$ кэша слоёв — по одному для каждого блока.
2. Оба кэша достигают логической длины $2$ и формы $[1,2,2,2]$.
3. Первый последующий токен использует абсолютную позицию $2$, равную прежней
   длине кэша.
4. Даже при тех же значениях, формах и конфигурации заново построенная модель
   содержит другие узлы параметров, поэтому проверка идентичности завершается
   ошибкой.
5. Обновление AdamW на месте сохраняет узлы параметров, но увеличивает версии
   их значений. Поэтому старый кэш возвращает
   `ModelParameterRevisionMismatch`, а не принимает обновлённые веса.
6. $2$ блока для $3$ строк токенов дают $6$ согласованных добавлений в кэши.
7. Есть $1\times2\times2=4$ комбинации «элемент пакета — слой — голова», и
   каждая образует $1+2+3=6$ значений оценок, что даёт
   $4\times6=24$.
8. Нет. При длине префикса $t$ новый запрос по-прежнему читает $t$ сохранённых
   ключей.
9. Нет. EOS возвращается как выбранный токен, а последующие логиты не нужны.
10. Сброс обнуляет логическую длину, фазу и счётчики, сохраняя выделенную память,
    прежние биты K/V, а также зафиксированные идентичности узлов и версии
    значений параметров. Он не меняет привязку кэша. При работе с той же
    неизменившейся моделью старые строки K/V находятся вне пустого логического
    префикса и будут перезаписаны; после обновления весов нужен новый кэш.

</details>

{/* chapter-section:decoder-connection */}

## Соедините вывод со всем процессом

Теперь полный декодер может сохранять совместимое состояние K/V без графа
вычислений во всех блоках, заполнять кэши по промпту и декодировать выбранные
токены лишь тогда, когда нужны следующие логиты. Он может сбрасывать логическое
состояние для повторной работы с той же неизменившейся моделью, отклонять вызовы
при изменении узлов параметров или версий их значений и в заданном примере
выбирать те же токены, что и эталонный расчёт по полному префиксу. После любого
обновления весов необходимо создать новый кэш. Глава 39 соединит этот путь вывода
с полным процессом обучения, оценки, сохранения, загрузки и генерации.

В последней главе одна программа объединит весь пройденный путь: разделит данные,
обучит и применит BPE, обучит и выберет декодер, оценит его на нетронутых данных,
сохранит и снова загрузит контрольную точку, заполнит кэши по промпту,
сгенерирует продолжение с состоянием KV-кэшей всей модели и декодирует полученные
идентификаторы токенов обратно в текст.
