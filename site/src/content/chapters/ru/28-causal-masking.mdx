---
{
  "chapter_id": "28-causal-masking",
  "locale": "ru",
  "content_revision": 2,
  "order": 28,
  "concept_id": "causal-attention-mask",
  "title": "Закройте доступ к будущим ключам каузальной маской",
  "description": "Разберите, как нижнетреугольная каузальная маска с разрешённой диагональю закрывает доступ к будущим ключам Transformer, делает их вероятности внимания строго нулевыми и сохраняет выходы предыдущих позиций.",
  "objective": "Примените нижнетреугольную маску с разрешённой диагональю, чтобы каждый запрос мог учитывать только доступный ему префикс.",
  "worked_inputs": "Добавьте к примеру из главы 27 третью позицию: $Q=[[[0,3],[2,-1],[1,1]]]$, $K=[[[3,0],[-1,2],[2,1]]]$ и $V=[[[3,-3],[1,3],[-2,4]]]$. До запуска примера определите шесть доступных и три закрытые ячейки запрос–ключ.",
  "formula": {
    "latex": "M_{ij}=\\begin{cases}0&j\\le i\\\\-\\infty&j>i\\end{cases},\\quad A=\\operatorname{softmax}(S+M)",
    "symbols": [
      { "symbol": "S", "meaning": "тензор масштабированных оценок соответствия запросов и ключей до ограничения видимости" },
      { "symbol": "M", "meaning": "аддитивная каузальная маска с одной строкой для каждой позиции запроса и одним столбцом для каждой позиции ключа" },
      { "symbol": "i", "meaning": "индекс позиции запроса, отсчитываемый от нуля" },
      { "symbol": "j", "meaning": "индекс позиции ключа, отсчитываемый от нуля" },
      { "symbol": "A", "meaning": "нормированные по строкам вероятности внимания после исключения будущих ключей" }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "neural-architecture",
      "limitation": "Граница префикса возникает из последовательной рекуррентной обработки, где состояние приходится обновлять шаг за шагом.",
      "later_advance": "Поэтому при обучении строки внимания для известных целевых позиций можно вычислять вместе, не позволяя более ранней строке использовать более позднюю цель. При этом обычная авторегрессионная генерация по-прежнему добавляет по одному токену.",
      "modern_llm_role": "Декодерный Transformer применяет эту каузальную границу в каждом слое самовнимания.",
      "sources": [
        {
          "role": "earlier",
          "year": 2013,
          "name": "Graves, Generating Sequences With Recurrent Neural Networks",
          "source_url": "https://arxiv.org/abs/1308.0850",
          "claim": "При генерации каждый выбранный элемент становится следующим рекуррентным входом, поэтому будущих элементов ещё не существует."
        },
        {
          "role": "later",
          "year": 2017,
          "name": "Vaswani et al., Attention Is All You Need",
          "source_url": "https://arxiv.org/abs/1706.03762",
          "claim": "Недопустимым оценкам до softmax присваивается $-\\infty$; вместе с эмбеддингами выходной последовательности, сдвинутыми на одну позицию, это означает, что строка $i$ зависит только от известных выходов до позиции $i$."
        }
      ]
    },
    "approach": "От префикса при рекуррентной генерации к явной маске видимости декодера Transformer",
    "summary": "При рекуррентном предсказании следующего элемента из-за последовательных вычислений модели доступен только уже созданный префикс. Во время обучения Transformer известные целевые позиции доступны одновременно, поэтому самовнимание декодера явно восстанавливает ту же информационную границу: каждому запросу доступны ключи до его позиции включительно, а все последующие ключи исключаются до нормировки. Сама генерация остаётся последовательной.",
    "rust_source": "rust/crates/llm-from-scratch/src/attention/causal_mask.rs"
  },
  "rust_sources": [
    {
      "path": "rust/crates/llm-from-scratch/src/attention/causal_mask.rs",
      "region": "causal-mask-construction",
      "purpose": "Построить доступную для проверки нижнетреугольную аддитивную маску: нули в доступных ячейках и минус бесконечность в будущих."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/autograd/model_ops.rs",
      "region": "causal-softmax-forward",
      "purpose": "Нормировать только доступный префикс каждой строки, сохраняя записанные в ленте дифференцирования значения конечными."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/attention/causal_mask.rs",
      "region": "causal-self-attention-forward",
      "purpose": "Объединить построение масштабированных оценок, каузальную нормировку и смешивание значений в один проверяемый результат внимания."
    },
    {
      "path": "rust/demos/ch28-causal-masking/src/main.rs",
      "purpose": "Запустить полный пример каузального маскирования из главы 28."
    }
  ],
  "visualization": {
    "decision": "useful",
    "id": "causal-masking",
    "rationale": "Нижний треугольник наглядно разделяет доступные, закрытые и диагональные ячейки оценок, а замена суффикса показывает на этом примере, что более ранние строки выхода не зависят от будущих ключей и значений."
  },
  "decoder_connection": "Теперь в накопительном декодере есть одна голова самовнимания, выход которой в позиции $i$ зависит только от ключей и значений до позиции $i$ включительно. В главе 29 появится относительная информация о позициях, но граница видимости не расширится."
}
---

import RustSource from '../../../components/RustSource.astro';
import CausalMaskingDiagram from '../../../components/chapters/CausalMaskingDiagram.astro';

export const diagramLabels = {
  title: 'Проследите каузальную границу в каждой строке внимания',
  description:
    'Проследите путь строк запросов, ключей и значений через нижнетреугольное ограничение видимости, затем сравните исходные выходы с выходами после замены суффикса и изучите градиенты.',
  sections: {
    calculation: 'Проследите один расчёт по нижнему треугольнику',
    prefix: 'Измените суффикс и проверьте предыдущие выходы',
    evidence: 'Проверьте обратный проход и граничные случаи',
    history: 'От префикса при рекуррентной генерации к декодеру Transformer',
  },
  stages: {
    inputs: 'Начните со строк запросов, ключей и значений',
    mask: 'Отметьте нижний треугольник вместе с диагональю',
    maskedScores: 'Исключите будущие оценки до нормировки',
    probabilities: 'Нормируйте каждый доступный префикс',
    outputs: 'Смешайте только доступные строки значений',
    perturbation: 'Замените последний ключ и последнее значение',
  },
  fields: {
    shape: 'Форма',
    queryByKey: 'Строки запросов и столбцы ключей',
    visibility: 'Доступность по префиксу',
    maskValue: 'Значение аддитивной маски',
    rowSum: 'Сумма вероятностей по доступному префиксу',
    weightedTerms: 'Слагаемые после умножения на веса',
    output: 'Строка выхода',
    originalOutput: 'Исходный выход',
    perturbedOutput: 'После замены суффикса',
    status: 'Результат для префикса',
    backward: 'Начальный градиент по всем выходам',
    prefixGradient: 'Начальный градиент только по префиксу',
    singleToken: 'Граничный случай с одним токеном',
    emptyBatch: 'Сохранение форм для пустого пакета',
    errors: 'Отклонённые недопустимые входы',
    proof: 'Проверенные каузальные свойства',
    earlier: 'Префикс при рекуррентной генерации',
    transformer: 'Явная маска декодера Transformer',
    before: 'До замены',
    after: 'После замены',
  },
  roles: {
    query: 'Строки запросов: какую часть префикса может использовать эта позиция?',
    key: 'Строки ключей: с какими доступными позициями можно сопоставить запрос?',
    value: 'Строки значений: какое содержимое могут внести видимые позиции?',
  },
  cues: {
    allowed: 'Доступно: сплошная рамка',
    blocked: 'Закрыто: штриховая рамка',
    diagonal: 'Разрешённая диагональ: двойная рамка',
    unchanged: 'Побитно совпадает с исходным',
    changed: 'Изменилось',
    verified: 'Проверено',
    rejected: 'Отклонено',
  },
  cellCues: {
    allowed: 'Доступно',
    blocked: 'Закрыто',
    diagonal: 'Диагональ',
  },
  captions: {
    legend: 'Значение рамок и текстовых обозначений',
    calculation: 'В каждой строке доступны диагональная ячейка и все предыдущие столбцы ключей; различия обозначены и рамкой, и текстом.',
    prefix: 'Изменяются только последний ключ и последнее значение; первые две строки выхода побитно совпадают с исходными.',
    evidence: 'Начальные градиенты по всем выходам и только по префиксу показывают, куда градиент может пройти, а куда — нет.',
    history: 'Рекуррентные вычисления неявно ограничивают модель префиксом, а самовнимание декодера Transformer задаёт эту границу явно.',
  },
  evidence: {
    finiteTape: 'Записанные значения для автоматического дифференцирования остаются конечными',
    futureProbabilities: 'Вероятности будущих ключей',
    prefixOutputs: 'Предыдущие выходы после замены суффикса',
    suffixGradient: 'Для функции потерь только по префиксу изменённый суффикс получает нулевой градиент.',
    exactZero: 'Строго равны нулю',
    bitwise: 'Побитно совпадают с исходными',
  },
  historyDetails: {
    earlier: 'При рекуррентной генерации существует только уже созданный префикс, а рекуррентное состояние продвигается на одну позицию за шаг.',
    transformer: 'При обучении сдвинутые входы декодера и каузальная маска позволяют вместе обрабатывать известные целевые позиции, не давая более ранним строкам использовать более поздние цели; генерация остаётся последовательной.',
  },
  scrollers: {
    inputs: 'Прокручиваемые входные строки запросов, ключей и значений',
    triangles: 'Прокручиваемые таблицы каузальной маски, замаскированных оценок и вероятностей',
    outputs: 'Прокручиваемые смеси значений на выходе каузального внимания',
    prefix: 'Прокручиваемое сравнение исходных выходов с выходами после замены суффикса',
    gradients: 'Прокручиваемая таблица градиентов каузального внимания',
    history: 'Прокручиваемое сравнение этапов развития каузальных языковых моделей',
  },
  errorCases: {
    attentionEmpty: 'Внимание без позиций токенов',
    softmaxRank: 'Ранг тензора оценок меньше двух',
    softmaxShape: 'Последние две оси тензора оценок имеют разную длину',
    queryRank: 'Неверный ранг тензора запросов',
    tokenMismatch: 'Число токенов в запросах, ключах и значениях различается',
    releasedScore: 'Операнд оценок относится к уже освобождённой ленте операций',
  },
};

{/* chapter-section:worked-example */}

## Предскажите видимый треугольник

Добавьте к примеру из главы 27 третью позицию:

$$
Q=\begin{bmatrix}0&3\\2&-1\\1&1\end{bmatrix},\quad
K=\begin{bmatrix}3&0\\-1&2\\2&1\end{bmatrix},\quad
V=\begin{bmatrix}3&-3\\1&3\\-2&4\end{bmatrix}.
$$

Здесь один пакет, три позиции токенов, ширина запросов и ключей равна двум,
ширина значений — тоже двум. Следовательно,

$$
Q,K,V\in\mathbb{R}^{1\times3\times2}.
$$

До маскирования строки исходных оценок равны $[0,6,3]$, $[6,-4,3]$ и
$[3,1,3]$. Сначала отметьте ячейки, доступные каждому запросу. Запросу $0$
доступен только ключ $0$; запросу $1$ — ключи $0$ и $1$; запросу $2$ — все три ключа.
Итого получается шесть доступных и три закрытые ячейки. Все три диагональные
ячейки входят в число доступных.

{/* chapter-section:formula */}

## Примените маску до softmax

Для строки запроса $i$ и столбца ключа $j$ используйте аддитивную маску

$$
M_{ij}=\begin{cases}0&j\le i\\-\infty&j>i\end{cases},\quad A=\operatorname{softmax}(S+M)
$$

Диагональ намеренно остаётся доступной. При обучении декодера целевые входы
сдвинуты на одну позицию. Поэтому представление на диагонали содержит уже
известный предыдущий токен, а не цель, которую модель предсказывает. Сдвиг и
маска вместе сохраняют авторегрессионную зависимость.

Пусть тензор масштабированных оценок равен

$$
S=\frac{QK^\top}{\sqrt{d_k}}.
$$

До нормировки добавьте ограничение видимости, затем смешайте значения:

$$
A=\operatorname{softmax}(S+M),\qquad O=AV.
$$

Для этого примера

$$
A\approx
\begin{bmatrix}
1&0&0\\
0.999151&0.000849&0\\
0.445808&0.108383&0.445808
\end{bmatrix}.
$$

Вероятность внимания к каждому закрытому ключу строго равна нулю. Сумма вероятностей по
каждому доступному префиксу равна единице:

$$
\sum_{j=0}^{i}A_{bij}=1.
$$

Получаются строки

$$
O\approx
\begin{bmatrix}
3&-3\\
2.998303&-2.994908\\
0.554192&0.770959
\end{bmatrix}.
$$

Нельзя сначала применить обычный softmax ко всем ключам, а затем обнулить
вероятности будущих ключей: в знаменателе вероятностей доступных ключей по-прежнему будут
учтены закрытые ключи. Большое по модулю конечное отрицательное число можно
использовать как приближение идеальной маски, но математически оно не равно $-\infty$.

{/* chapter-section:symbol-glossary */}

## Не путайте ограничение видимости с позиционной информацией

- $S$ — тензор масштабированных оценок соответствия запросов и ключей до маскирования.
- $M$ — аддитивная маска видимости.
- $i$ обозначает позицию запроса, а $j$ — позицию ключа.
- $A$ содержит вероятности внимания после исключения будущих ключей.
- $Q$, $K$ и $V$ сохраняют роли запросов, ключей и значений из главы 27.
- $O$ содержит по одной взвешенной смеси доступных строк значений для каждой позиции запроса.
- $B$ — размер пакета, $T$ — число токенов, $d_k$ — ширина запросов и ключей,
  а $d_v$ — ширина значений.

Полное правило для форм остаётся таким:

$$
Q,K\in\mathbb{R}^{B\times T\times d_k},\quad
V\in\mathbb{R}^{B\times T\times d_v},\quad
M\in\left(\mathbb{R}\cup\{-\infty\}\right)^{T\times T},\quad
S,A\in\mathbb{R}^{B\times T\times T},\quad
O\in\mathbb{R}^{B\times T\times d_v}.
$$

Для доступной ячейки нормировка строки имеет вид

$$
A_{bij}=
\frac{\exp(S_{bij})}{\sum_{r=0}^{i}\exp(S_{bir})},
\qquad j\le i,
$$

а для закрытой ячейки

$$
A_{bij}=0,\qquad j>i.
$$

Это правило задаёт видимость, но не положение. Чтобы различать порядок,
строкам нужен отдельный абсолютный или относительный позиционный сигнал. Маски
заполнения, переменная длина последовательности, несколько голов, выходная
проекция и кеш ключей и значений — тоже отдельные задачи.

{/* chapter-section:history */}

## От рекуррентного состояния префикса к явной маске декодера

[Грейвс, *Generating Sequences With Recurrent Neural Networks*](https://arxiv.org/abs/1308.0850)
обучает модель предсказывать следующий элемент, последовательно обрабатывая элементы
последовательности. При генерации каждый выбранный элемент становится следующим
рекуррентным входом, поэтому будущих элементов ещё не существует. Граница
префикса возникает из последовательной рекуррентной обработки, где состояние
приходится обновлять шаг за шагом. Это утверждение относится к генерации текста в
статье, а не к отдельной модели синтеза рукописного текста с дополнительным условным входом.

[Васвани и соавторы, *Attention Is All You Need*](https://arxiv.org/abs/1706.03762)
объединяют запросы в матрицы и маскируют самовнимание декодера так, чтобы строка
не могла обратиться к последующим позициям. Недопустимым оценкам до softmax
присваивается $-\infty$; вместе с эмбеддингами выходной последовательности, сдвинутыми на одну
позицию, это означает, что строка $i$ зависит только от известных выходов до
позиции $i$.

Поэтому при обучении строки внимания для известных целевых позиций можно
вычислять вместе, не позволяя более ранней строке использовать более позднюю цель. При этом
обычная авторегрессионная генерация по-прежнему добавляет по одному токену. Декодерный
Transformer применяет эту каузальную границу в каждом слое самовнимания. Маска
задаёт границу видимости, но не кодирует положение.

{/* chapter-section:rust-implementation */}

## Оставьте маску доступной для проверки, а записанные значения — конечными

`causal_additive_mask` строит обычный тензор формы $[T,T]$. В доступных ячейках
находится $0$, а в будущих — $-\infty$:

<RustSource
  path="rust/crates/llm-from-scratch/src/attention/causal_mask.rs"
  region="causal-mask-construction"
  caption="Постройте доступную для проверки нижнетреугольную маску с разрешённой диагональю"
  label="Исходный код на Rust для построения аддитивной каузальной маски из главы 28"
/>

Дифференцируемый `TensorValue` отклоняет неконечные данные листовых тензоров.
Операция `causal_softmax` реализует ту же математическую границу, не записывая
$-\infty$: она читает только $j\le i$, вычитает максимум доступного префикса,
нормирует доступные ячейки и записывает точное значение с плавающей точкой
$+0.0$ во все закрытые ячейки.

<RustSource
  path="rust/crates/llm-from-scratch/src/autograd/model_ops.rs"
  region="causal-softmax-forward"
  caption="Нормируйте доступные префиксы, сохраняя записанные значения для автоматического дифференцирования конечными"
  label="Исходный код на Rust для устойчивого дифференцируемого softmax с каузальной маской"
/>

Операция принимает тензоры оценок ранга не меньше двух, у которых размеры
последних двух осей равны. Поэтому она поддерживает форму $[B,H,T,T]$, которая
понадобится далее, не смешивая ведущие оси.
Операция отклоняет ранг меньше двух, разные размеры последних осей, пустую ось
токенов и уже освобождённый операнд.

`causal_scaled_dot_product_self_attention` повторно использует построение
оценок из главы 27, применяет `causal_softmax` и умножает результат на $V$:

<RustSource
  path="rust/crates/llm-from-scratch/src/attention/causal_mask.rs"
  region="causal-self-attention-forward"
  caption="Соберите проверяемое каузальное самовнимание на основе масштабированного скалярного произведения"
  label="Исходный код на Rust для накопительного прямого прохода внимания из главы 28"
/>

Для доступной оценки обратный проход подчиняется формуле

$$
\bar S_{bij}=A_{bij}
\left(\bar A_{bij}-\sum_{r=0}^{i}\bar A_{bir}A_{bir}\right)
,\qquad j\le i,
$$

а закрытая оценка получает

$$
\bar S_{bij}=0,\qquad j>i.
$$

Все шесть координат каждого из тензоров $Q$, $K$ и $V$ согласуются с
центральными разностями при шаге $10^{-6}$ и допуске $4\times10^{-6}$. Среди
проверенных граничных случаев также есть один токен, пустые пакеты, тензоры оценок ранга
два и четыре, независимые ведущие оси, очень большие закрытые оценки,
типизированные ошибки, операнды из освобождённой ленты и побитное совпадение при
повторном запуске.

Исполняемый пример выводит проверенные данные: маску, вероятности, выходы,
результат проверки неизменности префикса, градиенты и граничные случаи:

<RustSource
  path="rust/demos/ch28-causal-masking/src/main.rs"
  caption="Выведите пример каузального маскирования и проверенные граничные случаи"
  label="Точка входа на Rust для учебного примера из главы 28"
/>

Выполните `cargo run --quiet --locked -p ch28-causal-masking`, чтобы увидеть
полный результат примера.

{/* chapter-section:visualization */}

## Проследите путь внимания по нижнему треугольнику

<CausalMaskingDiagram labels={diagramLabels} />

Треугольные таблицы показывают, какие оценки сохраняются до нормировки.
Сплошная, штриховая и двойная рамки различают доступные, закрытые и
диагональные ячейки, не полагаясь только на цвет.

Если изменить только

$$
k_2:[2,1]\to[-2,4],\qquad v_2:[-2,4]\to[5,-1]
$$

то $o_0$ и $o_1$ побитно совпадут с исходными, а

$$
o'_2\approx[3.287932,-1.591834].
$$

{/* chapter-section:exercises */}

## Сначала сделайте прогноз, затем откройте ответы

1. Запишите множество индексов доступных ключей для строк запросов $0$, $1$ и $2$.
2. Предскажите, может ли замена только $k_2$ и $v_2$ изменить $o_0$ или $o_1$.
3. Объясните, почему все три диагональные ячейки доступны, хотя модель не может
   получить доступ к токену, который нужно предсказать.
4. Предскажите градиенты запроса и ключа для каузальной головы самовнимания с
   одним токеном.
5. Объясните, почему после обнуления будущих вероятностей вслед за обычным softmax
   сумма строки перестаёт быть равной единице.
6. Определите, позволяет ли маска параллельно обрабатывать позиции токенов при
   обучении, когда целевые токены известны, при авторегрессионной генерации, в обоих случаях
   или ни в одном.
7. Назовите отдельный механизм, который появится в главе 29, не изменяя
   нижнетреугольную границу.

<details>
  <summary>Проверьте прогнозы</summary>

  1. Множества равны $\{0\}$, $\{0,1\}$ и $\{0,1,2\}$.
  2. Ни один из предыдущих выходов не изменится; последний ключ и последнее значение доступны только $o_2$.
  3. Входы декодера сдвинуты на одну целевую позицию, поэтому на диагонали находится уже известный предыдущий токен, а не предсказываемая цель.
  4. Единственная вероятность постоянно равна $1$, поэтому оба градиента строго равны нулю.
  5. Обнуление после softmax удаляет часть вероятностной массы, но не пересчитывает знаменатель по доступному префиксу.
  6. При обучении строки для известных целевых позиций можно вычислять вместе; при генерации токены по-прежнему добавляются по одному.
  7. Относительная информация о позициях должна задавать порядок, сохраняя прежнее каузальное правило видимости.
</details>

{/* chapter-section:decoder-connection */}

## Сохраняйте границу префикса по мере роста декодера

Теперь накопительный декодер создаёт выход в позиции $i$, используя только
ключи и значения до позиции $i$ включительно. Если функция потерь зависит лишь
от первых двух позиций, градиент по будущему суффиксу строго равен нулю:

$$
\frac{\partial L_{\le1}}{\partial q_2}
=\frac{\partial L_{\le1}}{\partial k_2}
=\frac{\partial L_{\le1}}{\partial v_2}=0.
$$

Именно такая информационная граница нужна авторегрессионному декодеру. В главе
29 появится относительная информация о позициях, но граница не расширится.
