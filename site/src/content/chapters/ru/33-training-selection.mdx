---
{
  "chapter_id": "33-training-selection",
  "locale": "ru",
  "content_revision": 5,
  "order": 33,
  "concept_id": "training-selection",
  "title": "Выполните все шаги обучения и выберите модель по валидации",
  "description": "Разберитесь, как цикл обучения декодера упорядочивает обратное распространение, ограничение нормы градиента, шаги AdamW по расписанию, валидацию без записи графа и выбор состояния без обращения к тестовым данным.",
  "objective": "Выполнить все шаги плана обучения декодера с заранее заданным числом обновлений, измерить потери на валидационной выборке без записи графа в фиксированных контрольных точках и восстановить состояние модели, сохранённое в самой ранней точке с минимальными потерями, не обращаясь при этом к тестовым данным.",
  "worked_inputs": "Обучить детерминированный одноблочный декодер со 144 параметрами за восемь заданных обновлений на мини-пакетах, используя явное четырёхсегментное расписание скорости обучения, ограничение общей нормы градиента на уровне 0,35 и измерения на валидационной выборке после шагов 0, 2, 4, 6 и 8.",
  "formula": {
    "latex": "\\begin{aligned}g_s&=\\nabla_\\theta\\mathcal{L}_{tr}^{(s)}(\\theta_{s-1}),\\\\ \\widetilde g_s&=\\frac{c}{\\max(c,\\lVert g_s\\rVert_2)}g_s,\\\\ (\\theta_s,m_s,v_s)&=\\operatorname{AdamW}_{\\eta_s}\\!\\left(\\theta_{s-1},\\widetilde g_s,m_{s-1},v_{s-1}\\right),\\quad s=1,\\ldots,8,\\\\ s^*&=\\min\\left\\{s\\in\\mathcal{C}:\\mathcal{L}_{va}(\\theta_s)=\\min_{k\\in\\mathcal{C}}\\mathcal{L}_{va}(\\theta_k)\\right\\}\\end{aligned}",
    "symbols": [
      {
        "symbol": "\\theta_s",
        "meaning": "полное состояние параметров декодера со стабильными именами после ровно s запланированных обновлений; начальное состояние имеет индекс ноль"
      },
      {
        "symbol": "s",
        "meaning": "нумеруемый с единицы индекс обновления или индекс измеренной контрольной точки"
      },
      {
        "symbol": "\\mathcal{L}_{tr}^{(s)}",
        "meaning": "функция потерь следующего токена для обучающего мини-пакета s, вычисленная только по обучающей выборке"
      },
      {
        "symbol": "g_s",
        "meaning": "конечный градиент обновления s до ограничения нормы, вычисленный в предшествующем этому обновлению состоянии параметров"
      },
      {
        "symbol": "\\widetilde g_s",
        "meaning": "градиент после ограничения общей нормы, который получает AdamW"
      },
      {
        "symbol": "c",
        "meaning": "положительный верхний предел общей нормы; в рассматриваемом примере он равен 0,35"
      },
      {
        "symbol": "\\eta_s",
        "meaning": "заранее заданная скорость обучения для обновления s"
      },
      {
        "symbol": "m_s,v_s",
        "meaning": "состояния первого и второго моментов Adam после обновления s"
      },
      {
        "symbol": "\\operatorname{AdamW}",
        "meaning": "изученное в главе 22 обновление оптимизатора, которое обновляет параметры и оба состояния моментов"
      },
      {
        "symbol": "\\mathcal{C}",
        "meaning": "множество измеренных контрольных точек {0, 2, 4, 6, 8}"
      },
      {
        "symbol": "s^*",
        "meaning": "самая ранняя измеренная контрольная точка с минимальными потерями на валидационной выборке"
      },
      {
        "symbol": "\\mathcal{L}_{va}",
        "meaning": "взвешенная по токенам функция потерь на валидационной, но не тестовой выборке, вычисленная без записи графа"
      }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "training-practice",
      "limitation": "Обновления по всему корпусу или по одному примеру и отчёт только по обучающей выборке сами по себе не задают масштабируемую периодичность обновлений и независимое правило выбора между состояниями языковой модели.",
      "later_advance": "В работах по нейронным языковым моделям роли обучающей, валидационной и тестовой выборок были разделены; системы для последовательностей и Transformer добавили мини-пакеты, явные расписания, ограничение нормы и периодически сохраняемые состояния; в более поздних работах с преобразованием текста в текст прямо указано, что контрольную точку выбирают по валидации, не используя тестовые данные для выбора модели.",
      "modern_llm_role": "При обучении LLM только с декодером многократно формируют пакеты токенов, дифференцируют обучающую цель, контролируют величину градиента, применяют расписание скорости обучения и оценивают состояния-кандидаты на отложенной валидационной выборке, оставляя результаты на тестовой выборке для последующей однократной оценки.",
      "sources": [
        {
          "role": "earlier",
          "year": 2003,
          "name": "A Neural Probabilistic Language Model",
          "source_url": "https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf",
          "claim": "Бенжио и соавторы разделяют обучающий, валидационный и тестовый текст, явно связывают валидацию с выбором модели и ранней остановкой и описывают стохастические обновления по одному примеру для нейронной языковой модели прямого распространения."
        },
        {
          "role": "earlier",
          "year": 2014,
          "name": "Sequence to Sequence Learning with Neural Networks",
          "source_url": "https://arxiv.org/pdf/1409.3215",
          "claim": "Суцкевер, Виньялс и Ле сообщают о пакетах последовательностей, заранее заданном правиле уменьшения скорости обучения и масштабировании градиента, когда его общая норма превышает фиксированный порог в рекуррентной модели последовательностей."
        },
        {
          "role": "later",
          "year": 2017,
          "name": "Attention Is All You Need",
          "source_url": "https://arxiv.org/pdf/1706.03762",
          "claim": "Васвани и соавторы обучают Transformer на пакетах с заданным числом токенов, используют зависящее от номера шага расписание с разогревом и убыванием обратно пропорционально квадратному корню и периодически записывают контрольные точки."
        },
        {
          "role": "later",
          "year": 2020,
          "name": "Exploring the Limits of Transfer Learning with a Unified Text-to-Text Transformer",
          "source_url": "https://www.jmlr.org/papers/volume21/20-074/20-074.pdf",
          "claim": "Раффель и соавторы сохраняют контрольные точки дообучения с фиксированной периодичностью, выбирают лучшую по результату на валидации и прямо избегают использования тестовой выборки для выбора модели."
        },
        {
          "role": "later",
          "year": 2020,
          "name": "Language Models are Few-Shot Learners",
          "source_url": "https://arxiv.org/pdf/2005.14165",
          "claim": "Браун и соавторы переносят Adam, расписание скорости обучения, масштабирование размера пакета в зависимости от числа токенов и ограничение общей нормы градиента в обучение языковой модели только с декодером масштаба GPT-3."
        }
      ]
    },
    "approach": "Следующий этап — перейти от подгонки модели и отчёта по одному обученному состоянию к заранее заданным обновлениям на мини-пакетах. Такие обновления периодически создают состояния-кандидаты для валидации, а отдельная тестовая выборка остаётся закрытой.",
    "summary": "Путь к современному обучению LLM объединяет строгое разделение ролей выборок, воспроизводимые пакеты, конечные градиенты, контроль нормы, явное расписание скорости обучения, периодическую валидацию без записи графа и выбор контрольной точки. В этих работах используются разные архитектуры и схемы обучения, поэтому фиксированное начальное значение генератора, точно заданная периодичность и выбор самой ранней точки при равенстве — локальные учебные решения, а не общепринятая практика.",
    "rust_source": "rust/demos/ch33-training-selection/src/lib.rs"
  },
  "rust_sources": [
    {
      "path": "rust/crates/llm-from-scratch/src/training/trainer.rs",
      "region": "training-plan",
      "purpose": "Задать точный порядок обновления, одну скорость для каждого шага, контрольные точки валидации и предел нормы до изменения состояния модели."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/training/trainer.rs",
      "region": "no-grad-evaluation",
      "purpose": "Измерить среднюю функцию потерь за эпоху с весами по числу токенов, не записывая рёбра графа и не изменяя биты градиентов параметров."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/training/trainer.rs",
      "region": "complete-training-loop",
      "purpose": "Выполнить все запланированные обновления, сверяя возвращаемые номера шагов оптимизатора с номерами обновлений в плане, измерить потери на валидации в каждой заданной контрольной точке и восстановить состояние модели, сохранённое в самой ранней точке с минимальными потерями на валидации."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/training/adamw.rs",
      "region": "adamw-execution-and-trace-api",
      "purpose": "Выполнить атомарное обновление AdamW со скоростью из расписания: обычный метод возвращает только новый номер шага оптимизатора, а явно выбранный метод трассировки — подробные данные обновления."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/autograd/tensor_core.rs",
      "region": "no-grad-scope",
      "purpose": "Отключить запись ленты во вложенной области, безопасно восстанавливаемой при панике, сохранив обычные проверки прямого прохода."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/models/decoder.rs",
      "region": "decoder-parameter-rebuild",
      "purpose": "После обновления заново связать каждый компонент декодера с новыми листовыми узлами со стабильными именами."
    },
    {
      "path": "rust/demos/ch33-training-selection/src/lib.rs",
      "region": "historical-selection-contrast",
      "purpose": "Сопоставить выбор только по обучающим потерям с независимым выбором по валидации, не воспроизводя историческую архитектуру."
    },
    {
      "path": "rust/demos/ch33-training-selection/src/lib.rs",
      "region": "learner-evidence",
      "purpose": "Дважды выполнить ограниченный пример и собрать свидетельства выбора, ограничения нормы, владения, графа и воспроизводимости."
    },
    {
      "path": "rust/demos/ch33-training-selection/src/main.rs",
      "purpose": "Напечатать только зафиксированный учебный отчёт."
    },
    {
      "path": "rust/demos/ch33-training-selection/src/diagram_trace.rs",
      "region": "training-selection-trace",
      "purpose": "Записать точные обновления, контрольные точки, оси, выбор и проверяемые границы для схемы."
    }
  ],
  "visualization": {
    "decision": "useful",
    "id": "training-validation-checkpoints",
    "rationale": "Дискретная схема контрольных точек позволяет сравнить измерения на обучающей и валидационной выборках, не выдумывая значения между ними. Отметка выбранного состояния и карточки порядка операций показывают, почему выбор подтверждён измерениями, а не просто совпадает с последним обновлением."
  },
  "decoder_connection": "Теперь совокупный декодер умеет выполнить полный план обучения с заранее заданным числом шагов и вернуть зафиксированное состояние, выбранное по валидационной выборке. В главе 34 это состояние будет один раз оценено на тестовой выборке и корректно сопоставлено с зафиксированной базовой моделью."
}
---

import RustSource from "../../../components/RustSource.astro";
import TrainingSelectionDiagram from "../../../components/chapters/TrainingSelectionDiagram.astro";

export const diagramLabels = {
  title: "Разделите обновление параметров и выбор модели по валидации",
  description:
    "Проследите шесть последовательных операций в восьми измеренных обновлениях, затем сравните десять отдельных измерений на обучающей и валидационной выборках в пяти контрольных точках, не проводя между ними выдуманную кривую.",
  sections: {
    order: "Один успешный шаг состоит из шести операций",
    checkpoints: "Измеряйте состояния только в заданных контрольных точках",
    proof: "Разделяйте обучение, выбор модели и тестовую оценку",
  },
  operations: {
    forward: "Прямой проход на обучении",
    backward: "Обратный проход и освобождение графа",
    finiteCheck: "Проверка конечности градиентов",
    clip: "Ограничение общей нормы",
    adamwStep: "Шаг AdamW с заданной скоростью",
    zeroGrad: "Проверка и явное обнуление градиентов новых листовых узлов",
  },
  fields: {
    step: "Шаг",
    trainLoss: "Потери на обучении",
    validationLoss: "Потери на валидации",
    selected: "Выбранное состояние",
    learningRate: "Скорость обучения",
    rawNorm: "Норма до ограничения",
    clippedNorm: "Норма после ограничения",
  },
  cues: {
    trainMeasurement: "Круг: измерение на обучающей выборке",
    validationMeasurement: "Ромб: измерение на валидационной выборке",
    selectedState: "Двойное подчёркивание: состояние, выбранное по валидации",
    selectedCell: "Выбрано",
    gapsUnobserved: "В промежутках нет измеренных значений или интерполированной линии",
    clipped: "Норма ограничена до шага оптимизатора",
    notSelected: "Не выбрано",
    testRejected: "Отклонено до любого обновления",
    verified: "Подтверждено данными запуска",
  },
  proofs: {
    selection: "Первая точка с минимумом на валидации",
    noGrad: "Обучающие и валидационные измерения не записывают граф",
    partitions: "Информационная граница между выборками",
    optimization: "Ограничение нормы, расписание и воспроизводимость",
  },
  captions: {
    order:
      "Цикл освобождает граф обратного прохода, проверяет все градиенты, один раз ограничивает общую норму декодера, применяет заданную скорость, проверяет, что градиенты новых листовых узлов равны нулю, и явно обнуляет их.",
    checkpoints:
      "Таблица и отметки сохраняют точные значения с шестью знаками после запятой. Измерены только шаги 0, 2, 4, 6 и 8; расстояние между ними на схеме не добавляет новых вычислений.",
    proof:
      "Валидация может выбрать сохранённое состояние, но не обновить его, а тестовая выборка остаётся закрытой до следующей главы.",
  },
  scrollers: {
    checkpoints: "Прокручиваемые измерения на обучающей и валидационной выборках",
  },
};

{/* chapter-section:worked-example */}

## Задайте весь план до начала обучения

Выполните все шаги плана обучения декодера с заранее заданным числом
обновлений, измерьте потери на валидационной выборке без записи графа в
фиксированных контрольных точках и восстановите состояние модели, сохранённое
в самой ранней точке с минимальными потерями. При этом не обращайтесь к
тестовым данным. В примере обучается одноблочный декодер из предыдущей главы:
размер словаря $V=5$, ширина
остаточного потока $d_{\mathrm{model}}=4$, две головы внимания, ширина прямого
распространения $d_{\mathrm{ff}}=4$, длина контекста $T=2$ и $144$ обучаемых
скаляра.

Детерминированное обучение выполняет восемь заданных обновлений на
мини-пакетах. Используются четырёхсегментное расписание скорости обучения,
ограничение общей нормы градиента на уровне $0.35$ и измерения после шагов
$0,2,4,6,8$. Ещё до первого прямого прохода зафиксированы следующие решения:

1. пакеты обновлений берутся только из `Train` и следуют в заданном порядке;
2. каждому обновлению назначена одна конечная положительная скорость обучения;
3. успешный шаг следует порядку `forward>backward>finite-check>clip>adamw-step>zero-grad`;
4. обе функции потерь усредняются с весами по числу токенов и измеряются без записи графа;
5. состояние может выбирать только `Validation`, а `Test` на этой границе отклоняется.

Расписание имеет вид

$$
(\eta_1,\ldots,\eta_8)
=(0.040,0.040,0.025,0.025,0.015,0.015,0.008,0.008).
$$

Выполняются все восемь шагов: валидация не останавливает запуск досрочно. В
этом примере пять значений функции потерь на валидации равны

$$
(1.918167,1.696310,1.687788,1.642599,1.595297),
$$

поэтому выбран снимок $s^*=8$. Здесь он случайно совпал с последним
запланированным состоянием, но правило выбора не отдаёт последнему состоянию
никакого предпочтения.

{/* chapter-section:formula */}

## Обновляйте по обучающей выборке, выбирайте по валидационной

Для обновления $s\in\{1,\ldots,8\}$ градиент вычисляется в состоянии до этого
обновления, а параметры и моменты переходят в следующее состояние вместе:

$$
\begin{aligned}g_s&=\nabla_\theta\mathcal{L}_{tr}^{(s)}(\theta_{s-1}),\\
\widetilde g_s&=\frac{c}{\max(c,\lVert g_s\rVert_2)}g_s,\\
(\theta_s,m_s,v_s)&=\operatorname{AdamW}_{\eta_s}\!\left(\theta_{s-1},\widetilde g_s,m_{s-1},v_{s-1}\right),\quad s=1,\ldots,8,\\
s^*&=\min\left\{s\in\mathcal{C}:\mathcal{L}_{va}(\theta_s)=\min_{k\in\mathcal{C}}\mathcal{L}_{va}(\theta_k)\right\}\end{aligned}
$$

У двух функций потерь разные задачи. Градиент берётся из
$\mathcal{L}_{tr}^{(s)}$ и изменяет параметры. Значение $\mathcal{L}_{va}$
измеряется только в заданных контрольных точках и может заменить сохранённое
состояние, но по нему не выполняют дифференцирование и оно не обновляет декодер.

Для градиента $g_s$ до ограничения и верхнего предела $c=0.35$ ко всем
тензорам параметров применяется один общий множитель:

$$
\alpha_s=\frac{c}{\max(c,\lVert g_s\rVert_2)},
\qquad \widetilde g_s=\alpha_s g_s.
$$

Поскольку $c>0$, знаменатель не обращается в ноль. При нулевой или уже
достаточно малой норме $\alpha_s=1$, а большая норма уменьшается до $c$. На
каждой заданной границе между участками расписания AdamW сохраняет накопленные
моменты и счётчик шагов, но начинает использовать новое значение $\eta_s$;
смена скорости не перезапускает оптимизатор.

Потери на валидации взвешиваются по числу предсказываемых токенов, а не по
числу пакетов. Если валидационный пакет $j$ содержит $n_j$ целевых токенов и
имеет среднее значение $\mathcal{L}^{(j)}_{va}$, то

$$
\mathcal{L}_{va}
=\frac{\sum_j n_j\mathcal{L}^{(j)}_{va}}{\sum_j n_j}.
$$

Выбор выполняется только по измеренному множеству
$\mathcal{C}=\{0,2,4,6,8\}$. При точном равенстве строгое сравнение сохраняет
более раннее состояние:

$$
s^*=\min\left\{s\in\mathcal{C}:\mathcal{L}_{va}(\theta_s)
=\min_{k\in\mathcal{C}}\mathcal{L}_{va}(\theta_k)\right\}.
$$

{/* chapter-section:symbol-glossary */}

## Не смешивайте шаги, градиенты и роли выборок

- $\theta_0$ — начальное состояние декодера, а $\theta_s$ — состояние после обновления $s$.
- $s\geq1$ — нумеруемый с единицы индекс обновления; среди контрольных точек есть и $0$.
- $\mathcal{L}_{tr}^{(s)}$ — функция потерь следующего токена для обучающего мини-пакета $s$.
- $g_s=\nabla_\theta\mathcal{L}_{tr}^{(s)}(\theta_{s-1})$ — конечный градиент до ограничения нормы.
- $\widetilde g_s$ — градиент после ограничения общей нормы, который получает AdamW.
- $c$ — положительный верхний предел общей нормы.
- $\eta_s$ — заранее заданная скорость обучения для обновления $s$.
- $m_s$ и $v_s$ — состояния первого и второго моментов Adam, сохраняемые между шагами.
- $\mathcal{L}_{va}$ — функция потерь на валидации без записи графа, используемая только для выбора.
- $\mathcal{C}$ — множество индексов измеренных контрольных точек.
- $s^*$ — самая ранняя измеренная точка с минимальными потерями на валидации.

В программе `Train`, `Validation` и `Test` — конкретные варианты `Partition`.
Их смысловые роли таковы: обучающая выборка подгоняет параметры,
валидационная выбирает состояние, а тестовая один раз оценивает уже выбранную
модель. Меньшие потери на обучающей выборке не заменяют независимое
свидетельство на отложенной валидационной выборке.

Замена листовых узлов создаёт ещё одну задачу владения. AdamW заменяет элементы
списка параметров новыми листовыми узлами. Перед следующим прямым проходом
эмбеддинг, блок, нормализация и общая выходная проекция декодера должны быть
заново связаны именно с этими узлами. Замена отдельного реестра не обновила бы
ссылки внутри компонентов, и они продолжили бы использовать старые параметры.

{/* chapter-section:history */}

## От отчёта по обучению к контрольной точке LLM, выбранной по валидации

Следующий этап — перейти от подгонки модели и отчёта по одному обученному
состоянию к заранее заданным обновлениям на мини-пакетах. Такие обновления
периодически создают состояния-кандидаты для валидации, а отдельная тестовая
выборка остаётся закрытой. Эти приёмы — часть пути к современным LLM.

[A Neural Probabilistic Language Model](https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf)
показывает ранний пример такого разделения данных в нейронной языковой модели.
Бенжио и соавторы разделяют обучающий, валидационный и тестовый текст, явно
связывают валидацию с выбором модели и ранней остановкой и описывают
стохастические обновления по одному примеру для нейронной языковой модели
прямого распространения. Обновления по всему корпусу или по одному примеру и
отчёт только по обучающей выборке сами по себе не задают масштабируемую
периодичность обновлений и независимое правило выбора между состояниями
языковой модели.

[Sequence to Sequence Learning with Neural Networks](https://arxiv.org/pdf/1409.3215)
добавляет практические средства управления обучением последовательностей.
Суцкевер, Виньялс и Ле сообщают о пакетах последовательностей, заранее заданном
правиле уменьшения скорости обучения и масштабировании градиента, когда его
общая норма превышает фиксированный порог в рекуррентной модели
последовательностей.

[Attention Is All You Need](https://arxiv.org/pdf/1706.03762) переносит эти идеи
в эпоху Transformer. Васвани и соавторы обучают Transformer на пакетах с
заданным числом токенов, используют зависящее от номера шага расписание с
разогревом и убыванием обратно пропорционально квадратному корню и периодически
записывают контрольные точки. Использованное ими усреднение контрольных точек не совпадает с выбором
валидационного минимума в этой главе.

[Exploring the Limits of Transfer Learning with a Unified Text-to-Text Transformer](https://www.jmlr.org/papers/volume21/20-074/20-074.pdf)
прямо описывает выбор контрольной точки по результатам валидации. Раффель и
соавторы сохраняют контрольные точки дообучения с фиксированной периодичностью,
выбирают лучшую по результату на валидации и прямо избегают использования
тестовой выборки для выбора модели.

[Language Models are Few-Shot Learners](https://arxiv.org/pdf/2005.14165)
показывает, как эти приёмы применяются при крупномасштабном обучении модели
только с декодером. Браун и соавторы переносят Adam, расписание скорости
обучения, масштабирование размера пакета в зависимости от числа токенов и
ограничение общей нормы градиента в обучение языковой модели только с декодером
масштаба GPT-3.

В работах по нейронным языковым моделям роли обучающей, валидационной и тестовой
выборок были разделены; системы для последовательностей и Transformer добавили
мини-пакеты, явные расписания, ограничение нормы и периодически сохраняемые
состояния; в более поздних работах с преобразованием текста в текст прямо
указано, что контрольную точку выбирают по валидации, не используя тестовые
данные для выбора модели. При обучении LLM только с декодером многократно
формируют пакеты токенов, дифференцируют обучающую цель, контролируют величину
градиента, применяют расписание скорости обучения и оценивают
состояния-кандидаты на отложенной валидационной выборке, оставляя результаты на
тестовой выборке для последующей однократной оценки.

Путь к современному обучению LLM объединяет строгое разделение ролей выборок,
воспроизводимые пакеты, конечные градиенты, контроль нормы, явное расписание
скорости обучения, периодическую валидацию без записи графа и выбор контрольной
точки. В этих работах используются разные архитектуры и схемы обучения, поэтому
фиксированное начальное значение генератора, точно заданная периодичность и
выбор самой ранней точки при равенстве — локальные учебные решения, а не
общепринятая практика. Небольшой пример в коде показывает, зачем нужен отдельный
сигнал выбора: ряд обучающих потерь выбирает последнюю точку, тогда как
валидационный ряд может выбрать более раннюю.

<RustSource
  path="rust/demos/ch33-training-selection/src/lib.rs"
  region="historical-selection-contrast"
  caption="Сопоставьте выбор по обучающим потерям с выбором по независимому валидационному ряду"
  label="Исходный код Rust для исторического сопоставления практик обучения LLM в главе 33"
/>

{/* chapter-section:rust-implementation */}

## Явно задайте полный план обучения в Rust

`LearningRateSchedule` хранит ровно одну конечную положительную скорость для
каждого обновления. `TrainerConfig` требует измерения при $s=0$, строго
возрастающих номеров контрольных точек, последней точки после заключительного
обновления и положительного конечного предела общей нормы. Порядок операций
представлен данными, а не скрыт в логике цикла.

<RustSource
  path="rust/crates/llm-from-scratch/src/training/trainer.rs"
  region="training-plan"
  caption="Проверьте расписание, контрольные точки, предел нормы и порядок обновления до начала выполнения"
  label="Исходный код Rust, задающий план обучения главы 33"
/>

Для оценки без записи графа нужна настоящая граница ленты. Вызов `value` или
`detach` после прямого прохода был бы запоздалым: промежуточный граф уже успел
бы появиться. Область `no_grad`, локальная для потока выполнения, подавляет родительские
рёбра при создании каждой операции, поддерживает вложенность и восстанавливает
запись при выходе, в том числе из-за паники.

<RustSource
  path="rust/crates/llm-from-scratch/src/autograd/tensor_core.rs"
  region="no-grad-scope"
  caption="Подавите рёбра обратного режима во вложенной и безопасной при панике области"
  label="Исходный код Rust с поддержкой оценки главы 33 без записи графа"
/>

`evaluate_no_grad` вычисляет среднее за эпоху с весами по числу токенов и
проверяет, что ни один результат не отслеживает градиент. До оценки сохраняются
биты градиентов; изменение хотя бы одного бита отклоняет вызов.

<RustSource
  path="rust/crates/llm-from-scratch/src/training/trainer.rs"
  region="no-grad-evaluation"
  caption="Измерьте взвешенные по токенам потери без записи графа и изменения градиентов"
  label="Исходный код Rust для оценки эпохи главы 33 без записи графа"
/>

Цикл обучения вызывает обычный метод `AdamW::step_with_learning_rate`, а не
`AdamW::step_with_learning_rate_and_trace`, поскольку ему нужен только
возвращаемый номер шага оптимизатора. До изменения текущего состояния метод
проверяет скорость обучения для очередного обновления и подготавливает новые
значения моментов, следующее значение счётчика шагов и полный набор замен
параметров. Метод фиксирует новые моменты, счётчик и параметры только после
успешной подготовки всего набора замен. Затем он возвращает новый номер шага
оптимизатора, и цикл напрямую сравнивает его с номером обновления в плане. В
главе 22 явно запрашивается одна трассировка с данными по каждому параметру,
поскольку там разбираются все слагаемые расчёта AdamW; этому циклу обучения
такие подробные данные не нужны. Затем цикл обучения проверяет новые листовые
узлы, созданные при замене параметров. `gradient()` предоставляет временный
доступ только для чтения к тензору градиента одного параметра. Проверив, что
каждое значение этого тензора равно нулю, цикл завершает временный доступ.
Только после этого `zero_grad()` получает изменяемый доступ к тому же тензору и
обнуляет его. Такой порядок позволяет отдельно показать градиенты, изначально
равные нулю в новых листовых узлах, и последующую операцию очистки.

<RustSource
  path="rust/crates/llm-from-scratch/src/training/adamw.rs"
  region="adamw-execution-and-trace-api"
  caption="Выполните обычное обновление AdamW со скоростью из расписания и получите новый номер шага оптимизатора"
  label="Исходный код Rust, различающий обычные обновления AdamW и обновления с трассировкой в главе 33"
/>

Поскольку AdamW заменяет листовые узлы, `DecoderModel::from_parameters` заново
собирает каждый компонент из одного вектора параметров в стабильном порядке.
Он отклоняет расхождение числа, имён и форм компонентов и сохраняет один общий
листовой узел эмбеддингов для выбора строк и выходной проекции.

<RustSource
  path="rust/crates/llm-from-scratch/src/models/decoder.rs"
  region="decoder-parameter-rebuild"
  caption="Заново свяжите каждый компонент декодера с новыми листовыми узлами оптимизатора"
  label="Исходный код Rust для повторного связывания декодера после шага оптимизатора"
/>

`train_decoder` сначала отклоняет выборки с неверными ролями и несовместимые
эпохи. Затем он создаёт глубокую копию модели, переданной вызывающей стороной,
и независимо копирует оптимизатор, выполняет все обновления, измеряет только
заданные контрольные точки, заменяет выбранный снимок лишь при строгом
уменьшении валидационных потерь и в конце восстанавливает этот снимок. Исходные
модель и оптимизатор остаются неизменными даже при ошибке.

<RustSource
  path="rust/crates/llm-from-scratch/src/training/trainer.rs"
  region="complete-training-loop"
  caption="Выполните все обновления, сверяя возвращаемые номера шагов оптимизатора с соответствующими номерами обновлений в плане, и восстановите состояние модели, сохранённое в самой ранней точке с минимальными потерями на валидации, не обращаясь к тестовым данным"
  label="Исходный код Rust с полным циклом обучения главы 33"
/>

Детерминированный пример дважды выполняет весь план, проверяет побитовую
воспроизводимость, подтверждает, что ограничение нормы сработало, подтверждает
отсутствие графов при измерении и сравнивает восстановленную модель с битами
сохранённых параметров.

Для точного сравнения состояний нужен собственный вектор битовых представлений,
а не отдельные копии тензоров параметров. `parameter_bits` временно читает каждый
тензор параметра, преобразует его скалярные значения в `u64` и после завершения
чтения сохраняет только полученный вектор битовых представлений.

<RustSource
  path="rust/demos/ch33-training-selection/src/lib.rs"
  region="learner-evidence"
  caption="Соберите свидетельства ограничения нормы, графа, владения, выбора и воспроизводимости из двух запусков"
  label="Исходный код Rust, собирающий учебные свидетельства главы 33"
/>

<RustSource
  path="rust/demos/ch33-training-selection/src/main.rs"
  caption="Напечатайте только зафиксированный учебный отчёт главы 33"
  label="Точка входа Rust для учебного отчёта главы 33"
/>

Выполните `cargo run --quiet --locked -p ch33-training-selection`. Отчёт
перечисляет пять измеренных контрольных точек, выбранное по валидации состояние,
свидетельства ограничения и очистки градиентов, подтверждение отклонения попытки
передать тестовую выборку, проверки владения и побитовую воспроизводимость.

{/* chapter-section:visualization */}

## Читайте только измеренные точки и не дорисовывайте кривую

Записанный запуск содержит фиксированную конфигурацию, порядок из шести
операций, четыре сегмента расписания, все восемь обновлений, ось по измеренным
потерям, пять пар контрольных точек, одну запись выбора и проверенные границы.
Вместе они показывают связь оптимизации с выбором модели, не заполняя
неизмеренные шаги.

<RustSource
  path="rust/demos/ch33-training-selection/src/diagram_trace.rs"
  region="training-selection-trace"
  caption="Запишите измеренные обновления, контрольные точки, выбор и поля проверок"
  label="Исходный код Rust с данными о выборе состояния в главе 33"
/>

<TrainingSelectionDiagram labels={diagramLabels} />

На схеме десять отдельных отметок: одно измерение на обучающей и одно на
валидационной выборке в каждой из пяти контрольных точек. Между ними нет
отрезков. Линия утверждала бы существование значений на неизмеренных шагах,
поэтому точная таблица остаётся основой сравнения.

При $s=0$ потери равны $2.095016$ и $1.918167$, а при $s=8$ — $1.322897$ и
$1.595297$. Двойное подчёркивание валидационной отметки показывает $s^*=8$,
поскольку $1.595297$ — наименьшее измеренное значение. Запись выбора также
содержит `criterion=validation-only`, `snapshot=true` и
`test_partition_rejected=true`.

Во всех обновлениях именно этого примера общая норма градиента до ограничения
превышает $0.35$, поэтому AdamW каждый раз получает норму $0.35$. Это свойство
конкретного запуска, а не требование к каждому обновлению реальной LLM. Общий
цикл принимает и конечные градиенты, норма которых уже ниже предела.

{/* chapter-section:exercises */}

## Проверьте информационную границу и владение состоянием

1. Замените роль эпохи обновлений `Train` на `Test`. Предскажите ошибку предварительной проверки и объясните, почему исходные декодер и оптимизатор не меняются.
2. Замените контрольные точки $[0,2,4,6,8]$ на $[0,2,4,6]$. Объясните, зачем после всех запланированных обновлений нужна заключительная точка.
3. Возьмите валидационные потери $[1.8,1.6,1.6]$. Определите, какую точку сохранит строгое сравнение, и свяжите ответ с определением $s^*$.
4. Усредните средние значения двух пакетов без весов, хотя числа токенов различаются. Сравните результат с формулой взвешивания по токенам.
5. После AdamW замените только реестр параметров, оставив ссылки компонентов без изменений. Определите, какое состояние использует следующий прямой проход.
6. Проведите линию между пятью валидационными отметками. Перечислите добавленные ею неизмеренные утверждения, затем удалите линию и вернитесь к дискретным свидетельствам.

<details>
  <summary>Проверьте рассуждение</summary>

  1. `Test` отклоняется до прямого прохода, потому что тестовая выборка не может обновлять или выбирать модель.
  2. Расписание контрольных точек должно включать последнее запланированное обновление, чтобы у завершённого запуска было заключительное состояние для сравнения.
  3. Побеждает первое значение, равное минимуму: состояние заменяется только при строгом уменьшении.
  4. Средние значения пакетов нужно взвесить пропорционально фактическому числу предсказываемых токенов.
  5. Старые ссылки компонентов продолжат выполнять вычисления со старыми листовыми узлами, даже если отдельный реестр уже содержит новые значения.
  6. Обоснованы только десять измеренных отметок и точная таблица; значения между контрольными точками неизвестны.
</details>

{/* chapter-section:decoder-connection */}

## Зафиксируйте выбранный декодер для одной тестовой оценки

Теперь совокупный декодер умеет выполнить полный план обучения с заранее
заданным числом шагов и вернуть зафиксированное состояние, выбранное по
валидационной выборке. В главе 34 это состояние будет один раз оценено на
тестовой выборке и корректно сопоставлено с зафиксированной базовой моделью.

После просмотра тестового результата нельзя менять расписание, предел нормы,
число обновлений или выбранную контрольную точку. Глава 33 заканчивается
завершённым выбором модели и проверенным отклонением `Test` на границе обучения.
Глава 34 откроет отложенную тестовую выборку ровно один раз для оценки, а не для
выбора.
