---
{
  "chapter_id": "24-residual-connections",
  "locale": "ru",
  "content_revision": 2,
  "order": 24,
  "concept_id": "residual-connections",
  "title": "Для каждого обучаемого обновления сохраняйте тождественный путь",
  "description": "Проследите остаточное сложение при точном совпадении форм, тождественный путь градиента и путь через обучаемую ветвь, обучение ветви с нулевыми весами и повторные преобразования с остаточными связями и без них.",
  "objective": "Добавить остаточную ветвь без изменения формы и проверить тождественный путь и прохождение градиента через цепочку преобразований.",
  "worked_inputs": "Возьмите $x=[2,-1]$ и квадратную линейную ветвь без смещения, для которой $F(x)=[-1,-2.25]$. Сначала предскажите $y=x+F(x)$, а затем — что сохранится в прямом и обратном проходах, если все веса ветви равны нулю.",
  "formula": {
    "latex": "y=x+F(x)",
    "symbols": [
      {
        "symbol": "x",
        "meaning": "входной тензор, который переносится по тождественному пути"
      },
      {
        "symbol": "F",
        "meaning": "обучаемое отображение остаточной ветви"
      },
      {
        "symbol": "F(x)",
        "meaning": "обновление ветви, имеющее в точности ту же форму, что и x"
      },
      {
        "symbol": "y",
        "meaning": "поэлементная сумма той же формы, что и x"
      }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "neural-architecture",
      "limitation": "Хэ и соавторы обнаружили эффект деградации: более глубокие сети без остаточных связей могли иметь большую ошибку на обучающей выборке, хотя добавленные слои в принципе должны были уметь реализовать тождественное отображение.",
      "later_advance": "Остаточное обучение явно выделило тождественный путь, а позднее в Transformer остаточное сложение стало охватывать каждый подслой внимания и сети прямого распространения при общей ширине модели.",
      "modern_llm_role": "Transformer только с декодером сохраняет остаточный поток через всю цепочку блоков, а обучаемые ветви внимания и сети прямого распространения вносят обновления той же формы; в следующих главах добавятся нормализация и сборка этих ветвей.",
      "sources": [
        {
          "role": "earlier",
          "year": 2015,
          "name": "He et al., Deep Residual Learning for Image Recognition",
          "source_url": "https://arxiv.org/pdf/1512.03385",
          "claim": "Хэ и соавторы описывают деградацию глубоких сетей без остаточных связей и представляют блок неизменной размерности как сумму обучаемой остаточной функции и тождественной обходной связи без параметров."
        },
        {
          "role": "later",
          "year": 2017,
          "name": "Vaswani et al., Attention Is All You Need",
          "source_url": "https://arxiv.org/pdf/1706.03762",
          "claim": "Васвани и соавторы помещают остаточную связь вокруг каждого подслоя энкодера и декодера и сохраняют общую ширину выходов подслоёв, необходимую для сложения."
        }
      ]
    },
    "approach": "От глубоких преобразований без остаточных связей, которые трудно оптимизировать, — через явные тождественные обходные связи — к остаточному потоку вокруг подслоёв Transformer",
    "summary": "Остаточное сложение даёт каждому подслою Transformer, сохраняющему ширину модели, явный тождественный путь наряду с обучаемым обновлением. Этот путь сохраняет непосредственный вклад в прямой проход и добавляет непосредственный вклад в градиент при обратном проходе, но не гарантирует успешного обучения любой глубокой модели.",
    "rust_source": "rust/demos/ch24-residual-connections/src/lib.rs"
  },
  "rust_sources": [
    {
      "path": "rust/demos/ch24-residual-connections/src/lib.rs",
      "region": "residual-stack",
      "purpose": "Сравнить одну и ту же последовательность преобразований с фиксированными значениями в обычной и остаточной цепочках."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/nn/residual.rs",
      "region": "residual-add",
      "purpose": "Перед дифференцируемым сложением потребовать точного совпадения форм и сохранить оба ребра операндов."
    },
    {
      "path": "rust/demos/ch24-residual-connections/src/lib.rs",
      "region": "residual-fixture",
      "purpose": "Показать известные значения прямого и обратного проходов, параметры ветви и поведение ветви с нулевыми весами."
    },
    {
      "path": "rust/demos/ch24-residual-connections/src/lib.rs",
      "region": "residual-gradcheck",
      "purpose": "Сверить градиенты входа и весов ветви с выборочными центральными разностями."
    },
    {
      "path": "rust/demos/ch24-residual-connections/src/lib.rs",
      "region": "learner-residual-report",
      "purpose": "Сформировать полный детерминированный отчёт по проверенному примеру."
    },
    {
      "path": "rust/demos/ch24-residual-connections/src/main.rs",
      "purpose": "Вывести детерминированный отчёт по примеру."
    },
    {
      "path": "rust/demos/ch24-residual-connections/src/diagram_trace.rs",
      "region": "residual-connections-trace",
      "purpose": "Вывести точные записи прямого и обратного проходов, цепочек и проверенных свойств."
    }
  ],
  "visualization": {
    "decision": "useful",
    "id": "residual-connections",
    "rationale": "На схеме с разветвлением и последующим слиянием проще различить два пути прямого прохода и два вклада обратного, а таблица по глубине позволяет напрямую сопоставить повторные преобразования с остаточными связями и без них."
  },
  "decoder_connection": "Теперь в собираемом декодере есть операция слияния, требующая точного совпадения форм; благодаря ей единый остаточный поток пройдёт в обход обучаемых подслоёв. В главе 25 значения на входе каждой ветви будут нормализованы, а тождественный путь минует эту нормализацию."
}
---

import RustSource from '../../../components/RustSource.astro';
import ResidualConnectionsDiagram from '../../../components/chapters/ResidualConnectionsDiagram.astro';

export const diagramLabels = {
  title: 'Проследите тождественный путь и обучаемое обновление',
  description:
    'Проследите точные значения примера через прямое слияние и оба вклада обратного прохода, затем сопоставьте ветвь с нулевыми весами, ошибку формы и две четырёхслойные цепочки.',
  sections: {
    forward: 'Разведите и снова объедините пути в прямом проходе',
    backward: 'Сложите два вклада обратного прохода',
    evidence: 'Проверьте параметры, тождественный путь и формы',
    stack: 'Сравните повторяющиеся обычные и остаточные преобразования',
  },
  paths: {
    input: 'Вход',
    identity: 'Тождественный путь',
    branch: 'Обучаемая ветвь',
    merge: 'Сложение',
    output: 'Выход остаточной связи',
    upstream: 'Входящий градиент',
    identityGradient: 'Вклад тождественного пути',
    branchGradient: 'Вклад ветви',
    inputGradient: 'Градиент по входу',
  },
  fields: {
    fixture: 'Пример с заданными значениями',
    parameter: 'Параметры ветви',
    parameterGradient: 'Градиент весов',
    zeroBranch: 'Ветвь с нулевыми весами',
    zeroBranchNote: 'В прямом проходе выход совпадает со входом, полный градиент по входу равен входящему градиенту, а градиент весов ветви остаётся ненулевым.',
    shapeInvariant: 'Формы остаточных путей должны точно совпадать',
    shapeError: 'Согласование форм здесь недопустимо',
    genericAdd: 'Обычное сложение',
    residualMerge: 'Остаточное сложение',
    depth: 'Глубина',
    plain: 'Без остаточных связей',
    residual: 'С остаточными связями',
    inputGradient: 'Градиент по входу',
    numericCheck: 'Выборочная проверка градиента',
    proof: 'Проверенные свойства остаточной связи',
  },
  cues: {
    identity: 'Сплошная линия — тождественный путь',
    branch: 'Пунктирная линия — обучаемая ветвь',
    merge: 'Двойная рамка — сложение',
    accepted: 'Допустимо',
    rejected: 'Отклонено',
  },
  captions: {
    forward: 'Точные значения одинаковой формы в прямом проходе',
    backward: 'Тождественный вклад и векторно-якобианное произведение ветви складываются',
    stack: 'Точные значения на каждом уровне глубины',
  },
  scrollers: {
    forward: 'Прокручиваемая схема прямого прохода через остаточную связь',
    backward: 'Прокручиваемая схема обратного прохода через остаточную связь',
    stack: 'Прокручиваемая таблица глубины цепочек с остаточными связями и без них',
  },
};

{/* chapter-section:worked-example */}

## Предскажите, что сохраняет тождественный путь

Возьмите $x=[2,-1]$ и квадратную линейную ветвь без смещения, выход которой
равен $F(x)=[-1,-2.25]$. Перед запуском примера предскажите $y=x+F(x)$.
Поэлементное сложение даёт $[1,-3.25]$ и сохраняет форму $[2]$.

Теперь обнулите веса ветви. После этого ветвь тождественно равна нулю, а не
только обращается в ноль на данном входе. Для входящего градиента
$\bar y=[1,1]$ предскажите три величины: выход, градиент по входу и градиент весов
ветви. Первые две в точности следуют тождественному пути. Градиент весов при
этом может оставаться ненулевым, поскольку изменение нулевого веса изменило бы
выход ветви.

{/* chapter-section:formula */}

## Добавьте обучаемое обновление к неизменённому потоку

Вся остаточная связь задаётся формулой:

$$
y=x+F(x)
$$

Полные формы обоих слагаемых должны совпадать:

$$
\operatorname{shape}(F(x))=\operatorname{shape}(x)=\operatorname{shape}(y)
$$

Это условие строже обычного согласования форм (broadcasting). Ветвь формы $[2]$
можно согласовать с входом формы $[2,2]$ при обычном сложении, но она не
принадлежит тому же остаточному потоку, поэтому в этой главе такое сложение
отклоняется.

В обратном режиме вычисление проходит по обоим родительским рёбрам и складывает
их вклады:

$$
\bar{x}=\bar{y}+J_F(x)^\top\bar{y}
$$

Тождественный путь напрямую вносит $\bar y$. Ветвь вносит своё
векторно-якобианное произведение $J_F(x)^\top\bar y$. Эти вклады могут усиливать
друг друга, взаимно сокращаться или увеличивать итог, поэтому тождественный
член не гарантирует, что каждый градиент останется большим или будет вести себя
устойчиво.

Для понимания полезна форма с коэффициентом:

$$
y=x+\alpha F(x)
$$

При $\alpha=0$ в прямом проходе остаётся только тождественный член; при
$\alpha=1$ получается обычная остаточная связь, реализованная в этой главе.
В этой главе отдельная политика масштабирования остаточной ветви не вводится.

{/* chapter-section:symbol-glossary */}

## Не смешивайте роли потока и обновления

- $x$ — входной тензор, который тождественный путь передаёт без изменений.
- $F$ — обучаемое отображение ветви; позднее им может стать внимание или сеть прямого распространения.
- $F(x)$ — обновление ветви, форма которого в точности совпадает с формой $x$.
- $y$ — их поэлементная сумма той же формы.
- $\bar y$ — входящая сопряжённая величина выхода, а $\bar x$ — накопленная сопряжённая величина входа.
- $J_F(x)^\top\bar y$ — вклад ветви при обратном проходе.
- $\alpha$ — необязательный поясняющий коэффициент, а не обучаемое значение в этой реализации.

Сложение — это не конкатенация: размер оси признаков не увеличивается. Это также
не нормализация: среднее, дисперсия и среднеквадратическое значение не
вычисляются.

{/* chapter-section:history */}

## От глубоких преобразований без остаточных связей к остаточному потоку Transformer

Хэ и соавторы обнаружили эффект деградации: более глубокие сети без остаточных
связей могли иметь большую ошибку на обучающей выборке, хотя добавленные слои в
принципе должны были уметь реализовать тождественное отображение.

Этот результат описан в первоисточнике — [Хэ и соавторы, *Deep Residual Learning for Image Recognition*](https://arxiv.org/pdf/1512.03385). Хэ и соавторы
описывают деградацию глубоких сетей без остаточных связей и представляют блок
неизменной размерности как сумму обучаемой остаточной функции и тождественной
обходной связи без параметров. Остаточное обучение явно выделило тождественный
путь, а позднее в Transformer остаточное сложение стало охватывать каждый
подслой внимания и сети прямого распространения при общей ширине модели.

Следующий первоисточник — [Васвани и соавторы, *Attention Is All You Need*](https://arxiv.org/pdf/1706.03762). Васвани и соавторы
помещают остаточную связь вокруг каждого подслоя энкодера и декодера и сохраняют
общую ширину выходов подслоёв, необходимую для сложения. В исходной архитектуре
Transformer за каждой суммой следует LayerNorm. В этом курсе позднее будет
собран декодер с предварительной RMSNorm, поэтому статья не служит подтверждением
такого порядка операций.

Transformer только с декодером сохраняет остаточный поток через всю цепочку
блоков, а обучаемые ветви внимания и сети прямого распространения вносят
обновления той же формы; в следующих главах добавятся нормализация и сборка
этих ветвей.

Остаточное сложение даёт каждому подслою Transformer, сохраняющему ширину модели,
явный тождественный путь наряду с обучаемым обновлением. Этот путь сохраняет
непосредственный вклад в прямой проход и добавляет непосредственный вклад в
градиент при обратном проходе, но не гарантирует успешного обучения любой
глубокой модели.

Остаточное обучение сделало тождественный обходной путь явным в глубоких сетях
для распознавания изображений. Затем архитектуры Transformer применили тот же
механизм вокруг подслоёв внимания и сетей прямого распространения. Сравнение
четырёх слоёв выделяет одно архитектурное следствие: каждый обычный слой даёт
только результат обучаемого преобразования, а каждый остаточный слой добавляет
этот результат к явному тождественному пути:

<RustSource
  path="rust/demos/ch24-residual-connections/src/lib.rs"
  region="residual-stack"
  caption="Сравните повторяющиеся обычные и остаточные преобразования"
  label="Исходный код Rust для исторического сравнения нейросетевых архитектур в главе 24"
/>

{/* chapter-section:rust-implementation */}

## Перед сложением отклоните несовпадающие формы

Переиспользуемая функция не владеет параметрами. Сначала она сравнивает полные
формы, а затем выполняет допустимое слияние с помощью уже реализованной
дифференцируемой операции сложения. Благодаря этому в ленте операций сохраняются
оба ребра от операндов и, следовательно, оба вклада при обратном проходе:

<RustSource
  path="rust/crates/llm-from-scratch/src/nn/residual.rs"
  region="residual-add"
  caption="Потребуйте точного совпадения форм и сохраните оба ребра графа"
  label="Исходный код Rust, реализующий остаточное сложение из главы 24"
/>

Пример с квадратным слоем `Linear` и заданными значениями владеет параметром
`residual.branch.weight`. В прямом проходе он выдаёт $F(x)=[-1,-2.25]$. При
$\bar y=[1,1]$ тождественный вклад равен $[1,1]$, вклад ветви —
$[-0.5,2.25]$, а их сумма — $\bar x=[0.5,3.25]$. Градиент матрицы весов ветви в
построчном порядке равен $[2,2,-1,-1]$:

<RustSource
  path="rust/demos/ch24-residual-connections/src/lib.rs"
  region="residual-fixture"
  caption="Покажите известные градиенты остаточной ветви и ветви с нулевыми весами"
  label="Исходный код Rust для примера прямого и обратного проходов в главе 24"
/>

Ветвь с нулевыми весами выдаёт $F(x)=0$ для любого входа, поэтому её вклад в
градиент по входу здесь равен нулю. Градиент её весов остаётся
$[2,2,-1,-1]$. Нельзя переносить этот вывод на ветвь, выход которой случайно
обращается в ноль только в одной точке: из $F(x)=0$ само по себе не следует
$J_F(x)=0$.

Численная проверка вычисляет независимую реализацию скалярной целевой функции
$\sum_i(x+xW)_i^2$ на обычных тензорах в каждой возмущённой координате, а затем
проверяет обе координаты входа и все четыре координаты весов:

<RustSource
  path="rust/demos/ch24-residual-connections/src/lib.rs"
  region="residual-gradcheck"
  caption="Проверьте градиенты входа и весов ветви численно"
  label="Исходный код Rust для выборочной проверки градиентов в главе 24"
/>

Функция формирования отчёта принимает проверенный пример и печатает
детерминированный текст:

<RustSource
  path="rust/demos/ch24-residual-connections/src/lib.rs"
  region="learner-residual-report"
  caption="Сформируйте полный отчёт по главе 24"
  label="Исходный код Rust, формирующий ожидаемый вывод главы 24"
/>

<RustSource
  path="rust/demos/ch24-residual-connections/src/main.rs"
  caption="Выведите детерминированный отчёт"
  label="Точка входа Rust для отчёта по главе 24"
/>

Выполните `cargo run --quiet --locked -p ch24-residual-connections`. Стандартный
вывод зафиксирован в `rust/demos/ch24-residual-connections/expected.txt`.

{/* chapter-section:visualization */}

## Проследите оба пути и не потеряйте место сложения

Трассировка содержит каждый показанный ниже вектор, решение о форме, строку
цепочки, проверку градиента и проверенное свойство остаточной связи. Две строки
схемы обозначают одновременно действующие пути: значение по тождественному пути
проходит без изменений, обучаемая ветвь создаёт обновление, а в месте слияния они
складываются покоординатно:

<RustSource
  path="rust/demos/ch24-residual-connections/src/diagram_trace.rs"
  region="residual-connections-trace"
  caption="Выведите точные записи прямого и обратного проходов, цепочек и проверенных свойств"
  label="Исходный код Rust, формирующий трассировку схемы главы 24"
/>

<ResidualConnectionsDiagram labels={diagramLabels} />

Верхний путь со сплошной рамкой переносит тождественное значение, нижний путь с
пунктирной рамкой — обучаемое обновление, а сложение в месте их встречи отмечено
двойной рамкой. В обратном проходе такое же разветвление показывает, почему два
вклада в градиент складываются.

Каждая из четырёх диагональных матриц ветвей умножает свой вход на $-0.25$.
После четырёх обычных слоёв градиент по входу сохраняет множитель
$0.00390625$, а после четырёх остаточных — $0.31640625$. Эти точные значения
игрушечного примера показывают, что в данном случае меняют явные тождественные
члены. Они не задают универсальной границы для оптимизации.

{/* chapter-section:exercises */}

## Сначала сделайте предсказания

1. Вычислите $y$ для заданных $x$ и $F(x)$.
2. Разделите $\bar x$ на тождественный вклад и вклад ветви.
3. Предскажите выход ветви с нулевыми весами, градиент по входу и градиент весов.
4. Определите, образуют ли формы $[2,2]$ и $[2]$ допустимую остаточную связь.
5. Предскажите градиент, если обоими операндами сложения служит один и тот же отслеживаемый тензор.
6. Вычислите множители обычной и остаточной цепочек глубины четыре при коэффициенте ветви $-0.25$.
7. Объясните, почему остаточное сложение не конкатенирует и не нормализует признаки.
8. Определите, гарантирует ли тождественный путь успешное обучение глубокой модели.

<details>
  <summary>Проверить предсказания</summary>

  1. $y=[1,-3.25]$.
  2. $\bar x=[1,1]+[-0.5,2.25]=[0.5,3.25]$.
  3. Выход равен $[2,-1]$, градиент по входу — $[1,1]$, а градиент весов — $[2,2,-1,-1]$; последний ненулевой.
  4. Нет. Обычное сложение может согласовать эти формы, но остаточное сложение требует их точного совпадения.
  5. Два разных ребра операндов приходят к одному листовому тензору, поэтому он получает удвоенный входящий градиент.
  6. Для обычной цепочки множитель равен $(-0.25)^4=0.00390625$, а для остаточной — $(1-0.25)^4=0.31640625$.
  7. Сложение сохраняет ширину признаков и не вычисляет статистик нормализации.
  8. Нет. Тождественный член создаёт прямой путь, но якобиан ветви по-прежнему может сократить, усилить или дестабилизировать итоговый градиент.
</details>

{/* chapter-section:decoder-connection */}

## Далее нормализуйте вход ветви

Теперь в собираемом декодере есть операция слияния, требующая точного совпадения
форм; благодаря ей единый остаточный поток пройдёт в обход обучаемых подслоёв.
В главе 25 значения на входе каждой ветви будут нормализованы, а тождественный
путь минует эту нормализацию.

Сейчас ветвь представляет собой небольшое квадратное линейное отображение,
выбранное ради прозрачных вычислений. Более поздние ветви внимания и SwiGLU
будут соблюдать тот же инвариант слияния: вернуть тензор ширины модели, а затем
сложить его с неизменённым остаточным потоком.
