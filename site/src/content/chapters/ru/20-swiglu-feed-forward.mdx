---
{
  "chapter_id": "20-swiglu-feed-forward",
  "locale": "ru",
  "content_revision": 3,
  "order": 20,
  "concept_id": "swiglu-feed-forward",
  "title": "Управляйте ветвью расширения с помощью обучаемого вентиля",
  "description": "Соберите применяемый отдельно к каждой позиции слой прямого распространения SwiGLU, проследите его вентильную ветвь с активацией и ветвь расширения и проверьте точные выходы и градиенты.",
  "objective": "Скомпонуйте три проекции без смещения и дифференцируемый вентиль SiLU, не смешивая ведущие позиции, и проверьте точные значения прямого и обратного проходов.",
  "worked_inputs": "Возьмите X=[[1,0],[0,1]], матрицы весов вентильной ветви и ветви расширения формы [2,3] и матрицу весов проекции сжатия формы [3,2]. Предскажите значения на входе вентиля -1, 0 и 1, два поэлементных произведения ширины 3 и определите, какая строка выхода может измениться, если заменить только строку 0 входа на [0,0].",
  "formula":
    {
      "latex": "\\operatorname{FFN}(X)=\\left(\\operatorname{SiLU}(XW_g)\\odot(XW_u)\\right)W_2",
      "symbols":
        [
          {
            "symbol": "X",
            "meaning": "входной тензор формы [...,d_{in}], в котором каждая ведущая координата задаёт отдельную позицию"
          },
          {
            "symbol": "W_g",
            "meaning": "матрица весов вентильной ветви без смещения формы [d_{in},d_{ff}]"
          },
          {
            "symbol": "W_u",
            "meaning": "матрица весов ветви расширения без смещения формы [d_{in},d_{ff}]"
          },
          {
            "symbol": "W_2",
            "meaning": "матрица весов проекции сжатия без смещения формы [d_{ff},d_{out}]"
          },
          {
            "symbol": "\\operatorname{SiLU}(z)",
            "meaning": "поэлементная активация, равная произведению входного значения на его сигмоиду; она может быть отрицательной и сама по себе не является вероятностным вентилем"
          },
          {
            "symbol": "\\odot",
            "meaning": "поэлементное умножение одинаковых по форме вентильной ветви и ветви расширения"
          },
          { "symbol": "d_{in}", "meaning": "ширина входных признаков" },
          {
            "symbol": "d_{ff}",
            "meaning": "ширина ветвей в расширенном пространстве признаков"
          },
          {
            "symbol": "d_{out}",
            "meaning": "ширина выходных признаков после сжатия; позже декодер выбирает её равной ширине модели"
          },
          {
            "symbol": "...",
            "meaning": "ноль или больше ведущих осей, которые сохраняются без смешивания позиций"
          }
        ]
    },
  "history":
    {
      "llm_evolution":
        {
          "predecessor_kind": "neural-architecture",
          "limitation": "В ранней нейросетевой модели языка с прямым распространением использовалось одно поэлементное преобразование скрытого слоя tanh над контекстом фиксированной длины. В исходной архитектуре Transformer сеть прямого распространения стала применяться отдельно к каждой позиции и получила более широкое внутреннее представление, однако единственная ветвь с активацией всё ещё не создавала зависящего от входа мультипликативного взаимодействия между двумя обучаемыми проекциями.",
          "later_advance": "Transformer применяет два обучаемых преобразования с ReLU отдельно и одинаково в каждой позиции. Затем Shazeer исследует замены из семейства GLU, в которых две спроецированные ветви соединяются поэлементным умножением; в варианте SwiGLU одна ветвь перед произведением активируется функцией Swish при $\\beta=1$.",
          "modern_llm_role": "Современный декодер может использовать подслой SwiGLU без смещений: расширять представление каждого токена, модулировать одну обучаемую ветвь другой, а затем возвращать результат к ширине модели, необходимой для остаточного пути. Смешивание позиций при этом остаётся задачей внимания.",
          "sources":
            [
              {
                "role": "earlier",
                "year": 2003,
                "name": "Bengio et al., A Neural Probabilistic Language Model",
                "source_url": "https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf",
                "claim": "Бенжио и соавторы помещают скрытый слой с поэлементным гиперболическим тангенсом между обучаемыми признаками слов контекста и оценками следующего слова в нейросетевой модели языка с прямым распространением."
              },
              {
                "role": "later",
                "year": 2017,
                "name": "Vaswani et al., Attention Is All You Need",
                "source_url": "https://papers.nips.cc/paper_files/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf",
                "claim": "Васвани и соавторы применяют одну и ту же сеть прямого распространения с двумя преобразованиями и ReLU отдельно к каждой позиции последовательности; в опубликованной конфигурации ширина модели 512 расширяется до внутренней ширины 2048."
              },
              {
                "role": "later",
                "year": 2020,
                "name": "Shazeer, GLU Variants Improve Transformer",
                "source_url": "https://arxiv.org/pdf/2002.05202",
                "claim": "Shazeer определяет варианты GLU для Transformer без смещений с тремя матрицами весов и записывает SwiGLU как проекцию с активацией Swish, поэлементно умноженную на вторую проекцию перед выходной проекцией."
              }
            ]
        },
      "approach": "От скрытого слоя tanh в нейросетевой модели языка — через применяемый отдельно к каждой позиции блок прямого распространения Transformer с ReLU — к мультипликативным вариантам GLU и SwiGLU",
      "summary": "Нелинейные скрытые слои сделали ранние нейросетевые модели языка выразительнее одного аффинного преобразования; Transformer закрепил расширенный блок с ReLU, применяемый отдельно к каждой позиции; SwiGLU добавляет вторую спроецированную ветвь и поэлементное взаимодействие. В этой главе это взаимодействие выводится напрямую, а архитектурные утверждения из источников не смешиваются с локальными свойствами реализации: размерами, вариантом без смещений, именами и детерминированными результатами.",
      "rust_source": "rust/demos/ch20-swiglu-feed-forward/src/lib.rs"
    },
  "rust_sources":
    [
      {
        "path": "rust/demos/ch20-swiglu-feed-forward/src/lib.rs",
        "region": "historical-activation-contrast",
        "purpose": "Сопоставить tanh и ReLU на одних и тех же скалярных входах в рамках исторического сравнения."
      },
      {
        "path": "rust/crates/llm-from-scratch/src/nn/swiglu.rs",
        "region": "swiglu-errors",
        "purpose": "Объявить ошибки отдельных этапов создания, проверки форм, инициализации и делегированных операций."
      },
      {
        "path": "rust/crates/llm-from-scratch/src/nn/swiglu.rs",
        "region": "swiglu-layer",
        "purpose": "Скомпоновать три проекции без смещения с уже реализованными SiLU и поэлементным умножением."
      },
      {
        "path": "rust/demos/ch20-swiglu-feed-forward/src/lib.rs",
        "region": "known-swiglu-forward",
        "purpose": "Выполнить точный пример для двух позиций и сохранить тензор каждой ветви."
      },
      {
        "path": "rust/demos/ch20-swiglu-feed-forward/src/lib.rs",
        "region": "swiglu-gradients",
        "purpose": "Провести обратный проход с начальным градиентом в виде единичной матрицы и получить точные градиенты входа, ветвей и параметров."
      },
      {
        "path": "rust/demos/ch20-swiglu-feed-forward/src/lib.rs",
        "region": "initialized-swiglu",
        "purpose": "Проверить детерминированную инициализацию и убедиться, что клоны ссылаются на те же параметры."
      },
      {
        "path": "rust/demos/ch20-swiglu-feed-forward/src/lib.rs",
        "region": "position-independence",
        "purpose": "Заменить одну строку входа и доказать, что другая строка выхода остаётся неизменной."
      },
      {
        "path": "rust/demos/ch20-swiglu-feed-forward/src/main.rs",
        "region": "learner-swiglu-output",
        "purpose": "Подготовить полный детерминированный учебный отчёт перед печатью."
      },
      {
        "path": "rust/demos/ch20-swiglu-feed-forward/src/diagram_trace.rs",
        "region": "swiglu-feed-forward-trace",
        "purpose": "Вывести точные данные прямого и обратного проходов и проверки независимости для статической схемы."
      }
    ],
  "visualization":
    {
      "decision": "useful",
      "id": "swiglu-feed-forward",
      "rationale": "Две параллельные цепочки проекций позволяют наглядно проследить активацию SiLU, поэлементный вентиль, расширение и сжатие признаков, независимость позиций и разветвление градиентов при обратном проходе; по одному текстовому объяснению или конечному вектору эти связи увидеть сложнее."
    },
  "decoder_connection": "Совокупная модель получает именованный дифференцируемый модуль SwiGLU без смещений, который отдельно обрабатывает каждую позицию и предоставляет стабильный набор параметров. В последующих блоках декодера ширины входа и выхода будут одинаковыми, чтобы этот подслой можно было поместить в остаточный путь; сначала глава 21 определит, как объединяются функции потерь и градиенты нескольких примеров токенов."
}
---

import RustSource from "../../../components/RustSource.astro";
import SwigluFeedForwardDiagram from "../../../components/chapters/SwigluFeedForwardDiagram.astro";

export const diagramLabels = {
  title:
    "Проследите две ветви проекций в слое SwiGLU, применяемом к каждой позиции",
  description:
    "На примере из двух позиций показаны вычисленные в Rust значения прямого прохода, градиенты ветвей, суммы для общих параметров и проверка независимости позиций.",
  summary: {
    inputWidth: "Ширина входа",
    hiddenWidth: "Ширина ветвей",
    outputWidth: "Ширина выхода",
    bias: "Вариант проекций",
    parameterCount: "Скалярных параметров",
    inputShape: "Форма входа",
    branchShape: "Форма ветвей",
    outputShape: "Форма выхода"
  },
  stages: {
    positions: "Обработайте каждую позицию отдельно",
    gate: "Вентильная ветвь: проекция и активация",
    up: "Ветвь расширения: проекция без активации",
    merge: "Перемножьте соответствующие координаты",
    down: "Сожмите признаки после вентиля",
    independence: "Измените одну позицию; проверьте другую",
    gradients: "Разделите локальные градиенты и сложите вклады в общие веса"
  },
  fields: {
    computation: "Вычисление",
    position: "Позиция",
    input: "Вход",
    gateProjection: "Вентильная проекция",
    preActivation: "До SiLU",
    activatedGate: "После SiLU",
    upProjection: "Проекция расширения",
    upBranch: "Спроецированные значения",
    gated: "Признаки после вентиля",
    output: "Выход",
    upstream: "Входящий градиент",
    gatedGradient: "Градиент по признакам после вентиля",
    gateGradient: "Градиент до SiLU",
    upGradient: "Градиент по выходу ветви расширения",
    inputGradient: "Градиент входа",
    parameter: "Общий параметр",
    shape: "Форма",
    accumulation: "Сумма по позициям",
    gradient: "Накопленный градиент",
    changedPosition: "Меняемая позиция",
    replacement: "Новый вход",
    observedPosition: "Наблюдаемая позиция",
    before: "Выход до замены",
    after: "Выход после замены",
    result: "Результат"
  },
  notes: {
    positionWise:
      "Обе строки используют три общие матрицы весов; позиции не связаны.",
    gate: "SiLU даёт число любого знака или ноль; это не вероятностная маска.",
    gradients:
      "Каждый вход получает локальный градиент. В трёх общих матрицах весов складываются вклады обеих позиций.",
    independence:
      "Rust заменяет вход позиции 0 нулями и пересчитывает слой; выход позиции 1 побайтно не меняется.",
    precision:
      "Схема округляет до шести знаков; отчёт сохраняет двенадцать."
  },
  symbols: {
    biasFree: "Без смещений",
    unchanged: "Без изменений"
  },
  captions: {
    positionGradients:
      "Градиенты, возвращаемые через две ветви в каждой позиции",
    parameterGradients: "Градиенты, накопленные в трёх общих матрицах весов"
  },
  scrollers: {
    forward: "Прокручиваемая таблица прямого прохода по позициям",
    parameters: "Прокручиваемая таблица градиентов общих параметров"
  }
};

{/* chapter-section:worked-example */}

## Предскажите произведения двух ветвей

В главе 19 появились готовые проекции без смещения. В этом примере две строки,
соответствующие позициям, проходят через три общие матрицы весов:

$$
\begin{aligned}
X &= \begin{bmatrix}1&0\\0&1\end{bmatrix}, \\
W_g &= \begin{bmatrix}-1&0&1\\0&1&-1\end{bmatrix}, \\
W_u &= \begin{bmatrix}1&2&3\\3&2&1\end{bmatrix}, \\
W_2 &= \begin{bmatrix}1&0\\0&1\\1&-1\end{bmatrix}.
\end{aligned}
$$

Первая строка $XW_g$ равна $[-1,0,1]$. Прежде чем читать дальше, предскажите
$\operatorname{SiLU}([-1,0,1])$, поэлементно умножьте результат на первую
строку $XW_u=[1,2,3]$, а затем примените $W_2$.

После округления этапы выглядят так:

$$
\begin{aligned}
XW_g &=
\begin{bmatrix}-1&0&1\\0&1&-1\end{bmatrix}, \\
\operatorname{SiLU}(XW_g) &\approx
\begin{bmatrix}-0.268941&0&0.731059\\0&0.731059&-0.268941\end{bmatrix}, \\
XW_u &=
\begin{bmatrix}1&2&3\\3&2&1\end{bmatrix}, \\
\operatorname{SiLU}(XW_g)\odot(XW_u) &\approx
\begin{bmatrix}-0.268941&0&2.193176\\0&1.462117&-0.268941\end{bmatrix}, \\
Y &\approx
\begin{bmatrix}1.924234&-2.193176\\-0.268941&1.731059\end{bmatrix}.
\end{aligned}
$$

Исполняемый пример один раз вызывает слой и сохраняет точные
промежуточные значения для трассировки:

<RustSource
  path="rust/demos/ch20-swiglu-feed-forward/src/lib.rs"
  region="known-swiglu-forward"
  caption="Вычислить фиксированный пример SwiGLU для двух позиций"
  label="Исходный код на Rust с прямым проходом SwiGLU из примера главы 20"
/>

{/* chapter-section:formula */}

## Активируйте одну ветвь, затем перемножьте

Общая формула прямого прохода:

$$
\operatorname{FFN}(X)=\left(\operatorname{SiLU}(XW_g)\odot(XW_u)\right)W_2
$$

Внутри SiLU используется сигмоида, но результат активации не является
вероятностью:

$$
\sigma(z)=\frac{1}{1+e^{-z}},
\qquad
\operatorname{SiLU}(z)=z\sigma(z).
$$

Поэтому $\operatorname{SiLU}(-1)<0$, $\operatorname{SiLU}(0)=0$, а при
большом положительном $z$ выполняется $\operatorname{SiLU}(z)\approx z$.
Считать, что вся активированная ветвь принимает значения только от нуля до
единицы, было бы ошибкой.

Для обратного прохода явно обозначим промежуточные результаты прямого прохода:

$$
A=XW_g,\qquad
S=\operatorname{SiLU}(A),\qquad
U=XW_u,\qquad
H=S\odot U,\qquad
Y=HW_2.
$$

Пусть $G=\partial L/\partial Y$ — градиент скалярной функции потерь $L$, а
$dA$, $dS$, $dU$, $dH$, $dX$ и $dW$ обозначают градиенты по соответствующим
величинам прямого прохода. Индекс $p$ перебирает все сохранённые ведущие
позиции. При обратном проходе сначала вычисляются градиенты через проекцию
сжатия, затем градиент разветвляется в узле произведения и проходит через
производную SiLU:

$$
\begin{aligned}
dH &= GW_2^\top, \\
dS &= dH\odot U, \\
dU &= dH\odot S, \\
dA &= dS\odot\operatorname{SiLU}'(A), \\
dX_p &= dA_pW_g^\top+dU_pW_u^\top, \\
dW_g &= \sum_p X_p^\top dA_p, \\
dW_u &= \sum_p X_p^\top dU_p, \\
dW_2 &= \sum_p H_p^\top G_p.
\end{aligned}
$$

Здесь
$\operatorname{SiLU}'(a)=\sigma(a)+a\sigma(a)(1-\sigma(a))$. В схеме
градиент до SiLU обозначен $dA_p$. Результаты прямого прохода и $dX_p$ остаются
локальными для позиции $p$. В градиентах трёх матриц весов суммируются вклады
всех позиций, поскольку эти веса общие.

{/* chapter-section:symbol-glossary */}

## Расширяйте признаки, не смешивая позиции

- $X$ имеет форму $[\ldots,d_{in}]$; каждая ведущая координата задаёт отдельную позицию.
- $W_g$ и $W_u$ имеют форму $[d_{in},d_{ff}]$.
- $W_2$ имеет форму $[d_{ff},d_{out}]$.
- $\operatorname{SiLU}(z)$ — поэлементное произведение $z\sigma(z)$.
- $\odot$ перемножает соответствующие координаты ветвей без матричного умножения.
- $d_{in}$, $d_{ff}$ и $d_{out}$ — ширины входных признаков, ветвей и выходных признаков.
- $\ldots$ обозначает любые сохраняемые ведущие оси.

Слой из примера расширяет признаки как $2\to3$ и сжимает как $3\to2$. Входы
форм $[2]$, $[2,2]$ и $[1,2,2]$ дают выходы тех же форм, потому что в этом
примере выбрано $d_{out}=d_{in}=2$. Этот модуль допускает и другие значения
$d_{out}$; позже в декодере с остаточными связями ширины входа и выхода будут
намеренно одинаковыми.

{/* chapter-section:history */}

## От нелинейных нейросетевых моделей языка к SwiGLU

В ранней нейросетевой модели языка с прямым распространением использовалось одно
поэлементное преобразование скрытого слоя tanh над контекстом фиксированной
длины. В исходной архитектуре Transformer сеть прямого распространения стала
применяться отдельно к каждой позиции и получила более широкое внутреннее
представление, однако единственная ветвь с активацией всё ещё не создавала
зависящего от входа мультипликативного взаимодействия между двумя обучаемыми
проекциями.

[Бенжио и соавторы, _A Neural Probabilistic Language Model_](https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf): Бенжио и соавторы помещают скрытый слой с поэлементным гиперболическим тангенсом между обучаемыми признаками слов контекста и оценками следующего слова в нейросетевой модели языка с прямым распространением.

Опубликованное ими вычисление оценок включает:

$$
y=b+Wx+U\tanh(d+Hx).
$$

Transformer применяет два обучаемых преобразования с ReLU отдельно и одинаково
в каждой позиции. Затем Shazeer исследует замены из семейства GLU, в которых две
спроецированные ветви соединяются поэлементным умножением; в варианте SwiGLU одна
ветвь перед произведением активируется функцией Swish при $\beta=1$.

[Васвани и соавторы, _Attention Is All You Need_](https://papers.nips.cc/paper_files/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf): Васвани и соавторы применяют одну и ту же сеть прямого распространения с двумя преобразованиями и ReLU отдельно к каждой позиции последовательности; в опубликованной конфигурации ширина модели $512$ расширяется до внутренней ширины $2048$.

$$
\operatorname{FFN}(x)=\max(0,xW_1+b_1)W_2+b_2.
$$

[Shazeer, _GLU Variants Improve Transformer_](https://arxiv.org/pdf/2002.05202): Shazeer определяет варианты GLU для Transformer без смещений с тремя матрицами весов и записывает SwiGLU как проекцию с активацией Swish, поэлементно умноженную на вторую проекцию перед выходной проекцией.

В обозначениях Shazeer $\operatorname{Swish}_1(z)=z\sigma(z)$; в этом курсе
используется равнозначное название $\operatorname{SiLU}(z)$, а одно и то же
преобразование независимо применяется ко всем сохранённым ведущим позициям.

Современный декодер может использовать подслой SwiGLU без смещений: расширять
представление каждого токена, модулировать одну обучаемую ветвь другой, а затем
возвращать результат к ширине модели, необходимой для остаточного пути.
Смешивание позиций при этом остаётся задачей внимания.

Так проходит путь от нелинейных вычислений в нейросетевых моделях языка к
современным блокам прямого распространения LLM, а не история языков
программирования. В работе Shazeer приведены результаты для конкретных
исследованных конфигураций, но сами по себе эти эксперименты не объясняют,
почему SwiGLU работает. Источники определяют архитектуру, но не размеры, вариант со смещениями
или без них, имена параметров, начальное состояние генератора и обработку ошибок
в этой реализации.

Исторический пример на Rust вычисляет tanh и ReLU для одних и тех же трёх
входов. Сравниваются функции активации, а код на Rust лишь позволяет исполнить
этот пример:

<RustSource
  path="rust/demos/ch20-swiglu-feed-forward/src/lib.rs"
  region="historical-activation-contrast"
  caption="Сравнить tanh и ReLU на отрицательном, нулевом и положительном входах"
  label="Исходный код на Rust для исторического сравнения функций активации в главе 20"
/>

{/* chapter-section:rust-implementation */}

## Скомпонуйте уже реализованные дифференцируемые операции

Ошибки создания и прямого прохода указывают конкретный этап проекции или
несовместимые размеры ветвей:

<RustSource
  path="rust/crates/llm-from-scratch/src/nn/swiglu.rs"
  region="swiglu-errors"
  caption="Связать каждую ошибку с проекцией или составной операцией"
  label="Исходный код на Rust с ошибками SwiGLU из главы 20"
/>

`SwiGlu` содержит вентильный модуль `Linear`, модуль расширения и модуль
сжатия. Во всех трёх смещение отключено. Прямой проход вызывает уже реализованные
проекцию, SiLU и умножение; отдельная слитная тензорная операция или ещё один
механизм вычисления градиентов здесь не появляются:

<RustSource
  path="rust/crates/llm-from-scratch/src/nn/swiglu.rs"
  region="swiglu-layer"
  caption="Скомпоновать три проекции без смещения вокруг вентиля SiLU"
  label="Исходный код на Rust с реализацией слоя SwiGLU из главы 20"
/>

Входящий градиент в виде единичной матрицы позволяет получить градиенты обеих
ветвей и всех общих матриц весов. Чтобы увидеть локальные
векторно-якобианские произведения сохранённых промежуточных тензоров, в
небольших вспомогательных проверках их временно считают листовыми
узлами-параметрами. После обратного прохода в этих листьях остаются искомые
градиенты; вычисление самого слоя при этом не меняется:

<RustSource
  path="rust/demos/ch20-swiglu-feed-forward/src/lib.rs"
  region="swiglu-gradients"
  caption="Провести точный пример назад через обе ветви"
  label="Исходный код на Rust, проверяющий градиенты SwiGLU в главе 20"
/>

Все три веса инициализируются во временной копии одного потока генератора в
порядке «вентильная ветвь, ветвь расширения, проекция сжатия». Одинаковые
начальные состояния воспроизводят значения. Если создание всего слоя завершается
ошибкой, поток генератора вызывающего кода остаётся прежним, а клоны слоя
ссылаются на те же листовые узлы-параметры:

<RustSource
  path="rust/demos/ch20-swiglu-feed-forward/src/lib.rs"
  region="initialized-swiglu"
  caption="Воспроизводимо инициализировать все три матрицы весов без частичного изменения состояния"
  label="Исходный код на Rust, связывающий детерминированную инициализацию со SwiGLU"
/>

Если заменить вход позиции 0 полностью нулевой строкой, становится видно, что
прямой проход не смешивает позиции:

<RustSource
  path="rust/demos/ch20-swiglu-feed-forward/src/lib.rs"
  region="position-independence"
  caption="Изменить одну позицию входа и сохранить выход другой"
  label="Исходный код на Rust, доказывающий независимую обработку позиций в главе 20"
/>

Учебный отчёт охватывает точные этапы, активации, значения обратного прохода,
формы, параметры, инициализацию, идентичность, независимость позиций, пустой вход
и ошибки:

<RustSource
  path="rust/demos/ch20-swiglu-feed-forward/src/main.rs"
  region="learner-swiglu-output"
  caption="Подготовить детерминированный учебный отчёт главы 20"
  label="Исходный код на Rust с учебными результатами SwiGLU из главы 20"
/>

Команда `cargo run --quiet --locked -p ch20-swiglu-feed-forward` выводит
детерминированный отчёт. Реализация также проверяет устойчивые предельные значения
SiLU, точный порядок и число параметров, очерёдность проверок, последовательные
случайные выборки, все варианты ранга и выборочную проверку конечными разностями
с шагом $10^{-6}$ и допуском $3\times10^{-6}$.

{/* chapter-section:visualization */}

## Проследите вентиль, слияние и разветвление обратного прохода

Трасса из одиннадцати строк содержит две строки прямого прохода, две строки
обратного прохода, три градиента общих матриц весов и одну проверку независимости
позиций:

<RustSource
  path="rust/demos/ch20-swiglu-feed-forward/src/diagram_trace.rs"
  region="swiglu-feed-forward-trace"
  caption="Вывести точные значения ветвей и градиентов"
  label="Исходный код на Rust с данными схемы SwiGLU из главы 20"
/>

<SwigluFeedForwardDiagram labels={diagramLabels} />

На схеме две ветви прямого прохода сгруппированы отдельно для каждой позиции, а
локальные градиенты обратного прохода отделены от сумм, накопленных в общих
матрицах весов. В проверке независимости меняется только позиция 0. Выход в
позиции 1 остаётся прежним — это непосредственно показывает, что SwiGLU
обрабатывает позиции независимо.

{/* chapter-section:exercises */}

## Сначала предскажите, затем сверьтесь с исполняемым примером

1. Предскажите $\operatorname{SiLU}(-1)$, $\operatorname{SiLU}(0)$ и $\operatorname{SiLU}(1)$.
2. Предскажите $h_0$ после умножения активированной вентильной ветви для позиции 0 на $u_0=[1,2,3]$.
3. Предскажите формы трёх матриц параметров и общее число скаляров.
4. Определите, какие строки выхода изменятся, если только позиция 0 станет равна $[0,0]$.
5. Определите, используют ли $dW_g$, $dW_u$ и $dW_2$ одну позицию или обе.
6. Предскажите формы выходов для входов $[2]$, $[2,2]$ и $[1,2,2]$.
7. Объясните, почему такую вентильную обработку обычно нельзя свести к одной постоянной матрице.
8. Сопоставьте Бенжио, Васвани и Shazeer соответственно с нейросетевой моделью языка с tanh, блоком прямого распространения с ReLU, который одинаково применяется к каждой позиции, и SwiGLU.

<details>
  <summary>Сверить ответы</summary>

1. Приближённые значения равны $[-0.268941,0,0.731059]$.
2. $h_0\approx[-0.268941,0,2.193176]$.
3. Формы равны $[2,3]$, $[2,3]$ и $[3,2]$: всего 18 скаляров и ни одного смещения.
4. Изменится только строка выхода 0; строка выхода 1 останется в точности прежней.
5. В градиентах всех трёх общих матриц весов складываются вклады обеих позиций.
6. При $d_{out}=2$ формы выходов остаются равны $[2]$, $[2,2]$ и $[1,2,2]$.
7. Из-за произведения эффективный отклик одной ветви зависит от значений другой ветви, которые, в свою очередь, зависят от входа.
8. Бенжио задаёт контекст нейросетевой модели языка с tanh, Васвани — одинаково применяемый к каждой позиции блок с ReLU, а Shazeer — более позднюю формулу SwiGLU.
</details>

{/* chapter-section:decoder-connection */}

## Перейдите от нелинейного преобразования токенов к пакетной обработке

Совокупная модель получает именованный дифференцируемый модуль SwiGLU без смещений, который отдельно обрабатывает каждую позицию и предоставляет стабильный набор параметров. В последующих блоках декодера ширины входа и выхода будут одинаковыми, чтобы этот подслой можно было поместить в остаточный путь; сначала глава 21 определит, как объединяются функции потерь и градиенты нескольких примеров токенов.

SwiGLU изменяет признаки внутри одной позиции токена, но не переносит информацию
между токенами: это позже сделает каузальное внимание. Теперь глава 21 объединит
каузальные примеры и точно задаст усреднение функций потерь токенов и их
градиентов.
