---
{
  "chapter_id": "22-adamw",
  "locale": "ru",
  "content_revision": 2,
  "order": 22,
  "concept_id": "adamw",
  "title": "Не включайте затухание весов в моменты градиента",
  "description": "Соберите AdamW на основе градиентов именованных параметров и моментов с поправкой на смещение, примените затухание весов отдельной ветвью, а затем атомарно зафиксируйте все проверенные обновления.",
  "objective": "Обновлять стабильный набор именованных параметров декодера, используя первый момент градиента и второй нецентрированный момент градиента после внесения поправки на смещение, при этом не включая затухание весов в адаптивную градиентную ветвь.",
  "worked_inputs": "Возьмите $\\theta_0=[1,-2]$, накопленный градиент $g_1=[0.2,-0.4]$, скорость обучения $\\eta=0.1$, коэффициенты сглаживания моментов $\\beta_1=\\beta_2=0.5$, стабилизатор $\\varepsilon=0.1$ и коэффициент затухания $\\lambda=0.1$. До вычисления $\\theta_1$ предскажите адаптивную поправку и поправку затухания.",
  "formula": {
    "latex": "\\hat m_t=\\frac{m_t}{1-\\beta_1^t},\\quad \\hat v_t=\\frac{v_t}{1-\\beta_2^t},\\quad \\theta_t=(1-\\eta\\lambda)\\theta_{t-1}-\\eta\\frac{\\hat m_t}{\\sqrt{\\hat v_t}+\\varepsilon}",
    "symbols": [
      {
        "symbol": "\\theta_{t-1}",
        "meaning": "значение именованного параметра перед шагом оптимизатора t"
      },
      {
        "symbol": "\\theta_t",
        "meaning": "новое значение параметра, которое фиксируется после успешного вычисления всех именованных обновлений"
      },
      {
        "symbol": "t",
        "meaning": "положительный номер шага оптимизатора, используемый в обеих поправках на смещение"
      },
      {
        "symbol": "m_t",
        "meaning": "экспоненциально сглаженная оценка первого момента градиента до поправки на смещение"
      },
      {
        "symbol": "v_t",
        "meaning": "экспоненциально сглаженная оценка второго нецентрированного момента градиента до поправки на смещение"
      },
      {
        "symbol": "\\beta_1",
        "meaning": "коэффициент сглаживания первого момента, принимающий значения от нуля включительно до единицы не включительно"
      },
      {
        "symbol": "\\beta_2",
        "meaning": "коэффициент сглаживания второго момента, принимающий значения от нуля включительно до единицы не включительно"
      },
      {
        "symbol": "\\eta",
        "meaning": "положительная скорость обучения, которая масштабирует как адаптивное направление обновления, так и отдельное затухание весов"
      },
      {
        "symbol": "\\lambda",
        "meaning": "эффективный неотрицательный коэффициент затухания для данного именованного параметра: настроенное значение затухания или ноль"
      },
      {
        "symbol": "\\hat m_t",
        "meaning": "первый момент градиента после поправки на смещение из-за нулевой инициализации"
      },
      {
        "symbol": "\\hat v_t",
        "meaning": "второй нецентрированный момент градиента после поправки на смещение из-за нулевой инициализации"
      },
      {
        "symbol": "\\varepsilon",
        "meaning": "положительный стабилизатор знаменателя, добавляемый после извлечения квадратного корня"
      }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "training-practice",
      "limitation": "В нейронной языковой модели Бенжио и соавторов после предъявления одного слова из обучающего корпуса вместе с его контекстом выполняется прямое стохастическое обновление параметров. На каждом шаге единственная скалярная скорость обучения умножает градиент текущего примера; памяти о предыдущих градиентах по отдельным координатам нет.",
      "later_advance": "По сравнению с этим прямым правилом SGD метод импульса добавляет затухающую память о прошлых направлениях. Позднее Adam хранит две экспоненциально сглаженные оценки — первый момент и второй нецентрированный момент градиента — и вносит поправку на их начальное смещение к нулю; если член $L_2$ включён в градиент, эта пропорциональная параметру составляющая попадает в обе скользящие оценки. В AdamW поправка, стягивающая параметр к нулю, напротив, вынесена из градиента, по которому обновляются эти адаптивные моменты.",
      "modern_llm_role": "В статье о LLaMA описано применение AdamW при предобучении декодерных языковых моделей с числом параметров от $7$B до $65$B. Оптимизатор курса теперь превращает усреднённые по токенам градиенты, полученные автоматическим дифференцированием, в новые именованные листовые узлы-параметры; расписание скорости обучения, ограничение нормы градиента, смешанная точность и распределённое состояние относятся к последующим этапам конвейера.",
      "sources": [
        {
          "role": "earlier",
          "year": 2003,
          "name": "Bengio et al., A Neural Probabilistic Language Model",
          "source_url": "https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf",
          "claim": "В нейронной языковой модели Бенжио и соавторов после предъявления одного слова из обучающего корпуса вместе с его контекстом выполняется прямое стохастическое обновление параметров."
        },
        {
          "role": "later",
          "year": 2014,
          "name": "Kingma and Ba, Adam: A Method for Stochastic Optimization",
          "source_url": "https://arxiv.org/pdf/1412.6980",
          "claim": "Adam Кингмы и Ба хранит две экспоненциально сглаженные оценки — первый момент и второй нецентрированный момент градиента — и компенсирует их начальное смещение к нулю."
        },
        {
          "role": "later",
          "year": 2017,
          "name": "Loshchilov and Hutter, Decoupled Weight Decay Regularization",
          "source_url": "https://arxiv.org/pdf/1711.05101",
          "claim": "В AdamW Лошчилова и Хуттера пропорциональное параметру затухание вынесено из градиента, по которому обновляются адаптивные моменты."
        },
        {
          "role": "later",
          "year": 2023,
          "name": "Touvron et al., LLaMA: Open and Efficient Foundation Language Models",
          "source_url": "https://arxiv.org/pdf/2302.13971",
          "claim": "В статье о LLaMA описано применение AdamW при предобучении декодерных языковых моделей с числом параметров от $7$B до $65$B."
        }
      ]
    },
    "approach": "От прямого SGD по отдельным примерам нейронной языковой модели — через метод импульса и Adam со связанной штрафной добавкой — к адаптивным моментам с поправкой на смещение и затуханию весов, отделённому от градиентного обновления, при современном предобучении декодерных моделей",
    "summary": "AdamW — часть пути к современным LLM, поскольку он превращает шумные градиенты предсказания следующего токена в покоординатные адаптивные обновления, при этом поправка, стягивающая веса к нулю, не входит в оценки моментов. Статьи подтверждают развитие методов оптимизации; стабильные имена, откат всего набора, новые листовые узлы-параметры, константы и трассировка — решения этого курса.",
    "rust_source": "rust/demos/ch22-adamw/src/lib.rs"
  },
  "rust_sources": [
    {
      "path": "rust/demos/ch22-adamw/src/lib.rs",
      "region": "historical-optimizer-road",
      "purpose": "Сопоставить два шага SGD, метода импульса, Adam со штрафом, включённым в градиент, и AdamW, а также построить заданные траектории SGD и AdamW при разной кривизне по координатам, не превращая историю в сравнение языков программирования."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/training/adamw.rs",
      "region": "adamw-configuration",
      "purpose": "Проверить допустимые значения всех скалярных параметров до создания состояния оптимизатора."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/training/adamw.rs",
      "region": "adamw-parameter-groups",
      "purpose": "Разбить полный набор стабильных имён на непересекающиеся группы с затуханием и без затухания."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/training/adamw.rs",
      "region": "adamw-moment-state",
      "purpose": "Связать каждую пару векторов моментов с одним стабильным именем параметра и его формой."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/training/adamw.rs",
      "region": "adamw-state-and-evidence",
      "purpose": "Предоставить точные подготовленные векторы моментов, поправок на смещение, отдельных поправок и новых значений."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/training/adamw.rs",
      "region": "transactional-adamw-step",
      "purpose": "Подготовить все моменты с поправкой, отдельные поправки и новые листовые узлы-параметры до атомарной фиксации полного набора."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/training/adamw.rs",
      "region": "adamw-errors",
      "purpose": "Возвращать типизированные ошибки скалярных параметров, идентичности, формы, счётчика, арифметики, тензора и инициализации."
    },
    {
      "path": "rust/demos/ch22-adamw/src/lib.rs",
      "region": "chapter-adamw-fixture",
      "purpose": "Выполнить заданное именованное обновление, проверку с нулевым градиентом, проверку нового листового узла-параметра и откат при изменившемся наборе имён."
    },
    {
      "path": "rust/demos/ch22-adamw/src/main.rs",
      "region": "learner-adamw-output",
      "purpose": "Вывести полный детерминированный отчёт для учащегося."
    },
    {
      "path": "rust/demos/ch22-adamw/src/diagram_trace.rs",
      "region": "adamw-trace",
      "purpose": "Вывести для статической схемы точные данные о моментах, поправках, заменах и атомарной операции."
    }
  ],
  "visualization": {
    "decision": "useful",
    "id": "adamw",
    "component": "AdamwDiagram",
    "rationale": "Подробная схема оставляет градиент функции потерь внутри адаптивных моментов, а по отдельной ветви старое значение параметра из группы с затуханием поступает непосредственно в вычитаемую поправку затухания.",
    "supplementary": [
      {
        "id": "adamw-evidence",
        "component": "AdamwEvidenceDiagram",
        "rationale": "Компактная запись параметра без затухания явно показывает границу между группами, а отдельные данные траекторий и полного набора позволяют сопоставить заданные пути оптимизаторов и атомарную замену, не перегружая схему обновления."
      }
    ]
  },
  "decoder_connection": "Теперь совокупный путь обучения может связать каждый накопленный градиент функции потерь, усреднённой по токенам, со стабильным именем его параметра, сохранять под этим именем первый и второй моменты между шагами и атомарно фиксировать новый листовой узел-параметр с нулевым градиентом. В главе 23 этот оптимизатор используется для обучения нейронной языковой модели с фиксированным контекстом и проверки снижения функции потерь на валидационной выборке."
}
---

import RustSource from '../../../components/RustSource.astro';
import AdamwDiagram from '../../../components/chapters/AdamwDiagram.astro';
import AdamwEvidenceDiagram from '../../../components/chapters/AdamwEvidenceDiagram.astro';

export const diagramLabels = {
  title: 'Проследите раздельные ветви AdamW — адаптивную и ветвь затухания',
  description:
    'Проследите, как один именованный параметр из группы с затуханием проходит через моменты градиента и поправку на смещение, а затем получает отдельные адаптивную поправку и поправку затухания перед заменой.',
  evidenceTitle: 'Сопоставьте группу без затухания, траектории оптимизаторов и атомарную замену',
  evidenceDescription:
    'На параметре масштаба нормализации с нулевым градиентом проверьте, как выбор группы управляет затуханием; затем сравните точные точки SGD и AdamW и инварианты замены полного набора.',
  summary: {
    step: 'Зафиксированный шаг',
    learningRate: 'Скорость обучения',
    momentRates: 'Коэффициенты сглаживания',
    stabilizer: 'Стабилизатор знаменателя',
    decay: 'Затухание весов',
  },
  stages: {
    inputs: 'Возьмите значение именованного параметра и его накопленный градиент',
    moments: 'Обновите моменты и внесите поправку на смещение',
    deltas: 'Разделите адаптивную поправку и поправку затухания',
    trajectory: 'Сравните траектории при неодинаковой кривизне',
    replacement: 'Замените все проверенные листовые узлы одновременно',
    proof: 'Проверьте замену узлов, сброс градиентов и откат',
  },
  fields: {
    parameter: 'Именованный параметр',
    parameterGroup: 'Группа параметра',
    shape: 'Форма',
    before: 'Значение до обновления',
    gradient: 'Накопленный градиент',
    firstMoment: 'Первый момент',
    secondMoment: 'Второй нецентрированный момент',
    correctedFirst: 'Первый момент с поправкой',
    correctedSecond: 'Второй нецентрированный момент с поправкой',
    adaptiveDelta: 'Адаптивная поправка',
    decayDelta: 'Поправка затухания',
    after: 'Новое значение',
    curvature: 'Кривизна квадратичной функции',
    trajectoryPoint: 'Шаг оптимизатора',
    stateNames: 'Ключи состояния',
    freshGradient: 'Градиент нового листового узла',
    leafIdentity: 'Листовой узел параметра',
    zeroGradientDecay: 'Новое состояние: затухание при нулевом градиенте',
    failedTransaction: 'Попытка с другим набором имён',
    commit: 'Успешная фиксация',
  },
  notes: {
    moments:
      'В экспоненциально сглаживаемые моменты поступает только накопленный градиент функции потерь. Поправка на первом шаге точно восстанавливает текущий градиент и его квадрат.',
    deltas:
      'Адаптивная ветвь использует моменты с поправкой. Ветвь затухания напрямую читает старое значение параметра и не попадает ни в один момент.',
    trajectory:
      'Обе траектории вычислены на одной заданной квадратичной функции. Точные точки получены программой на Rust, а не восстановлены по рисунку.',
    replacement:
      'Rust сначала готовит обе именованные замены. Новые листовые узлы устанавливаются только после успешных арифметических проверок и создания всех тензоров.',
    proof:
      'Моменты связаны со стабильными именами, а не с порядком отображения. При изменении набора имён прежние листовые узлы и состояние оптимизатора остаются без изменений.',
  },
  symbols: {
    adaptive: 'Адаптивная ветвь',
    decay: 'Ветвь затухания в обход моментов',
    applyDecay: 'Применить затухание',
    skipDecay: 'Не применять затухание',
    sgd: 'Траектория SGD',
    adamw: 'Траектория AdamW',
    subtract: 'Вычесть обе поправки',
    zero: 'Точный ноль',
    replaced: 'Заменён',
    unchanged: 'Без изменений',
    atomic: 'Атомарно',
  },
  captions: {
    parameterFlow: 'Точные данные поэлементного обновления',
    trajectory: 'Траектории оптимизаторов на одной анизотропной квадратичной функции, вычисленные программой на Rust',
    transactionProof: 'Инварианты полного набора, вычисленные программой на Rust',
  },
  scrollers: {
    parameterFlow: 'Прокручиваемая область с векторными данными AdamW для параметра',
    trajectory: 'Прокручиваемая область с данными траекторий SGD и AdamW',
  },
};

{/* chapter-section:worked-example */}

## Предскажите две поправки, которые будут вычтены

Глава 21 завершается координатами градиента функции потерь, усреднённой по токенам.
Прежде чем AdamW обновит параметры, свяжите каждый градиент тензорной формы со
стабильным именем того листового узла-параметра, к которому он относится. Задайте
первому именованному листовому узлу-параметру такое небольшое состояние:

$$
\theta_0=[1,-2],
\qquad
g_1=[0.2,-0.4].
$$

Возьмите $\eta=0.1$, $\beta_1=\beta_2=0.5$, $\varepsilon=0.1$ и
$\lambda=0.1$. Отдельно предскажите адаптивную поправку и поправку затухания.

При нулевой инициализации моментов $m_1=[0.1,-0.2]$, а
$v_1=[0.02,0.08]$. Оба знаменателя поправки равны $0.5$, поэтому
$\hat m_1=[0.2,-0.4]$ и $\hat v_1=[0.04,0.16]$. Следовательно:

$$
\eta\frac{\hat m_1}{\sqrt{\hat v_1}+\varepsilon}
\approx[0.066667,-0.08],
\qquad
\eta\lambda\theta_0=[0.01,-0.02].
$$

Вычтите обе поправки из старого значения и получите прогноз
$\theta_1\approx[0.923333,-1.9]$. Знаки здесь важны: при затухании отрицательной
координаты вычитается отрицательная поправка, поэтому координата приближается к нулю.

{/* chapter-section:formula */}

## Скорректируйте моменты, а затухание примените в обход них

Обновите поэлементно первый момент и второй нецентрированный момент:

$$
m_t=\beta_1m_{t-1}+(1-\beta_1)g_t,
\qquad
v_t=\beta_2v_{t-1}+(1-\beta_2)g_t^2.
$$

Первый момент переносит вперёд недавнее направление градиента — в этом состоит
интуиция метода импульса: совпадающие направления усиливают друг друга, а резкая
смена направления смягчается. Второй нецентрированный момент отслеживает недавний
квадрат величины градиента. Деление первого момента с поправкой на его
среднеквадратичный масштаб адаптирует каждую координату по соотношению недавнего
направления и величины; это не означает, что больший градиент всегда даёт меньший
по модулю шаг.

Нулевая инициализация занижает первые значения моментов. Компенсируйте это
занижение:

$$
\hat m_t=\frac{m_t}{1-\beta_1^t},
\qquad
\hat v_t=\frac{v_t}{1-\beta_2^t}.
$$

Теперь примените полную формулу этой главы:

$$
\hat m_t=\frac{m_t}{1-\beta_1^t},\quad \hat v_t=\frac{v_t}{1-\beta_2^t},\quad \theta_t=(1-\eta\lambda)\theta_{t-1}-\eta\frac{\hat m_t}{\sqrt{\hat v_t}+\varepsilon}
$$

То же обновление можно представить как вычитание двух явных слагаемых:
адаптивной поправки и $\eta\lambda\theta_{t-1}$. Затухание весов, отделённое от
градиентного обновления, означает, что второе слагаемое обходит $g_t$, $m_t$ и
$v_t$; итоговая замена при этом по-прежнему учитывает адаптивную ветвь.

В этой формуле $\lambda$ — эффективный коэффициент для текущего именованного
параметра: он равен заданному затуханию для параметра из группы с затуханием и
$0$ для параметра из группы без затухания.

Карта групп явно разбивает набор стабильных имён параметров: группы с затуханием
и без него не пересекаются, а их объединение содержит каждое имя. В этом примере
затухание применяется к `decoder.output.weight`, но не к
`decoder.norm.scale`, поэтому показаны оба варианта. Для рабочих моделей правила
группировки выбирают и описывают отдельно; исключение масштаба нормализации
позволяет не уменьшать напрямую этот аффинный параметр масштаба.

{/* chapter-section:symbol-glossary */}

## Различайте состояние шага и состояние параметра

- $\theta_{t-1}$ — один именованный тензор параметра до шага $t$.
- $g_t$ — его накопленный градиент функции потерь, усреднённой по токенам.
- $m_t$ и $v_t$ — экспоненциально сглаживаемые оценки той же формы, хранящиеся по стабильным именам.
- $\beta_1$ и $\beta_2$ задают сглаживание соответственно первого момента и второго нецентрированного момента.
- $\hat m_t$ и $\hat v_t$ вносят поправку на смещение моментов к нулю в начале обучения.
- $\eta>0$ — скорость обучения, а $\lambda\geq0$ — коэффициент затухания.
- $\varepsilon>0$ стабилизирует знаменатель адаптивной ветви после извлечения квадратного корня.
- $\theta_t$ — новый обучаемый листовой узел-параметр, который устанавливается лишь после успешной подготовки всего набора.

При проверке с новым состоянием $m_0=v_0=0$ и $g_1=0$, поэтому оба новых
момента и адаптивная поправка равны нулю. Параметр из группы с затуханием всё равно
уменьшается на $\eta\lambda\theta_0$, а параметр из группы без затухания не
меняется. Если раньше градиент был ненулевым, равенство $g_t=0$ убирает лишь новый
вклад: сохранённые моменты затухают, но всё ещё могут дать ненулевое адаптивное
обновление. Принадлежность к группе управляет затуханием весов, а не памятью
моментов.

{/* chapter-section:history */}

## От градиента по одному слову к декодерам, обучаемым с AdamW

[Bengio et al., *A Neural Probabilistic Language Model*](https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf)
описывают более ранний этап развития языковых моделей. В нейронной языковой
модели Бенжио и соавторов после предъявления одного слова из обучающего корпуса
вместе с его контекстом выполняется прямое стохастическое обновление параметров.
На каждом шаге единственная скалярная скорость обучения умножает градиент
текущего примера; памяти о предыдущих градиентах по отдельным координатам нет. В
обозначениях минимизации функции потерь основное действие выглядит просто:

$$
\theta\leftarrow\theta-\eta g.
$$

По сравнению с этим прямым правилом SGD метод импульса добавляет затухающую
память о прошлых направлениях. Позднее Adam хранит две экспоненциально сглаженные
оценки — первый момент и второй нецентрированный момент градиента — и вносит
поправку на их начальное смещение к нулю; если член $L_2$ включён в градиент, эта
пропорциональная параметру составляющая попадает в обе скользящие оценки. В AdamW
поправка, стягивающая параметр к нулю, напротив, вынесена из градиента, по которому
обновляются эти адаптивные моменты.

В кратких обозначениях минимизации функции потерь метод импульса использует
$u_t=\mu u_{t-1}+g_t$ и $\theta_t=\theta_{t-1}-\eta u_t$. Если штраф $L_2$
включён в градиент, в моменты Adam поступает $g_t+\lambda\theta_{t-1}$; в AdamW
входом для моментов остаётся $g_t$, а затухание весов применяется отдельно.
Здесь $u_t$ — накопленная скорость обновления, а $0\leq\mu\lt1$ — коэффициент её
сглаживания.

[Kingma and Ba, *Adam: A Method for Stochastic Optimization*](https://arxiv.org/pdf/1412.6980)
обосновывают адаптивный этап: Adam Кингмы и Ба хранит две экспоненциально
сглаженные оценки — первый момент и второй нецентрированный момент градиента — и
компенсирует их начальное смещение к нулю. Деление на
$1-\beta_1^t$ и $1-\beta_2^t$ нужно потому, что нулевая инициализация смещает
первые оценки к нулю. Adam — оптимизатор общего назначения, а не архитектура LLM.

[Loshchilov and Hutter, *Decoupled Weight Decay Regularization*](https://arxiv.org/pdf/1711.05101)
показывают, что штраф $L_2$, добавленный к градиенту функции потерь, не
равнозначен затуханию весов при использовании адаптивного оптимизатора. В AdamW
Лошчилова и Хуттера пропорциональное параметру затухание вынесено из градиента,
по которому обновляются адаптивные моменты.

В статье о LLaMA описано применение AdamW при предобучении декодерных языковых
моделей с числом параметров от $7$B до $65$B. Оптимизатор курса теперь превращает
усреднённые по токенам градиенты, полученные автоматическим дифференцированием, в
новые именованные листовые узлы-параметры; расписание скорости обучения,
ограничение нормы градиента, смешанная точность и распределённое состояние
относятся к последующим этапам конвейера.

Именно эта последовательность — от непосредственных градиентов предсказания
следующего слова до адаптивных обновлений с отдельным затуханием, применяемых при
предобучении декодеров, — определяет место AdamW на пути к современным LLM.

[Touvron et al., *LLaMA: Open and Efficient Foundation Language Models*](https://arxiv.org/pdf/2302.13971)
подтверждают современный пример: в статье о LLaMA описано применение AdamW при
предобучении декодерных языковых моделей с числом параметров от $7$B до $65$B. В
их конфигурации используются $\beta_1=0.9$,
$\beta_2=0.95$, затухание весов $0.1$, ограничение нормы градиента, разогрев и косинусное
снижение скорости обучения. В этой главе реализован механизм оптимизатора, а не
вся конфигурация крупномасштабного обучения.

Исторический пример выполняет два шага обычного SGD, метода импульса, Adam со
штрафом $L_2$, включённым в градиент, и AdamW для одной и той же скалярной
последовательности градиентов функции потерь, а затем строит заданную траекторию
при разной кривизне по координатам. Rust служит лишь исполняемой средой для этого
сравнения, которое не зависит от языка программирования:

<RustSource
  path="rust/demos/ch22-adamw/src/lib.rs"
  region="historical-optimizer-road"
  caption="Сопоставьте SGD, метод импульса, Adam со штрафом, включённым в градиент, и AdamW с отдельным затуханием"
  label="Исходный код на Rust для сопоставления этапов истории оптимизаторов LLM в главе 22"
/>

{/* chapter-section:rust-implementation */}

## Подготовьте все именованные замены до фиксации

Допустимые диапазоны скалярных параметров проверяются до создания состояния
оптимизатора:

<RustSource
  path="rust/crates/llm-from-scratch/src/training/adamw.rs"
  region="adamw-configuration"
  caption="Проверьте скорость обучения, коэффициенты сглаживания моментов, стабилизатор и затухание"
  label="Исходный код на Rust с проверкой конфигурации AdamW в главе 22"
/>

Явная карта групп назначает каждое имя ровно один раз:
`decoder.output.weight` входит в группу с затуханием, а
`decoder.norm.scale` — в группу без затухания. Пустое назначение в целом,
повторение имени внутри группы, пересечения, пропущенные имена и лишние назначения
отклоняются до фиксации; при этом любая из двух групп по отдельности может быть
пустой:

<RustSource
  path="rust/crates/llm-from-scratch/src/training/adamw.rs"
  region="adamw-parameter-groups"
  caption="Разбейте стабильные имена на явные группы с затуханием и без затухания"
  label="Исходный код на Rust с определением групп параметров в главе 22"
/>

Состояние моментов имеет ту же форму, что и соответствующий параметр, и хранится
по его стабильному имени:

<RustSource
  path="rust/crates/llm-from-scratch/src/training/adamw.rs"
  region="adamw-moment-state"
  caption="Свяжите каждую пару векторов моментов с одним стабильным именем"
  label="Исходный код на Rust с определением состояния моментов оптимизатора в главе 22"
/>

Данные шага сохраняют точные входные векторы, моменты, поправки и новые значения:

<RustSource
  path="rust/crates/llm-from-scratch/src/training/adamw.rs"
  region="adamw-state-and-evidence"
  caption="Покажите точные векторы, подготовленные для одного полного шага AdamW"
  label="Исходный код на Rust с определением подготовленных данных обновления в главе 22"
/>

Основная операция сначала копирует предполагаемое новое состояние, подготавливает
каждый новый листовой узел-параметр и фиксирует изменения только после успешной подготовки
всех кандидатов:

<RustSource
  path="rust/crates/llm-from-scratch/src/training/adamw.rs"
  region="transactional-adamw-step"
  caption="Вычислите и создайте полное именованное обновление как единую операцию"
  label="Исходный код на Rust с реализацией шага AdamW в главе 22"
/>

Типизированные ошибки охватывают недопустимые скалярные значения, пустые наборы и
повторы, изменившиеся имена и формы, отсутствующие либо несовпадающие градиенты,
переполнение счётчика, неконечные значения на промежуточных этапах и ошибки
создания тензоров:

<RustSource
  path="rust/crates/llm-from-scratch/src/training/adamw.rs"
  region="adamw-errors"
  caption="Отклоняйте недопустимые обновления без частичной замены параметров"
  label="Исходный код на Rust с определением ошибок AdamW в главе 22"
/>

Заданный пример обновляет два имени в стиле декодера, подтверждает нулевой
градиент новых листовых узлов-параметров, проверяет обновление только за счёт затухания при новом состоянии и
пытается выполнить обновление с изменившимся набором имён, чтобы проверить откат:

<RustSource
  path="rust/demos/ch22-adamw/src/lib.rs"
  region="chapter-adamw-fixture"
  caption="Подготовьте точные данные первого шага и атомарной операции"
  label="Исходный код на Rust для примера AdamW из главы 22"
/>

Исполняемый пример выводит все точные векторы и инварианты:

<RustSource
  path="rust/demos/ch22-adamw/src/main.rs"
  region="learner-adamw-output"
  caption="Выведите детерминированный отчёт для учащегося по главе 22"
  label="Исходный код на Rust для вывода результатов главы 22"
/>

Выполните `cargo run --quiet --locked -p ch22-adamw`, чтобы изучить точный первый
шаг, состояние, хранящееся по стабильным именам, замену листовых узлов-параметров и откат при
изменившемся наборе имён. Та же реализация поддерживает изменённый порядок
предъявления параметров, накопленные градиенты, обновление только за счёт затухания при новых нулевых
моментах и движение за счёт сохранённых моментов после последующего нулевого
градиента. При недопустимых значениях, идентичностях, формах, арифметических
результатах или наборах параметров операция завершается с ошибкой, не заменяя ни
одного листового узла-параметра и не изменяя состояния оптимизатора. Повторный
детерминированный запуск 200 шагов на анизотропной квадратичной функции побитно
совпадает с первым запуском и в конце даёт значение функции меньше $10^{-12}$.

{/* chapter-section:visualization */}

## Проследите, как затухание обходит ветвь моментов

Трассировка из четырнадцати строк содержит две записи параметров с указанием
групп, по пять точек траекторий SGD и AdamW и одно доказательство свойств полного
набора. Первая схема проводит вес из группы с затуханием через все моменты и
поправки. Во второй собраны параметр масштаба нормализации без затухания, обе траектории и
доказательство атомарной операции. Все показанные числа сформированы программой
на Rust:

<RustSource
  path="rust/demos/ch22-adamw/src/diagram_trace.rs"
  region="adamw-trace"
  caption="Выведите точные данные о моментах, поправках, заменах и откате"
  label="Исходный код на Rust, формирующий трассировку для схемы AdamW в главе 22"
/>

<AdamwDiagram labels={diagramLabels} instanceId="chapter-22" />

<AdamwEvidenceDiagram labels={diagramLabels} instanceId="chapter-22" />

В трассировке записаны точные имена, формы, векторы, знаменатели поправок и данные
атомарной операции. В подробной схеме сплошная адаптивная ветвь и пунктирная
ветвь затухания дополнительно различаются подписями и границами. Затем компактная
запись группы показывает нулевую поправку затухания для параметра без затухания.
Обе траектории используют одни и те же заданные точки функции
$q(x,y)=\frac12(x^2+4y^2)$. Проследите путь градиента
функции потерь через моменты, сопоставьте его с отдельным затуханием,
пропорциональным параметру, и объедините оба вклада при итоговом вычитании.

{/* chapter-section:exercises */}

## Сначала предскажите результат, затем запустите оптимизатор

1. Выведите $m_1$, $v_1$, $\hat m_1$ и $\hat v_1$ для заданного вектора.
2. До округления до шести знаков предскажите две поправки и $\theta_1$.
3. Создайте новое состояние оптимизатора для значения $3$ из группы с затуханием при $g_1=0$ и предскажите новое значение. Затем объясните, почему после ненулевой предыстории более поздний нулевой градиент всё равно может дать вклад адаптивной ветви.
4. Объясните, почему при изменении порядка двух записей параметры должны сохранить моменты, связанные с их стабильными именами.
5. Предскажите результат, если после локальной успешной подготовки первого кандидата при вычислении второго возникает переполнение.
6. Предскажите сохранённый градиент и объясните, будет ли у параметра после успешной замены новый узел ленты операций.
7. Расположите по порядку обычный SGD, метод импульса, Adam со штрафом $L_2$, включённым в градиент, и AdamW, а затем укажите, какие слагаемые попадают в память оптимизатора.
8. Объясните, почему к выходной матрице весов применяется затухание, а к масштабу нормализации — нет.
9. Сопоставьте заданные траектории SGD и AdamW на квадратичной функции с разной кривизной по координатам.

<details>
  <summary>Сверить предсказания</summary>

  1. $m_1=[0.1,-0.2]$, $v_1=[0.02,0.08]$, $\hat m_1=[0.2,-0.4]$, а $\hat v_1=[0.04,0.16]$.
  2. Адаптивная поправка приблизительно равна $[0.066667,-0.08]$, поправка затухания — $[0.01,-0.02]$, а $\theta_1\approx[0.923333,-1.9]$.
  3. При новых нулевых моментах адаптивная поправка остаётся нулевой, поправка затухания равна $0.03$, а новое значение — $2.97$. После ненулевой предыстории сохранённые моменты могут поддерживать ненулевую адаптивную поправку даже при нулевом текущем градиенте.
  4. Карта состояния использует стабильные имена; порядок в срезе влияет только на представление.
  5. Ничего не фиксируется: оба прежних листовых узла-параметра и всё состояние оптимизатора остаются без изменений.
  6. У нового листового узла-параметра градиент в точности равен нулю; после замены параметр связан с новым узлом ленты операций.
  7. Обычный SGD использует только текущий градиент; метод импульса помнит затухающее направление; при штрафе $L_2$, включённом в градиент, пропорциональная параметру штрафная составляющая попадает в моменты Adam; AdamW оставляет затухание весов вне моментов. Бенжио задаёт отправную точку в виде нейронной языковой модели, а Туврон и соавторы описывают AdamW при предобучении LLaMA.
  8. В этом примере `decoder.output.weight` находится в группе с затуханием, а `decoder.norm.scale` — в группе без затухания, поэтому исключение задано явно.
  9. SGD гораздо быстрее уменьшает координату с высокой кривизной; AdamW масштабирует координаты с учётом истории их моментов и дополнительно применяет отдельный вклад затухания.
</details>

{/* chapter-section:decoder-connection */}

## Далее обучите первую цельную нейронную языковую модель

Теперь совокупный путь обучения умеет связывать каждый накопленный градиент
функции потерь, усреднённой по токенам, со стабильным именем соответствующего
параметра, сохранять под этим именем первый и второй моменты между шагами и
атомарно фиксировать новый листовой узел-параметр с нулевым градиентом. В главе 23 этот
оптимизатор будет использован для обучения нейронной языковой модели с
фиксированным контекстом и проверки снижения потерь на валидационной выборке.

AdamW изменяет параметры, но ещё не определяет модель, градиенты которой он
получает. В следующей главе этот контур замкнётся, при этом разбиение данных на выборки,
автоматическое дифференцирование, пакетная обработка, имена параметров и состояние
оптимизатора останутся явными.
