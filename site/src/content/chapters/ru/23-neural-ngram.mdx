---
{
  "chapter_id": "23-neural-ngram",
  "locale": "ru",
  "content_revision": 5,
  "order": 23,
  "concept_id": "neural-ngram",
  "title": "Обучите нейронную языковую модель с фиксированным контекстом",
  "description": "Объедините эмбеддинги, скрытый слой SwiGLU, функцию потерь следующего токена с выбором по индексу, мини-пакеты и AdamW в детерминированной нейронной n-граммной модели со снижением валидационных потерь.",
  "objective": "Обучить языковую модель с фиксированным контекстом, использующую эмбеддинги и SwiGLU, так чтобы её функция потерь на валидационной выборке снизилась относительно начального значения.",
  "worked_inputs": "Возьмите один полный двухтокенный контекст $[z_{t-2},z_{t-1}]$ и единственный следующий за ним целевой токен $z_t$. При $D=4$, $H=8$ и $V=266$ заранее определите формы тензоров после выбора строк эмбеддингов по ID токенов, конкатенации, SwiGLU и проекции в пространство словаря, прежде чем смотреть на числовые результаты.",
  "formula": {
    "latex": "h=\\operatorname{SwiGLU}([E_{z_{t-C}},\\ldots,E_{z_{t-1}}]),\\quad \\ell=hW_o",
    "symbols": [
      {
        "symbol": "h",
        "meaning": "скрытый вектор, вычисленный по одному полному фиксированному контексту"
      },
      {
        "symbol": "\\operatorname{SwiGLU}",
        "meaning": "вентильное преобразование сети прямого распространения, переводящее конкатенированные признаки контекста в скрытый вектор"
      },
      {
        "symbol": "E",
        "meaning": "обучаемая таблица эмбеддингов токенов с осями словаря и признаков"
      },
      {
        "symbol": "z_i",
        "meaning": "целочисленный ID токена в позиции i последовательности"
      },
      {
        "symbol": "t",
        "meaning": "позиция последовательности, токен в которой предсказывает модель"
      },
      {
        "symbol": "C",
        "meaning": "фиксированное положительное число предшествующих ID токенов, используемых для одного предсказания"
      },
      {
        "symbol": "[\\,\\cdot\\,]",
        "meaning": "конкатенация C векторов эмбеддингов вдоль одной оси признаков"
      },
      {
        "symbol": "W_o",
        "meaning": "выходная матрица проекции из скрытого пространства в пространство словаря"
      },
      {
        "symbol": "\\ell",
        "meaning": "вектор из V логитов следующего токена"
      }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "language-model",
      "limitation": "Классические счётные n-граммные модели оценивают каждый короткий контекст отдельно, поэтому по мере роста числа возможных последовательностей для редких и невстречавшихся сочетаний остаётся мало полезной статистики.",
      "later_advance": "В этой модели распределённые векторы слов и функция сети прямого распространения, применяемая к конкатенированному фиксированному контексту, обучаются совместно. Благодаря этому сведения об одной последовательности помогают оценивать последовательности, составленные из слов с близкими векторными представлениями.",
      "modern_llm_role": "Позднее модели Transformer заменяют смешивание фиксированного контекста маскированным механизмом самовнимания, сохраняя обучаемые эмбеддинги, преобразования сети прямого распространения, одинаково применяемые к каждой позиции, и проекцию в пространство словаря для предсказания следующего токена.",
      "sources": [
        {
          "role": "earlier",
          "year": 2003,
          "name": "Bengio et al., A Neural Probabilistic Language Model",
          "source_url": "https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf",
          "claim": "Бенжио и соавторы преобразуют фиксированный контекст с помощью обучаемых распределённых представлений слов и сети прямого распространения в распределение вероятностей следующего слова."
        },
        {
          "role": "later",
          "year": 2017,
          "name": "Vaswani et al., Attention Is All You Need",
          "source_url": "https://arxiv.org/pdf/1706.03762",
          "claim": "Васвани и соавторы заменяют рекуррентные и свёрточные слои механизмом внимания и маскируют самовнимание декодера, чтобы при авторегрессионном предсказании позиция не могла использовать последующие позиции."
        }
      ]
    },
    "approach": "От раздельного подсчёта коротких контекстов — через обучаемые распределённые признаки в языковой модели на основе сети прямого распространения — к маскированному вниманию по последовательностям декодера",
    "summary": "Нейронная n-граммная модель — важный связующий этап на пути к современным LLM: эмбеддинги позволяют разным токенам использовать общие признаки, обучаемое нелинейное преобразование объединяет полный контекст, а функция потерь следующего токена совместно обучает все матрицы. Позднее механизм внимания устраняет ограничение, связанное с конкатенацией фиксированного контекста.",
    "rust_source": "rust/demos/ch23-neural-ngram/src/lib.rs"
  },
  "rust_sources": [
    {
      "path": "rust/demos/ch23-neural-ngram/src/lib.rs",
      "region": "historical-context-road",
      "purpose": "Сопоставить раздельно подсчитанные точные контексты с общими обучаемыми признаками и показать переход от счётных n-грамм к обучаемым представлениям языковой модели."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/models/neural_ngram.rs",
      "region": "neural-ngram-config-and-errors",
      "purpose": "Проверить все размерности модели, производные размеры, набор параметров, пакет, целевые токены и делегированные операции с помощью типизированных ошибок."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/models/neural_ngram.rs",
      "region": "neural-ngram-parameter-owner",
      "purpose": "Хранить объекты эмбеддингов, SwiGLU и выходной проекции на протяжении всей жизни модели; они и упорядоченный реестр параметров ссылаются на одни и те же пять листовых узлов."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/models/neural_ngram.rs",
      "region": "neural-ngram-forward",
      "purpose": "Выполнить точную цепочку форм, выбрать последний сдвинутый целевой токен, вычислить среднее отрицательное логарифмическое правдоподобие по индексам и детерминированно выбрать жадное продолжение."
    },
    {
      "path": "rust/demos/ch23-neural-ngram/src/lib.rs",
      "region": "chapter-neural-ngram-fixture",
      "purpose": "Обучить два независимых запуска с заданными начальными условиями и проверить улучшение целевой функции, нормы градиентов, сохранение узлов параметров, явное обнуление градиентов, генерацию и побитовый повтор."
    },
    {
      "path": "rust/demos/ch23-neural-ngram/src/main.rs",
      "region": "learner-neural-ngram-output",
      "purpose": "Вывести полный детерминированный отчёт для учащегося."
    },
    {
      "path": "rust/demos/ch23-neural-ngram/src/diagram_trace.rs",
      "region": "neural-ngram-trace",
      "purpose": "Сформировать строгие локализационно-нейтральные записи конвейера, функции потерь, генерации и проверок для статической схемы."
    }
  ],
  "visualization": {
    "decision": "useful",
    "id": "neural-ngram",
    "rationale": "Пятиэтапная схема не смешивает оси контекста, эмбеддингов, конкатенированных признаков, скрытого пространства и словаря, а расположенная рядом последовательность значений функции потерь показывает улучшение на отложенных данных."
  },
  "decoder_connection": "Теперь собранные компоненты обучают полную модель следующего токена с фиксированным контекстом, используя зафиксированное разбиение данных и AdamW, а затем детерминированно генерируют токены жадным алгоритмом. В главах 24–32 конкатенацию фиксированного контекста заменит каузальное смешивание информации между позициями последовательности на основе остаточных связей, нормализации и механизма внимания."
}
---

import RustSource from '../../../components/RustSource.astro';
import NeuralNgramDiagram from '../../../components/chapters/NeuralNgramDiagram.astro';

export const diagramLabels = {
  title: 'Проследите путь одного контекста через обучение',
  description:
    'Проследите, как точные ID токенов из вывода Rust проходят через эмбеддинги, конкатенацию, скрытое состояние и логиты словаря, а затем сравните функции потерь на полных обучающей и валидационной выборках.',
  summary: {
    model: 'Заданная конфигурация модели',
    data: 'Зафиксированное разбиение данных',
    optimizer: 'Заданные настройки оптимизатора',
  },
  stages: {
    pipeline: 'Преобразуйте один полный контекст',
    checkpoints: 'Сравните функции потерь на полных выборках',
    result: 'Измерьте улучшение на отложенных данных',
    generation: 'Продолжите последовательность обученной моделью',
    proof: 'Проверьте границы обучения',
  },
  stageNames: {
    context_ids: 'ID токенов контекста',
    embeddings: 'Строки эмбеддингов',
    concatenated: 'Конкатенированные признаки',
    hidden: 'Скрытое состояние SwiGLU',
    logits: 'Логиты словаря',
  },
  fields: {
    shape: 'Форма',
    values: 'Значения Rust',
    argmax: 'ID токена с максимальным начальным логитом',
    argmaxLogit: 'Максимальный начальный логит',
    step: 'Шаг оптимизатора',
    trainLoss: 'Функция потерь на обучающей выборке',
    validationLoss: 'Функция потерь на валидационной выборке',
    initialValidation: 'Начальная функция потерь на валидации',
    finalValidation: 'Итоговая функция потерь на валидации',
    improvement: 'Снижение функции потерь на валидации',
    prompt: 'Начальный текст',
    promptIds: 'ID начального текста',
    generatedIds: 'Сгенерированные ID токенов',
    stop: 'Причина остановки',
    replay: 'Независимый повторный запуск',
    testText: 'Граница тестового текста',
    target: 'Правило выбора цели',
    gradientL1: 'Проверка градиентов матриц на первом шаге',
    parameterNodes: 'Сохранение узлов параметров',
    gradients: 'Градиенты после явного обнуления',
    generationPolicy: 'Правило генерации',
  },
  cues: {
    input: 'Целочисленные индексы',
    learned: 'Обучаемые признаки',
    output: 'Оценки классов',
    checkpoint: 'Контрольное измерение',
    final: 'Заданный конечный шаг',
  },
  notes: {
    pipeline:
      'Все тензоры и показанные значения получены при первом прямом проходе с заданным зерном инициализации. Конкатенация меняет расположение данных, но не их значения.',
    checkpoints:
      'Каждая функция потерь охватывает всю соответствующую выборку и взвешивает каждый пакет по фактическому числу строк. В ходе предварительного эксперимента результаты на валидационной выборке использовали, чтобы установить бюджет из 15 обновлений. В опубликованном запуске этот бюджет задан до начала обучения, а показанные измерения не используются для выбора контрольного шага.',
    generation:
      'Это ID токенов и сохранённые байты намеренно маленькой недообученной модели, а не утверждение о получении читаемого текста.',
  },
  captions: {
    pipeline: 'Точные данные прямого прохода с фиксированным контекстом',
    checkpoints: 'Полученная в Rust последовательность функций потерь на обучении и валидации',
    proof: 'Полученные в Rust проверки повторяемости и границ выборок',
  },
  scrollers: {
    pipeline: 'Прокручиваемая схема модели с фиксированным контекстом',
    generation: 'Прокручиваемая последовательность ID сгенерированных токенов',
  },
};

{/* chapter-section:worked-example */}

## Предскажите один токен по полному контексту

Возьмите один полный двухтокенный контекст $[z_{t-2},z_{t-1}]$ и единственный
следующий за ним целевой токен $z_t$. При $D=4$, $H=8$ и $V=266$ заранее
определите формы тензоров после выбора строк эмбеддингов по ID токенов,
конкатенации, SwiGLU и проекции в пространство словаря, прежде чем смотреть на
числовые результаты.

Для размера пакета $B=1$ получается:

$$
[1,2]\to[1,2,4]\to[1,8]\to[1,8]\to[1,266].
$$

Заданная начальная строка `At` кодируется буквальными программными ID
`[67, 118]`. Сначала найдите обе строки таблицы эмбеддингов и лишь затем
определяйте предсказание модели. При инициализации с заданным зерном логиты почти
равны; точный $\operatorname{argmax}$ приходится на токен с ID `44`,
но это ещё не выученный лингвистический вывод.

{/* chapter-section:formula */}

## Перед скрытым слоем конкатенируйте эмбеддинги

Используйте точную общую формулу главы:

$$
h=\operatorname{SwiGLU}([E_{z_{t-C}},\ldots,E_{z_{t-1}}]),\quad \ell=hW_o
$$

$E\in\mathbb{R}^{V\times D}$ — одна обучаемая таблица, общая для всех позиций
токенов. Каждый из $C$ индексов извлекает строку из
$\mathbb{R}^{D}$. Скобки обозначают конкатенацию этих строк в хронологическом
порядке, поэтому на вход SwiGLU поступают $CD$ признаков. Преобразование
возвращает $h\in\mathbb{R}^{H}$, а матрица
$W_o\in\mathbb{R}^{H\times V}$ проецирует скрытый вектор в
$\ell\in\mathbb{R}^{V}$.

Для пакета итоговое умножение имеет вид
$[B,H][H,V]=[B,V]$. На выходе находится по одному логиту для каждого элемента
словаря, а не по одному признаку эмбеддинга для каждого токена.

{/* chapter-section:symbol-glossary */}

## Не смешивайте оси контекста, признаков и словаря

- $z_i$ — целочисленный ID токена в позиции $i$ последовательности.
- $t$ — предсказываемая позиция, а $C>0$ — фиксированная длина её контекста.
- $E$ сопоставляет ID токена с $D$ обучаемыми признаками.
- $[\,\cdot\,]$ конкатенирует $C$ строк эмбеддингов вдоль оси признаков.
- $\operatorname{SwiGLU}$ преобразует $CD$ признаков контекста в скрытый вектор $h$.
- $W_o$ переводит $H$ скрытых признаков в $V$ оценок элементов словаря.
- $\ell$ — вектор из $V$ логитов следующего токена.

Каждой строке контекста соответствует один целевой токен $y_b$. Средняя функция
потерь с выбором целевого логита по индексу равна

$$
L=-\frac{1}{B}\sum_{b=1}^{B}\log
\frac{\exp(\ell_{b,y_b})}{\sum_{j=0}^{V-1}\exp(\ell_{b,j})}.
$$

В главе 21 в каждой строке хранятся $C$ сдвинутых целевых токенов. Эта модель
намеренно использует только
$y_b=\operatorname{target\_row}(b)_{C-1}$ — токен сразу после полного контекста.
Если использовать все $C$ элементов, незаметно изменятся и целевая функция, и
её знаменатель.

{/* chapter-section:history */}

## От разреженных счётчиков к обучаемым контекстам и вниманию

Классические счётные n-граммные модели оценивают каждый короткий контекст отдельно,
поэтому по мере роста числа возможных последовательностей для редких и
невстречавшихся сочетаний остаётся мало полезной статистики. В показанном ниже
расчёте `historical_context_evidence` биграмма, ключом которой служит только
последний токен $11$,
объединяет статистику по двум возможным следующим токенам, тогда как более
широкие контексты $[10,11]$ и $[20,11]$ сохраняют по одному различному следующему
токену.

[*A Neural Probabilistic Language Model*](https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf)
описывает следующий, нейронный этап. Бенжио и соавторы преобразуют фиксированный
контекст с помощью обучаемых распределённых представлений слов и сети прямого
распространения в распределение вероятностей следующего слова. В этой модели
распределённые векторы слов и функция сети прямого распространения, применяемая
к конкатенированному фиксированному контексту, обучаются совместно. Благодаря
этому сведения об одной последовательности помогают оценивать
последовательности, составленные из слов с близкими векторными представлениями.
[Васвани и соавторы, *Attention Is All You Need*](https://arxiv.org/pdf/1706.03762)
показывают следующий этап развития моделей последовательностей. Васвани и
соавторы заменяют рекуррентные и свёрточные слои механизмом внимания и маскируют
самовнимание декодера, чтобы при авторегрессионном предсказании позиция не могла
использовать последующие позиции. Позднее модели Transformer заменяют смешивание
фиксированного контекста маскированным механизмом самовнимания, сохраняя обучаемые
эмбеддинги, преобразования сети прямого распространения, одинаково применяемые к
каждой позиции, и проекцию в пространство словаря для предсказания следующего
токена.

Нейронная n-граммная модель — важный связующий этап на пути к современным LLM:
эмбеддинги позволяют разным токенам использовать общие признаки, обучаемое
нелинейное преобразование объединяет полный контекст, а функция потерь следующего
токена совместно обучает все матрицы. Позднее механизм внимания устраняет
ограничение, связанное с конкатенацией фиксированного контекста.

В этих работах не заданы используемые в курсе словарь BPE, SwiGLU, размерности,
константы оптимизатора, зёрна генератора, правило выбора последнего целевого
токена и предел генерации. Этот расчёт показывает переход между моделями:
точные таблицы счётчиков уступают место общим обучаемым признакам, а затем
маскированное внимание устраняет фиксированное окно конкатенации.

<RustSource
  path="rust/demos/ch23-neural-ngram/src/lib.rs"
  region="historical-context-road"
  caption="Сопоставьте точные счётные контексты с общими обучаемыми признаками"
  label="Исходный код Rust для исторического сопоставления языковых моделей в главе 23"
/>

{/* chapter-section:rust-implementation */}

## Храните единый набор параметров на всех шагах

Конфигурация и ошибки позволяют обнаружить неверные размерности модели,
переполнение производных размеров, несоответствие параметров, некорректные пакеты,
выход целевых токенов за границы словаря и неконечные оценки жадного выбора до
того, как их можно будет принять за результат обучения:

<RustSource
  path="rust/crates/llm-from-scratch/src/models/neural_ngram.rs"
  region="neural-ngram-config-and-errors"
  caption="Проверьте размерности модели и типизированные границы ошибок"
  label="Исходный код Rust с конфигурацией и ошибками нейронной n-граммной модели главы 23"
/>

Модель владеет ровно пятью матричными узлами. Упорядоченный реестр параметров и
объекты слоёв — эмбеддинги, SwiGLU и выходная проекция — ссылаются на одни и те
же узлы. AdamW записывает проверенные новые значения в существующие узлы,
поэтому следующий прямой проход использует обновлённые параметры через уже
созданные слои. Повторно собирать слои после шага оптимизатора не нужно:

<RustSource
  path="rust/crates/llm-from-scratch/src/models/neural_ngram.rs"
  region="neural-ngram-parameter-owner"
  caption="Свяжите реестр и объекты слоёв с одним набором из пяти узлов"
  label="Исходный код Rust с владельцем параметров главы 23"
/>

Прямой проход обеспечивает точную цепочку форм, выбирает только последний
сдвинутый целевой токен, вычисляет среднее отрицательное логарифмическое
правдоподобие с выбором по индексу вдоль оси словаря $1$ и использует для жадной
генерации стабильное разрешение ничьей в пользу меньшего ID:

<RustSource
  path="rust/crates/llm-from-scratch/src/models/neural_ngram.rs"
  region="neural-ngram-forward"
  caption="Выполните предсказание с фиксированным контекстом, функцию потерь и детерминированный выбор максимума"
  label="Исходный код Rust с прямым проходом и функцией потерь главы 23"
/>

Зафиксированный пример обучает восемь правил BPE только по обучающим документам,
не читает текст тестовых документов, создаёт один порядок пакетов с зерном `23`
и использует первые $15$ пакетов. AdamW использует каждый исходный градиент при
вычислении обновления и оставляет этот градиент в том же узле параметра. После
обновления пример явно вызывает `zero_grad()` для всех пяти узлов, прежде чем
начинать следующий прямой проход. Полные целевые функции на
обучающей и валидационной выборках вычисляются на шагах $0$, $8$ и $15$ с
взвешиванием по фактическому числу строк. Два независимо инициализированных
запуска должны побитово совпасть по всем проверяемым значениям:

<RustSource
  path="rust/demos/ch23-neural-ngram/src/lib.rs"
  region="chapter-neural-ngram-fixture"
  caption="Обучите и повторите заданный пример со снижением потерь на отложенных данных"
  label="Исходный код Rust с обучающим примером главы 23"
/>

Исполняемый файл выводит точный отчёт для учащегося:

<RustSource
  path="rust/demos/ch23-neural-ngram/src/main.rs"
  region="learner-neural-ngram-output"
  caption="Выведите детерминированный отчёт главы 23"
  label="Исходный код Rust с выводом для учащегося в главе 23"
/>

Запустите `cargo run --quiet --locked -p ch23-neural-ngram`. Стандартный вывод
зафиксирован в `rust/demos/ch23-neural-ngram/expected.txt`, поэтому формы, функции
потерь, сгенерированные ID и данные повторного запуска можно точно сопоставить со
значениями, разобранными в главе.

{/* chapter-section:visualization */}

## Проследите один контекст и функцию потерь на отложенных данных

Точная трассировка содержит все пять этапов первого прямого прохода, три измерения
целевой функции на полных выборках, заданное итоговое сравнение, ID сгенерированных
токенов и записи проверок. Совместное чтение этих записей связывает один прямой
проход с измеренным улучшением на отложенных данных, не добавляя второй набор
значений, вычисленных вручную:

<RustSource
  path="rust/demos/ch23-neural-ngram/src/diagram_trace.rs"
  region="neural-ngram-trace"
  caption="Сформируйте точные данные конвейера, потерь на отложенной выборке, генерации и повторяемости"
  label="Исходный код Rust с трассировкой для схемы главы 23"
/>

<NeuralNgramDiagram labels={diagramLabels} />

Начальные строки контекста конкатенируются без изменения порядка чисел. До
фиксации этого учебного примера в ходе предварительного эксперимента результаты
на валидационной выборке использовали, чтобы установить бюджет из $15$
обновлений: такого бюджета оказалось достаточно для заданного в главе порога
улучшения. В опубликованном запуске этот бюджет уже задан до начала обучения:
измерения на шагах $0$, $8$ и $15$ только попадают в отчёт и не используются для
динамического выбора контрольного шага. В этом запуске полная функция потерь на
обучающей выборке снижается с $5.583505$ до $5.555850$, а на отложенной
валидационной выборке — с $5.583482$ до $5.557362$. При показанной точности
снижение составляет $0.026120$ нат; глава не утверждает, что функция потерь
снижается монотонно.

Жадное продолжение выдаёт ID токенов
$[259,211,211,211,211,211,211,211,211,211,211,211]$ и останавливается после
двенадцати новых токенов. После первого токена сохранённые байты не образуют
корректную строку UTF-8, поэтому схема показывает их как ID намеренно маленькой
недообученной модели, а не приписывает им вымышленный читаемый текст.

{/* chapter-section:exercises */}

## Сначала сделайте предсказания

1. Определите все формы для $B=64$, $C=2$, $D=4$, $H=8$ и $V=266$.
2. Выберите целевой токен из сдвинутой строки ширины $C$.
3. Определите, у каких из пяти матриц после первого шага должна быть положительная конечная $L_1$-норма градиента.
4. Объясните, как итоговый пакет из $300$ строк входит в среднее по всей обучающей выборке.
5. Решите, обязана ли функция потерь на валидации снижаться на каждом контрольном шаге.
6. Решите, что увидят постоянный объект слоя и копия дескриптора
   `NamedParameter` после обновления их общего узла с помощью AdamW, а также
   обнуляет ли AdamW градиент этого узла.
7. Предскажите результат жадной генерации, если наибольший логит принадлежит BOS.
8. Укажите операцию, которая привела бы к утечке тестовых данных.

<details>
  <summary>Проверьте предсказания</summary>

  1. Цепочка форм: $[64,2]\to[64,2,4]\to[64,8]\to[64,8]\to[64,266]$.
  2. Используется только элемент строки целей с индексом $C-1$ — токен после полного контекста.
  3. Матрицы эмбеддингов, вентильной ветви, ветви расширения, проекции сжатия и выходной проекции имеют положительные конечные $L_1$-нормы градиентов.
  4. Умножьте среднее по этому пакету на $300$, прибавьте взвешенные суммы остальных пакетов и разделите на все $1836$ контекстов.
  5. Нет. В этом примере итоговая функция потерь на валидации должна быть меньше начальной более чем на $0.01$ нат, но монотонность не требуется.
  6. При копировании `NamedParameter` копируется дескриптор, а не данные тензора и не сам узел. Копия дескриптора и постоянный объект слоя по-прежнему ссылаются на один узел `TensorValue`, поэтому оба увидят новое значение. AdamW оставляет накопленный градиент в этом узле, а цикл обучения явно вызывает `zero_grad()` после обновления. Для независимого повторного запуска создаётся отдельная модель с тем же зерном инициализации.
  7. BOS маскируется, поэтому $\operatorname{argmax}$ выбирает наибольший из оставшихся конечных логитов; при точном равенстве побеждает меньший ID.
  8. Границу нарушило бы чтение текста тестовых документов для подгонки, кодирования, выбора, ранней остановки, оценки или показа.
</details>

{/* chapter-section:decoder-connection */}

## Далее замените фиксированный контекст каузальным обменом информацией

Теперь собранные компоненты обучают полную модель следующего токена с
фиксированным контекстом, используя зафиксированное разбиение данных и AdamW, а
затем детерминированно генерируют токены жадным алгоритмом. В главах 24–32
конкатенацию фиксированного контекста заменит каузальное смешивание информации
между позициями последовательности на основе остаточных связей, нормализации и
механизма внимания.

Модель уже работоспособна, но намеренно ограничена: каждое предсказание видит
ровно $C$ предшествующих ID независимо от объёма более раннего контекста. Следом
появятся остаточные пути, а затем нормализация и маскированное самовнимание
расширят вычисления по последовательности.
