---
{
  "chapter_id": "27-self-attention",
  "locale": "ru",
  "content_revision": 2,
  "order": 27,
  "concept_id": "scaled-dot-product-self-attention",
  "title": "Вычислите одну голову самовнимания без маски",
  "description": "Разберите, как одна голова самовнимания Transformer без маски сопоставляет запросы с ключами, нормирует каждую строку и смешивает значения на основе проверяемого примера на Rust.",
  "objective": "Вычислите одну голову внимания без маски и объясните каждую оценку, вероятность внимания и взвешенное значение.",
  "worked_inputs": "Используйте полученные в главе 26 тензоры $Q=[[[0,3],[2,-1]]]$, $K=[[[3,0],[-1,2]]]$ и $V=[[[3,-3],[1,3]]]$. До запуска примера предскажите, строка какого значения получит больший вес для каждого запроса.",
  "formula": {
    "latex": "A=\\operatorname{softmax}\\left(\\frac{QK^\\top}{\\sqrt{d_k}}\\right),\\quad O=AV",
    "symbols": [
      { "symbol": "Q", "meaning": "тензор запросов, каждая строка которого задаёт, какое содержимое следует учесть в соответствующей позиции токена" },
      { "symbol": "K", "meaning": "тензор ключей, строки которого сопоставляются с каждой строкой запросов" },
      { "symbol": "d_k", "meaning": "общая ширина признаков запросов и ключей, используемая для масштабирования их скалярных произведений" },
      { "symbol": "A", "meaning": "тензор нормированных вероятностей внимания, полученных нормировкой каждой строки запроса по позициям ключей" },
      { "symbol": "V", "meaning": "тензор значений, строки которого несут содержимое для смешивания с весами внимания" },
      { "symbol": "O", "meaning": "выходной тензор с одной взвешенной смесью значений для каждой позиции запроса" }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "neural-architecture",
      "limitation": "Базовая рекуррентная модель энкодера–декодера может пропускать всё исходное предложение через один вектор фиксированного размера; при аддитивном внимании декодер всё ещё обрабатывает выходную последовательность рекуррентно и на каждом шаге заново вычисляет выравнивание с исходной последовательностью.",
      "later_advance": "Transformer объединяет множество запросов в матрицы и с помощью самовнимания на основе масштабированного скалярного произведения сопоставляет позиции одной доступной последовательности, поэтому слой может сформировать всю матрицу оценок пакетными матричными операциями.",
      "modern_llm_role": "Каждая голова самовнимания декодера превращает обученные запросы и ключи в построчно нормированные веса внимания, а затем смешивает обученные значения; каузальный декодер перед нормировкой дополнительно скрывает будущие позиции ключей.",
      "sources": [
        {
          "role": "earlier",
          "year": 2014,
          "name": "Bahdanau, Cho, and Bengio, Neural Machine Translation by Jointly Learning to Align and Translate",
          "source_url": "https://arxiv.org/abs/1409.0473",
          "claim": "Бахданау, Чо и Бенжио описывают возможное узкое место базовой модели энкодера–декодера в виде вектора фиксированной длины и вычисляют каждый новый контекст как взвешенную по softmax сумму аннотаций энкодера, оценки для которых зависят от предыдущего состояния декодера."
        },
        {
          "role": "later",
          "year": 2017,
          "name": "Vaswani et al., Attention Is All You Need",
          "source_url": "https://arxiv.org/abs/1706.03762",
          "claim": "Васвани и соавторы определяют внимание на основе масштабированного скалярного произведения как нормированную по softmax матрицу масштабированных скалярных произведений запросов и ключей, применяемую к значениям, объединяют одновременно обрабатываемые запросы в матрицу и определяют самовнимание как установление связей между позициями одной последовательности."
        }
      ]
    },
    "approach": "От фиксированного рекуррентного контекста через аддитивное выравнивание энкодера и декодера к самовниманию на основе масштабированного скалярного произведения",
    "summary": "Аддитивное внимание заменило одно фиксированное представление исходной последовательности выбором контекста из аннотаций энкодера с учётом состояния декодера. В самовнимании на основе масштабированного скалярного произведения каждая позиция формирует запрос, а значение каждой доступной позиции может войти в результат: механизм строит матрицу соответствий, нормирует каждую строку запроса и смешивает строки значений. В этой главе видны все позиции; глава 28 добавит каузальное ограничение видимости.",
    "rust_source": "rust/demos/ch27-self-attention/src/lib.rs"
  },
  "rust_sources": [
    {
      "path": "rust/demos/ch27-self-attention/src/lib.rs",
      "region": "historical-attention-contrast",
      "purpose": "Сопоставить раздельные источники запросов и ключей при выравнивании энкодера и декодера со всеми парами запросов и ключей из одной последовательности."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/attention/self_attention.rs",
      "region": "self-attention-forward",
      "purpose": "Составить одну голову внимания без маски на основе масштабированного скалярного произведения из уже реализованных дифференцируемых операций над тензорами."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/attention/self_attention.rs",
      "region": "self-attention-errors",
      "purpose": "Явно представить типизированные ошибки входов и этапов прямого прохода с однозначным порядком проверки."
    },
    {
      "path": "rust/demos/ch27-self-attention/src/lib.rs",
      "region": "self-attention-fixture",
      "purpose": "Собрать данные прямого и обратного проходов, масштабирования, форм, границ, проверки градиентов и точной воспроизводимости для разобранного примера."
    },
    {
      "path": "rust/demos/ch27-self-attention/src/main.rs",
      "purpose": "Запустить полный пример самовнимания и вывести вычисленные данные."
    }
  ],
  "visualization": {
    "decision": "useful",
    "id": "self-attention",
    "rationale": "Если проследить путь одного запроса через строку оценок, нормированные вероятности, взвешенные слагаемые значений и выход, становится наглядно видно само вычисление и построчную нормировку."
  },
  "decoder_connection": "Накопительный декодер теперь преобразует одну тройку спроецированных тензоров запросов, ключей и значений в выход головы внимания без маски. В главе 28 будущие позиции ключей будут исключены до нормировки каждой строки оценок."
}
---

import RustSource from '../../../components/RustSource.astro';
import SelfAttentionDiagram from '../../../components/chapters/SelfAttentionDiagram.astro';

export const diagramLabels = {
  title: 'Проследите путь каждой оценки до взвешенной смеси значений',
  description:
    'Проследите точные запросы, ключи, значения, оценки, вероятности, смеси, градиенты, формы, исторические этапы и отклонённые недопустимые входы одной головы внимания без маски.',
  sections: {
    calculation: 'Проследите полный расчёт внимания без маски',
    evidence: 'Проверьте градиенты, формы и отклонение недопустимых входов',
    history: 'Проследите развитие нейронного внимания на пути к современным LLM',
  },
  stages: {
    inputs: 'Начните со строк запросов, ключей и значений',
    dotProducts: 'Сопоставьте каждый запрос с каждым ключом',
    scaledScores: 'Масштабируйте матрицу оценок',
    probabilities: 'Нормируйте каждую строку запроса по ключам',
    mixtures: 'Смешайте строки значений в выходные строки',
  },
  fields: {
    shape: 'Форма',
    scale: 'Масштаб внимания',
    gradientTolerance: 'Допуск проверки градиентов',
    softmaxAxis: 'Ось нормировки',
    mask: 'Граница видимости',
    rowSum: 'Проверка суммы строки вероятностей',
    weightedTerms: 'Слагаемые после умножения на веса',
    output: 'Выходная строка',
    backward: 'Данные обратного прохода',
    batchShape: 'Независимость элементов пакета и формы',
    singleToken: 'У одного токена нет другого ключа для сравнения',
    errors: 'Отклонённые недопустимые входы',
    proof: 'Численные проверки',
    checkCount: 'Проверенные координаты градиентов',
    batchIsolation: 'Независимость элементов пакета',
    queryGradient: 'Градиент запроса',
    keyGradient: 'Градиент ключа',
    replay: 'Точная воспроизводимость',
    gradientCheck: 'Проверка градиентов',
    errorKind: 'Вид ошибки',
    errorEvidence: 'Недопустимая форма входа',
    queryRowsKeyColumns: 'Строки запросов и столбцы ключей',
    tokenRowsFeatureColumns: 'Строки токенов и столбцы признаков',
    earlier: 'Фиксированный рекуррентный контекст',
    bridge: 'Аддитивное выравнивание энкодера и декодера',
    transformer: 'Самовнимание на основе масштабированного скалярного произведения',
  },
  roles: {
    query: 'Строки запросов: какое содержимое следует учесть для каждой позиции?',
    key: 'Строки ключей: по каким признакам сопоставлять позиции?',
    value: 'Строки значений: какое содержимое может внести каждая позиция?',
  },
  cues: {
    query: 'Сплошная рамка запроса',
    key: 'Штриховая рамка ключа',
    value: 'Двойная рамка значения',
    score: 'Пунктирная рамка оценки',
    probability: 'Двойная рамка вероятности',
    verified: 'Проверено',
    rejected: 'Отклонено',
    unmasked: 'Видны все позиции ключей',
  },
  errorReasons: {
    queryRank: 'Во входе запросов должны быть явно заданы оси пакета, токенов и признаков.',
    batchMismatch: 'У тензоров запросов, ключей и значений должен совпадать размер пакета.',
    tokenMismatch: 'У тензоров запросов, ключей и значений должно совпадать число токенов.',
    emptyTokens: 'Для нормировки каждой строки вниманию нужна хотя бы одна позиция ключа.',
    queryKeyWidth: 'Для скалярного произведения ширины признаков запросов и ключей должны совпадать.',
  },
  historyDetails: {
    earlier: 'Один вектор исходной последовательности фиксированного размера повторно используется на всех рекуррентных шагах декодера.',
    bridge: 'На каждом шаге декодер получает новый взвешенный контекст из аннотаций энкодера.',
    transformer: 'Один слой формирует оценку для каждой пары позиций запроса и ключа в доступной последовательности.',
    comparison: 'В этой голове без маски каждому запросу разрешены все доступные позиции ключей.',
  },
  captions: {
    calculation: 'Строки и столбцы точно сохраняют расчёт для двух токенов; рамки и подписи передают все различия без опоры на цвет.',
    evidence: 'Градиенты, формы и граничные случаи показывают, что сохраняет и что отклоняет одна и та же операция внимания.',
    history: 'Сравнивается устройство моделей, а не история языков программирования или производительность оборудования.',
  },
  scrollers: {
    inputs: 'Входные матрицы запросов, ключей и значений',
    scores: 'Оценки до и после масштабирования',
    probabilities: 'Строки вероятностей внимания',
    mixtures: 'Взвешенные смеси значений',
    gradients: 'Таблица градиентов внимания',
    history: 'Сравнение этапов развития нейронного внимания',
  },
};

{/* chapter-section:worked-example */}

## Предскажите, какое значение учтёт каждый запрос

Продолжим непосредственно с результата главы 26. Три проекции дали

$$
Q=\begin{bmatrix}0&3\\2&-1\end{bmatrix},\quad
K=\begin{bmatrix}3&0\\-1&2\end{bmatrix},\quad
V=\begin{bmatrix}3&-3\\1&3\end{bmatrix}.
$$

В пакете две позиции токенов. Ширина признаков запросов и ключей, как и ширина
значений, равна двум, поэтому $Q,K,V\in\mathbb{R}^{1\times2\times2}$.

До нормировки softmax предскажите, строка какого значения получит больший вес
для каждого запроса. Для первого запроса два скалярных произведения равны

$$
q_0K^\top=[0,6],
$$

поэтому больший вес должно получить $v_1=[1,3]$. Для второго запроса получаем

$$
q_1K^\top=[6,-4],
$$

поэтому намного больший вес должно получить $v_0=[3,-3]$. Важны знаки и
разности оценок: после нормировки каждая строка станет набором весов внимания.

{/* chapter-section:formula */}

## Вычислите оценки, масштабируйте, нормируйте и смешайте значения

Одна голова внимания без маски на основе масштабированного скалярного
произведения вычисляется по формуле

$$
A=\operatorname{softmax}\left(\frac{QK^\top}{\sqrt{d_k}}\right),\quad O=AV
$$

В разобранном примере матрицы оценок до и после масштабирования равны

$$
QK^\top=\begin{bmatrix}0&6\\6&-4\end{bmatrix},\qquad
\frac{QK^\top}{\sqrt{2}}\approx
\begin{bmatrix}0&4.242641\\4.242641&-2.828427\end{bmatrix}.
$$

Для каждого запроса softmax независимо нормирует строку по позициям ключей:

$$
A\approx
\begin{bmatrix}
0.014166&0.985834\\
0.999151&0.000849
\end{bmatrix},\qquad
\sum_j A_{ij}=1.
$$

Первая взвешенная смесь значений равна

$$
o_0
=0.014166[3,-3]+0.985834[1,3]
\approx[1.028332,2.915004].
$$

Вторая равна

$$
o_1
=0.999151[3,-3]+0.000849[1,3]
\approx[2.998303,-2.994908].
$$

Итак,

$$
O\approx
\begin{bmatrix}
1.028332&2.915004\\
2.998303&-2.994908
\end{bmatrix}.
$$

{/* chapter-section:symbol-glossary */}

## Не смешивайте строки запросов со столбцами ключей

- В $Q$ каждой позиции токена соответствует строка запроса: какое содержимое ей следует учесть?
- В $K$ находятся строки ключей-кандидатов: по каким признакам позицию можно сопоставить с запросом?
- $d_k$ — общая ширина запросов и ключей.
- В $A$ каждому запросу соответствует одна нормированная строка весов внимания.
- В $V$ находятся строки значений с содержимым для смешивания.
- В $O$ каждому запросу соответствует итоговая смесь.
- $B$ — размер пакета, $T$ — число токенов, а $d_v$ — ширина значения.

Полное правило для форм имеет вид

$$
Q,K\in\mathbb{R}^{B\times T\times d_k},\quad
V\in\mathbb{R}^{B\times T\times d_v},\quad
A\in\mathbb{R}^{B\times T\times T},\quad
O\in\mathbb{R}^{B\times T\times d_v}.
$$

При фиксированных элементе пакета $b$ и позиции запроса $i$ позиция ключа $j$
меняется внутри одной строки вероятностей:

$$
S_{bij}=\frac{q_{bi}\cdot k_{bj}}{\sqrt{d_k}},\qquad
A_{bij}=\frac{\exp(S_{bij})}{\sum_{r=0}^{T-1}\exp(S_{bir})}.
$$

Из-за такой построчной нормировки $A_{bij}$ служит весом внимания. Это не
калиброванная вероятность того, что токен или утверждение верны.

Васвани и соавторы обосновывают знаменатель при конкретных предположениях: если
компоненты запросов и ключей независимы, имеют нулевое среднее и единичную
дисперсию, то дисперсия их скалярного произведения равна $d_k$. При этих
предположениях деление на $\sqrt{d_k}$ не позволяет дисперсии входа softmax
расти вместе с $d_k$. Это не универсальная теорема, не гарантия от переполнения
и не доказательство того, что масштаб задаёт оптимальную температуру.

Наглядный эффект виден в примере с двумя координатами. Для оценок $[1,0]$ вес
первой координаты без масштабирования равен

$$
p_{\mathrm{unscaled}}=\frac{e^1}{e^1+e^0}\approx0.731059.
$$

При $d_k=2$ после масштабирования на квадратный корень тот же вес равен

$$
p_{\mathrm{scaled}}
=\frac{e^{1/\sqrt{2}}}{e^{1/\sqrt{2}}+e^0}
\approx0.669762.
$$

В этом примере распределение стало менее резким. Приведённые выше предположения
объясняют пользу знаменателя, но не задают резкость любой строки внимания.

{/* chapter-section:history */}

## От рекуррентного контекста к учёту всех доступных позиций

Это история развития нейронного внимания на пути к современным LLM, а не история
языков программирования.

Базовая рекуррентная модель энкодера–декодера может пропускать всё исходное
предложение через один вектор фиксированного размера; при аддитивном внимании
декодер всё ещё обрабатывает выходную последовательность рекуррентно и на каждом
шаге заново вычисляет выравнивание с исходной последовательностью.

[Бахданау, Чо и Бенжио, *Neural Machine Translation by Jointly Learning to Align and Translate*](https://arxiv.org/abs/1409.0473)
предложили важный промежуточный шаг. Бахданау, Чо и Бенжио описывают возможное
узкое место базовой модели энкодера–декодера в виде вектора фиксированной длины
и вычисляют каждый новый контекст как взвешенную по softmax сумму аннотаций
энкодера, оценки для которых зависят от предыдущего состояния декодера.

Название *аддитивное внимание* для этой обучаемой функции оценивания — более
поздняя ретроспективная классификация, которую используют Васвани и соавторы.
В ранней работе нет обозначений запросов, ключей и значений из этой главы,
масштабированных скалярных произведений или матрицы оценок внутри одной
последовательности.

Transformer объединяет множество запросов в матрицы и с помощью самовнимания на
основе масштабированного скалярного произведения сопоставляет позиции одной
доступной последовательности, поэтому слой может сформировать всю матрицу
оценок пакетными матричными операциями.

[Васвани и соавторы, *Attention Is All You Need*](https://arxiv.org/abs/1706.03762)
сделали следующий шаг. Васвани и соавторы определяют внимание на основе
масштабированного скалярного произведения как нормированную по softmax матрицу
масштабированных скалярных произведений запросов и ключей, применяемую к
значениям, объединяют одновременно обрабатываемые запросы в матрицу и определяют
самовнимание как установление связей между позициями одной последовательности.

Каждая голова самовнимания декодера превращает обученные запросы и ключи в
построчно нормированные веса внимания, а затем смешивает обученные значения;
каузальный декодер перед нормировкой дополнительно скрывает будущие позиции
ключей.

Одновременное формирование матрицы оценок не делает общий объём работы
постоянным и не распараллеливает авторегрессионную генерацию токенов. Важное
историческое изменение относится к структуре модели: рекуррентное выравнивание
вычисляет новый контекст по мере продвижения декодера, а самовнимание связывает
все доступные позиции внутри одного слоя.

Следующий исполняемый пример выделяет различие в том, откуда берутся позиции
для запросов и для ключей со значениями:

<RustSource
  path="rust/demos/ch27-self-attention/src/lib.rs"
  region="historical-attention-contrast"
  caption="Сопоставьте источники выравнивания энкодера и декодера с парами запросов и ключей из одной последовательности"
  label="Исходный код Rust для исторического сравнения механизмов внимания на пути к LLM в главе 27"
/>

{/* chapter-section:rust-implementation */}

## Соберите голову из дифференцируемых операций над тензорами

`scaled_dot_product_self_attention` принимает три объекта `TensorValue` третьего
ранга. Функция последовательно применяет уже реализованные транспонирование,
пакетный `matmul`, умножение на скаляр, устойчивый `log_softmax`, `exp` и второй
`matmul`. Результат позволяет проверить оценки до масштабирования,
масштабированные оценки, вероятности, выход, коэффициент масштабирования, ширину
ключей и ширину значений:

<RustSource
  path="rust/crates/llm-from-scratch/src/attention/self_attention.rs"
  region="self-attention-forward"
  caption="Соберите одну доступную для проверки голову внимания без маски на основе масштабированного скалярного произведения"
  label="Исходный код Rust с прямым проходом самовнимания, добавленным в накопительную реализацию главы 27"
/>

Тензоры запросов, ключей и значений должны иметь третий ранг, а размеры пакета и
оси токенов должны совпадать. Ось токенов и все оси признаков должны быть
непустыми. Ширины запросов и ключей должны совпадать, но $d_v$ может отличаться
от $d_k$. Пустой пакет допустим. Типизированные ошибки сохраняют сведения о
ранге входа, размерах пакета и оси токенов, пустой оси токенов, ширине признаков
и этапе прямого прохода:

<RustSource
  path="rust/crates/llm-from-scratch/src/attention/self_attention.rs"
  region="self-attention-errors"
  caption="Отклоните недопустимые входы самовнимания, не возвращая незавершённый результат"
  label="Исходный код Rust, определяющий границу ошибок самовнимания в главе 27"
/>

В примере обратного прохода черта сверху обозначает градиент в обратном режиме:

$$
\bar X=\frac{\partial L}{\partial X}.
$$

Зададим входящий градиент по выходу

$$
\bar O=\begin{bmatrix}1&0\\0&1\end{bmatrix},
$$

который задаёт скалярную целевую функцию

$$
L=\langle O,\bar O\rangle=O_{00}+O_{11}\approx-1.966576.
$$

Сначала обратный проход через матричное умножение, вычисляющее выход, даёт

$$
\bar V_b=A_b^{\mathsf T}\bar O_b,\qquad
\bar A_b=\bar O_bV_b^{\mathsf T}.
$$

Для каждой строки запроса правило обратного прохода через softmax имеет вид

$$
\bar S_{bij}
=A_{bij}\left(\bar A_{bij}-\sum_r A_{bir}\bar A_{bir}\right).
$$

Затем масштабированные скалярные произведения передают градиенты запросам и
ключам:

$$
\bar Q_b=\frac{\bar S_bK_b}{\sqrt{d_k}},\qquad
\bar K_b=\frac{\bar S_b^{\mathsf T}Q_b}{\sqrt{d_k}}.
$$

Здесь $\mathsf T$ означает транспонирование последних двух осей отдельно внутри
каждого элемента пакета.

Для разобранных входов эти уравнения дают

$$
\bar Q\approx
\begin{bmatrix}
0.079000&-0.039500\\
-0.014389&0.007195
\end{bmatrix},
$$

$$
\bar K\approx
\begin{bmatrix}
-0.007195&0.062847\\
0.007195&-0.062847
\end{bmatrix},
$$

$$
\bar V\approx
\begin{bmatrix}
0.014166&0.999151\\
0.985834&0.000849
\end{bmatrix}.
$$

Все четыре координаты каждого входа совпадают с центральными разностями при
шаге $10^{-6}$ и допуске $2\times10^{-6}$. Одинаковые ключи дают одинаковые
веса; у единственного токена вес единственного значения равен единице;
одинаковая перестановка $Q$, $K$ и $V$ так же переставляет выход; элементы
пакета остаются независимыми. Для пустого пакета тензоры $Q,K$ имеют форму
$[0,2,2]$, а $V$ — форму $[0,2,3]$; результатом служат $A$ формы $[0,2,2]$ и
$O$ формы $[0,2,3]$. Ширина значений может отличаться от ширины запросов и
ключей: значения формы $[1,2,1]$ дают выход формы $[1,2,1]$.

Полный пример объединяет данные прямого и обратного проходов, масштабирования,
форм, границ и воспроизводимости:

<RustSource
  path="rust/demos/ch27-self-attention/src/lib.rs"
  region="self-attention-fixture"
  caption="Соберите данные разобранного примера самовнимания из ранее реализованных операций над тензорами"
  label="Исходный код Rust для полного примера самовнимания в главе 27"
/>

<RustSource
  path="rust/demos/ch27-self-attention/src/main.rs"
  caption="Запустите полный пример самовнимания без маски"
  label="Точка входа Rust для примера самовнимания в главе 27"
/>

Команда `cargo run --quiet --locked -p ch27-self-attention` выводит те же оценки,
вероятности, смеси, градиенты, формы и результаты проверки границ.

{/* chapter-section:visualization */}

## Проследите путь каждой строки внимания от оценок до выхода

Диаграмма проводит точные входы через оценки до и после масштабирования, строки
вероятностей, взвешенные слагаемые значений, выходы, градиенты, формы,
отклонённые входы и исторический переход к самовниманию:

<SelfAttentionDiagram labels={diagramLabels} />

Сначала прочитайте строку вероятностей вдоль позиций ключей, затем проследите,
как она превращается в соответствующие взвешенные слагаемые значений. Сплошные,
штриховые, двойные и пунктирные рамки различают запросы, ключи, значения, оценки
и вероятности без опоры на цвет. Пока видны все позиции; глава 28 добавит
каузальную границу.

{/* chapter-section:exercises */}

## Сначала сделайте предсказания

1. Вычислите все четыре элемента $QK^\top$ для разобранного примера.
2. До применения softmax предскажите, какой ключ получит больший вес для каждого запроса.
3. Объясните, почему сумма каждой строки $A$, а не каждого столбца, равна единице.
4. Предскажите форму выхода для $Q,K\in\mathbb{R}^{4\times7\times3}$ и $V\in\mathbb{R}^{4\times7\times5}$.
5. Предскажите вероятность внимания и выход для одного токена со значением $[5,-2]$.
6. Определите, что произойдёт, если одинаково переставить две позиции токенов в $Q$, $K$ и $V$.
7. Предскажите вероятности для двух одинаковых строк ключей.
8. Укажите, какой доступ к будущему привёл бы к утечке целевой информации при каузальном обучении декодера.
9. Сопоставьте рекуррентное выравнивание со структурой матрицы оценок самовнимания.

<details>
  <summary>Проверить предсказания</summary>

  1. Строки оценок равны $[0,6]$ и $[6,-4]$.
  2. Запрос с номером ноль отдаёт больший вес ключу один, а запрос один — ключу ноль.
  3. Каждый запрос выбирает среди позиций ключей, поэтому softmax нормирует его ось ключей.
  4. Форма выхода равна $[4,7,5]$.
  5. Единственная вероятность равна $[1]$, а выход — $[5,-2]$.
  6. Совместная перестановка строк $Q$, $K$ и $V$ так же переставляет строки выхода. Входы, зависящие от позиции, и маску нужно рассматривать отдельно.
  7. Одинаковые оценки дают равномерную строку $[0.5,0.5]$.
  8. Запрос может использовать более поздние целевые позиции, потому что у этой головы нет маски; при авторегрессионном обучении такой доступ привёл бы к утечке целевой информации.
  9. Рекуррентное выравнивание вычисляет новый контекст по мере продвижения декодера, а самовнимание устанавливает связи между всеми доступными позициями внутри одного слоя. Это не делает общий объём работы постоянным и не распараллеливает авторегрессионную генерацию.
</details>

{/* chapter-section:decoder-connection */}

## Далее скройте будущие ключи

Накопительный декодер теперь преобразует одну тройку спроецированных тензоров
запросов, ключей и значений в выход головы внимания без маски. В главе 28
будущие позиции ключей будут исключены до нормировки каждой строки оценок.

Голова уже сохраняет оси пакета и позиций запросов и возвращает как доступную
для проверки матрицу вероятностей, так и смешанные значения. Для
авторегрессионного декодирования она пока небезопасна: каждый запрос может
использовать любой ключ. Следующая граница — каузальная маска; позиционная
информация, несколько голов, выходная проекция, остаточная структура и
кэширование появятся в следующих главах.
