---
{
  "chapter_id": "06-bigram-baseline",
  "locale": "ru",
  "content_revision": 2,
  "order": 6,
  "concept_id": "smoothed-bigram-distribution",
  "title": "От подсчета переходов к биграммной модели",
  "description": "Подсчитать каждый переход между соседними токенами ровно один раз, нормировать строку таблицы и различить два случая: нулевую вероятность отдельного продолжения и отсутствие распределения MLE для всей строки.",
  "objective": "Построить и проверить сглаженное распределение вероятностей следующего токена в биграммной модели. Для этого каждый переход между соседними токенами внутри исходных документов обучающей выборки с BOS и EOS нужно подсчитать ровно один раз.",
  "worked_inputs": "Пусть BOS=0, EOS=1, A=2, B=3, C=4 и α=1; токен C входит в словарь, но не встречается в документах обучающей выборки. Для документов [0,2,2,3,1] и [0,2,3,1] подсчитать переходы, а затем получить для контекстов A и C строки счётчиков, распределения MLE и сглаженные распределения.",
  "formula": {
    "latex": "C_{ij}=\\sum_{d\\in\\mathcal{D}_{tr}}\\sum_{t=0}^{|d|-2}\\mathbf{1}[z_t^{(d)}=i\\land z_{t+1}^{(d)}=j],\\quad N_i=\\sum_{k\\in V}C_{ik},\\quad \\widehat P_{\\mathrm{MLE}}(j\\mid i)=\\frac{C_{ij}}{N_i}\\;(N_i>0),\\quad \\widehat P_{\\alpha}(j\\mid i)=\\frac{C_{ij}+\\alpha}{N_i+\\alpha|V|}\\;(\\alpha>0)",
    "symbols": [
      {"symbol": "\\mathcal{D}_{tr}", "meaning": "множество исходных документов обучающей выборки; каждый документ начинается с BOS и заканчивается EOS"},
      {"symbol": "d", "meaning": "один документ обучающей выборки"},
      {"symbol": "|d|", "meaning": "число токенов в документе d"},
      {"symbol": "t", "meaning": "позиция с нумерацией от нуля, после которой в документе d есть следующий токен"},
      {"symbol": "z_t^{(d)}", "meaning": "ID токена в позиции t документа d"},
      {"symbol": "i", "meaning": "ID текущего токена и номер строки в таблице счётчиков"},
      {"symbol": "j", "meaning": "ID возможного следующего токена и номер столбца в таблице счётчиков"},
      {"symbol": "k", "meaning": "индекс, перебирающий все ID токенов из V"},
      {"symbol": "V", "meaning": "множество допустимых ID токенов"},
      {"symbol": "|V|", "meaning": "число ID токенов в V, то есть размер словаря"},
      {"symbol": "C_{ij}", "meaning": "сколько раз в обучающей выборке встретился переход i→j"},
      {"symbol": "N_i", "meaning": "сколько наблюдавшихся переходов начинается с i"},
      {"symbol": "\\mathbf{1}[\\cdot]", "meaning": "единица, если условие выполнено, и ноль в противном случае"},
      {"symbol": "\\widehat P_{\\mathrm{MLE}}(j\\mid i)", "meaning": "оценка вероятности следующего токена методом максимального правдоподобия; определена только при N_i>0"},
      {"symbol": "\\alpha", "meaning": "положительная псевдочастота, добавляемая к каждому возможному продолжению при сглаживании"},
      {"symbol": "\\widehat P_{\\alpha}(j\\mid i)", "meaning": "сглаженная с параметром α оценка вероятности следующего токена"}
    ]
  },
  "history": {
    "approach": "Оценка максимального правдоподобия и сглаживание в n-граммных моделях",
    "summary": "Биграммная модель учитывает только текущий токен и оценивает вероятность каждого следующего токена по тому, сколько раз соответствующая пара встретилась в обучающей выборке. Если конкретное продолжение не встречалось после известного контекста, его оценка максимального правдоподобия равна нулю. Если же после текущего токена не наблюдалось ни одного продолжения, строку нельзя нормировать этим методом. Аддитивное сглаживание задаёт распределение и для такой строки: к каждому возможному продолжению добавляется одинаковая псевдочастота. В курсе этот метод используется ради прозрачности расчёта, а не потому, что считается сильным практическим методом.",
    "rust_source": "rust/demos/ch06-bigram-baseline/src/main.rs"
  },
  "rust_sources": [
    {"path": "rust/demos/ch06-bigram-baseline/src/lib.rs", "region": "wrapped-training-fixture", "purpose": "Задать два отдельных документа обучающей выборки с маркерами границ, которые используются во всех расчётах главы."},
    {"path": "rust/crates/llm-from-scratch/src/bigram.rs", "region": "fit-training-documents", "purpose": "Подсчитать соседние пары внутри каждого документа и использовать только обучающую выборку."},
    {"path": "rust/crates/llm-from-scratch/src/bigram.rs", "region": "probability-rows", "purpose": "Представить нулевую вероятность отдельного перехода и неопределённую строку MLE как разные случаи."},
    {"path": "rust/demos/ch06-bigram-baseline/src/main.rs", "region": "learner-output", "purpose": "Вывести точные счётчики, оценки MLE, сглаженные вероятности и результат проверки границы документов."}
  ],
  "visualization": {
    "decision": "useful",
    "id": "bigram-baseline",
    "rationale": "Две строки с подписями токенов позволяют сопоставить результаты подсчёта, суммы строк, псевдочастоты, знаменатели и итоговые вероятности. На строке C видно, почему строку с нулевой суммой нельзя нормировать методом максимального правдоподобия и как правило сглаживания строит для неё распределение."
  },
  "decoder_connection": "Модель один раз просматривает каждый исходный документ обучающей выборки с BOS и EOS и не использует перекрывающиеся пары из главы 5. После обучения таблица остаётся неизменной: глава 7 вычисляет потери и перплексию на обучающей и валидационной выборках без повторной подгонки, а интерфейс расчёта метрики позволяет выбрать только обучающую или валидационную выборку."
}
---

import BigramBaselineDiagram from '../../../components/chapters/BigramBaselineDiagram.astro';

export const diagramLabels = {
  title: 'Проследите, как две строки счётчиков превращаются в вероятности',
  description:
    'Один и тот же пример на Rust задаёт два отдельных документа обучающей выборки и строки таблицы для A и C. Сравните строку A, в которой не встретился переход A→C, со строкой C, в которой нет ни одного наблюдавшегося продолжения.',
  summary: {
    vocabularySize: 'Размер словаря',
    alpha: 'Параметр сглаживания α',
    trainingDocuments: 'Документов в обучающей выборке',
    countedTransitions: 'Подсчитано переходов',
  },
  sections: {
    trainingDocuments: 'Переходы, учтённые внутри каждого документа',
    tokenLegend: 'Токены словаря и их роли',
    knownContext: 'Контекст A: переход A→C не встретился',
    unseenContext: 'Контекст C: после C нет ни одного наблюдения',
    boundaryGuard: 'Проверка границы между документами',
  },
  fields: {
    document: 'Документ обучающей выборки',
    context: 'Текущий токен',
    rowTotal: 'Общее число наблюдений в строке Nᵢ',
    denominator: 'Знаменатель сглаженного распределения',
  },
  columns: {
    nextToken: 'Следующий токен',
    count: 'Число наблюдений',
    pseudocount: 'Добавленная псевдочастота α',
    smoothedNumerator: 'Числитель Cᵢⱼ + α',
    mle: 'Оценка MLE',
    smoothed: 'Сглаженная вероятность',
  },
  values: {
    undefinedMle: 'не определена: сумма строки равна нулю',
    boundaryTransition: 'не соединяется с',
  },
  roles: {
    boundary: 'служебный маркер начала или конца документа',
    content: 'токен содержимого, встречающийся в обучающей выборке',
    unseen: 'есть в словаре, но не встречается в этих документах',
  },
  notes: {
    countOnce:
      'Каждый переход между соседними токенами внутри документа учитывается ровно один раз. Конец одного документа не соединяется с началом следующего.',
    unseenSuccessor:
      'Переход A→C не встретился, но сумма строки A равна трём. Поэтому его оценка MLE определена и равна нулю; после сглаживания с α=1 вероятность становится равной 1/8.',
    unseenContext:
      'В обучающих документах ни один переход не начинается с C, поэтому сумма строки равна нулю и получить распределение MLE нельзя. Равномерное распределение задаёт правило сглаживания; из данных не следует, что продолжения после C действительно равновероятны.',
    boundary:
      'Если склеить документы, между ними появится искусственный переход EOS→BOS. При обработке документов по отдельности он не попадает в таблицу.',
  },
};

{/* chapter-section:worked-example */}

## Подсчитайте семь переходов и постройте прогноз для A

В главе 5 мы разбивали документы на перекрывающиеся пары «вход — цель», чтобы
обучать последовательную модель. Для биграммной таблицы нужны данные до такого
разбиения: исходные закодированные документы обучающей выборки. Если считать
переходы по перекрывающимся окнам, один и тот же переход может попасть в расчёт
несколько раз. Здесь каждая соседняя пара должна увеличить ровно один счётчик.

Во всей главе используем один и тот же порядок токенов:

| Токен | `BOS` | `EOS` | `A` | `B` | `C` |
| :--- | ---: | ---: | ---: | ---: | ---: |
| ID | `0` | `1` | `2` | `3` | `4` |

Токен `C` входит в словарь, но в этих двух документах обучающей выборки не
встречается. Документы обрабатываем по отдельности:

```text
d1: BOS(0) → A(2) → A(2) → B(3) → EOS(1)
d2: BOS(0) → A(2) → B(3) → EOS(1)
```

Прежде чем продолжить, ответьте на четыре вопроса:

1. Сколько соседних переходов содержат оба документа вместе?
2. Какие токены встречаются сразу после `A` и сколько раз?
3. Какой токен должен оказаться самым вероятным продолжением `A`?
4. Чем случай «после `A` ни разу не встретился `C`» отличается от случая «после `C` не встретилось ничего»?

В первом документе четыре перехода, во втором — три:

```text
d1: BOS→A, A→A, A→B, B→EOS
d2: BOS→A, A→B, B→EOS
```

После объединения результатов ненулевы только четыре счётчика:
`BOS→A = 2`, `A→A = 1`, `A→B = 2` и `B→EOS = 2`. Для текущего токена `A`
расположим возможные продолжения в порядке `[BOS, EOS, A, B, C]`:

```text
счётчики для A = [0, 0, 1, 2, 0]
сумма строки    = 3
```

Так устроена **биграммная модель**: текущий токен выбирает строку таблицы, а
каждому возможному следующему токену соответствует столбец. Всё, что было до
текущего токена, модель отбрасывает. Разделив каждый счётчик в строке `A` на её
сумму, получаем оценку максимального правдоподобия (MLE):

$$
[0/3,0/3,1/3,2/3,0/3]=[0,0,1/3,2/3,0].
$$

Наибольшая вероятность только у `B`. Оценка MLE для перехода `A→C` определена,
но равна нулю: из `A` наблюдалось три перехода, поэтому строку можно нормировать.

Для контекста `C` ситуация иная. Его строка равна `[0,0,0,0,0]`, а сумма строки —
нулю. Деление каждого счётчика на эту сумму привело бы к делению на ноль, поэтому
**распределение MLE для `C` не определено**. Нулевая ячейка в существующем
распределении и отсутствие всего распределения — разные случаи.

Теперь применим аддитивное сглаживание с `α=1`. При вычислении вероятностей
прибавим единицу к каждому счётчику. Эта единица — не новое наблюдение, а
**псевдочастота**, которую вводит правило сглаживания. Исходная таблица счётчиков
при этом не меняется. Для `A` получаем:

```text
исходные счётчики       [0, 0, 1, 2, 0]
с учётом псевдочастоты  [1, 1, 2, 3, 1]
знаменатель             3 + 1×5 = 8
сглаженные вероятности  [1/8, 1/8, 2/8, 3/8, 1/8]
```

После `C` наблюдений нет. Пять одинаковых псевдочастот дают знаменатель
`0 + 1×5 = 5` и равномерное распределение `[1/5,1/5,1/5,1/5,1/5]`. Это
распределение задано правилом сглаживания, а не выведено из данных о `C`.
Поэтому нельзя заключать, что в реальном языке все продолжения после `C`
одинаково правдоподобны.

Остаётся проверить границу документов. Если склеить их в последовательность
`[0,2,2,3,1,0,2,3,1]`, получится восемь соседних пар вместо семи. Лишняя пара —
`EOS(1)→BOS(0)`: ни в одном исходном документе такого перехода нет.

{/* chapter-section:formula */}

## Запишите подсчёт внутри документов и нормировку строк

Весь расчёт можно записать одной формулой:

$$
C_{ij}=\sum_{d\in\mathcal{D}_{tr}}\sum_{t=0}^{|d|-2}\mathbf{1}[z_t^{(d)}=i\land z_{t+1}^{(d)}=j],\quad N_i=\sum_{k\in V}C_{ik},\quad \widehat P_{\mathrm{MLE}}(j\mid i)=\frac{C_{ij}}{N_i}\;(N_i>0),\quad \widehat P_{\alpha}(j\mid i)=\frac{C_{ij}+\alpha}{N_i+\alpha|V|}\;(\alpha>0)
$$

Начнём с двух сумм в левой части. Внешняя сумма перебирает исходные документы
обучающей выборки по одному. Во внутренней сумме позиции нумеруются с нуля, как
индексы среза в Rust. Если `|d|≥2`, индекс `t` принимает значения от `0` до
`|d|-2`, а `t+1` остаётся внутри того же документа. Для более короткого документа
внутренняя сумма пуста. Верхний индекс `(d)` подчёркивает, что оба токена пары
принадлежат одному документу.

ID текущего токена `i` задаёт строку, а ID возможного продолжения `j` — столбец.
Индикатор добавляет единицу, только когда соседние токены равны `i` и `j`.
Следовательно, `C_{ij}` показывает, сколько раз встретился переход `i→j`.
Суммирование по всем столбцам `k` даёт `N_i` — общее число переходов,
начинающихся с `i`.

Оценка MLE делит `C_{ij}` на `N_i`, поэтому она определена только при `N_i>0`.
При аддитивном сглаживании числитель каждой ячейки становится `C_{ij}+α`.
Поскольку в строке `|V|` ячеек, знаменатель увеличивается на `α|V|`, а не только
на `α`. В каждой строке сумма новых числителей совпадает со знаменателем, поэтому
сумма вероятностей остаётся равной единице. При `α>0` знаменатель положителен
даже у строки, в которой нет ни одного наблюдения.

Реализация на Rust хранит таблицу в одном построчном массиве: ячейка `(i,j)`
находится по смещению `i|V|+j`. Это лишь способ размещения данных в памяти;
вероятностный смысл ячейки остаётся прежним: «следующим будет токен `j`, если
текущий токен — `i`».

{/* chapter-section:symbol-glossary */}

## Разберите каждое обозначение

| Обозначение | Смысл в этой главе |
| :--- | :--- |
| $\mathcal{D}_{tr}$ | Множество исходных документов обучающей выборки с `BOS` в начале и `EOS` в конце. Валидационная и тестовая выборки сюда не входят. |
| $d$ | Один документ обучающей выборки. |
| $|d|$ | Число ID токенов в документе $d$, включая маркеры границ. |
| $t$ | Позиция с нумерацией от нуля, после которой в $d$ ещё есть следующий токен. |
| $z_t^{(d)}$ | ID токена в позиции $t$ документа $d$. |
| $i$ | ID текущего токена; задаёт строку таблицы счётчиков. |
| $j$ | ID возможного следующего токена; задаёт столбец таблицы счётчиков. |
| $k$ | Индекс для перебора всех ID из $V$ при вычислении суммы строки. |
| $V$ | Множество допустимых ID токенов. |
| $|V|$ | Число ID токенов в $V$, то есть размер словаря. |
| $C_{ij}$ | Сколько раз в обучающей выборке встретился переход $i\to j$. |
| $N_i$ | Сколько наблюдавшихся переходов начинается с $i$; сумма строки $i$. |
| $\mathbf{1}[\cdot]$ | Индикатор: единица при выполненном условии и ноль в противном случае. |
| $\widehat P_{\mathrm{MLE}}(j\mid i)$ | Оценка вероятности следующего токена методом максимального правдоподобия; определена только при $N_i>0$. |
| $\alpha$ | Положительная псевдочастота, добавляемая к каждому возможному продолжению при сглаживании. В примере $\alpha=1$. |
| $\widehat P_{\alpha}(j\mid i)$ | Сглаженная с параметром $\alpha$ оценка вероятности того, что после $i$ следует $j$. |

Важно понимать, к какой части таблицы относится утверждение. Фраза «`C` не
встретился после `A`» описывает одну ячейку в строке `A`. Фраза «после `C` не
встретился ни один токен» описывает всю строку `C`. Если перепутать строку и
столбец, можно ошибочно решить, что распределение MLE существует или, наоборот,
не существует.

{/* chapter-section:history */}

## Оцените возможности и ограничения классического подхода

До появления нейросетевых языковых моделей для прогноза следующего слова часто
использовали n-граммные модели. Они учитывают только контекст фиксированной
длины; у биграммной модели контекст состоит из одного текущего токена. Такую
таблицу легко проверить, а прогноз по ней быстро вычисляется. Однако для любых
двух последовательностей, которые оканчиваются одним и тем же токеном, модель
выдаёт одинаковое распределение.

[Чен и Гудман (1996)](https://aclanthology.org/P96-1041.pdf) определяют
биграммную оценку максимального правдоподобия как отношение числа наблюдений
пары к числу наблюдений её контекста и объясняют, почему нулевая вероятность
n-граммы, отсутствовавшей в обучающих данных, создаёт серьёзную проблему для
языковой модели. В работе сравниваются несколько семейств методов сглаживания.
Аддитивное сглаживание устроено просто, но это не самый сильный практический
метод: одинаковая псевдочастота не позволяет отличить друг от друга более и
менее правдоподобные продолжения, которые не встречались в обучающей выборке.

[Бенжио, Дюшарм и Венсан (2000)](https://proceedings.neurips.cc/paper/2000/hash/728f206c2a01bf572b5940d7d9a8fa4c-Abstract.html)
описывают традиционную n-граммную модель как таблицу условных вероятностей для
коротких контекстов. В предложенной ими нейросетевой модели распределённые
представления слов позволяют обобщать наблюдения на похожие последовательности.
У независимых ячеек нашей таблицы такой возможности нет.

В этой главе используется сглаживание с `α=1`, потому что все числители и
знаменатели легко проверить вручную. Ограничение метода видно в том же примере:
после сглаживания вероятность `1/8` получает не только переход `A→C`, но и
переход `A→BOS`, хотя `BOS` допустим только в начале документа. Одинаковая
псевдочастота в каждой ячейке ничего не знает ни о роли токенов, ни о синтаксисе.

В статьях не задаются принятые в этой главе ID токенов, границы документов,
значение `α`, выбор только обучающей части корпуса и порядок вывода кандидатов с
одинаковой вероятностью. Это учебные и инженерные решения курса. Программа на
Rust вычисляет MLE и сглаженные оценки по одной и той же таблице, поэтому разницу
между ними можно проверить непосредственно.

{/* chapter-section:rust-implementation */}

## Сверьте ручной расчёт с реализацией на Rust

Демонстрационная программа использует те же два документа, что и ручной расчёт.
Они передаются отдельными срезами, поэтому переход между документами не
возникает. Универсальная функция, однако, принимает обычные срезы и полагается
на вызывающий код: каждый срез должен содержать один документ с `BOS` в начале и
`EOS` в конце, но без `PAD`.

<RustSource
  path="rust/demos/ch06-bigram-baseline/src/lib.rs"
  region="wrapped-training-fixture"
  caption="Два документа обучающей выборки с маркерами BOS и EOS"
  label="Код на Rust: два отдельных документа обучающей выборки и одна построенная по ним биграммная модель"
/>

`fit_training_documents` проверяет размер словаря, параметр сглаживания,
возможность выделить память под таблицу и каждый переданный ID. Внешний цикл
перебирает документы, а `windows(2)` создаёт соседние пары только внутри текущего
документа. Пустой срез и срез из одного токена не добавляют переходов; при этом
все имеющиеся в них ID всё равно проверяются. Метод
`fit_encoded_training_partition` явно запрашивает `Partition::Train`, поэтому
валидационные и тестовые документы не попадают в подсчёт.

<RustSource
  path="rust/crates/llm-from-scratch/src/bigram.rs"
  region="fit-training-documents"
  caption="Подсчёт соседних пар отдельно внутри каждого документа обучающей выборки"
  label="Код на Rust: проверка входных данных и заполнение биграммной таблицы только по обучающей выборке"
/>

Тип возвращаемого значения сохраняет важное различие. `Some(0.0)` означает, что
строка MLE существует, но выбранный переход имеет нулевую вероятность. `None`
означает, что сумма строки равна нулю и нормировать её нельзя. После сглаживания
распределение существует для любого корректного контекста. Если максимальное
значение счётчика достигается для нескольких токенов, `most_likely_tokens`
возвращает все их ID по возрастанию. Этот порядок нужен только для
воспроизводимого вывода: первый ID не становится более вероятным. Аддитивное
сглаживание не меняет ранжирование токенов и сохраняет все равенства, потому что
к каждой ячейке прибавляется одна и та же `α`, а затем все числители делятся на
общий знаменатель.

<RustSource
  path="rust/crates/llm-from-scratch/src/bigram.rs"
  region="probability-rows"
  caption="Разные представления для нулевой вероятности и неопределённой строки MLE"
  label="Код на Rust: расчёт MLE, аддитивное сглаживание и воспроизводимый вывод всех кандидатов с максимальной вероятностью"
/>

Демонстрационная программа сначала печатает счётчики, а затем обе оценки
вероятности. Отдельно показаны переход `A→C`, строка `C` и искусственный переход,
который появился бы при склеивании документов.

<RustSource
  path="rust/demos/ch06-bigram-baseline/src/main.rs"
  region="learner-output"
  caption="Счётчики, оценки MLE, сглаженные вероятности и проверка границы документов"
  label="Код на Rust: вывод нулевой вероятности перехода A→C, неопределённой строки MLE для C и результата сглаживания"
/>

Запустите пример в подготовленном Docker-окружении проекта:

```bash
./course run cargo run --quiet --locked -p ch06-bigram-baseline
```

Тесты фиксируют все пять строк таблицы, общее число переходов, отсутствие
`EOS→BOS`, нормировку распределений, вывод всех кандидатов с одинаковой
максимальной вероятностью и обработку ошибочных входных данных. Отдельный тест
проверяет, что конструктор для обучающей выборки даёт тот же результат, что и
явная передача тех же закодированных документов.

{/* chapter-section:visualization */}

## Сопоставьте исходные документы с двумя строками таблицы

Перед просмотром таблиц попробуйте предсказать ответы:

1. К каким ячейкам будет добавлена псевдочастота `α`?
2. Изменится ли самое вероятное продолжение `A` после сглаживания?
3. Появится ли единственный максимум в сглаженном распределении после `C`?
4. Какой маркер, допустимый только в начале документа, всё же получит ненулевую вероятность после `A`?

<BigramBaselineDiagram labels={diagramLabels} />

Документы и все числовые данные на схеме получены из детерминированной
трассировки той же программы на Rust. Браузер не реализует биграммную модель
повторно. Подписи, суммы строк, структура таблиц и перечёркнутая стрелка передают
смысл без опоры на цвет; клиентский JavaScript для просмотра схемы не нужен.

Теперь проверьте ответы по полным таблицам. Псевдочастота `α` добавляется к
каждой ячейке. После `A` единственным максимумом остаётся `B`. После `C` все
токены имеют одинаковую сглаженную вероятность. Даже структурно недопустимый
здесь переход `A→BOS` получает ненулевую вероятность.

{/* chapter-section:exercises */}

## Сначала решите задачи, затем проверьте расчёты

Во всех заданиях документы остаются раздельными, а столбцы идут в порядке
`[BOS, EOS, A, B, C]`. Сначала запишите ответы и только потом запускайте пример.

1. Перечислите семь переходов и укажите исходный документ для каждого из них.
2. Для контекста `A` вычислите строку счётчиков, строку MLE и сглаженную строку при `α=1`.
3. Объясните, почему `MLE(A→C)=0`, а распределение MLE для контекста `C` не определено.
4. Мысленно склейте документы. Какой искусственный переход появится и сколько соседних пар окажется в общей последовательности?
5. Почему подсчёт по перекрывающимся окнам главы 5 с `T=3` и `S=1` привёл бы к повторному учёту некоторых переходов в документе `d1`?
6. Пересчитайте сглаженную строку `A` для `α=1/2`.
7. Проверьте, что обе сглаженные строки из примера дают сумму вероятностей, равную единице.
8. Назовите все токены с максимальной вероятностью в сглаженном распределении после `A` и после `C`.
9. Допустим, в валидационной выборке часто встречается `A→C`. Нужно ли менять таблицу перед оцениванием в главе 7?

<details>
  <summary>Проверьте ответы и ход вычислений</summary>

1. Документ `d1` даёт `BOS→A`, `A→A`, `A→B`, `B→EOS`; документ `d2` — `BOS→A`, `A→B`, `B→EOS`.
2. Строка счётчиков равна `[0,0,1,2,0]`, строка MLE — `[0,0,1/3,2/3,0]`. После добавления псевдочастоты получаем числители `[1,1,2,3,1]` и знаменатель `8`, поэтому сглаженная строка равна `[1/8,1/8,2/8,3/8,1/8]`.
3. Для `A` значение `N_A=3`, поэтому строку можно нормировать, а ячейка в столбце `C` даёт `0/3=0`. Для `C` значение `N_C=0`; нормирование потребовало бы деления на ноль, поэтому распределения MLE нет.
4. Между документами появится `EOS(1)→BOS(0)`. В склеенной последовательности девять токенов, а значит, восемь соседних пар — на одну больше, чем в исходных документах.
5. Два полных окна перекрываются. Переходы `A→A` и `A→B` входят в оба окна, поэтому каждый из них был бы учтён дважды.
6. Знаменатель равен `3+(1/2)×5=11/2`. Числители `[1/2,1/2,3/2,5/2,1/2]` дают строку `[1/11,1/11,3/11,5/11,1/11]`.
7. Для `A` сумма числителей равна `1+1+2+3+1=8`, для `C` — `5`. В обоих случаях она совпадает со знаменателем, поэтому после деления сумма вероятностей равна единице.
8. В сглаженном распределении после `A` единственный максимум у `B`. После `C` все ID `[0,1,2,3,4]` имеют одинаковую максимальную вероятность. Порядок ID по возрастанию нужен только для воспроизводимого вывода и не выражает предпочтение модели.
9. Нет. Валидационная выборка нужна для оценки уже зафиксированной модели. Если пересчитать таблицу с учётом этих переходов, сведения из валидационной выборки попадут в параметры модели.

</details>

**Проверка понимания:** фраза «`C` не встретился после `A`, поэтому строка `C`
не определена» смешивает столбец и строку. Отсутствие перехода `A→C` относится к
одной ячейке существующей строки `A`. Распределения MLE для `C` нет по другой
причине: в обучающих данных ни один переход не начинается с `C`.

{/* chapter-section:decoder-connection */}

## Зафиксируйте первую модель, которая возвращает полное распределение вероятностей

Теперь в курсе есть модель, которая принимает ID текущего токена `i` и возвращает
вероятность для каждого возможного следующего токена `j`. Она учитывает только
один токен контекста и не умеет переносить наблюдения из одного контекста на
похожие контексты. Тем не менее результат уже имеет ту же форму, что и выход
декодера: распределение вероятностей по всему словарю.

В главе 7 мы возьмём вероятность фактически наблюдавшегося следующего токена для
каждого перехода и вычислим отрицательное логарифмическое правдоподобие и
перплексию. Таблица при этом останется неизменной: при вычислении метрик
обучающие примеры не добавляются к счётчикам повторно, валидационная выборка
используется только для оценки, а интерфейс расчёта метрики главы 7 позволяет
выбрать только обучающую или валидационную выборку. Для этой таблицы не нужны
градиенты; оптимизация обучаемых параметров появится в следующих главах.
