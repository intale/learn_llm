---
{
  "chapter_id": "00-llm-parts",
  "chapter_kind": "orientation",
  "locale": "ru",
  "content_revision": 3,
  "order": 0,
  "concept_id": "llm-parts",
  "title": "Карта устройства современной LLM",
  "description": "Посмотрите, как токенизация, эмбеддинги, блоки декодера, внимание, ветви прямого распространения, обучение, выбор токена и KV-кэш соединяются в LLM только с декодером.",
  "objective": "Разобраться, из каких основных частей состоит LLM только с декодером, как эти части связаны и в какой главе курса реализуется каждая из них.",
  "worked_inputs": "Проследите путь текста запроса через токенизацию, эмбеддинги, повторяющиеся блоки декодера и проекцию на словарь до выбора следующего токена; затем сравните генерацию с обучением, где используются целевые токены, функция потерь, градиенты, обновление весов, итоговая оценка и контрольные точки.",
  "formula": null,
  "history": {
    "llm_evolution": {
      "predecessor_kind": "language-model",
      "limitation": "Частотные n-граммные модели учитывают только фиксированный короткий контекст и не умеют обучать распределённые признаки, пригодные в разных контекстах, или выбирать информацию из более длинного префикса в зависимости от его содержимого.",
      "later_advance": "Нейронные языковые модели научились строить распределённые представления токенов, Transformer добавил маскированное самовнимание по префиксу, а более поздние авторегрессионные модели на основе Transformer сохранили задачу предсказания следующего токена при масштабировании.",
      "modern_llm_role": "Современная LLM только с декодером многократно преобразует признаки токенов: перед ветвями каузального внимания и вентильного прямого распространения выполняется нормализация, а затем результат проецируется в распределение вероятностей следующего токена, которое используется при обучении или генерации.",
      "sources": [
        {
          "role": "earlier",
          "year": 2003,
          "name": "A Neural Probabilistic Language Model",
          "source_url": "https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf",
          "claim": "Бенжио и соавторы сопоставляют обобщение в n-граммных моделях с нейронной вероятностной функцией, которая обучает распределённые представления слов; их модель появилась до Transformer и до блока декодера, который рассматривается в этом курсе."
        },
        {
          "role": "later",
          "year": 2017,
          "name": "Attention Is All You Need",
          "source_url": "https://arxiv.org/pdf/1706.03762",
          "claim": "Васвани и соавторы описывают Transformer и задают маску, запрещающую самовниманию декодера обращаться к более поздним позициям; опубликованная ими система состоит из энкодера и декодера, а не только из декодера, как модель в этом курсе."
        },
        {
          "role": "later",
          "year": 2020,
          "name": "Language Models are Few-Shot Learners",
          "source_url": "https://arxiv.org/pdf/2005.14165",
          "claim": "Браун и соавторы описывают GPT-3 как масштабированную авторегрессионную языковую модель; её масштаб и результаты измерения её возможностей нельзя переносить на небольшую учебную модель этого курса."
        }
      ]
    },
    "approach": "Начните с частотной языковой модели с коротким контекстом, а затем проследите единый путь к современной LLM только с декодером через обучаемые представления, маскированное самовнимание, блоки декодера, оптимизацию и авторегрессионную генерацию.",
    "summary": "Полезнее представлять LLM не как единый загадочный чёрный ящик, а как текстовый интерфейс, повторяющуюся в блоках декодера цепочку обучаемых преобразований признаков, выходную проекцию на словарь и процесс обучения, который корректирует эти веса по примерам с известным следующим токеном.",
    "rust_source": null
  },
  "rust_sources": [],
  "visualization": {
    "decision": "useful",
    "id": "llm-system-map",
    "component": "LlmSystemDiagram",
    "rationale": "Связанная блок-схема понятнее плоского списка глав: на ней видны повторяющиеся вычисления декодера, возврат выбранного токена на следующий шаг и ответвление обучения от общего пути прямого распространения.",
    "supplementary": [
      {
        "id": "llm-parts-map",
        "component": "LlmPartsDiagram",
        "rationale": "Отдельная подробная карта раскрывает блок декодера и связывает каждую названную часть с главой, где она реализуется, не перегружая схему всей системы."
      }
    ]
  },
  "decoder_connection": "Карта заранее показывает весь декодер: ID токенов превращаются в векторы признаков, повторяющиеся блоки с предварительной нормализацией смешивают доступный контекст и преобразуют признаки, выходная проекция с весами, общими с таблицей эмбеддингов, выдаёт логиты, выбор следующего токена продолжает генерацию, а при обучении те же веса изменяются с помощью функции потерь, градиентов и AdamW."
}
---

import LlmPartsDiagram from "../../../components/chapters/LlmPartsDiagram.astro";
import LlmSystemDiagram from "../../../components/chapters/LlmSystemDiagram.astro";

export const diagramLabels = {
  title: "Сначала рассмотрите LLM целиком, затем собирайте её по частям",
  description: "Проследите единый путь от текста до логитов, сравните генерацию с обучением, а затем откройте подробные схемы и перейдите к главе, где реализуется нужная часть.",
  detailTitle: "Устройство LLM по главам курса",
  detailDescription: "Сравните генерацию с обучением и разберите блок декодера по главам.",
  sections: {
    system: "Как связана вся система",
    inference: "Генерация следующего токена",
    decoder: "Внутри блока декодера",
    learning: "Обучение и оценка тех же весов",
  },
  captions: {
    system: "Путь прямого распространения выдаёт логиты. При генерации выбирается новый входной токен, а при обучении меняются веса, которые позволили получить эти логиты.",
    inference: "Запрос токенизируется один раз; по выбранному ID снова извлекается эмбеддинг, а KV‑кэш сохраняется.",
    decoder: "Блок добавляет результат внимания, затем результат SwiGLU.",
    learning: "По потерям и градиентам обновляются те же веса.",
  },
  parts: {
    "input-text": {
      name: "Текст / примеры",
      purpose: "Запрос; отдельные документы для обучения.",
    },
    tokenizer: {
      name: "Токенизатор",
      purpose: "Преобразует текст в ID токенов и обратно.",
    },
    "numeric-core": {
      name: "Тензорная основа",
      purpose: "Тензорные операции; граф вычислений — только при обучении.",
    },
    embeddings: {
      name: "Эмбеддинги",
      purpose: "ID токенов → обучаемые признаки.",
    },
    "decoder-block": {
      name: "Блоки декодера",
      purpose: "Внимание и SwiGLU в каждом блоке.",
    },
    rmsnorm: {
      name: "RMSNorm",
      purpose: "Задаёт масштаб входа.",
    },
    "causal-attention": {
      name: "Каузальное внимание",
      purpose: "Читает доступный префикс.",
    },
    "residual-stream": {
      name: "Остаточное сложение",
      purpose: "Добавляет результат ветви к потоку.",
    },
    swiglu: {
      name: "SwiGLU",
      purpose: "Вентильная обработка признаков по позициям.",
    },
    "vocabulary-head": {
      name: "Проекция на словарь",
      purpose: "Итоговые признаки → логиты токенов.",
    },
    sampler: {
      name: "Выбор токена",
      purpose: "Из логитов получает вероятности и выбирает токен.",
    },
    "kv-cache": {
      name: "KV‑кэш слоя",
      purpose: "Хранит ключи и значения.",
    },
    loss: {
      name: "Функция потерь",
      purpose: "Измеряет ошибку по целевому токену.",
    },
    optimizer: {
      name: "AdamW и выбор состояния",
      purpose: "Обновляет веса; отбор — по валидации.",
    },
    evaluation: {
      name: "Итоговая оценка",
      purpose: "Тестирует фиксированную модель на новых примерах.",
    },
    checkpoint: {
      name: "Контрольная точка",
      purpose: "Сохраняет модель и ход обучения.",
    },
    capstone: {
      name: "Итоговая сборка",
      purpose: "Объединяет все части.",
    },
  },
  system: {
    forwardTitle: "Общий путь прямого распространения",
    generationTitle: "Генерация",
    learningTitle: "Обучение",
    supportTitle: "Состояние и вычислительная основа",
    logits: {
      name: "Логиты",
      purpose: "По одной оценке для каждого токена словаря.",
    },
    learningLogits: {
      name: "Логиты модели",
      purpose: "Оценки для этой позиции.",
    },
    learningJoin: "и",
    nextToken: {
      name: "ID выбранного токена",
      purpose: "Добавляется к контексту.",
    },
    target: {
      name: "Целевой токен",
      purpose: "Фактически следующий токен.",
    },
    gradients: {
      name: "Градиенты",
      purpose: "Показывают влияние весов на потери.",
    },
    optimizer: {
      name: "Оптимизатор AdamW",
      purpose: "Обновляет веса по градиентам.",
    },
    weights: {
      name: "Обновлённые веса",
      purpose: "Валидация выберет одно состояние.",
    },
    generationFeedback: "Выбранный ID возвращается к эмбеддингам на следующем шаге с KV‑кэшем.",
    weightFeedback: "Следующий прямой проход использует обновлённые веса.",
    cacheRelationship: "Внимание читает и дополняет KV‑кэш при генерации.",
    numericRelationship: "Тензорные операции; граф вычислений — только при обучении.",
    evaluationRelationship: "Выбранные веса → веса не меняются → тестовая оценка.",
    checkpointRelationship: "Состояние модели и обучения ↔ контрольная точка.",
  },
  cues: {
    chapterLinks: "связанные главы курса",
    chapterShort: "Гл.",
    chapterLong: "Глава",
    referenceLocaleBadge: "EN",
    referenceLocaleDestination: "страница на английском",
    repeated: "в каждом слое",
    nextTokenLoop: "ID токена → эмбеддинг",
    reuseTitle: "Общий путь до логитов",
    sharedForward: "текст → токенизатор → эмбеддинги → декодер → проекция на словарь",
    learningBoundary: "при обучении вместо выбора берём целевой токен",
  },
};

{/* chapter-section:overview */}

## Сначала взгляните на систему целиком

LLM только с декодером легче понять как связанную систему, а не как один
непрозрачный чёрный ящик. Сначала текст запроса превращается в ID токенов. По ним
из таблицы эмбеддингов извлекаются обучаемые признаки. Затем стек блоков декодера
многократно смешивает информацию из доступного префикса и преобразует признаки в
каждой позиции. Наконец, проекция на словарь выдаёт по одной оценке для каждого
возможного следующего токена.

Эти оценки используются двумя способами. При генерации стратегия декодирования
выбирает ID токена, добавляет его к последовательности и снова запускает модель,
используя уже заполненный KV-кэш внимания. При обучении фактически следующий токен
служит целью: функция потерь измеряет ошибку прогноза, градиенты показывают её
чувствительность к каждому весу, а оптимизатор обновляет веса эмбеддингов, блоков
декодера и проекции на словарь.

Этот обзор намеренно ограничивается названиями, назначением и связями частей. В
следующих главах выводятся формулы, а каждый механизм реализуется на Rust тогда,
когда его место в модели уже понятно.

{/* chapter-section:history */}

## Короткий путь к современной блок-схеме

Частотные n-граммные модели учитывают только фиксированный короткий контекст и не
умеют обучать распределённые признаки, пригодные в разных контекстах, или выбирать
информацию из более длинного префикса в зависимости от его содержимого. Нейронные
языковые модели научились строить распределённые представления токенов, Transformer
добавил маскированное самовнимание по префиксу, а более поздние авторегрессионные
модели на основе Transformer сохранили задачу предсказания следующего токена при
масштабировании. Современная LLM только с декодером многократно преобразует
признаки токенов: перед ветвями каузального внимания и вентильного прямого
распространения выполняется нормализация, а затем результат проецируется в
распределение вероятностей следующего токена, которое используется при обучении
или генерации.

Переход от подсчёта частот к обучаемым признакам хорошо виден в работе
[A Neural Probabilistic Language Model](https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf).
Бенжио и соавторы сопоставляют обобщение в n-граммных моделях с нейронной
вероятностной функцией, которая обучает распределённые представления слов; их
модель появилась до Transformer и до блока декодера, который рассматривается в
этом курсе.

Следующий архитектурный рубеж описан в работе
[Attention Is All You Need](https://arxiv.org/pdf/1706.03762). Васвани и соавторы
описывают Transformer и задают маску, запрещающую самовниманию декодера обращаться
к более поздним позициям; опубликованная ими система состоит из энкодера и
декодера, а не только из декодера, как модель в этом курсе. Схема только из
декодерных блоков, показанная в курсе, — более поздняя архитектурная специализация,
а не копия той системы.

Масштабирование авторегрессионного подхода показано в работе
[Language Models are Few-Shot Learners](https://arxiv.org/pdf/2005.14165). Браун
и соавторы описывают GPT-3 как масштабированную авторегрессионную языковую модель;
её масштаб и результаты измерения её возможностей нельзя переносить на небольшую
учебную модель этого курса.

{/* chapter-section:visualization */}

## Проследите путь по единой системе

Первая схема показывает всю систему. Общий путь прямого распространения
заканчивается логитами, после чего генерация и обучение расходятся в разные
стороны. Стрелки обратной связи показывают, куда возвращается выбранный токен и где
обновлённые веса влияют на следующий шаг обучения. KV-кэш и тензорная основа
расположены рядом с основным путём: они поддерживают вычисления модели, но не
являются дополнительными этапами обработки токенов.

Во второй схеме сохранены подробные представления. Три её панели показывают путь
вычислений при генерации, раскрывают повторяющийся блок декодера и отделяют
операции обучения после получения логитов. Каждая названная часть ведёт к главе,
где она реализуется. Ссылки на главы 1–7 открывают русские страницы. Пометка EN
означает, что русская версия соответствующей главы пока недоступна и ссылка ведёт
на английскую страницу.

<LlmSystemDiagram labels={diagramLabels} />

<LlmPartsDiagram labels={diagramLabels} locale="ru" />

{/* chapter-section:course-path */}

## Используйте карту как оглавление

Запоминать эту карту не нужно. Возвращайтесь к ней, когда очередной механизм
кажется оторванным от общей картины, и спрашивайте только, где он находится: до
декодера, внутри каждого повторяющегося блока, после логитов или в процессе,
который обучает и сохраняет веса.

Главы 1–7 вводят текст, токенизацию, каузальные примеры, базовую частотную модель и
метрики языковой модели. В главах 8–23 строятся тензорная основа и механизмы
обучения. В главах 24–32 собирается декодер. Главы 33–38 посвящены обучению,
итоговой оценке, сохранению состояния, выбору следующего токена и генерации с
KV-кэшем. В главе 39 все части объединяются в одну небольшую работающую LLM.

{/* chapter-section:decoder-connection */}

## Начните с входной границы модели

В главе 1 начинается реализация и вводится различие, от которого зависят все
последующие блоки: сам текст модель ещё не принимает. Сначала его нужно превратить
в стабильные ID токенов. Затем каждая глава раскроет одну подпись на этой карте:
даст объяснение, исполняемую реализацию на Rust и проверяемое подтверждение того,
что соответствующая часть работает.
