---
{
  "chapter_id": "26-qkv-projections",
  "locale": "ru",
  "content_revision": 2,
  "order": 26,
  "concept_id": "qkv-projections",
  "title": "Создайте представления запросов, ключей и значений",
  "description": "Разберите, как самовнимание Transformer создаёт из одной последовательности скрытых состояний тензоры запросов, ключей и значений с помощью трёх независимых проекций без смещения.",
  "objective": "Спроецируйте одну последовательность скрытых состояний в отдельные тензоры запросов, ключей и значений, сохранив оси пакета и токенов.",
  "worked_inputs": "Возьмите пакет из двух скрытых состояний, $X=[[[1,2,-1],[0,1,2]]]$, и три разные матрицы весов формы $[3,2]$. До запуска примера предскажите три выхода формы $[1,2,2]$.",
  "formula": {
    "latex": "Q=XW_Q,\\quad K=XW_K,\\quad V=XW_V",
    "symbols": [
      { "symbol": "X", "meaning": "тензор скрытых состояний с одним вектором признаков для каждой позиции пакета и токена" },
      { "symbol": "W_Q", "meaning": "обучаемая матрица весов, формирующая представление запросов" },
      { "symbol": "W_K", "meaning": "обучаемая матрица весов, формирующая представление ключей" },
      { "symbol": "W_V", "meaning": "обучаемая матрица весов, формирующая представление значений" },
      { "symbol": "Q", "meaning": "тензор запросов, который далее задаёт, какое содержимое следует учитывать для каждой позиции токена" },
      { "symbol": "K", "meaning": "тензор ключей, который далее описывает признаки, по которым позиции сопоставляются с запросами" },
      { "symbol": "V", "meaning": "тензор значений с содержимым, которое затем взвешивается и смешивается в соответствии с весами внимания" }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "neural-architecture",
      "limitation": "Состояние, играющее роль запроса, и содержимое аннотаций по-прежнему поступают из двух разных частей модели энкодера–декодера.",
      "later_advance": "В самовнимании вместо выравнивания двух источников одна последовательность предыдущего слоя служит входом для всех трёх ролей до вычисления оценок.",
      "modern_llm_role": "Декодер из этого курса перед каузальным вниманием преобразует одну последовательность с помощью трёх отдельных проекций.",
      "sources": [
        {
          "role": "earlier",
          "year": 2014,
          "name": "Bahdanau, Cho, and Bengio, Neural Machine Translation by Jointly Learning to Align and Translate",
          "source_url": "https://arxiv.org/abs/1409.0473",
          "claim": "На каждом шаге декодирования Бахданау, Чо и Бенжио сопоставляют каждую аннотацию энкодера с предыдущим состоянием декодера и по полученным весам формируют вектор контекста."
        },
        {
          "role": "later",
          "year": 2017,
          "name": "Vaswani et al., Attention Is All You Need",
          "source_url": "https://arxiv.org/abs/1706.03762",
          "claim": "Васвани и соавторы используют отдельные обучаемые линейные проекции для запросов, ключей и значений и определяют самовнимание внутри одной последовательности."
        }
      ]
    },
    "approach": "От сопоставления состояния декодера с содержимым энкодера к трём обучаемым представлениям одной последовательности для самовнимания Transformer",
    "summary": "Аддитивное внимание показало, что обучаемая функция соответствия позволяет выбирать контекст источника для состояния декодера. В самовнимании источник общий, а запросы, признаки сопоставления и смешиваемое содержимое формируются отдельными проекциями запросов, ключей и значений. В этой главе реализуются только проекции; вычисление оценок и смешивание остаются для главы 27.",
    "rust_source": "rust/demos/ch26-qkv-projections/src/lib.rs"
  },
  "rust_sources": [
    {
      "path": "rust/demos/ch26-qkv-projections/src/lib.rs",
      "region": "historical-attention-source-contrast",
      "purpose": "Зафиксировать, что аддитивное внимание получает данные из двух частей модели, а самовнимание — из одной последовательности, не вычисляя оценок внимания."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/attention/qkv.rs",
      "region": "qkv-layer",
      "purpose": "Составить три независимые проекции без смещения по последней оси и предоставить для них единый API, принимающий тензор строго третьего ранга."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/attention/qkv.rs",
      "region": "qkv-errors",
      "purpose": "Явно представить типизированные ошибки ветвей, форм, размерностей и инициализации."
    },
    {
      "path": "rust/demos/ch26-qkv-projections/src/lib.rs",
      "region": "qkv-fixture",
      "purpose": "Собрать точные данные прямого и обратного проходов, форм, инициализации, независимости ветвей, ошибок, проверки градиентов и воспроизводимости."
    },
    {
      "path": "rust/demos/ch26-qkv-projections/src/main.rs",
      "purpose": "Запустить полный пример проекций и вывести вычисленные данные."
    }
  ],
  "visualization": {
    "decision": "useful",
    "id": "qkv-projections",
    "rationale": "Разветвление на три проекции наглядно показывает, что последовательность скрытых состояний сохраняет координаты пакета и токенов, а три независимые матрицы весов формируют разные признаки запросов, ключей и значений."
  },
  "decoder_connection": "Накопительный декодер теперь преобразует каждую нормализованную последовательность скрытых состояний в тензоры запросов, ключей и значений. В главе 27 запросы будут сопоставлены с ключами, а полученные веса будут использованы для смешивания значений."
}
---

import RustSource from '../../../components/RustSource.astro';
import QkvProjectionsDiagram from '../../../components/chapters/QkvProjectionsDiagram.astro';

export const diagramLabels = {
  title: 'Получите из одной последовательности скрытых состояний три обучаемых представления',
  description:
    'Проследите, как одна последовательность скрытых состояний проходит через проекции запросов, ключей и значений, а затем сравните формы, градиенты, независимость, источники данных в исторических механизмах внимания и отклонённые входы.',
  sections: {
    projections: 'Сохраните позиции, изменив пространство признаков',
    history: 'От двух источников данных для внимания к одному',
    evidence: 'Проверьте независимость, градиенты и граничные случаи',
  },
  stages: {
    input: 'Общий вход: скрытые состояния',
    query: 'Проекция запросов',
    key: 'Проекция ключей',
    value: 'Проекция значений',
  },
  fields: {
    shape: 'Форма',
    weight: 'Матрица весов проекции',
    parameter: 'Параметр с фиксированным именем',
    output: 'Результат проекции',
    biasPolicy: 'Наличие смещения',
    inputGradient: 'Суммарный градиент по входу',
    weightGradient: 'Градиенты весов отдельных ветвей',
    independence: 'Измените только веса запросов',
    emptyBatch: 'Пустая ось пакета',
    emptyTokens: 'Пустая ось токенов',
    errors: 'Отклонённые граничные случаи',
    proof: 'Численные проверки',
    earlierAttention: 'Аддитивное внимание энкодера–декодера',
    selfAttention: 'Самовнимание Transformer',
    decoderState: 'Предыдущее состояние декодера',
    encoderAnnotations: 'Аннотации энкодера',
    oneSequence: 'Одна последовательность предыдущего слоя',
  },
  roles: {
    query: 'Запрос: какую информацию следует учесть для этой позиции?',
    key: 'Ключ: по каким признакам эта позиция сопоставляется с запросом?',
    value: 'Значение: какое содержимое эта позиция передаёт дальше?',
  },
  cues: {
    shared: 'Рамка общего входа',
    query: 'Сплошная рамка — запрос',
    key: 'Пунктирная рамка — ключ',
    value: 'Двойная рамка — значение',
    accepted: 'Проверено',
    rejected: 'Отклонено',
    changed: 'Изменено',
    unchanged: 'Без изменений',
    notUsed: 'Не используется',
  },
  errorReasons: {
    rankTwo: 'Во входе должны быть явно представлены оси пакета, токенов и признаков.',
    inputWidth: 'Последняя ось входа должна совпадать с шириной модели.',
    branchMismatch: 'У всех трёх матриц проекций должна быть одинаковая входная ширина модели.',
  },
  captions: {
    projections: 'У трёх ветвей общие координаты, но разные веса и значения признаков.',
    history: 'Сходство ролей запросов, ключей и значений с ранним механизмом из двух частей модели — лишь ограниченная ретроспективная аналогия.',
    evidence: 'Пример проверяет каждую координату входа и весов и подтверждает воспроизводимость и независимость проекций.',
  },
  scrollers: {
    branches: 'Ветви запросов, ключей и значений',
    history: 'Сравнение источников данных для внимания',
    gradients: 'Таблица градиентов запросов, ключей и значений',
  },
};

{/* chapter-section:worked-example */}

## Предскажите три результата для одной последовательности

Начните с одного пакета, содержащего два состояния токенов:

$$
X=\begin{bmatrix}1&2&-1\\0&1&2\end{bmatrix},\qquad
X\in\mathbb{R}^{1\times2\times3}.
$$

Используйте три разные матрицы весов:

$$
W_Q=\begin{bmatrix}1&0\\0&1\\1&-1\end{bmatrix}.
$$

$$
W_K=\begin{bmatrix}0&1\\1&0\\-1&1\end{bmatrix}.
$$

$$
W_V=\begin{bmatrix}1&1\\1&-1\\0&2\end{bmatrix}.
$$

До запуска примера умножьте первый токен на каждую матрицу весов. Ожидаемые
векторы — $q_0=[0,3]$, $k_0=[3,0]$ и $v_0=[3,-3]$. Для второго токена должны
получиться $q_1=[2,-1]$, $k_1=[-1,2]$ и $v_1=[1,3]$.

В зафиксированном примере три результата различаются, потому что различаются
матрицы весов. Независимые наборы параметров не гарантируют разных чисел для
любого возможного входа, но позволяют обучать для каждой роли собственное
представление. Соответствие между элементами пакета и позициями токенов
сохраняется.

{/* chapter-section:formula */}

## Спроецируйте последнюю ось признаков тремя способами

Полное правило прямого прохода:

$$
Q=XW_Q,\quad K=XW_K,\quad V=XW_V
$$

Формы тензоров должны удовлетворять условиям

$$
X\in\mathbb{R}^{B\times T\times d_{model}}.
$$

$$
W_Q,W_K,W_V\in\mathbb{R}^{d_{model}\times d_{head}}.
$$

$$
Q,K,V\in\mathbb{R}^{B\times T\times d_{head}}.
$$

В каждой ветви одна и та же операция матричного умножения по последней оси
независимо применяется к каждому элементу пакета и каждой позиции токена. Она не
сопоставляет токены друг с другом. Сами по себе эти три проекции не создают
оценок сходства, вероятностей, каузальной маски или взвешенной смеси значений.

Для зафиксированного примера полный результат равен

$$
Q=\begin{bmatrix}0&3\\2&-1\end{bmatrix},\quad
K=\begin{bmatrix}3&0\\-1&2\end{bmatrix},\quad
V=\begin{bmatrix}3&-3\\1&3\end{bmatrix}.
$$

{/* chapter-section:symbol-glossary */}

## Различайте роли, оси и размерности

- $X$ — тензор скрытых состояний, поступающий в самовнимание.
- $B$ — размер пакета, а $T$ — число позиций токенов.
- $d_{model}$ — ширина входных признаков.
- $d_{head}$ — ширина выхода единственной головы в этой главе.
- $W_Q$, $W_K$ и $W_V$ — независимые обучаемые матрицы весов.
- $Q$ — представление запросов: с его помощью каждая позиция задаст, какую информацию следует учесть.
- $K$ — представление ключей: оно опишет, по каким признакам позиции сопоставляются с запросами.
- $V$ — представление значений: оно содержит информацию для последующего смешивания.

Сами обозначения не вычисляют внимание. Эта глава также не вводит ограничений
делимости, связанных с несколькими головами. В главе 30 появится число голов
$h$ и обычное соотношение $d_{model}=h\,d_{head}$, используемое при разделении и
объединении нескольких голов.

{/* chapter-section:history */}

## От обучаемого выравнивания к проекциям самовнимания

Здесь рассматривается развитие нейронного внимания на пути к современным LLM, а
не история языков программирования.

Базовые рекуррентные модели энкодер–декодер сжимали исходную последовательность
в один фиксированный вектор. [Бахданау, Чо и Бенжио, *Neural Machine Translation by Jointly Learning to Align and Translate*](https://arxiv.org/abs/1409.0473)
заменяют это узкое место дифференцируемым выравниванием. На каждом шаге
декодирования Бахданау, Чо и Бенжио сопоставляют каждую аннотацию энкодера с
предыдущим состоянием декодера и по полученным весам формируют вектор контекста.
В их обозначениях $s_{i-1}$ — предыдущее состояние декодера, а $h_j$ — одна из
аннотаций энкодера, поступающих в обучаемую функцию совместимости. Состояние,
играющее роль запроса, и содержимое аннотаций по-прежнему поступают из двух
разных частей модели энкодера–декодера.

Состояние декодера удобно считать аналогом запроса, а аннотацию — аналогом ключа
и значения, но это лишь ретроспективное сопоставление. Бахданау и соавторы не
используют терминологию запросов, ключей и значений из этой главы и не задают
схему из трёх матриц.

[Васвани и соавторы, *Attention Is All You Need*](https://arxiv.org/abs/1706.03762)
описывают внимание как отображение запроса и пар ключ–значение в выход. Васвани
и соавторы используют отдельные обучаемые линейные проекции для запросов, ключей
и значений и определяют самовнимание внутри одной последовательности. В
самовнимании вместо выравнивания двух источников одна последовательность
предыдущего слоя служит входом для всех трёх ролей до вычисления оценок.

Так представления для сопоставления отделяются от представления, содержащего
информацию для смешивания. Декодер из этого курса перед каузальным вниманием
преобразует одну последовательность с помощью трёх отдельных проекций.
Отсутствие смещений и точные размерности весов — решения, принятые в реализации
курса, а не требования ко всем моделям Transformer.

Следующий пример выделяет только различие в источниках данных для внимания:

<RustSource
  path="rust/demos/ch26-qkv-projections/src/lib.rs"
  region="historical-attention-source-contrast"
  caption="Сопоставьте две части модели с одной последовательностью, спроецированной тремя способами"
  label="Исходный код на Rust для исторического сопоставления механизмов внимания на пути к LLM в главе 26"
/>

{/* chapter-section:rust-implementation */}

## Составьте слой из трёх существующих дифференцируемых линейных слоёв

`QkvProjections` содержит три слоя `Linear` без смещения. При создании слоя веса
запросов, ключей и значений инициализируются именно в таком порядке с помощью
временной копии генератора, запущенного с заданным начальным значением. Исходный
генератор обновляется только после успешной проверки всех трёх ветвей. При
создании слоя из готовых матриц каждая ветвь, общие размерности и уникальность
имён проверяются до возврата результата:

<RustSource
  path="rust/crates/llm-from-scratch/src/attention/qkv.rs"
  region="qkv-layer"
  caption="Спроецируйте тензор скрытых состояний третьего ранга в независимые тензоры запросов, ключей и значений"
  label="Исходный код на Rust с реализацией слоя проекций Q, K и V, добавленного в главе 26"
/>

Обёртка принимает вход строго третьего ранга, чтобы оси пакета и токенов
оставались явными. Проверки выполняются в определённом порядке: сначала ранг,
затем ширина, а ветви проверяются по очереди: сначала запросы, затем ключи и
значения:

<RustSource
  path="rust/crates/llm-from-scratch/src/attention/qkv.rs"
  region="qkv-errors"
  caption="Явно различайте ошибки ветвей и форм"
  label="Исходный код на Rust с ошибками проекций Q K V из главы 26"
/>

В примере обратного прохода строится одна скалярная целевая функция:

$$
L=\langle Q,U_Q\rangle+\langle K,U_K\rangle+\langle V,U_V\rangle.
$$

Если черта сверху обозначает градиент обратного режима, вклады всех трёх путей
суммируются в градиент общего входа:

$$
\bar X=\bar QW_Q^{\mathsf T}+\bar KW_K^{\mathsf T}+\bar VW_V^{\mathsf T}.
$$

Для вычисления градиентов весов объединим оси пакета и токенов в одну ось строк,
обозначенную индексом $(BT)$. После этого каждая ветвь сохраняет собственный
градиент:

$$
\bar W_Q=X_{(BT)}^{\mathsf T}\bar Q_{(BT)},\quad
\bar W_K=X_{(BT)}^{\mathsf T}\bar K_{(BT)},\quad
\bar W_V=X_{(BT)}^{\mathsf T}\bar V_{(BT)}.
$$

Для входящих градиентов из примера градиент общего входа равен

$$
\bar X=\begin{bmatrix}3&1.5&1.5\\-1.5&3.5&-5\end{bmatrix},
$$

а у каждой ветви остаётся собственный градиент весов. Центральные разности
проверяют все шесть координат каждого из тензоров $X$, $W_Q$, $W_K$ и $W_V$ с
шагом $10^{-6}$ и допуском $2\times10^{-6}$. Для пустых осей пакета и токенов
сохраняется путь обратного распространения, а два запуска дают одинаковые
битовые представления всех чисел:

<RustSource
  path="rust/demos/ch26-qkv-projections/src/lib.rs"
  region="qkv-fixture"
  caption="Соберите точные данные прямого и обратного проходов, форм, ошибок, инициализации и градиентов"
  label="Исходный код на Rust с полным учебным примером из главы 26"
/>

<RustSource
  path="rust/demos/ch26-qkv-projections/src/main.rs"
  caption="Запустите полный пример проекций запросов, ключей и значений"
  label="Точка входа Rust для примера проекций запросов, ключей и значений из главы 26"
/>

Выполните `cargo run --quiet --locked -p ch26-qkv-projections`, чтобы увидеть
те же значения прямого прохода, градиенты, проверки форм и отклонённые входы,
полученные исполняемым примером.

{/* chapter-section:visualization */}

## Проследите, как одна последовательность превращается в три обучаемых представления

На схеме рядом показаны общий вход, три матрицы весов проекций, точные выходы,
суммарный путь обратного прохода, отдельные градиенты весов ветвей, формы
тензоров с пустыми осями, отклонённые входы и сравнение источников данных в
раннем и современном механизмах внимания:

<QkvProjectionsDiagram labels={diagramLabels} />

Проследите три ветви от одной и той же входной позиции. Сплошная,
пунктирная и двойная рамки различают запрос, ключ и значение без опоры на цвет.
Схема заканчивается до сопоставления запросов с ключами: она показывает
представления, которые получит механизм внимания, но ещё не результат внимания.

{/* chapter-section:exercises */}

## Сначала сделайте предсказания

1. Предскажите формы всех трёх выходов для входа $[4,7,3]$ при $d_{head}=2$.
2. Вычислите $q_0$, $k_0$ и $v_0$ для первого токена из зафиксированного примера.
3. Определите, может ли изменение только $W_Q$ изменить $K$ или $V$.
4. Подсчитайте число обучаемых скаляров в трёх матрицах весов формы $[3,2]$ без смещения.
5. Предскажите формы трёх выходов для входов $[0,2,3]$ и $[2,0,3]$.
6. Объясните, почему эта обёртка отклоняет вход второго ранга $[T,d_{model}]$.
7. Определите, должен ли здесь $d_{model}$ делиться на $d_{head}$.
8. Укажите, какой исторический механизм получает данные из двух частей модели, а какой — из одной последовательности.
9. Назовите вычисление, которого ещё не хватает, чтобы эти тензоры образовали выход внимания.

<details>
  <summary>Сверить ответы</summary>

  1. Каждый выход имеет форму $[4,7,2]$.
  2. Получаются векторы $[0,3]$, $[3,0]$ и $[3,-3]$.
  3. Нет. Ветви имеют независимые веса, поэтому при изменении только матрицы запросов битовые представления ключей и значений остаются прежними.
  4. Число параметров равно $3\times3\times2=18$.
  5. Для каждой ветви выходы имеют формы $[0,2,2]$ и $[2,0,2]$.
  6. API сохраняет оси пакета и токенов явными, поэтому требует вход третьего ранга.
  7. Нет. Ограничение делимости появится позднее при разделении на несколько голов.
  8. Аддитивное внимание энкодера–декодера получает данные со стороны декодера и энкодера; самовнимание проецирует одну последовательность тремя способами.
  9. В главе 27 предстоит вычислить оценки запрос–ключ, вероятности и взвешенную смесь значений.
</details>

{/* chapter-section:decoder-connection */}

## Далее сопоставьте запросы с ключами и смешайте значения

Накопительный декодер теперь принимает нормализованную последовательность
скрытых состояний формы $[B,T,d_{model}]$ и выдаёт отдельные тензоры $Q$, $K$ и
$V$ формы $[B,T,d_{head}]$. В главе 27 эти подготовленные представления
превратятся в одну голову внимания без маски: для этого будут вычислены оценки,
вероятности и смесь значений.
