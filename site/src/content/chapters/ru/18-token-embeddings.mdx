---
{
  "chapter_id": "18-token-embeddings",
  "locale": "ru",
  "content_revision": 6,
  "order": 18,
  "concept_id": "token-embeddings",
  "title": "Сопоставьте ID токенов с обучаемыми векторами",
  "description": "Реализуйте на Rust обучаемые эмбеддинги токенов: выбирайте строки таблицы по ID, сохраняйте форму пакета и последовательности и складывайте градиенты повторных обращений к одной строке.",
  "objective": "Выбирать по ID строки обучаемой таблицы эмбеддингов и накапливать градиентные вклады повторяющихся ID в общих строках.",
  "worked_inputs": "Возьмите таблицу [4,2] [[10,11],[20,21],[30,31],[40,41]] и ID токенов [[2,1,2]]. Предскажите выход формы [1,3,2], затем задайте для обратного прохода начальную сопряжённую величину [[[1,0],[0,2],[3,4]]] и определите, в каких строках таблицы появятся градиенты.",
  "formula": {
    "latex": "X_{b,t,:}=E_{z_{b,t},:},\\quad \\bar{E}_{i,:}=\\sum_{(b,t):z_{b,t}=i}\\bar{X}_{b,t,:}",
    "symbols": [
      {
        "symbol": "E",
        "meaning": "обучаемая таблица токенов формы [V,d]"
      },
      {
        "symbol": "V",
        "meaning": "размер словаря и число строк в E"
      },
      {
        "symbol": "d",
        "meaning": "ширина эмбеддинга и число признаков в каждой строке"
      },
      {
        "symbol": "z_{b,t}",
        "meaning": "целочисленный ID токена в элементе пакета b и позиции последовательности t"
      },
      {
        "symbol": "b",
        "meaning": "индекс одного элемента пакета на ведущей оси"
      },
      {
        "symbol": "t",
        "meaning": "индекс одной позиции последовательности"
      },
      {
        "symbol": ":",
        "meaning": "все координаты признаков на последней оси"
      },
      {
        "symbol": "X_{b,t,:}",
        "meaning": "выбранный в позиции (b,t) вектор эмбеддинга ширины d"
      },
      {
        "symbol": "\\bar{X}_{b,t,:}",
        "meaning": "сопряжённая величина обратного режима: производная скалярной функции потерь L по X в выходной позиции (b,t)"
      },
      {
        "symbol": "\\bar{E}_{i,:}",
        "meaning": "сопряжённая величина обратного режима: производная скалярной функции потерь L по всем признакам строки i таблицы, накопленная по совпадающим позициям"
      },
      {
        "symbol": "i",
        "meaning": "индекс одной строки словаря"
      },
      {
        "symbol": "\\sum_{(b,t):z_{b,t}=i}",
        "meaning": "сумма по всем элементам пакета и позициям последовательности, в которых ID токена выбирает строку i"
      }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "language-model",
      "limitation": "Разреженное one-hot-представление слова отводит одну координату каждому элементу словаря, но не выражает степень сходства между словами. Кроме того, явно переносить такой вектор размером со словарь расточительно, когда нужна только одна строка.",
      "later_advance": "Bengio и соавторы обучают общую плотную таблицу признаков слов вместе с нейросетевой моделью следующего слова. В Transformer используются обучаемые эмбеддинги подсловных токенов, к которым перед стеком слоёв с механизмами внимания и сетями прямого распространения добавляется позиционная информация.",
      "modern_llm_role": "ID токенов поступают в численную часть декодера, выбирая строки одной обучаемой таблицы «словарь на признаки». Повторяющиеся ID используют одну строку параметров, поэтому их вклады при обратном проходе складываются; позиционная информация, масштабирование эмбеддингов в прямом проходе, внимание и совместное использование весов с выходной проекцией рассматриваются позже.",
      "sources": [
        {
          "role": "earlier",
          "year": 2003,
          "name": "Bengio et al., A Neural Probabilistic Language Model",
          "source_url": "https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf",
          "claim": "Bengio и соавторы задают обучаемое отображение индекса слова из словаря в распределённые признаки в виде матрицы с одной строкой на элемент словаря и одним столбцом на обучаемый признак, используют эту матрицу во всех позициях контекста и обучают её вместе с предсказанием следующего слова."
        },
        {
          "role": "later",
          "year": 2017,
          "name": "Vaswani et al., Attention Is All You Need",
          "source_url": "https://papers.nips.cc/paper_files/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf",
          "claim": "Vaswani и соавторы используют обучаемые эмбеддинги ширины модели для токенов BPE или word-piece и добавляют позиционное кодирование перед стеком Transformer; масштабирование эмбеддингов в прямом проходе не относится к инициализации параметров."
        }
      ]
    },
    "approach": "От разреженных индикаторов размером со словарь к обучаемым распределённым векторам токенов и прямому выбору строк таблицы",
    "summary": "One-hot-векторы явно указывают выбранный токен, но имеют размер словаря и почти целиком состоят из нулей. Плотные обучаемые признаки слов позволили нейросетевым языковым моделям переносить статистическую информацию между словами, а в Transformer обучаемые эмбеддинги служат численным входом в более глубокие вычисления над последовательностью. Алгебраическое тождество для one-hot-вектора объясняет прямой выбор строки, а общая обучаемая строка — сложение градиентов повторяющихся токенов.",
    "rust_source": "rust/demos/ch18-token-embeddings/src/lib.rs"
  },
  "rust_sources": [
    {
      "path": "rust/demos/ch18-token-embeddings/src/lib.rs",
      "region": "one-hot-baseline",
      "purpose": "Явно построить маленький one-hot-вариант и умножить его на таблицу для алгебраического сравнения."
    },
    {
      "path": "rust/demos/ch18-token-embeddings/src/lib.rs",
      "region": "known-token-lookup",
      "purpose": "Выполнить точный выбор строк для повторяющегося ID и сопоставить результат с one-hot-вариантом."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/nn/embedding.rs",
      "region": "embedding-errors",
      "purpose": "Задать детерминированные ошибки создания, формы и количества токенов, границ, преобразования и делегированных операций."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/nn/embedding.rs",
      "region": "embedding-layer",
      "purpose": "Хранить одну именованную обучаемую таблицу, проверять селекторы u32 и делегировать дифференцируемый выбор строк."
    },
    {
      "path": "rust/demos/ch18-token-embeddings/src/lib.rs",
      "region": "repeated-token-gradient",
      "purpose": "Провести обратный проход с точной неоднородной начальной сопряжённой величиной и получить градиент общей таблицы."
    },
    {
      "path": "rust/demos/ch18-token-embeddings/src/lib.rs",
      "region": "initialized-token-embedding",
      "purpose": "Соединить слой эмбеддингов с инициализацией из главы 17 и проверить воспроизводимость и идентичность клона."
    },
    {
      "path": "rust/demos/ch18-token-embeddings/src/lib.rs",
      "region": "embedding-edge-cases",
      "purpose": "Обработать пустую форму токенов и отклонить первый ID за пределами таблицы."
    },
    {
      "path": "rust/demos/ch18-token-embeddings/src/main.rs",
      "region": "learner-token-embeddings-output",
      "purpose": "Вывести точный выбранный результат и градиент общей таблицы из разобранного примера."
    },
    {
      "path": "rust/demos/ch18-token-embeddings/src/diagram_trace.rs",
      "region": "token-embeddings-trace",
      "purpose": "Записать каждую строку таблицы, one-hot-индикатор, выбранный результат, входящий вклад и накопленный градиент строки."
    }
  ],
  "visualization": {
    "decision": "useful",
    "id": "token-embeddings",
    "rationale": "Сопоставление каждого one-hot-индикатора с выбранной строкой таблицы и выходным вектором показывает повторное использование строки, а отдельный путь обратного прохода — схождение градиентных вкладов двух позиций в одной обучаемой строке."
  },
  "decoder_connection": "Теперь совокупная модель умеет превращать тензоры ID токенов в дифференцируемые тензоры признаков: к исходной форме добавляется ось ширины эмбеддинга, а параметром остаётся одна общая именованная таблица «словарь на признаки». В главе 19 последняя ось эмбеддинга станет входной шириной обучаемой проекции: выбор строки по индексу извлекает вектор токена, а линейный слой смешивает его координаты."
}
---

import TokenEmbeddingsDiagram from '../../../components/chapters/TokenEmbeddingsDiagram.astro';

export const diagramLabels = {
  title: 'Проследите повторяющиеся ID в общей таблице',
  description:
    'Проследите точные ID токенов, строки таблицы, равносильность one-hot-умножению, выходные векторы и обратные вклады из примера на Rust, которые накапливаются в общей таблице эмбеддингов.',
  summary: {
    parameter: 'Именованный параметр',
    vocabulary: 'Строки словаря',
    width: 'Ширина эмбеддинга',
    tableShape: 'Форма таблицы',
    idShape: 'Форма ID токенов',
    outputShape: 'Форма выхода',
    gradientShape: 'Форма градиента таблицы',
  },
  stages: {
    ids: 'Целочисленные ID токенов',
    table: 'Общая обучаемая таблица',
    lookup: 'Выбор строк без нулей',
    gradients: 'Накопление в общих строках',
  },
  fields: {
    position: 'Позиция',
    tokenId: 'ID токена',
    row: 'Строка таблицы',
    uses: 'Выборов',
    state: 'Статус',
    tableValues: 'Обучаемый вектор',
    oneHot: 'One-hot-индикатор',
    operation: 'Произведение',
    selectedRow: 'Выбранная строка',
    output: 'Выходной вектор',
    upstream: 'Входящий градиент',
    positions: 'Плоские позиции',
    contributions: 'Вклады по признакам',
    rule: 'Обратное правило',
    accumulated: 'Итоговый градиент',
  },
  states: {
    unused: 'Строка не выбрана',
    selectedOnce: 'Выбрана один раз',
    selectedRepeated: 'Общая для повторов',
    singleRow: 'Строка без повторов',
    repeatedRow: 'Общая с другой позицией',
    unusedZero: 'Нет выборов — ноль',
    singleCopy: 'Один выбор — скопировать',
    repeatedSum: 'Повторы — сложить вклады',
    none: 'Нет',
  },
  notes: {
    oneHot:
      'One-hot-индикатор показывает алгебру: одна активная координата выбирает строку. Реализация обращается к ней напрямую.',
    selectors:
      'ID задают строки вне ленты. Повторный ID обращается к той же строке параметров.',
    accumulation:
      'При обратном проходе каждый градиент возвращается в выбранную строку. Для повторной строки два вклада складываются.',
  },
  symbols: {
    unused: '○',
    selectedOnce: '●',
    selectedRepeated: '◆',
  },
  scrollers: {
    ids: 'Прокручиваемые позиции ID токенов',
    table: 'Прокручиваемая таблица эмбеддингов',
    lookup: 'Прокручиваемое доказательство равносильности выбора строк и one-hot-умножения',
    gradients: 'Прокручиваемое доказательство накопления градиентов строк',
  },
};

{/* chapter-section:worked-example */}

## Предскажите три выбранные строки и один общий градиент

Возьмём обучаемую таблицу из четырёх строк словаря, по $2$ признака в каждой:

$$
E=\begin{bmatrix}
10 & 11 \\
20 & 21 \\
30 & 31 \\
40 & 41
\end{bmatrix}
$$

До запуска прочитайте ID $[[2,1,2]]$ слева направо. Первая и третья позиции
выбирают строку $2$, а средняя — строку $1$. Значит, ожидаемый результат равен
$[[[30,31],[20,21],[30,31]]]$ и имеет форму $[1,3,2]$. Повторение копирует
значение в две позиции, но не создаёт вторую обучаемую строку.

One-hot-обозначение наглядно показывает этот выбор. Например, ID $2$
соответствует $[0,0,1,0]$. При умножении такого индикатора на $E$ остаётся
$[30,31]$. Только в исполняемом сравнении с one-hot-вектором программа явно
создаёт весь индикатор, включая нулевые значения:

<RustSource
  path="rust/demos/ch18-token-embeddings/src/lib.rs"
  region="one-hot-baseline"
  caption="Умножьте явные one-hot-строки на маленькую таблицу как алгебраический эталон"
  label="Исходный код на Rust для сравнения с one-hot-умножением из главы 18"
/>

Сам слой напрямую выбирает строки и проверяет, что оба способа дают в точности
одинаковые значения:

<RustSource
  path="rust/demos/ch18-token-embeddings/src/lib.rs"
  region="known-token-lookup"
  caption="Выберите строки по повторяющимся ID и сравните точный результат с эталоном"
  label="Исходный код на Rust для выбора эмбеддингов известных токенов из главы 18"
/>

{/* chapter-section:formula */}

## Выбирайте строки в прямом проходе и накапливайте вклады в обратном

Полное правило выбора строки и обратного прохода имеет вид:

$$
X_{b,t,:}=E_{z_{b,t},:},\quad \bar{E}_{i,:}=\sum_{(b,t):z_{b,t}=i}\bar{X}_{b,t,:}
$$

В прямом проходе в каждую выходную позицию копируется одна выбранная строка.
Обозначим скалярную функцию потерь через $L$. Черта сверху — краткое обозначение
сопряжённой величины обратного режима:
$\bar{X}_{b,t,:}=\partial L/\partial X_{b,t,:}$ — градиент, приходящий в
выходную позицию, а $\bar{E}_{i,:}=\partial L/\partial E_{i,:}$ — градиент
строки $i$ таблицы. При обратном проходе градиенты передаются по тем же связям
от выходных позиций к выбранным строкам. Если несколько позиций выбрали одну строку, их входящие векторы
складываются по признакам в этой общей строке параметров.

При заданной начальной сопряжённой величине $[[[1,0],[0,2],[3,4]]]$ строка $1$
получает $[0,2]$, а строка $2$ — $[1,0]+[3,4]=[4,4]$. Неиспользованные строки
$0$ и $3$ получают нули. Градиента по ID токенов нет: это дискретные селекторы,
а не операнды ленты.

{/* chapter-section:symbol-glossary */}

## Не смешивайте оси словаря и признаков

- $E$ — обучаемая таблица токенов формы $[V,d]$.
- $V$ — размер словаря и, следовательно, число строк в $E$.
- $d$ — ширина эмбеддинга и, следовательно, число признаков в каждой строке.
- $z_{b,t}$ — целочисленный ID токена в элементе пакета $b$ и позиции последовательности $t$.
- $b$ обозначает один элемент пакета, а $t$ — одну позицию его последовательности.
- $:$ означает все координаты на последней оси признаков.
- $X_{b,t,:}$ — вектор ширины $d$, выбранный в позиции $(b,t)$.
- $\bar{X}_{b,t,:}=\partial L/\partial X_{b,t,:}$ — вектор входящей
  сопряжённой величины в этой выходной позиции.
- $\bar{E}_{i,:}=\partial L/\partial E_{i,:}$ — градиент строки таблицы
  после накопления вкладов всех совпадающих позиций.
- $i$ — индекс одной строки словаря.
- Сумма перебирает все $(b,t)$, для которых $z_{b,t}$ равно $i$.

Числовое расстояние между ID не является смысловым расстоянием. ID $1$ и $2$
соседствуют только в нумерации токенизатора; похожими ли станут соответствующие
обучаемые векторы, определяет обучение.

{/* chapter-section:history */}

## От разреженного кода принадлежности к векторному входу Transformer

Разреженное one-hot-представление слова отводит одну координату каждому элементу словаря, но не выражает степень сходства между словами. Кроме того, явно переносить такой вектор размером со словарь расточительно, когда нужна только одна строка.

[Bengio et al., *A Neural Probabilistic Language Model*](https://www.jmlr.org/papers/volume3/bengio03a/bengio03a.pdf): Bengio и соавторы задают обучаемое отображение индекса слова из словаря в распределённые признаки в виде матрицы с одной строкой на элемент словаря и одним столбцом на обучаемый признак, используют эту матрицу во всех позициях контекста и обучают её вместе с предсказанием следующего слова.

Bengio и соавторы обучают общую плотную таблицу признаков слов вместе с нейросетевой моделью следующего слова. В Transformer используются обучаемые эмбеддинги подсловных токенов, к которым перед стеком слоёв с механизмами внимания и сетями прямого распространения добавляется позиционная информация.

[Vaswani et al., *Attention Is All You Need*](https://papers.nips.cc/paper_files/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf): Vaswani и соавторы используют обучаемые эмбеддинги ширины модели для токенов BPE или word-piece и добавляют позиционное кодирование перед стеком Transformer; масштабирование эмбеддингов в прямом проходе не относится к инициализации параметров.

ID токенов поступают в численную часть декодера, выбирая строки одной обучаемой таблицы «словарь на признаки». Повторяющиеся ID используют одну строку параметров, поэтому их вклады при обратном проходе складываются; позиционная информация, масштабирование эмбеддингов в прямом проходе, внимание и совместное использование весов с выходной проекцией рассматриваются позже.

One-hot-векторы явно указывают выбранный токен, но имеют размер словаря и почти
целиком состоят из нулей. Плотные обучаемые признаки слов позволили нейросетевым языковым
моделям переносить статистическую информацию между словами, а в Transformer
обучаемые эмбеддинги служат численным входом в более глубокие вычисления над
последовательностью. Алгебраическое тождество для one-hot-вектора объясняет
прямой выбор строки, а общая обучаемая строка — сложение градиентов
повторяющихся токенов.

Главный переход здесь — от разреженных кодов принадлежности к обучаемым векторам,
общим для всех вхождений одного токена. Конкретная реализация может выбирать
другие целочисленные типы, схемы хранения, инициализаторы и соглашения об ошибках:
уравнения выбора строки и обратного режима от этого не меняются. Произведение с
one-hot-вектором служит алгебраическим пояснением, а не утверждением, что в
какой-либо из работ такие разреженные векторы хранятся явно.

{/* chapter-section:rust-implementation */}

## Оберните существующее правило выбора строк одной именованной таблицей

Слой сообщает об ошибках таблицы, формы и количества токенов, границ, выделения
памяти при преобразовании и операциях автодифференцирования, не возвращая
частично сформированный результат:

<RustSource
  path="rust/crates/llm-from-scratch/src/nn/embedding.rs"
  region="embedding-errors"
  caption="Сделайте ошибки создания эмбеддинга и проверки селекторов типизированными и детерминированными"
  label="Исходный код на Rust с ошибками эмбеддингов из главы 18"
/>

Слой принимает ID токенов в виде значений типа `u32`. Он проверяет ведущую
форму, точное число элементов и первый недопустимый ID до резервирования памяти
под преобразованные индексы, а затем вызывает операцию выбора строк из главы 16.
Алгоритмы выбора и накопления по индексам в слое не дублируются:

<RustSource
  path="rust/crates/llm-from-scratch/src/nn/embedding.rs"
  region="embedding-layer"
  caption="Храните одну именованную таблицу и делегируйте дифференцируемый выбор строк"
  label="Исходный код на Rust со слоем эмбеддингов из главы 18"
/>

Точная неоднородная начальная сопряжённая величина делает накопление повторяющихся
вкладов наблюдаемым:

<RustSource
  path="rust/demos/ch18-token-embeddings/src/lib.rs"
  region="repeated-token-gradient"
  caption="Проведите обратный проход через повторяющийся выбор и прочитайте сохранённый градиент таблицы"
  label="Исходный код на Rust, доказывающий накопление градиентов повторяющихся токенов"
/>

При создании слоя используется матричный инициализатор из главы 17, которому
$V$ и $d$ передаются как два размера таблицы. Это явное соглашение об
инициализации: оно не следует ни из операции выбора строки, ни из работы о
Transformer. Другая реализация может выбрать иной инициализатор, не меняя
уравнений прямого и обратного прохода. Повторный запрос с тем же начальным
значением генератора воспроизводит значения, но отдельно созданные слои остаются
разными листьями. Клонирование слоя намеренно сохраняет идентичность параметра:

<RustSource
  path="rust/demos/ch18-token-embeddings/src/lib.rs"
  region="initialized-token-embedding"
  caption="Воспроизводимо инициализируйте таблицу и проверьте идентичность клона"
  label="Исходный код на Rust, связывающий инициализацию из главы 17 с эмбеддингами из главы 18"
/>

Команда для ученика также проверяет вход формы $[0]$, не содержащий ни одного
ID, и первый ID за пределами таблицы:

<RustSource
  path="rust/demos/ch18-token-embeddings/src/lib.rs"
  region="embedding-edge-cases"
  caption="Примите пустую форму токенов и отклоните первый ID за пределами таблицы"
  label="Исходный код на Rust, проверяющий пустой вход и границы в главе 18"
/>

Запустите `cargo run --quiet --locked -p ch18-token-embeddings`, чтобы увидеть
выбранные строки и накопленный градиент таблицы. Один скалярный ID формы $[]$
даёт один вектор формы $[2]$. Вход формы $[0]$ содержит ноль ID, поэтому после
добавления ширины эмбеддинга выход имеет форму $[0,2]$ и по-прежнему не содержит
значений.
Ошибки формы и количества сообщаются раньше первого недопустимого селектора.
Проверка всех координат конечными разностями с шагом $10^{-6}$ совпадает с
аналитическим градиентом таблицы в пределах абсолютного допуска $2\times10^{-6}$.

<RustSource
  path="rust/demos/ch18-token-embeddings/src/main.rs"
  region="learner-token-embeddings-output"
  caption="Выведите выбранные векторы и градиент общей таблицы эмбеддингов"
  label="Исходный код на Rust с учебными результатами главы 18"
/>

{/* chapter-section:visualization */}

## Проследите каждый выбор обратно до общей строки

В прямой части каждая позиция сопоставляется со своим ID токена, one-hot-тождеством,
выбранной строкой таблицы и выходным вектором. В обратной части каждый входящий
вектор сопоставляется со строкой, которая его получает. Строка $2$ встречается в
двух позициях, поэтому различие видно явно: в прямом проходе её значение
копируется в два выхода, а в обратном оба градиентных вклада складываются в одной
строке параметров.

<RustSource
  path="rust/demos/ch18-token-embeddings/src/diagram_trace.rs"
  region="token-embeddings-trace"
  caption="Соберите строки таблицы, выборы и накопленные градиенты разобранного примера"
  label="Исходный код на Rust, собирающий данные об эмбеддингах из главы 18"
/>

<TokenEmbeddingsDiagram labels={diagramLabels} />

Читайте прямую и обратную части вместе. Повторный выбор обозначен текстом,
ромбом и двойной линией, а не только цветом, поэтому общая строка заметна при
любой цветовой схеме.

{/* chapter-section:exercises */}

## Сначала предскажите, затем сверьтесь с исполняемым примером

1. Предскажите все шесть выходных значений для ID $[[2,1,2]]$.
2. Запишите one-hot-строку длины четыре для ID $2$ и умножьте её на $E$.
3. Предскажите все значения градиента таблицы для заданной начальной сопряжённой величины.
4. Объясните, почему строка $2$ получает сумму, а не два независимых градиента параметров.
5. Предскажите формы выхода для форм ID $[2,3]$, $[]$ и $[0]$ при $d=2$.
6. Найдите первый недопустимый ID в $[1,4,9]$ для таблицы из четырёх строк.
7. Предскажите, создаёт ли клонирование слоя эмбеддингов новый обучаемый лист.
8. Объясните, почему ID не получают градиент и почему соседние номера ID не означают смыслового сходства.
9. Определите, требует ли работа Bengio и соавторов явно создавать one-hot-векторы.
10. Определите, получает ли повторное вхождение токена новый параметр эмбеддинга.

<details>
  <summary>Сверить ответы</summary>

  1. Результат равен $[[[30,31],[20,21],[30,31]]]$.
  2. ID $2$ соответствует $[0,0,1,0]$, поэтому умножение на $E$ даёт $[30,31]$.
  3. Градиент таблицы равен $[[0,0],[0,2],[4,4],[0,0]]$.
  4. Оба вхождения выбрали строку $2$ одного и того же листа-параметра, поэтому $[1,0]$ и $[3,4]$ складываются в градиенте этой строки.
  5. Формы выхода равны $[2,3,2]$, $[2]$ и $[0,2]$.
  6. ID $4$ в плоской позиции $1$ — первый недопустимый селектор; допустимы строки от $0$ до $3$.
  7. Нет. Клон — это другая ссылка на тот же именованный лист таблицы.
  8. ID — дискретные селекторы вне ленты, а их нумерация не задаёт геометрии.
  9. Нет. One-hot-умножение здесь служит только алгебраическим пояснением.
  10. Нет. Повторение снова использует одну строку таблицы; последующий учёт позиции различает вхождения, не создавая отдельных параметров эмбеддинга для каждой позиции.
</details>

{/* chapter-section:decoder-connection */}

## Передайте последнюю ось признаков обучаемой проекции

Теперь совокупная модель умеет превращать тензоры ID токенов в дифференцируемые тензоры признаков: к исходной форме добавляется ось ширины эмбеддинга, а параметром остаётся одна общая именованная таблица «словарь на признаки». В главе 19 последняя ось эмбеддинга станет входной шириной обучаемой проекции: выбор строки по индексу извлекает вектор токена, а линейный слой смешивает его координаты.

Это численный вход декодера, пока ещё не учитывающий позиции. Глава 19 сохраняет
все ведущие оси пакета и последовательности и применяет обучаемую матрицу к
последней оси признаков. В следующих главах RoPE сделает внимание
позиционно-зависимым, поворачивая уже спроецированные запросы и ключи; повторяющиеся
вхождения по-прежнему используют одну строку таблицы эмбеддингов.
