---
{
  "chapter_id": "04-apply-bpe-tokenizer",
  "locale": "ru",
  "content_revision": 2,
  "order": 4,
  "concept_id": "reversible-byte-bpe-tokenizer",
  "title": "Кодирование и декодирование с помощью BPE-токенизатора",
  "description": "Примените зафиксированные ранги слияний, добавьте управляющие токены по краям документа и восстановите исходные байты без потерь.",
  "objective": "Применять зафиксированные ранги слияний к любой строке UTF-8 или последовательности байтов, добавлять по краям документа зарезервированные управляющие ID и без потерь восстанавливать исходные байты.",
  "worked_inputs": "Предсказать, как зафиксированные ранги кодируют байты пробела, за которым следует кириллическая «а», а затем сравнить полученное каноническое кодирование с другой допустимой последовательностью ID, из которой восстанавливаются те же байты.",
  "formula": {
    "latex": "\\operatorname{decode}_{content}(\\operatorname{encode}_{content}(x))=\\operatorname{bytes}(x)",
    "symbols": [
      {
        "symbol": "x",
        "meaning": "входное содержимое, переданное как текст UTF-8 или непосредственно как байты"
      },
      {
        "symbol": "\\operatorname{encode}_{content}",
        "meaning": "преобразование каждого байта в начальный ID токена содержимого с учётом смещения и последующее применение всех зафиксированных правил слияния по возрастанию ранга, без BOS и EOS"
      },
      {
        "symbol": "\\operatorname{decode}_{content}",
        "meaning": "последовательное объединение байтов, сохранённых для каждого ID токена содержимого"
      },
      {
        "symbol": "\\operatorname{bytes}(x)",
        "meaning": "исходная последовательность байтов без нормализации Unicode и замен"
      }
    ]
  },
  "history": {
    "approach": "Закрытые словари целых слов с неизвестным токеном и побайтовое резервное кодирование",
    "summary": "В закрытом словаре написание любого слова, которое не встречалось при обучении, теряется за маркером <UNK>. Сеннич, Хэддоу и Бёрч предложили обучать подсловные единицы для работы с редкими словами. Позднее в отчёте GPT-2 описали BPE на уровне байтов UTF-8 с базовым алфавитом из 256 значений, достаточным для представления любой строки Unicode. Байтовая основа устраняет необходимость заменять незнакомые строки на <UNK>, но для одного символа может потребоваться несколько позиций в последовательности токенов. В этом курсе отдельно определены правила обработки границ документов и схема ID для BOS и EOS.",
    "rust_source": "rust/demos/ch04-apply-bpe-tokenizer/src/lib.rs"
  },
  "rust_sources": [
    {
      "path": "rust/demos/ch04-apply-bpe-tokenizer/src/lib.rs",
      "region": "unknown-token-loss",
      "purpose": "Показать, как закрытая таблица целых слов заменяет любое отсутствующее в словаре написание одним ID неизвестного токена и не позволяет восстановить исходное слово."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/tokenizer/bpe.rs",
      "region": "token-id-layout",
      "purpose": "Задать версионируемую схему диапазонов ID для управляющих, однобайтовых и составных токенов."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/tokenizer/bpe.rs",
      "region": "ranked-content-encoding",
      "purpose": "Сдвинуть ID байтов и применить зафиксированные ранги по порядку."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/tokenizer/bpe.rs",
      "region": "byte-exact-decoding",
      "purpose": "Сначала восстановить исходные байты, а корректность UTF-8 проверять только при отдельном преобразовании в строку."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/tokenizer/bpe.rs",
      "region": "document-wrapping",
      "purpose": "Добавить BOS и EOS после слияний содержимого и проверить, что они находятся только по краям документа."
    },
    {
      "path": "rust/demos/ch04-apply-bpe-tokenizer/src/main.rs",
      "region": "chapter-output",
      "purpose": "Вывести проверяемые данные о схеме ID, граничных случаях, канонизации и визуализации."
    }
  ],
  "visualization": {
    "decision": "useful",
    "id": "apply-bpe-tokenizer",
    "rationale": "Параллельные схемы для ASCII и кириллицы показывают, как слияния группируют байты, почему ID содержимого сдвинуты, где появляются управляющие токены и как из байтов отдельных токенов без потерь восстанавливается вход."
  },
  "decoder_connection": "Теперь токенизатор преобразует каждый документ в отдельную последовательность [BOS, содержимое..., EOS], из которой можно без потерь восстановить исходные байты. В главе 5 пары «контекст — целевая последовательность» будут строиться только внутри каждого документа, без объединения документов и пересечения границ между выборками."
}
---

import ApplyBpeTokenizerDiagram from '../../../components/chapters/ApplyBpeTokenizerDiagram.astro';

export const diagramLabels = {
  title: 'Из групп байтов без потерь восстанавливаются исходные данные',
  description:
    'Проследите, как примеры на ASCII и кириллице превращаются в канонические ID токенов содержимого и последовательности ID документов, а затем восстановите исходные байты, объединяя сохранённые последовательности в порядке токенов.',
  caseTitles: {
    asciiBee: 'ASCII: bee с пробелом в конце',
    cyrillicA: 'Кириллица: пробел перед «а»',
  },
  lanes: {
    input: 'Входной текст',
    bytes: 'Байты UTF-8 (hex)',
    initial: 'Начальные ID токенов содержимого (со сдвигом)',
    grouped: 'Канонические группы после слияний',
    document: 'Последовательность ID документа',
    decoded: 'Восстановленные байты (hex)',
  },
  fields: {
    layoutVersion: 'Версия схемы ID',
    contentOffset: 'Смещение ID токенов содержимого',
    tokenId: 'ID токена',
    byteExpansion: 'Сохранённые байты',
    appliedRank: 'Применённый ранг',
    byteFallback: 'Однобайтовый резервный токен',
    bos: 'начальная граница',
    eos: 'конечная граница',
  },
  values: {
    exactMatch: 'Байты совпадают с исходными',
  },
  invariantsLabel: 'Что подтверждают оба примера',
  invariants: {
    ranks: 'Слияния применяются по возрастанию ранга; новый вход не меняет их порядок.',
    offset: 'Каждый ID содержимого на два больше соответствующего ID из пространства обучения главы 3.',
    controls: 'BOS и EOS добавляются после кодирования и встречаются только по краям документа.',
    bytes: 'Последовательное объединение байтов токенов восстанавливает вход без нормализации.',
  },
};

{/* chapter-section:worked-example */}

## Предскажите первое слияние, прежде чем запускать программу

Возьмём пробел и следующую за ним кириллическую `а`:

```text
текст             = " а"
байты UTF-8 (hex) = [20, d0, b0]
ID обучения       = [32, 208, 176]
```

Из главы 3 нужны два правила:

| Ранг | Пара в пространстве обучения | Новый ID обучения | Байты токена (hex) |
| ---: | :--- | ---: | :--- |
| 0 | `(32,208)` | `256` | `20 d0` |
| 1 | `(208,176)` | `257` | `d0 b0` |

Какое правило сработает? Ранг задаёт приоритет; частоту пар для нового входа
считать заново не нужно. Сначала правило ранга 0 заменяет пару `32,208`, и
остаётся `[256,176]`. После этой замены операнды правила ранга 1 уже не стоят
рядом. Поскольку ID `0` и `1` зарезервированы для двух управляющих токенов,
каждый ID из пространства обучения сдвигается на два:

```text
канонические ID содержимого = [258, 178]
ID документа                = [0, 258, 178, 1]
                               ^             ^
                              BOS           EOS
```

Теперь восстановим содержимое. Для ID `258` сохранены байты `20 d0`, а ID `178`
соответствует исходному байту `b0`. Последовательное объединение даёт исходные
байты `20 d0 b0`. При этом последовательность `20 d0`, сохранённая для первого
токена, сама по себе не является корректным UTF-8: `d0` начинает двухбайтовую
кодировку, а байт продолжения `b0` находится в следующем токене. Поэтому границы
токенов не обязаны совпадать с границами символов.

Есть важная тонкость. `[34,259]` — тоже допустимая последовательность ID
содержимого: ID `34` соответствует пробелу `20`, а для ID `259`, созданного
слиянием ранга 1, сохранены байты `d0 b0`. Из неё восстанавливаются те же три
байта. Но если снова закодировать эти байты, получится каноническая
последовательность `[258,178]`, потому что правило ранга 0 имеет приоритет.
Декодирование без потерь ещё не делает любую допустимую последовательность ID
канонической.

{/* chapter-section:formula */}

## Односторонняя гарантия: точное восстановление байтов

Токенизатор гарантирует:

$$
\operatorname{decode}_{content}(\operatorname{encode}_{content}(x))=\operatorname{bytes}(x)
$$

`encode_content` сначала ставит в соответствие каждому байту один сдвинутый ID,
а затем применяет зафиксированные правила по возрастанию ранга, начиная с нулевого.
Функция не использует частоты из валидационной или тестовой выборки, не обучает
новые пары, не нормализует Unicode, не меняет регистр и не делит строку на слова.
`decode_content` последовательно объединяет байты, сохранённые для каждого токена
содержимого.

Формула намеренно не утверждает, что
`encode_content(decode_content(z)) = z` для любой последовательности `z`.
Контрпример — `[34,259]`. Для модели достаточно гарантии, записанной выше:
кодировщик всегда выбирает одну и ту же каноническую последовательность, а
декодировщик восстанавливает из неё исходные байты без потерь.

{/* chapter-section:symbol-glossary */}

## Обозначения и схема ID

| Обозначение | Смысл |
| :--- | :--- |
| $x$ | входное содержимое, переданное как текст UTF-8 или непосредственно как байты |
| $\operatorname{encode}_{content}$ | преобразование каждого байта в начальный ID токена содержимого с учётом смещения и последующее применение всех зафиксированных правил слияния по возрастанию ранга, без BOS и EOS |
| $\operatorname{decode}_{content}$ | последовательное объединение байтов, сохранённых для каждого ID токена содержимого |
| $\operatorname{bytes}(x)$ | исходная последовательность байтов без нормализации Unicode и замен |

В версии 1 используется единое непрерывное пространство ID:

| ID | Значение | Отображение |
| :--- | :--- | :--- |
| `0` | управляющий токен BOS | фиксирован |
| `1` | управляющий токен EOS | фиксирован |
| `2..=257` | 256 однобайтовых токенов содержимого | байт `b` переходит в `b+2` |
| `258..` | обученные токены содержимого | ранг `r` переходит в `258+r` |

В этой версии курса ID для PAD не предусмотрен: в последующих главах используются
окна фиксированной длины. Сценарии инференса, в которых последовательности
приходится часто дополнять до общей длины (padding), оставлены за рамками курса;
в рабочих системах токен PAD по-прежнему может быть необходим.

{/* chapter-section:history */}

## Как байтовый алфавит устраняет необходимость в `<UNK>`

Закрытый словарь целых слов может назначить отдельные ID частым словам из
обучающих данных, но в нём нет записи для написания, которое раньше не
встречалось. Исторический пример на Rust кодирует `lowering`, как и любое другое
отсутствующее в словаре слово, идентификатором `0`. Декодер возвращает лишь
маркер `<UNK>` и уже не может восстановить исходное написание:

<RustSource
  path="rust/demos/ch04-apply-bpe-tokenizer/src/lib.rs"
  region="unknown-token-loss"
  caption="Закрытая таблица слов не восстанавливает отсутствующее в словаре написание"
  label="Функции Rust, которые кодируют все отсутствующие в словаре целые слова одним ID и декодируют его только в маркер неизвестного токена"
/>

В [работе 2016 года о редких словах](https://aclanthology.org/P16-1162/)
Сеннич, Хэддоу и Бёрч применили многократное слияние пар к последовательностям
символов и получили подсловные единицы. Они занимают промежуточное положение
между целыми словами и отдельными символами. Однако в этой работе ещё не
предлагался универсальный байтовый алфавит.

В [разделе о представлении входа отчёта GPT-2](https://cdn.openai.com/better-language-models/language-models.pdf)
описан следующий шаг: базовый алфавит из 256 значений — по одному на каждый
возможный байт — позволяет представить любую строку Unicode в кодировке UTF-8.
За такую универсальность приходится платить более длинными последовательностями.
До слияний кириллическая буква занимает два байта UTF-8, а эмодзи — часто четыре.
Обученные слияния сокращают часто встречающиеся последовательности, но байтовая
основа не гарантирует ни минимального итогового словаря, ни одинаковой длины
токенизации для разных письменностей.

В GPT-2 перед применением BPE текст дополнительно разбивался по категориям
символов, а пробел обрабатывался особым образом. Наш токенизатор устроен иначе:
он применяет конкретную таблицу рангов из главы 3, разрешает слияние любой пары
внутри документа и никогда не пересекает его границу. Поэтому название «BPE» само
по себе ещё не определяет все важные правила реализации.

{/* chapter-section:rust-implementation */}

## Реализуйте на Rust зафиксированный токенизатор со строгой проверкой

Схема ID закрепляет разделение управляющих токенов и содержимого в самом коде, а
не только в комментариях. Реализация проверяет, что последний ID помещается в
`u32`, и предоставляет диапазоны ID для байтов и результатов слияний:

<RustSource
  path="rust/crates/llm-from-scratch/src/tokenizer/bpe.rs"
  region="token-id-layout"
  caption="Схема ID токенизатора версии 1 и проверяемые диапазоны"
  label="Константы Rust и тип с проверками для ID BOS, EOS, однобайтовых и составных токенов"
/>

`BpeTokenizer::from_training` копирует упорядоченную таблицу пар и заново строит
последовательность байтов для каждого токена. Поэтому готовый токенизатор не
зависит от времени жизни объекта, использованного при обучении. Есть и конструктор
непосредственно из таблицы пар: он выполняет те же проверки для небольших
упражнений и будущей загрузки контрольных точек. На ранге `r` оба операнда уже
должны существовать, а одна и та же пара не может встречаться дважды.

Основной метод кодировщика принимает `&[u8]`; вариант для `&str` просто передаёт
ему байты UTF-8. Для каждого правила оба ID операндов увеличиваются на `2`, после
чего правило применяется одним проходом слева направо, как в главе 3. Правила
перебираются по возрастанию ранга. Необязательная трассировка записывает только
слияния, которые изменили данный вход, и не влияет на результат:

<RustSource
  path="rust/crates/llm-from-scratch/src/tokenizer/bpe.rs"
  region="ranked-content-encoding"
  caption="Преобразуйте байты в ID и примените слияния по возрастанию ранга"
  label="Реализация на Rust канонического кодирования содержимого BPE в порядке рангов"
/>

Основной метод декодирования возвращает `Vec<u8>`. Благодаря этому можно без
потерь восстановить любое из 256 значений байта. Например, последовательность
`ff fe` восстанавливается как байты, хотя она не является корректным UTF-8.
Отдельный метод преобразует результат в `String` и, если UTF-8 некорректен,
возвращает позицию ошибочного байта вместо молчаливой подстановки символа:

<RustSource
  path="rust/crates/llm-from-scratch/src/tokenizer/bpe.rs"
  region="byte-exact-decoding"
  caption="Сначала восстановите байты, а корректность UTF-8 проверяйте только при преобразовании в текст"
  label="Декодер содержимого на Rust и функции преобразования в UTF-8 со строгой проверкой"
/>

Управляющие токены задают структуру документа отдельно от его содержимого.
Кодировщик сначала выполняет все слияния и лишь затем добавляет
`[BOS, ..., EOS]`, поэтому управляющий ID не может стать операндом правила.
Декодер требует обе границы и отклоняет BOS или EOS во внутренней позиции:

<RustSource
  path="rust/crates/llm-from-scratch/src/tokenizer/bpe.rs"
  region="document-wrapping"
  caption="После кодирования добавьте управляющие токены и проверьте их положение"
  label="Кодировщик и декодировщик документа на Rust, допускающие BOS и EOS только по краям"
/>

Исполняемый пример обучает те же восемь рангов, что и в главе 3, используя только
обучающую выборку, а затем фиксирует их. Он показывает потерю исходного написания
в исторической таблице, проверяет байты, отсутствовавшие при обучении, и
некорректный UTF-8, демонстрирует канонизацию и выводит трассировку строгого
формата для диаграммы:

<RustSource
  path="rust/demos/ch04-apply-bpe-tokenizer/src/main.rs"
  region="chapter-output"
  caption="Выведите проверяемые данные о токенизаторе и граничных случаях"
  label="Функция main на Rust, которая фиксирует восемь рангов и выводит обратимые схемы для ASCII и кириллицы"
/>

Запустите исполняемые проверки из корня репозитория:

```text
cargo test --workspace --locked
cargo run --quiet --locked -p ch04-apply-bpe-tokenizer | diff -u rust/demos/ch04-apply-bpe-tokenizer/expected.txt -
```

При успехе вторая команда ничего не печатает. Тесты охватывают все 256 значений
байта, пустое содержимое, ASCII, кириллицу, эмодзи, сочетания с NUL и переводом
строки, а также некорректный UTF-8. Отдельные тесты проверяют ошибки переполнения
ID, ссылки на ещё не созданный операнд слияния, повтор пары, неизвестный ID и
каждое недопустимое положение управляющего токена.

{/* chapter-section:visualization */}

## Проследите группировку и восстановление исходных байтов

<ApplyBpeTokenizerDiagram labels={diagramLabels} />

Обе схемы построены по эталонному выводу программы на Rust. В строке `bee `
слияние ранга 7 объединяет последнюю `e` с пробелом в токен `265`, а предыдущие
`b` и `e` остаются однобайтовыми токенами. В строке ` а` слияние ранга 0
объединяет пробел с первым байтом буквы `а`; байт продолжения остаётся отдельно.
Второй пример наглядно показывает, что границы токенов и символов могут не
совпадать.

Сначала пройдите по каждой схеме сверху вниз и проследите кодирование. Затем для
декодирования объедините сохранённые байты токенов слева направо, не меняя их
порядок. BOS и EOS не входят в содержимое. Совпадение обозначено не только цветом,
но и галочкой, рамкой, подписью и самими значениями байтов. На полосы с токенами
можно перейти с клавиатуры; при необходимости они прокручиваются по горизонтали
внутри диаграммы. На узком экране примеры располагаются друг под другом;
клиентский скрипт для этого не нужен.

{/* chapter-section:exercises */}

## Сначала предскажите, затем проверьте

1. Для байтов `20 d0 b0` предскажите результат при ошибочном применении ранга 1
   до ранга 0, а затем запишите канонический результат.
2. Переведите ASCII `?` (байт `3f`), ID слияния `257` из пространства обучения
   и ранг `7` в схему версии 1.
3. Предскажите `encode_content("")` и `encode_document("")`.
4. Предскажите ID содержимого для `🦀`, если ни одна обученная пара не
   совпадает.
5. Кириллическая `т` кодируется в UTF-8 как `d1 82`, а ранг 2 выучил
   `(209,130)`. Предскажите конечную последовательность.
6. Декодируйте `[34,259]`, затем предскажите результат повторного кодирования.
7. Найдите первую ошибку в документе `[0,100,0,1]` и содержимом `[100,1]`.
8. Решите, обязано ли точное восстановление байтов из ID `[257,256]` давать корректный
   `String`.

<details>
  <summary>Проверьте ответы</summary>

  1. Неверный порядок создал бы `[32,257]` в пространстве обучения и `[34,259]`
     после сдвига. Правильный порядок даёт `[256,176]` и `[258,178]`.
  2. `3f` — это 63, поэтому `?` получает ID `65`. ID обучения `257` переходит в
     `259`, а ранг 7 — в `258+7=265`.
  3. Пустое содержимое — `[]`, пустой документ — `[0,1]`. Оба декодируются в
     ноль байтов.
  4. `🦀` имеет байты `f0 9f a6 80`, поэтому резервные ID после сдвига равны
     `[242,161,168,130]`.
  5. Сдвинутые операнды `(211,132)` сливаются на ранге 2 в ID `260`, поэтому
     результат — `[260]`.
  6. `[34,259]` раскрывается в `20 d0 b0`, то есть в `" а"`. При повторном
     кодировании сначала срабатывает ранг 0, и получается `[258,178]`.
  7. BOS в позиции 2 документа является внутренним управляющим токеном. EOS в
     позиции 1 содержимого запрещён: `decode_content` не принимает управляющие ID.
  8. Нет. ID `[257,256]` точно восстанавливают `ff fe`, а строгое преобразование
     в UTF-8 отвергает результат на байте 0. Главный контракт — обратимость байтов.
</details>

**Распространённое заблуждение:** «Если последовательность декодируется без
потерь, то повторное кодирование восстановленных байтов обязательно вернёт ту же
последовательность токенов». Это неверно. Несколько вариантов разбиения могут
декодироваться без потерь, но зафиксированный порядок рангов выбирает только одно
каноническое кодирование.

{/* chapter-section:decoder-connection */}

## Сохраняйте каждый документ как отдельную последовательность с границами

Теперь общая реализация курса преобразует каждый исходный документ в отдельную
воспроизводимую последовательность `[BOS, содержимое..., EOS]`, из которой можно
восстановить исходные байты. Статистика слияний по-прежнему вычисляется только по
обучающим документам из главы 2. Применение зафиксированного токенизатора к
валидационной или тестовой выборке не изменяет его словарь и ранги.

Глава 5 будет обрабатывать эти последовательности по одной. Пары «контекст —
целевая последовательность» будут строиться только внутри каждого документа,
без объединения документов и пересечения границ между обучающей, валидационной и
тестовой выборками. BOS задаёт начальный контекст, а EOS становится последней
целью предсказания. Оба токена явно обозначают границы документа и не участвуют
в слияниях.
