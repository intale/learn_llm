---
{
  "chapter_id": "25-rmsnorm",
  "locale": "ru",
  "content_revision": 5,
  "order": 25,
  "concept_id": "rmsnorm",
  "title": "Нормализуйте масштаб, не вычитая среднее",
  "description": "Реализуйте RMSNorm по последней оси, проследите градиенты по входу и коэффициенту масштаба и отделите идеальную инвариантность от поведения вблизи нуля, где преобладает эпсилон.",
  "objective": "Реализовать дифференцируемую RMSNorm по последней оси и отличать идеальную инвариантность к положительному масштабированию от поведения вблизи нуля, где результат определяет эпсилон.",
  "worked_inputs": "Возьмите $x=[3,4]$, обучаемый коэффициент масштаба $g=[1.5,0.5]$ и $\\varepsilon=10^{-5}$. Предскажите среднее квадратов, вектор $\\hat{x}$ после масштабирования по RMS, но до применения $g$, и итоговый выход после применения $g$; затем сравните $\\hat{x}$ до и после умножения обычного и близкого к нулю входных векторов на десять, не применяя $g$.",
  "formula": {
    "latex": "\\operatorname{RMSNorm}(x)=g\\odot\\frac{x}{\\sqrt{\\frac{1}{d}\\sum_i x_i^2+\\varepsilon}}",
    "symbols": [
      { "symbol": "x", "meaning": "один входной вектор признаков на последней оси тензора" },
      { "symbol": "g", "meaning": "обучаемый вектор коэффициентов масштаба, по одному значению на признак" },
      { "symbol": "\\odot", "meaning": "покоординатное умножение" },
      { "symbol": "d", "meaning": "ненулевая ширина последней оси признаков" },
      { "symbol": "i", "meaning": "координата признака, входящая в вычисление среднеквадратичного значения" },
      { "symbol": "x_i", "meaning": "признак с индексом i во входном векторе" },
      { "symbol": "\\varepsilon", "meaning": "неотрицательная стабилизирующая добавка перед вычислением обратного квадратного корня" },
      { "symbol": "\\operatorname{RMSNorm}(x)", "meaning": "выход после применения коэффициента масштаба, имеющий ту же форму, что и x" }
    ]
  },
  "history": {
    "llm_evolution": {
      "predecessor_kind": "training-practice",
      "limitation": "В BatchNorm результат для обучающего примера зависит от статистик мини-батча. LayerNorm устраняет зависимость между примерами, но перед масштабированием по-прежнему вычисляет и вычитает среднее по признакам отдельного примера.",
      "later_advance": "RMSNorm отказывается от вычитания среднего и сохраняет масштабирование вектора признаков по его среднеквадратичному значению; позднее в LLaMA RMSNorm применили перед каждым подслоем Transformer.",
      "modern_llm_role": "В декодере с предварительной нормализацией в каждую ветвь внимания или сети прямого распространения поступает остаточный поток с контролируемым масштабом, а тождественный путь проходит в обход нормализации.",
      "sources": [
        {
          "role": "earlier",
          "year": 2015,
          "name": "Ioffe and Szegedy, Batch Normalization",
          "source_url": "https://arxiv.org/abs/1502.03167",
          "claim": "Иоффе и Сегеди включают нормализацию в архитектуру и вычисляют её статистики отдельно для каждого мини-батча при обучении."
        },
        {
          "role": "earlier",
          "year": 2016,
          "name": "Ba, Kiros, and Hinton, Layer Normalization",
          "source_url": "https://arxiv.org/abs/1607.06450",
          "claim": "Ба, Кирос и Хинтон вычисляют среднее и дисперсию по суммарным входным сигналам нейронов слоя для одного обучающего примера, устраняя зависимость между примерами."
        },
        {
          "role": "later",
          "year": 2019,
          "name": "Zhang and Sennrich, Root Mean Square Layer Normalization",
          "source_url": "https://arxiv.org/abs/1910.07467",
          "claim": "Чжан и Зеннрих исключают среднее из статистик, нормализуют по среднеквадратичному значению и сохраняют инвариантность формулы без эпсилона к положительному масштабированию, отказавшись от инвариантности к центрированию."
        },
        {
          "role": "later",
          "year": 2023,
          "name": "Touvron et al., LLaMA",
          "source_url": "https://arxiv.org/pdf/2302.13971",
          "claim": "Туврон и соавторы нормализуют вход каждого подслоя Transformer и используют для этого RMSNorm."
        }
      ]
    },
    "approach": "От статистик мини-батча через центрированную LayerNorm для отдельного примера к масштабированию по RMS без центрирования перед подслоями современных Transformer",
    "summary": "RMSNorm управляет масштабом вектора признаков, не вычитая его среднее. В идеальной формуле при нулевом эпсилоне положительное масштабирование сокращается, а рабочее ненулевое значение эпсилона определяет поведение нулевых и очень малых векторов. Поэтому вдали от нуля инвариантность лишь приближённая, а не точная для всех входов.",
    "rust_source": "rust/demos/ch25-rmsnorm/src/lib.rs"
  },
  "rust_sources": [
    {
      "path": "rust/demos/ch25-rmsnorm/src/lib.rs",
      "region": "historical-normalization-contrast",
      "purpose": "Сравнить нормализацию по статистикам батча, центрированную нормализацию отдельного примера и нормализацию только по RMS в контексте развития LLM."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/nn/rmsnorm.rs",
      "region": "rmsnorm-layer",
      "purpose": "Составить дифференцируемую RMSNorm по последней оси из ранее реализованных операций ленты."
    },
    {
      "path": "rust/crates/llm-from-scratch/src/nn/rmsnorm.rs",
      "region": "rmsnorm-errors",
      "purpose": "Описать типизированные ошибки конфигурации, формы, нулевой энергии и делегированные ошибки ленты."
    },
    {
      "path": "rust/demos/ch25-rmsnorm/src/lib.rs",
      "region": "rmsnorm-fixture",
      "purpose": "Собрать точные данные прямого и обратного проходов, поведения эпсилона, батча, политики оптимизатора и повторного воспроизведения."
    },
    {
      "path": "rust/demos/ch25-rmsnorm/src/lib.rs",
      "region": "rmsnorm-gradcheck",
      "purpose": "Сверить каждую координату входа и коэффициента масштаба с центральными разностями."
    },
    {
      "path": "rust/demos/ch25-rmsnorm/src/main.rs",
      "purpose": "Запустить проверенный пример RMSNorm и вывести его численные результаты."
    }
  ],
  "visualization": {
    "decision": "useful",
    "id": "rmsnorm",
    "rationale": "Сопоставленные полосы признаков и строки с результатами наглядно показывают, что RMSNorm изменяет масштаб без вычитания среднего, а эпсилон почти не влияет на обычный вектор, но заметно меняет вектор вблизи нуля."
  },
  "decoder_connection": "В накопительном декодере появился дифференцируемый нормализатор по последней оси для входа каждой обучаемой остаточной ветви. В главе 26 нормализованные признаки будут преобразованы в отдельные проекции запросов, ключей и значений."
}
---

import RustSource from '../../../components/RustSource.astro';
import RmsnormDiagram from '../../../components/chapters/RmsnormDiagram.astro';

export const diagramLabels = {
  title: 'Проследите один вектор признаков через RMSNorm',
  description:
    'Проследите точные значения из примера на Rust: от входа через масштабирование по RMS и обучаемый коэффициент до результатов сравнения масштабов, исторических методов, градиентов и отклонённых граничных случаев.',
  sections: {
    primary: 'Измените масштаб одного вектора признаков последней оси',
    scaling: 'Сравните случаи с нулевым и конечным эпсилоном',
    history: 'Сравните, откуда берётся статистика каждого метода',
    safeguards: 'Проверьте градиенты, формы и граничные случаи',
  },
  stages: {
    input: 'Вход',
    meanSquare: 'Среднее квадратов',
    inverseRms: 'Величина, обратная RMS',
    normalized: 'Вектор после масштабирования по RMS',
    gain: 'Обучаемый коэффициент масштаба',
    output: 'Выход после применения коэффициента',
  },
  fields: {
    ideal: 'Идеальный случай с нулевым эпсилоном',
    production: 'Обычный вход с рабочим эпсилоном',
    nearZero: 'Вход, для которого преобладает эпсилон',
    base: 'Исходный вектор',
    scaled: 'Вход, умноженный на десять',
    maximumDifference: 'Наибольшая абсолютная разность',
    zeroInput: 'Полностью нулевой вход',
    batchAxis: 'Независимые строки последней оси',
    batchNorm: 'BatchNorm',
    layerNorm: 'LayerNorm',
    rmsNorm: 'RMSNorm',
    companionA: 'Первый дополнительный пример',
    companionB: 'Второй дополнительный пример',
    outputMean: 'Среднее выхода RMSNorm',
    backward: 'Результаты обратного прохода',
    inputGradient: 'Градиент по входу',
    gainGradient: 'Градиент коэффициента масштаба',
    parameter: 'Параметр масштаба с фиксированным именем',
    optimizerPolicy: 'Группировка параметров оптимизатора',
    errors: 'Отклонённые граничные случаи',
    proof: 'Проверки из примера на Rust',
  },
  errorReasons: {
    rankZero: 'У входа должна быть хотя бы одна ось.',
    widthMismatch: 'Ширина последней оси признаков должна совпадать с шириной коэффициента масштаба.',
    zeroEnergy: 'При нулевом эпсилоне нельзя нормализовать строку с нулевым средним квадратов.',
  },
  cues: {
    input: 'Сплошная рамка — вход',
    normalized: 'Пунктирная рамка — масштабирование по RMS',
    scaled: 'Двойная рамка — применение коэффициента',
    accepted: 'Принято',
    rejected: 'Отклонено',
  },
  captions: {
    primary: 'Длины полос служат локальными ориентирами; точные значения указаны числами.',
    scaling: 'Для обычного входа разность мала, а для входа вблизи нуля хорошо заметна.',
    history: 'Опорный вектор сравнивается в двух отдельных батчах: сначала с первым дополнительным примером, затем со вторым.',
  },
  scrollers: {
    primary: 'Прокручиваемая последовательность значений RMSNorm',
    scales: 'Прокручиваемое сравнение масштабов при разных режимах эпсилона',
    history: 'Прокручиваемое сравнение методов нормализации',
  },
};

{/* chapter-section:worked-example */}

## Предскажите RMS, затем проверьте границу влияния эпсилона

Возьмите $x=[3,4]$, обучаемый коэффициент масштаба $g=[1.5,0.5]$ и
$\varepsilon=10^{-5}$. Прежде чем запускать пример, вычислите среднее квадратов:

$$
\frac{3^2+4^2}{2}=12.5.
$$

Величина, обратная RMS, приблизительно равна $0.282843$. Предскажите
нормализованный вектор до применения коэффициента, а затем покоординатно
умножьте его на $g$. Пример должен дать примерно $[0.848528,1.131370]$ до
применения коэффициента и $[1.272792,0.565685]$ после него.

Теперь умножьте вход на положительный коэффициент $10$. Чтобы проверить
инвариантность к масштабу, рассматривайте $\hat{x}$ — вектор после масштабирования
по RMS, но до применения обучаемого коэффициента $g$. Итоговый выход после
применения $g$ в это сравнение не входит.
Сначала предскажите $\hat{x}$ при $\varepsilon=0$, затем — при
$\varepsilon=10^{-5}$. Повторите то же сравнение для
$x=[0.0003,0.0004]$, по-прежнему не применяя обучаемый коэффициент; среднее
квадратов этого вектора меньше стабилизирующей добавки.

{/* chapter-section:formula */}

## Нормализуйте последнюю ось признаков

Для одного вектора на последней оси RMSNorm имеет вид

$$
\operatorname{RMSNorm}(x)=g\odot\frac{x}{\sqrt{\frac{1}{d}\sum_i x_i^2+\varepsilon}}
$$

Одна и та же статистика используется для всех $d$ координат этого вектора.
Затем обучаемый коэффициент отдельно масштабирует каждый признак. Среднее по
признакам не вычитается, а статистики одного примера не зависят от других.

Для положительного скаляра $a$ и ненулевого вектора идеальная формула с нулевым
эпсилоном удовлетворяет равенству

$$
\operatorname{RMSNorm}_{0}(ax)=\operatorname{RMSNorm}_{0}(x).
$$

При конечном эпсилоне знаменатель становится равен
$\sqrt{a^2\operatorname{RMS}(x)^2+\varepsilon}$: эпсилон не растёт вместе с
сигналом. Среднее квадратов нормализованного вектора равно

$$
\operatorname{mean}(\hat{x}^2)=
\frac{\operatorname{RMS}(x)^2}
{\operatorname{RMS}(x)^2+\varepsilon}.
$$

Когда сигнал значительно больше эпсилона, это значение приближается к $1$;
когда эпсилон преобладает — к $0$. Поэтому на практике RMSNorm приближённо
инвариантна к масштабу вдали от нуля, но не обладает точной инвариантностью для
всех входов.

{/* chapter-section:symbol-glossary */}

## Не смешивайте масштаб, коэффициент и оси

- $x$ — один вектор признаков, взятый с последней оси входа.
- $d$ — ненулевая ширина этой оси, а $i$ выбирает одну координату.
- $x_i$ — координата $i$, а $\sum_i x_i^2/d$ — среднее квадратов координат вектора $x$.
- $\varepsilon\ge0$ стабилизирует вычисление обратного квадратного корня.
- $\hat{x}$ — вектор после масштабирования по RMS, но до обучаемого покоординатного масштабирования.
- $g\in\mathbb{R}^{d}$ — обучаемый вектор коэффициентов масштаба, а $\odot$ обозначает покоординатное умножение.
- Полная форма выхода совпадает с формой входа.

RMSNorm не приводит модуль каждой координаты к $1$. Она управляет совокупным
среднеквадратичным масштабом вектора. Кроме того, RMSNorm не центрирует вектор,
не обрезает значения и не смешивает примеры мини-батча.

{/* chapter-section:history */}

## От статистик мини-пакета к языковым моделям с предварительной RMSNorm

В BatchNorm результат для обучающего примера зависит от статистик мини-батча.
LayerNorm устраняет зависимость между примерами, но перед масштабированием
по-прежнему вычисляет и вычитает среднее по признакам отдельного примера.

Первый ранний первоисточник — [Иоффе и Сегеди, *Batch Normalization*](https://arxiv.org/abs/1502.03167).
Иоффе и Сегеди включают нормализацию в архитектуру и вычисляют её статистики
отдельно для каждого мини-батча при обучении.

Второй ранний источник — [Ба, Кирос и Хинтон, *Layer Normalization*](https://arxiv.org/abs/1607.06450).
Ба, Кирос и Хинтон вычисляют среднее и дисперсию по суммарным входным сигналам
нейронов слоя для одного обучающего примера, устраняя зависимость между примерами.

RMSNorm отказывается от вычитания среднего и сохраняет масштабирование вектора
признаков по его среднеквадратичному значению; позднее в LLaMA RMSNorm применили
перед каждым подслоем Transformer.

Первый более поздний источник — [Чжан и Зеннрих, *Root Mean Square Layer Normalization*](https://arxiv.org/abs/1910.07467).
Чжан и Зеннрих исключают среднее из статистик, нормализуют по
среднеквадратичному значению и сохраняют инвариантность формулы без эпсилона к
положительному масштабированию, отказавшись от инвариантности к центрированию.
В формуле статьи эпсилона нет. В этой реализации положительный эпсилон добавлен,
чтобы нулевые и очень малые векторы оставались конечными; поведение вблизи нуля
следует из этого изменённого знаменателя.

Второй более поздний источник — [Туврон и соавторы, *LLaMA*](https://arxiv.org/pdf/2302.13971).
Туврон и соавторы нормализуют вход каждого подслоя Transformer и используют для
этого RMSNorm.

В декодере с предварительной нормализацией в каждую ветвь внимания или сети
прямого распространения поступает остаточный поток с контролируемым масштабом,
а тождественный путь проходит в обход нормализации.

Исполняемое сравнение фиксирует опорный вектор $[1,3]$ и меняет только другой
пример в том же батче. Выход BatchNorm для опорного вектора меняется; LayerNorm и
RMSNorm обрабатывают каждый пример независимо. LayerNorm центрирует опорный
вектор, а RMSNorm сохраняет ненулевое среднее выхода:

<RustSource
  path="rust/demos/ch25-rmsnorm/src/lib.rs"
  region="historical-normalization-contrast"
  caption="Сравните оси нормализации и поведение при центрировании"
  label="Исходный код Rust для исторического сравнения методов нормализации в контексте LLM в главе 25"
/>

{/* chapter-section:rust-implementation */}

## Соберите RMSNorm из уже реализованных операций ленты

`RmsNorm` владеет одним одномерным `NamedParameter` и не имеет смещения. Сначала
проверяются эпсилон, имя и ширина коэффициента масштаба, ранг входа и ширина
последней оси. Затем прямой проход вычисляет RMSNorm как последовательность уже
реализованных операций: умножения, среднего по последней оси с сохранением
размерности, добавления скаляра, логарифма, экспоненты, согласования форм
(broadcasting) и ещё одного умножения. Поэтому и вход, и коэффициент остаются
связаны с общей лентой
обратного автоматического дифференцирования:

<RustSource
  path="rust/crates/llm-from-scratch/src/nn/rmsnorm.rs"
  region="rmsnorm-layer"
  caption="Нормализуйте последнюю ось с помощью уже реализованных дифференцируемых операций"
  label="Исходный код Rust, реализующий переиспользуемый слой RMSNorm из главы 25"
/>

При положительном эпсилоне полностью нулевая строка превращается в конечные
нули. Если эпсилон равен нулю, строка с вычисленным средним квадратов, равным
нулю, отклоняется до логарифма. Это относится и к ненулевой строке, квадраты
элементов которой обнулились из-за потери значащих разрядов. Типизированные
ошибки делают эту границу явной:

<RustSource
  path="rust/crates/llm-from-scratch/src/nn/rmsnorm.rs"
  region="rmsnorm-errors"
  caption="Явно опишите ошибки конфигурации и численные границы"
  label="Исходный код Rust с ошибками RMSNorm из главы 25"
/>

В основном примере обратный проход получает $\bar y=[1,-2]$. Результат —
$\bar x\approx[0.407293,-0.305470]$ и
$\bar g\approx[0.848528,-2.262741]$. Пример также проверяет независимость строк
для входа формы $[2,2]$ и пустой внешний батч. Параметр с фиксированным именем
`decoder.block.0.attention_norm.gain` входит только во множество без затухания
`AdamWParameterGroups`. Такая группировка относится к политике оптимизатора, а
не к формуле RMSNorm:

<RustSource
  path="rust/demos/ch25-rmsnorm/src/lib.rs"
  region="rmsnorm-fixture"
  caption="Соберите данные прямого и обратного проходов, поведения эпсилона, форм и оптимизатора"
  label="Исходный код Rust для полного учебного примера главы 25"
/>

Центральные разности проверяют обе координаты входа и обе координаты коэффициента
масштаба с шагом $10^{-6}$ и допуском $2\times10^{-6}$:

<RustSource
  path="rust/demos/ch25-rmsnorm/src/lib.rs"
  region="rmsnorm-gradcheck"
  caption="Проверьте оба дифференцируемых входа численно"
  label="Исходный код Rust для проверки градиентов по входу и коэффициенту масштаба в главе 25"
/>

<RustSource
  path="rust/demos/ch25-rmsnorm/src/main.rs"
  caption="Запустите проверенный пример RMSNorm"
  label="Точка входа Rust, запускающая пример RMSNorm из главы 25"
/>

Выполните `cargo run --quiet --locked -p ch25-rmsnorm`, чтобы вывести те же
проверенные значения прямого и обратного проходов, масштабирования и граничных
случаев.

{/* chapter-section:visualization */}

## Сравните масштабирование по обе стороны границы влияния эпсилона

На схеме собраны проверенные значения основного примера, градиенты, сравнения
масштабов, поведение нулевого входа и батча, результаты исторических методов и
отклонённые запросы:

<RmsnormDiagram labels={diagramLabels} />

Читайте точные значения слева направо: вход, общий коэффициент RMS,
масштабированный по RMS вектор, обучаемый коэффициент и итоговый выход.
Сплошная, пунктирная и двойная рамки различают три состояния вектора, не
полагаясь только на цвет. Затем карточки масштабирования показывают, почему умножение
обычного вектора на десять почти не меняет результат с конечным эпсилоном, а
такое же действие выводит очень малый вектор далеко из режима, в котором
преобладает эпсилон.

{/* chapter-section:exercises */}

## Сначала сделайте предсказания

1. Вычислите среднее квадратов и величину, обратную RMS, для $x=[3,4]$.
2. Примените $g=[1.5,0.5]$ к нормализованному вектору.
3. Для ненулевого вектора и $\varepsilon=0$ предскажите вектор $\hat{x}$ после умножения входа на $10$ и масштабирования по RMS; рассматривайте его до применения обучаемого коэффициента $g$.
4. Объясните, почему положительный $\varepsilon$ намного сильнее меняет вектор вблизи нуля.
5. Предскажите результат для полностью нулевой строки при положительном и нулевом эпсилоне.
6. Определите, вычитает ли RMSNorm среднее по признакам или смешивает примеры батча.
7. Предскажите форму выхода и форму градиента коэффициента масштаба для входа $[B,T,d]$.
8. Объясните, почему исключение из затухания относится к политике оптимизатора, а не к формуле.
9. Укажите место RMSNorm относительно тождественного и обучаемого путей в остаточном блоке с предварительной нормализацией.

<details>
  <summary>Проверить предсказания</summary>

  1. Среднее квадратов равно $12.5$, а величина, обратная RMS, — примерно $0.282843$.
  2. Выход приблизительно равен $[1.272792,0.565685]$.
  3. При $\varepsilon=0$ положительный множитель, на который умножен вход, сокращается при вычислении $\hat{x}$. Поэтому до применения обучаемого коэффициента $g$ этот вектор не меняется.
  4. Конечный эпсилон не масштабируется вместе с сигналом и потому преобладает в знаменателе очень малого вектора.
  5. Положительный эпсилон возвращает $[0,0]$; при нулевом эпсилоне строка, у которой среднее квадратов равно нулю, отклоняется до логарифма.
  6. Ни то ни другое: используются только признаки последней оси одного примера, а их среднее не вычитается.
  7. Форма выхода — $[B,T,d]$, а форма градиента коэффициента масштаба — $[d]$.
  8. Оптимизатор решает, для каких именованных параметров применять затухание; в уравнении RMSNorm этого нет.
  9. Обучаемая ветвь получает выход RMSNorm, а тождественный путь переносит остаточный поток без изменений.
</details>

{/* chapter-section:decoder-connection */}

## Далее спроецируйте нормализованные признаки в тензоры запросов, ключей и значений

В накопительном декодере появилась дифференцируемая RMSNorm по последней оси для
каждой обучаемой остаточной ветви, а её коэффициент с фиксированным именем можно
исключить из затухания весов при обучении. В главе 26 нормализованные признаки
будут преобразованы в отдельные проекции запросов, ключей и значений без
смещений, с явно заданными размерностями внимания.
